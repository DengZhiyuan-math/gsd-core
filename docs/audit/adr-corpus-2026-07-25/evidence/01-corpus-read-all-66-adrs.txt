
==============================================================================
## AGENT: a1f30eca7b630a1d7
==============================================================================

--- docs/adr/0005-sdk-architecture-seam-map.md [ACCURATE] Superseded by ADR-0174 (2026-05-23); originally Accepted (2026-05-09)
    [medium|lacuna] ADR-0005 was the corpus's top-level seam map; ADR-0174 retired it without designating a successor map, so the orientation function now lives in hand-maintained prose inside a file that declares itself non-authoritative.
        EV: docs/adr/README.md:214 — '## Seam map ... This section is prose and hand-maintained; the index above is the authority on status.' The four seam ADRs ADR-0005 pointed at are all still live (0001 Accepted, 0003 Accepted, 0004 Accepted, 0006 Accepted — verified by grep of each file's Status line) but n…
        REC: Either promote the README 'Seam map' section into an Accepted ADR that owns seam enumeration, or add an amendment to ADR-0174 explicitly stating the seam map is retired with no successor and that seam discovery is via the generated index.
    (+1 low)

--- docs/adr/0007-sdk-package-seam-module.md [ACCURATE] Superseded by ADR-0174 (2026-05-23); originally Accepted (2026-05-07)
    [medium|lacuna] ADR-0007's second paragraph explicitly carved a 'Runtime-Global Skills Policy Module' OUT of this seam as a separate, surviving module. ADR-0174's supersession row retires only the compatibility scaffolding and is silent on the carve-out, so the carved-out pol…
        EV: docs/adr/0007-sdk-package-seam-module.md:8 — 'it now lives in a separate Runtime-Global Skills Policy Module consumed by `agent-skills` and `skill-manifest`.' ADR-0174:12 retires only 'install-layout probing, legacy-asset discovery, compatibility diagnostics'. Neither `agent-skills` nor `skill-manif…
        REC: Add a line to ADR-3660 (Runtime Artifact Layout Module) or ADR-58 (Runtime Install Policy Module) claiming the Runtime-Global Skills Policy carve-out from ADR-0007, so the pointer chain terminates somewhere live.
    (+1 low)

--- docs/adr/0010-file-operation-engine-module.md [ACCURATE] Superseded by ADR-0009 (Shell Command Projection Module expansion, Phases 3–4, #3467–#3468…
    [high|consequence-drift] The supersession note claims ADR-0009's Phase 3 made atomic write 'a single platform-conditional surface' and that Phase 4 removed the duplicated `atomicWriteFileSync` wrappers. A second, independent atomic-write implementation now lives outside that seam and …
        EV: docs/adr/0010-file-operation-engine-module.md:7 asserts the seam 'own[s] atomic write (tmp+rename) ... as a single platform-conditional surface'. But src/runtime-hooks-surface.cts:171 has the banner '// atomicWriteFileSync — shared canonical implementation.' and src/runtime-hooks-surface.cts:186 def…
        REC: Either route src/runtime-hooks-surface.cts's writes through `platformWriteSync`, or amend ADR-0009 to record a second sanctioned atomic-write owner and state why the temp-file-registry requirement forces it outside the seam. Today the code …
    [medium|missing-artifact] Every issue reference in this ADR is a dead pointer, and some resolve to unrelated live issues in the current repo — a false trail rather than a 404.
        EV: The ADR cites `#3467`, `#3468` (line 3, 7) and `#1755`, `#2866`, `#2979`, `#3002`, `#3017`, `#3439` (line 107). `gh issue view 3524 --repo open-gsd/gsd-core` → 'Could not resolve to an issue'; the repo's highest issue number is 2620 (`gh issue list --limit 1`). Meanwhile `gh issue view 1755 --repo o…
        REC: Add a corpus-wide banner (README.md or a provenance ADR) stating that four-digit `#3xxx`+ references and any pre-2026-05 issue number refer to the retired `get-shit-done-redux` numbering and must not be resolved against current issues.
    (+1 low)

--- docs/adr/0010-skill-surface-budget-module.md [ACCURATE] Superseded by ADR-0011 (0011-skill-surface-budget-module.md); originally Proposed (2026-05…
    [medium|quality] Plain-text 'ADR-0010' and 'ADR-0011' are ambiguous in this corpus: two files claim number 0010 and three claim 0011. Disambiguation depends entirely on link hrefs, and at least one live cross-reference has to disambiguate in prose.
        EV: `ls docs/adr/` shows 0010-file-operation-engine-module.md AND 0010-skill-surface-budget-module.md; 0011-review-default-reviewers.md, 0011-review-default-reviewers-prd.md AND 0011-skill-surface-budget-module.md. The generated index renders both as 'ADR-0010' (docs/adr/README.md:200, :201) and three a…
        REC: Record a disambiguation convention (e.g. always cite legacy ADRs as `ADR-0011-skill-surface-budget`) and have scripts/gen-adr-index.cjs emit the file stem alongside the number for any colliding number.
    (+1 low)

--- docs/adr/0011-review-default-reviewers-prd.md [ACCURATE] Legacy — frozen historical record; not a pattern to follow
    [medium|quality] A PRD occupies an ADR number and is enumerated in the generated ADR index as 'ADR-0011', colliding with two other ADR-0011 files — one of which is its own companion ADR.
        EV: docs/adr/README.md:202 renders it as '| [ADR-0011](0011-review-default-reviewers-prd.md) | PRD — ... | Legacy | — |' inside the ADR table, while docs/adr/README.md:42 simultaneously states 'PRDs live in [`docs/prd/`](../prd/), not here.' The generated index therefore counts a PRD toward '66 ADRs' (R…
        REC: Have scripts/gen-adr-index.cjs either exclude files whose title begins 'PRD —' from the ADR count, or render them in a separate 'Legacy PRDs' block with no ADR-NNNN label.
    [medium|lacuna] Q-1 is marked **Blocking** and is answered nowhere in the frozen record — not in the PRD, not in the companion ADR's ratification note. The answer only exists in code.
        EV: docs/adr/0011-review-default-reviewers-prd.md:189 — 'Q-1. Empty-array semantics. ... *Proposal: schema error.* **Blocking.**' The code resolved it as proposed: src/review-reviewer-selection.cts:118-123 returns `{ absent: false, values: [], errors: ['review.default_reviewers cannot be empty'] }` for …
        REC: Add one line to the companion ADR's ratification block recording that Q-1 resolved to 'schema error' with the src/review-reviewer-selection.cts:118 citation. A Blocking question whose answer lives only in an implementation is a re-litigatio…
    (+1 low)

--- docs/adr/0012-command-routing-hub.md [ACCURATE] Superseded by ADR-0174 (2026-05-23); originally Accepted (2026-05-20)
    [high|contradiction] ADR-0174's supersession row asserts `cjsRegistry` was deleted from the Hub. It was not — `cjsRegistry` is the Hub's sole dispatch mechanism. A reader trusting the supersession table would conclude the Hub has no handler registry at all.
        EV: docs/adr/0174-retire-gsd-sdk-package-boundary.md:13 — '`mode`, `sdkLoader`, `cjsRegistry`, and SDK-failure errorKinds are deleted because there is no second runtime to select.' Live code: src/command-routing-hub.cts:11 documents `createHub({ cjsRegistry, manifest }) -> hub`; :240 declares `cjsRegist…
        REC: Amend ADR-0174 §Supersedes row for ADR-0012 to strike `cjsRegistry` from the deletion list (only `mode`, `sdkLoader`, and the two SDK errorKinds were deleted). ADR-0174 already has an amendment mechanism (used for #1642), so this is a one-l…
    [medium|consequence-drift] The ADR's self-declared abstraction leak — the `mvp-mode` early-return bypass — is still in the code, but the entire justification for it (divergent CJS vs SDK implementations) was eliminated by ADR-0174. No ADR revisits it, and the deferral is pinned to a tra…
        EV: docs/adr/0012-command-routing-hub.md:42-46 justifies the bypass by 'Routing `mvp-mode` through the SDK hub would silently change observable CLI behaviour' and defers the fix: 'This work is tracked alongside SDK-CJS migration #3524 closure.' There is no SDK hub any more (ADR-0174), yet the bypass sur…
        REC: Either fold `mvp-mode` into the normal cjsRegistry (its stated blocker is gone) or record in ADR-2346 why the bypass is now permanent. As it stands the codebase carries a workaround whose rationale is provably obsolete.
    (+1 low)

--- docs/adr/3524-cjs-sdk-hard-seam.md [ACCURATE] Superseded by ADR-0174 (2026-05-23); originally Proposed (2026-05-14)
    [high|lacuna] ADR-3524 §1.5 forbade hand-synced pairs and shipped a lint to enforce it. ADR-0174 deleted the lint along with the seam but provided no replacement, and the post-collapse tree has recreated the same two-artifacts-for-one-behavior shape at a new pair boundary —…
        EV: docs/adr/3524-cjs-sdk-hard-seam.md:26 — '**Hand-synced pairs are forbidden.** A pre-merge `lint-shared-module-handsync.cjs` ... fails the build unless the pair is explicitly allow-listed.' That script no longer exists. ADR-0174 rejected 'Shape C' for precisely this root cause (docs/adr/0174-...:139 …
        REC: Add a tsc-output freshness check to `lint:generated-sync` (build src/ to a temp outDir, diff against the tracked .cjs files), or gitignore all nine and let them be build-at-publish artifacts like the other 141. Right now the drift class ADR…
    [medium|quality] Two substantial amendments were appended to this ADR on the same date it was superseded, and neither carries a note that it describes machinery deleted by ADR-0174. A reader entering at the Amendments section gets ~120 lines of confident implementation detail …
        EV: docs/adr/3524-cjs-sdk-hard-seam.md:3 gives the supersession date as 2026-05-23; the amendment headers at :105 ('### 2026-05-23 — validate.ts → verify.cjs generator pattern (issue #6)') and :207 ('### 2026-05-23: Phase * cooperating-sibling retirement ... (issue #4)') are the same day. They describe …
        REC: Prepend a one-line banner to the Amendments section: 'Everything below describes tooling deleted by ADR-0174; retained for provenance only.' Also record where the validate.ts↔verify.cjs behavioral fixes (W005/W006/I001/W007, phaseVariants, …
    [medium|missing-artifact] The ADR's entire evidentiary basis — the nine-item drift bug list that motivates the decision — is unresolvable, and several of the numbers now point at unrelated live issues.
        EV: docs/adr/3524-cjs-sdk-hard-seam.md:10 cites '#1535, #1542, #2047/#2052, #2638/#2655, #2653/#2670, #2687/#2706, #2798/#2816, #3055/#3116, #3523'. In the current repo: `gh issue view 1535` → 'fix(core): adr-parser drops 9 punctuated header synonyms'; `gh issue view 2047` → 'fix(#2043): extractPhaseTok…
        REC: Same corpus-wide provenance banner. This ADR is the worst case in the cluster because its justification is entirely a list of issue numbers that now silently mis-resolve.

--- docs/adr/0174-retire-gsd-sdk-package-boundary.md [ASPIRATIONAL_ACCEPTED] Accepted (2026-05-23); amended #1642 (2026-06-23)
    STATUS-EV: The core decision genuinely shipped: no `sdk/` tree, no `synckit` (package.json dependencies = ['@anthropic-ai/claude-agent-sdk','ws']), no release-sdk.yml, no `.generated.cjs` artifacts, single package (@opengsd/gsd-core@1.8.0), Hub simplified to four errorKinds (src/command-routing-hub.cts:50-59), §5 Result type matc…
    [high|consequence-drift] §2's entire build shape is wrong. There is no `dist/`, tsc does not target it, and `bin/gsd-tools.cjs` is not a thin shim. The real build model was decided by ADR-457, which cites ADR-0174 one-way; ADR-0174 was never amended.
        EV: docs/adr/0174-...:38 — '`tsc` on `prepublishOnly` compiles `src/` to CJS in `dist/`. `bin/gsd-tools.cjs` becomes a thin shim that `require`s from `dist/`.' Reality: `ls -d dist` → 'No such file or directory'; tsconfig.build.json sets `"outDir": "gsd-core/bin/lib"` with the comment 'ADR-457 build-at-…
        REC: Amend ADR-0174 §2 to record that the `dist/` target was superseded by ADR-457 build-at-publish (outDir `gsd-core/bin/lib`, tracked-vs-gitignored partition), and add ADR-457 to ADR-0174's relations. The amendment mechanism is already establi…
    [high|consequence-drift] §3's seam-aligned source tree is aspirational, not as-built: nine of the ten declared subdirectories do not exist. The ADR states it as a Decision in the present tense with no deferral marker, while CONTEXT.md quietly downgrades it to a 'canonical long-term sh…
        EV: docs/adr/0174-...:42-56 — '### 3. Source tree — seam-aligned subdirectories under `src/` ... Each subdirectory maps 1:1 to an architectural concern' followed by a table naming src/dispatch/, src/handlers/, src/errors/, src/manifest/, src/config/, src/state/, src/workstream/, src/runtime/, src/cli/, …
        REC: Either mark §3 explicitly as deferred/aspirational with a tracking issue, or amend it to describe the flat-tree-with-three-subdirs shape that was actually built. As written it fails the 'architecture reviews can classify drift quickly' prop…
    [high|consequence-drift] §6's observability seam is entirely dormant in production: the Hub defaults to a no-op logger and no production caller ever injects the real one, so the structured-stderr contract, the GSD_AUDIT trace file, and traceId correlation never fire — while user-facin…
        EV: src/command-routing-hub.cts:273-275 — `const _logger = (logger && typeof logger.onEvent === 'function') ? logger : createNoOpLogger();`. `grep -rn 'createDefaultLogger' src/ gsd-core/bin/ tests/` finds it defined at src/observability/logger.cts:119, re-exported at :166, and referenced ONLY in tests/…
        REC: Either wire `createDefaultLogger` into `routeCjsCommandFamily` / gsd-tools.cjs so §6 is real, or amend §6 to 'seam built, activation deferred' and strike GSD_AUDIT from docs/CONFIGURATION.md until it works. A documented env var that silentl…
    [medium|consequence-drift] §4 lists 'arg shape coercion' as one of the four cross-cutting concerns the Hub owns and calls all four 'unambiguously load-bearing'. The Hub performs no arg coercion — it passes args through untouched.
        EV: docs/adr/0174-...:65 — '3. **Arg shape coercion** — incoming arg shapes are normalized before handler invocation.' src/command-routing-hub.cts:376 is the only handler invocation: `const result = handler({ family, subcommand, args, cwd, raw });` — `args` is the raw slice destructured at :343, never t…
        REC: Amend §4 to three concerns (errors, manifest, observability) and cite ADR-2346 as the owner of arg-shape policy — or move `parseFamilyArgs` into the Hub as §4 promised.
    [medium|missing-artifact] §7's named module does not exist and its stated invariant is vacuous: no production code sets parentTraceId at all.
        EV: docs/adr/0174-...:112 — '`src/handlers/init/composer.ts` composes N atomic dispatches. Each child dispatch receives the parent's `traceId` as its `parentTraceId` ... The composer is the only caller that sets `parentTraceId`.' `ls src/handlers` → 'No such file or directory'; `grep -rn 'composer' src/…
        REC: Amend §7 to state that Init.* remains a monolithic handler (src/init.cts, 98.9K) and that traceId trees are unbuilt, or file the composer as work. Combined with the §6 finding, the entire trace-correlation story documented in CONTEXT.md:89 …
    [high|lacuna] ADR-0174 retires ADR-3524's drift-prevention lint as part of the collapse but never asks what replaces it. The result is that the single-source-of-truth property — the whole justification for the collapse — has no enforcement.
        EV: docs/adr/0174-...:14 supersedes ADR-3524 including its 'hand-sync pair lint'; :120 counts the deletion as a positive ('The parity tests, generator freshness checks, hand-sync pair lint ... are deleted outright'). But the ADR's own rejection of Shape C (:139) names the root cause as 'two artifacts fo…
        REC: Decide and record the tracked-vs-generated partition for `gsd-core/bin/lib/`, and add a freshness gate. This is the single highest-leverage gap in the cluster: the collapse's core promise is unguarded.
    [medium|consequence-drift] Phase 6 (docs cleanup) is recorded as a planned phase but never completed: live machine-read artifacts still name the deleted `sdk/` tree.
        EV: docs/adr/0174-...:127 — 'CONTEXT.md and ~20 doc files require updating across Phase 6 PRs' and the migration table at :160 lists 'Phase 6 — Docs cleanup ... ~4' PRs. Residue: CONTEXT.md:598 — '`PRED.k320.ci-paths-monitored=bin/ gsd-core/ src/ agents/ commands/ hooks/ sdk/src/ sdk/prompts/`', a machi…
        REC: Drop the two dead entries from scripts/changeset/lint.cjs and CONTEXT.md's k320 predicate; then either mark Phase 6 complete in ADR-0174 or record what remains.
    [medium|lacuna] ADR-0174 retains the Hub as 'the' dispatch seam but never decides whether routing through it is mandatory for a command family. Four routers — including the largest — bypass it entirely, with no recorded rationale.
        EV: 13 of 16 command routers reach the Hub (9 via `routeCjsCommandFamily` in src/cjs-command-router-adapter.cts plus direct imports in src/phase-command-router.cts:23, src/graphify-command-router.cts:32, src/intel-command-router.cts:49). The four that do not: src/check-command-router.cts (60.0K — the la…
        REC: State the rule explicitly — either 'every command family routes through the Hub' (then file the four) or 'the Hub is opt-in for families needing the typed-error contract' (then say why check-command-router is exempt). Without it, 'single di…
    (+2 low)

>> CLUSTER OBS [high]: Every issue reference in this cluster is a dead pointer, and a subset silently mis-resolve to unrelated live issues. The repository was renamed from `open-gsd/get-shit-done-redux` to `open-gsd/gsd-core` and issue numbering RESTARTED rather than carried over. This turns citation into misinformation: a maintainer following ADR-3524's drift-bug list lands on real, current, unrelated issues and has no…
   EV: `gh repo view open-gsd/get-shit-done-redux --json name` → {"name":"gsd-core"} (the old name redirects). But `gh issue list --repo open-gsd/gsd-core --limit 1 --json number` → [{"number":2620}] — max issue is 2620, so #3079, #3408, #3467, #3468, #3524, #3788 all return 'Could not resolve to an issue'. Worse, the low num…

>> CLUSTER OBS [low]: The superseded family is relationally SOUND — this is the cluster's good news and should be recorded as such. All five supersession claims are reciprocated in the target file, all are disambiguated by filename href despite two colliding ADR numbers, and the generated index agrees with every one. No one-way pointers were found.
   EV: ADR-0005→0174 reciprocated at 0174:11; ADR-0007→0174 at 0174:12; ADR-0012→0174 at 0174:13; ADR-3524→0174 at 0174:14 (and independently at docs/prd/3524-cjs-sdk-hard-seam.md:3); ADR-0010-file-operation→0009 reciprocated at 0009-shell-command-projection-module.md:4; ADR-0010-skill-surface→0011-skill reciprocated at 0011-…

>> CLUSTER OBS [medium]: The corpus has an ADR-number collision problem that plain-text citation cannot survive: two files claim ADR-0010 and three claim ADR-0011 (one of which is a PRD). Cross-references already have to disambiguate in prose, and the generated index renders duplicate labels without distinguishing them.
   EV: docs/adr/README.md:200 and :201 both render '[ADR-0010]'; :202 renders '[ADR-0011]' for the PRD while the Accepted table renders 'ADR-0011' for 0011-skill-surface-budget-module.md and 0011-review-default-reviewers.md. ADR-3524:47 is forced to write '**Defer — see ADR-0011** (accepted, not the 0011-superseded draft)'. C…

>> CLUSTER OBS [high]: ADR-0174 is the cluster's most consequential document and it drifted asymmetrically: §5 was formally amended when review caught drift (#1642), while §2, §3, §6 and §7 drifted silently and remain uncorrected. The pattern suggests conformance is checked only when a specific consumer complains about a specific field — there is no periodic conformance sweep against Accepted ADRs.
   EV: docs/adr/0174-...:85 — 'Drift note (amendment #1642, 2026-06-23) ... The drift was caught during architecture review (parent #1641).' That amendment reconciled the Result type. Meanwhile §2's `dist/` (tsconfig.build.json outDir = gsd-core/bin/lib), §3's nine missing subdirectories (`ls src/` → 3 subdirs), §6's dormant …

>> CLUSTER OBS [medium]: The payload-directory rename (`get-shit-done/` → `gsd-core/`) has silently invalidated path citations throughout the cluster, and the repo's own CLAUDE.md still documents the old layout plus a lint script that no longer exists. Path citations in ADRs are not gated by anything.  [gsd-allow-legacy-name: quoted finding about the rename itself]
   EV: ADR-0011-review-default-reviewers-prd.md:16, :245 cite `workflows/review.md`; the file is gsd-core/workflows/review.md. ADR-0010-file-operation-engine-module.md:39-56 cites `gsd-core/bin/lib/...` correctly but ADR-3524:22-46 cites `sdk/src/...` throughout. CLAUDE.md's 'Lint Suite Summary' table still lists `scripts/lin…

>> CLUSTER OBS [high]: Three separate ADR-declared contracts in this cluster are documented as live in machine-read or user-facing artifacts while being unreachable or false in code — the corpus is being trusted as ground truth by downstream docs without verification.
   EV: (1) CONTEXT.md:89 states the dispatch-observability contract as fact; the Hub defaults to createNoOpLogger (src/command-routing-hub.cts:273-275) and no caller injects the real one. (2) docs/CONFIGURATION.md:1644-1707 documents `GSD_AUDIT=1 gsd plan` producing `.planning/.gsd-trace.jsonl`; it cannot fire. (3) ADR-0174:1…

>> NO-ADR: Tracked-vs-generated partition of `gsd-core/bin/lib/*.cjs`, and the freshness gate for tsc output
   WHY: A real decision with real alternatives was made silently and inconsistently. ADR-457 decided the *generation model* (build-at-publish via tsc) but nothing decided *which* build outputs are committed. The result is a three-way split with no stated rule: ~141 files gitignored, 17 t… | EV: tsconfig.build.json: `"rootDir": "src", "outDir": "gsd-core/bin/lib"`. `git ls-files gsd-core/bin/lib/ | wc -l` → 17, against 158 files in the directory. Nine of the 17 have `.cts` sources (api-coverage, assumption-delta, claude-orchestrati…

>> NO-ADR: CommandRoutingHub adoption policy — is routing through the Hub mandatory for a command family?
   WHY: ADR-0012 introduced the Hub as 'the single dispatch seam' with one proof-of-concept router and deferred the rest to 'follow-up issues'; ADR-0174 inherited the Hub, called its four concerns 'unambiguously load-bearing', and never restated any adoption obligation; ADR-2346 defines … | EV: 13 of 16 command routers reach the Hub (9 via `routeCjsCommandFamily` in src/cjs-command-router-adapter.cts, plus direct imports at src/phase-command-router.cts:23, src/graphify-command-router.cts:32, src/intel-command-router.cts:49). Zero …

>> NO-ADR: Observability activation — who injects `createDefaultLogger`, at which seam, and under what default
   WHY: ADR-0174 §6 decided the observability *contract* (silent on success, structured JSON to stderr on error, opt-in file audit, traceId trees) but not the *activation*, and the implementer chose a no-op default with no caller. That is a load-bearing decision made by omission: it dete… | EV: src/command-routing-hub.cts:273-275 defaults to `createNoOpLogger()` with the comment 'Consumers can opt into the reference impl by importing createDefaultLogger' — no consumer does. `grep -rn 'createDefaultLogger' src/ gsd-core/bin/ tests/…

>> NO-ADR: Issue-number provenance across the get-shit-done-redux → gsd-core renumbering
   WHY: The repository rename restarted issue numbering, which retroactively broke every historical citation in the ADR corpus and — worse — made a subset resolve to unrelated live issues. This is a decision that was made (renumber rather than migrate) with a large, permanent, corpus-wid… | EV: `gh repo view open-gsd/get-shit-done-redux --json name` → gsd-core (name redirects); `gh issue list --repo open-gsd/gsd-core --limit 1 --json number` → 2620 (max). Dead: #3079, #3408, #3467, #3468, #3524, #3788. Colliding: `gh issue view 15…

>> NO-ADR: Ownership of the ADR seam map after ADR-0005's retirement
   WHY: ADR-0005 existed to answer 'which module owns this concern?' — the orientation function for a corpus that now holds 66 ADRs. ADR-0174 retired it while explicitly preserving the seam-Module *vocabulary*, but assigned the *map* to nobody. The function relocated into hand-maintained… | EV: docs/adr/README.md:214 — '## Seam map ... This section is prose and hand-maintained; the index above is the authority on status' — but the index carries status only, not ownership. ADR-0174:118 preserves 'the seam-Module vocabulary from ADR…

==============================================================================
## AGENT: aff50afee7b341241
==============================================================================

--- docs/adr/0001-dispatch-policy-module.md [SHOULD_BE_SUPERSEDED] Accepted
    STATUS-EV: Every module this ADR and its two amendments name is absent from the tree at aa0f7dee. `ls src/ | grep -iE 'dispatch|query|topology|runtime-context|cli-output|bridge'` returns nothing; `ls gsd-core/bin/lib/ | grep -iE 'dispatch|query|topology|bridge'` returns nothing. A repo-wide grep for the amendment's own API symbol…
    [high|status-drift] ADR-0001 is declared Accepted and indexed as an active decision, but nothing it decides exists in the tree.
        EV: docs/adr/0001-dispatch-policy-module.md:3 `- **Status:** Accepted`; docs/adr/README.md:125 lists it in 'Active decisions (50)'. Named artifacts absent: Dispatch Policy Module, Query Runtime Context Module, Native Dispatch Adapter Module, Query CLI Output Module, Command Topology Module (no matching …
        REC: Set status to `Superseded by ADR-0174` and add ADR-0001 to ADR-0174's Supersedes table alongside 0005/0007/0012/3524.
    [high|relation-asymmetry] ADR-0174's Supersedes table omits ADR-0001, even though ADR-0005 — which ADR-0174 does supersede — explicitly delegates dispatch policy to ADR-0001.
        EV: docs/adr/0174-retire-gsd-sdk-package-boundary.md 'Supersedes' table names only ADR-0005, ADR-0007, ADR-0012, ADR-3524. docs/adr/0005-sdk-architecture-seam-map.md:12: 'Keep dispatch transport/outcome policy behind the **Dispatch Policy Module** and **SDK Runtime Bridge Module** (see `0001-dispatch-po…
        REC: Extend ADR-0174's Supersedes table to cover the SDK-axis seam ADRs it silently retired (0001, 0003 partially, 0006).
    [medium|consequence-drift] CONTEXT.md — the repo's machine-greppable domain canon — still carries live glossary entries for all of ADR-0001's retired modules.
        EV: CONTEXT.md:29 '### Dispatch Policy Module', :43 '### Query Runtime Context Module', :46 '### Native Dispatch Adapter Module', :49 '### Query CLI Output Module', :70 '### Command Topology Module', plus '### Query Execution Policy Module', '### Query Subprocess Adapter Module', '### Query Command Reso…
        REC: Mark the eight SDK-era glossary entries retired in CONTEXT.md, or delete them — agents grep this file as canon.
    [medium|quality] The document is changelog-shaped, not decision-shaped: no Context section, no alternatives considered, and two appended amendments that re-decide scope in place.
        EV: The whole ADR is 42 lines: one decision paragraph (line 6) and two amendment blocks (lines 8-29, 31-41). The 'Dead-wrapper convergence' block (lines 20-28) is a list of five deleted filenames — a commit note, not a decision. No `## Context`, no `## Alternatives`, no `## Consequences`.
        REC: On supersession, do not repair; record it as legacy-form and rely on ADR-0174.
    [medium|lacuna] The ADR introduces a seam-retirement act (five wrapper modules deleted) but never writes the retirement policy: when a seam Module may be deleted, who must be notified, and what happens to its CONTEXT.md glossary entry.
        EV: Lines 20-28 list five modules removed 'after call-site convergence' with no stated criterion. The consequence of that unwritten policy is visible today: the modules are gone from disk but CONTEXT.md:29-72 still declares them as owning Modules, and docs/adr/README.md:125 still calls the ADR active. N…
        REC: Write the retirement rule once (likely in docs/adr/README.md lifecycle rules): deleting a Module requires (a) an ADR status change, (b) a CONTEXT.md glossary edit, (c) an index regeneration.
    (+1 low)

--- docs/adr/0002-command-contract-validation-module.md [ACCURATE] Accepted
    [medium|lacuna] `CANONICAL_TOOLS` — the allowlist that rule 3 validates against — has no stated owner, provenance, or update policy, yet it gates every command file in the repo.
        EV: scripts/lint-command-contract.cjs:59-63 validates each `allowed-tools` entry against `CANONICAL_TOOLS` imported from scripts/command-contract-helpers.cjs, with one hardcoded wildcard carve-out (`tool.startsWith('mcp__context7__') && CANONICAL_TOOLS.has('mcp__context7__*')`). The ADR (line 14) says o…
        REC: Record in the ADR: the set's source of truth (host runtime tool docs vs. GSD's own choice), the review bar for adding an entry, and whether wildcard entries are permitted generally or are a one-off.
    (+4 low)

--- docs/adr/0003-model-catalog-module.md [ACCURATE] Accepted
    [medium|consequence-drift] The Decision's shipping clause names two packages, one of which never existed under that name any more and one of which was retired entirely.
        EV: docs/adr/0003:32-36: 'The canonical file lives in a location both packages ship: repo root package (`@opengsd/get-shit-done-redux`) includes it; standalone SDK package (`@opengsd/gsd-sdk`) includes it. Both CJS and SDK load this exact file.' Actual: `package.json` name is `@opengsd/gsd-core` @ 1.8.0…
        REC: Amend §32-36 to state the single-package reality and, if the `bin/shared/` location survives only for historical reasons, say so — otherwise a future reader will preserve a directory for a constraint that no longer exists.
    [medium|consequence-drift] Three of the five stated Consequences reference SDK files that no longer exist, so they can neither be checked nor relied on.
        EV: docs/adr/0003:44 'resolve-model in SDK and CJS read the same registry'; :46 '`sdk/src/query/helpers.ts` runtime list comes from the catalog, fixing drift like the missing `hermes` runtime'; :47 '`sdk/src/session-runner.ts` uses the catalog's Claude runtime tier defaults'. `ls sdk` → No such file or …
        REC: Amend the Consequences to the surviving single-runtime form; the substantive invariants (one registry, catalog↔docs parity, profile-semantic fallback) all still hold and are tested.
    [medium|lacuna] The ADR decides the catalog is the single source but never decides the *admission* policy: who may add an agent, what the default profile tiers are for a new agent, and whether the coverage test is a merge gate or an advisory.
        EV: The catalog owns 'the full agent registry for model resolution' (:26) and tests/model-profiles.test.cjs:39 checks every `agents/gsd-*.md` is present — but nothing in the ADR says what a contributor adding `agents/gsd-foo.md` must choose for `golden`/`balanced`/`budget`/`phaseType`/`routingTier`, or …
        REC: Add an 'Adding an agent' subsection: the required fields, the default tier heuristic, and a pointer to the test as the gate.
    (+2 low)

--- docs/adr/0004-worktree-workstream-seam-module.md [ACCURATE] Accepted
    [high|consequence-drift] 'The Module owns `.planning` path resolution' is violated by two private re-implementations that diverge from the Module on the project segment — and one of them also drops the Module's path-traversal guard.
        EV: src/configuration.cts:74-78 defines a private `planningDir(cwd, workstream)` returning `join(cwd,'.planning')` or `join(cwd,'.planning','workstreams',workstream)`, used at src/configuration.cts:198 to resolve `config.json`. src/active-workstream-store.cts:38-40 defines a private `planningRoot(cwd)` …
        REC: Delete both private copies and import `planningDir`/`planningRoot` from planning-workspace. If a cycle blocks that for active-workstream-store, invert it: pass the resolved root in rather than recomputing it.
    [medium|consequence-drift] Worktree-root composition — deciding *when* to apply the Adapter before entering the Module — lives in the CLI entrypoint, not in the seam, and carries an undocumented monorepo escape hatch.
        EV: `resolveWorktreeRoot` has exactly one production caller: gsd-core/bin/gsd-tools.cjs:2381-2387, which applies it only when `!fs.existsSync(path.join(cwd,'.planning'))`, with the inline comment 'in monorepo worktrees where the subdirectory itself owns .planning/, skip worktree resolution'. src/plannin…
        REC: Move the resolve-then-compose step into the Module (e.g. an explicit `resolvePlanningRoot(cwd)` entry point) and record the monorepo carve-out as a decision in this ADR rather than as a code comment.
    [medium|lacuna] The ADR asserts workstream naming is 'one invariant across create/migrate/set/get/env-pointer paths' but never says where that invariant is enforced — and as built it is enforced at the CLI adapter, not at the seam, which inverts the ADR's own deep-module/thin…
        EV: src/workstream-name-policy.cts:88 `isValidActiveWorkstreamName` is called from src/active-workstream-store.cts:42 (get/set/clear) and src/workstream.cts:357 (create/migrate). The env path is validated only in `resolveActiveWorkstream` (src/active-workstream-store.cts:322-324), which is invoked from …
        REC: Have `planningDir` run the same `isValidActiveWorkstreamName` on the env-sourced value, and state in the ADR which layer is authoritative.
    [medium|lacuna] 'Worktree metadata cleanup remains non-destructive by default' names a default without ever defining the non-default: no ADR says who may pass `allowDestructive: true`, under what evidence, or with what user consent.
        EV: docs/adr/0004:16 says prune 'runs `git worktree prune` only and does not remove linked worktree directories'. src/worktree-safety.cts:1379 hardcodes `{ allowDestructive: false }`, but `planWorktreePrune` accepts the flag, so a destructive path is designed-in and unreachable-by-policy rather than abs…
        REC: Either delete the `allowDestructive` parameter (the honest form of 'we decided not to') or write its gate the way ADR-0008 writes its own.
    (+1 low)

--- docs/adr/0006-planning-path-projection-module.md [SHOULD_BE_SUPERSEDED] Accepted
    STATUS-EV: Every named symbol in the Decision is absent. Grep across src/ and tests/ for `resolveWorkspaceContext`, `workspacePlanningPaths`, `initExecutePhase`, `initPlanPhase`, `initPhaseOp`, `initMilestoneOp`, `relPlanningPath` returns zero hits. `helpers.planningPaths` does not exist (no `sdk/` tree; `ls sdk` → No such file o…
    [high|status-drift] An ADR whose entire subject is the SDK query-handler layer is still Accepted and indexed as active, six ADR-generations after that layer was deleted.
        EV: docs/adr/0006:1 title 'Planning Path Projection Module for SDK query handlers'; :3 Status Accepted. ADR-0174 (Accepted 2026-05-23) retired the SDK package and states 'There is no `sdk/` tree' (echoed at docs/adr/README.md:216). Its Supersedes table lists only 0005/0007/0012/3524. docs/adr/README.md:…
        REC: Mark `Superseded by ADR-0174`, or — since one of its invariants is still load-bearing (see below) — rewrite it as a single-runtime ADR naming `src/planning-workspace.cts` and fold it into ADR-0004.
    [medium|relation-asymmetry] The hand-maintained seam map cross-references ADR-0006 as live in the same section where it announces that the SDK is gone.
        EV: docs/adr/README.md:216 'The SDK seam map is gone. ADR-0005 ... is superseded by ADR-0174 ... There is no `sdk/` tree.' Two lines later, README.md:218: 'ADR-0006 documents how query handlers project planning paths (`cwd → effectiveRoot → .planning/<project>/...`). Cross-reference the Planning Workspa…
        REC: Rewrite README.md:218 to point at ADR-0004 / src/planning-workspace.cts once ADR-0006's status is resolved.
    [medium|consequence-drift] The one invariant of this ADR that survived — `.planning/<project>`, never `.planning/projects/<project>` — is honoured in exactly one place and silently contradicted elsewhere, so the ADR's core promise ('One fix in planning path policy updates all handlers')…
        EV: docs/adr/0006:14 'SDK project scope for planning is `.planning/<project>` (never `.planning/projects/<project>`), aligned with CJS planning workspace behavior.' The shape is honoured at src/planning-workspace.cts:127-129 (`base = path.join(base, project)`), and nowhere else: src/configuration.cts:74…
        REC: Before retiring this ADR, migrate the two private composers; otherwise the invariant loses its only written record.
    [medium|lacuna] The ADR states a precedence ('explicit workstream > env workstream > env project > root') that mixes two independent axes as if they were one ordered chain — and the as-built code treats them as independent, so the written rule cannot be checked against anythi…
        EV: docs/adr/0006:12 'Policy precedence is explicit and stable: `explicit workstream > env workstream > env project > root`.' src/planning-workspace.cts:122-130 resolves *project* and *workstream* independently — each falls back to its own env var — and then composes both segments. There is no case in w…
        REC: Restate as two independent resolutions (project: explicit > GSD_PROJECT > none; workstream: explicit > GSD_WORKSTREAM > stored pointer > none) and note that they compose.
    [medium|lacuna] Multi-project scoping is a documented, user-facing feature with no owner: exactly one code site implements it, nothing writes it, and no ADR decides what it means for the pointer store, the lock, or the config.
        EV: `GSD_PROJECT` is read at exactly one place in the tree — src/planning-workspace.cts:123 — and is set nowhere (grep across src/*.cts, bin/install.js, gsd-core/bin/gsd-tools.cjs finds only that read and its adjacent validation at :129). It is nonetheless published to users at docs/CONFIGURATION.md:164…
        REC: Decide it explicitly: either promote multi-project scope to a real feature with a single composer and a documented interaction with workstreams/locks/config, or remove `GSD_PROJECT` and its doc row.

--- docs/adr/0008-installer-migration-module.md [ACCURATE] Accepted
    [high|consequence-drift] The Runtime Contract Decision covers three trigger classes but the guard enforces only one, and two shipped migrations that hit the uncovered classes carry no registry citation.
        EV: docs/adr/0008:22-27: 'Every migration that rewrites runtime config, **moves an invocation surface**, or **retires a generated runtime artifact** must cite the registry row ... If the migration changes where a runtime loads commands, skills, agents, hooks, or rules, the PR must update both the regist…
        REC: Extend the guard: require `runtimeContract` whenever the record declares a runtime scope and any action is `remove-managed`, `backup-and-remove`, `record-baseline`, or `baseline-preserve-user` — i.e. cover 'moves an invocation surface' and …
    [medium|consequence-drift] The citation is validated as a non-empty string only — nothing checks the cited anchor exists, so the registry link can rot undetected.
        EV: src/installer-migration-authoring.cts:127-130 throws only when `getStr(migration,'runtimeContract')` is empty. The single live citation is the string `'docs/installer-migrations.md#runtime-configuration-contract-registry'` (002-codex-legacy-hooks-json.cts:116). The anchor does currently resolve (doc…
        REC: Add an anchor-resolution check to the guard or to `lint:generated-sync`; the repo already has `scripts/check-glossary-refs.cjs` as a precedent for exactly this shape of check.
    [medium|consequence-drift] The 'detailed module contract' the ADR delegates to has drifted from the code: it specifies action types that were never implemented, and the one it calls provisional is the only one that exists.
        EV: docs/adr/0008:50 'The detailed module contract lives in `docs/installer-migrations.md`.' That doc's Action Types section documents `move-managed` (:201, 'Use for layout migrations such as command directories moving into skills') and `rewrite-config` (:208) as first-class types, with `rewrite-json` d…
        REC: Either implement `move-managed` or mark it Not Implemented in docs/installer-migrations.md with the 003 precedent as the substitute pattern — a contract doc that specifies unbuilt primitives will get cited by a future author.
    [medium|lacuna] 'Migration authors must define ... non-interactive behavior' is listed as a consequence, but the guard does not require it and no ADR states the non-TTY default per action type.
        EV: docs/adr/0008:42 'Migration authors must define ownership evidence, conflict behavior, runtime scope, and non-interactive behavior.' src/installer-migration-authoring.cts validates ownership evidence (:123-125) and scopes (:94) but has no field for conflict behavior or non-interactive behavior. The …
        REC: Add a required `nonInteractive: 'preserve' | 'remove'` field to any record emitting `prompt-user`, enforced in the guard, and record the default in the ADR rather than in a session log.
    (+2 low)

--- docs/adr/0009-shell-command-projection-module.md [AMBIGUOUS] Accepted
    STATUS-EV: The Module is real and load-bearing — src/shell-command-projection.cts (33.6K) → gsd-core/bin/lib/shell-command-projection.cjs (27.2K) — and the file I/O expansion is genuinely built (`platformWriteSync` :823, `platformReadSync` :854, `platformEnsureDir` :868, `normalizeContent` :760, `retryRenameSync` :818). But the d…
    [high|consequence-drift] The seam now contains a full GSD command dispatcher — a third scope reversal, unrecorded in the ADR, that directly contradicts ADR-0174's decision that the Command Routing Hub is the dispatch seam.
        EV: src/shell-command-projection.cts:624 `export function dispatchGsdCommand({...})`, with `resolveGsdToolsPath()` at :585 and a `DispatchGsdCommandResult` contract at :567. Its docblock (:588-596) asserts: 'Subprocess-shim dispatch to gsd-tools.cjs (ADR-1239 #2102 Stage 2). No fully-populated in-proces…
        REC: Amend ADR-0009 to record the dispatch expansion and its relation to ADR-0174, or relocate `dispatchGsdCommand` out of the projection seam. The code comment currently carries an architectural claim ('no fully-populated in-process hub exists'…
    [high|consequence-drift] The atomic-write duplication that Phase 4 claims to have eliminated has re-grown in a second module, with materially different failure semantics.
        EV: docs/adr/0009:123 'legacy wrappers `atomicWriteFileSync` / `safeReadFile` / `normalizeMd` removed from `core.cjs` (Phase 4, `#3468`)'; CONTEXT.md:846 repeats it as an invariant. But src/runtime-hooks-surface.cts:186 defines `function atomicWriteFileSync(target, data, options)` under the header comme…
        REC: Either route runtime-hooks-surface through `platformWriteSync` (adding the tmp-tracking Set to the seam), or amend ADR-0009 and CONTEXT.md:846 to state that a second sanctioned atomic-write implementation exists and why.
    [high|consequence-drift] The entire Migration Inventory is stale, and — more importantly — the surfaces it lists did not migrate into this seam. They migrated into `src/runtime-hooks-surface.cts`, a module this ADR never names.
        EV: Every cited line number in docs/adr/0009:38-52 is wrong: bin/install.js:605-608 (claimed `formatHookCommandForShell`) is `--minimal`/`--profile` arg parsing; :8855-8857 (claimed `localCmd()`) is a JSONC parser comment; :10487-10510 (claimed `buildWindowsShimTriple`) is Kimi verification-style branch…
        REC: Amend ADR-0009 to record that the managed-hook rendering surface went to a Runtime Hooks Surface Module instead, and delete the line-numbered inventory (line numbers in a 13,486-line file are guaranteed to rot).
    [medium|consequence-drift] CONTEXT.md declares the seam the 'Single seam for platform-conditional logic', but 23 direct subprocess call sites remain outside it in 8 modules, with no stated bypass policy.
        EV: CONTEXT.md:841 'Single seam for platform-conditional logic — one place to fix any shell or file write regression across Windows, macOS, and Linux', and :844 'Platform policy owned at the seam: `shell: process.platform === 'win32'` lives only in execNpm'. Counting `spawnSync|execFileSync|execSync` in…
        REC: State the rule: either 'all subprocess execution goes through execGit/execNpm/execTool' (and add an ESLint rule, as the repo already does for `local/require-fs-op-fallback` — cited at src/shell-command-projection.cts:817), or name the sanct…
    [medium|quality] An Accepted ADR written in proposal voice that twice supersedes its own Decision inline. The corpus has a working supersession pattern (ADR-0010) and this ADR chose self-amendment instead, which destroys the reader's ability to know which constraints are live.
        EV: docs/adr/0009:3 'Status: Accepted'; :7 'We **propose** introducing a Shell Command Projection Module'; :34 'It should **not** in the first pass expand into...'; then :116 'The seam grew beyond the original "rendering only" scope. The "does not become a generic command runner" and "does not replace s…
        REC: Split: keep ADR-0009 as the rendering decision and open a successor ADR for the OS-I/O seam (exec* + platform fs + dispatch). One ADR that has absorbed three unrelated scopes cannot be cited precisely.
    [medium|lacuna] Three of four Open Questions were never resolved and have no owner or deadline; the fourth was resolved inside the ADR without a corresponding entry anywhere a maintainer would look.
        EV: docs/adr/0009:127-130: 'Q4 (installer-only vs shared seam): **resolved — shared.** ... Q1, Q2, Q3 (`hooks.shell_preference`, Windows Git Bash modeling, shim/script builder migration timing): unresolved, carried forward as projection-design concerns independent of the I/O expansion.' Q2 (Windows Git …
        REC: File the three as issues and link them from the ADR, or close them with a stated answer. 'Carried forward' with no ticket is how a design question becomes a recurring bug class.
    [medium|lacuna] The seam owns write-availability policy — when it is acceptable to abandon atomicity — and that policy is decided only in code comments, never in the ADR.
        EV: src/shell-command-projection.cts:837-851 encodes a three-way decision: tmp-write failure → non-atomic direct write ('nothing was published, so a direct fallback write cannot truncate a concurrent reader'); rename failure with a Windows lock errno → throw ('A non-atomic direct write here would trunca…
        REC: Promote the three-branch write policy into the ADR (or the successor). It is the seam's most consequential rule and currently exists only as a comment one refactor away from deletion.
    (+1 low)

>> CLUSTER OBS [high]: The ADR-0174 supersession sweep was incomplete along exactly the SDK axis. It enumerated four superseded ADRs (0005, 0007, 0012, 3524) and missed the three seam ADRs whose subject matter was the SDK itself — 0001 (SDK dispatch/runtime bridge), 0003 (dual-package catalog shipping), and 0006 (SDK query-handler path projection). All three still declare Accepted and all three sit in the generated inde…
   EV: docs/adr/0174-retire-gsd-sdk-package-boundary.md 'Supersedes' table = {0005, 0007, 0012, 3524}. docs/adr/0005-sdk-architecture-seam-map.md:12-15 delegates to 0001, 0003, 0004, 0006 by name. docs/adr/README.md:125, :127, :129 list 0001/0003/0006 as Accepted active decisions. Zero repo hits for the SDK-era symbols those …

>> CLUSTER OBS [high]: The seam pattern is coherent as vocabulary but has no enforcement, and every ownership invariant in this cluster that is not backed by a lint has drifted. ADR-0002 is the only cluster member with a named CI check that cites the ADR back at violators — and it is the only one whose invariants hold. The other six assert ownership in prose and each has measurably lost it.
   EV: Enforced: scripts/lint-command-contract.cjs:110 prints 'See docs/adr/0002-command-contract-validation-module.md for the contract spec.', wired at package.json:106; runs clean (71 files, 0 violations). Unenforced and drifted: ADR-0004 'The Module owns `.planning` path resolution' vs 53 `path.join(..., '.planning')` site…

>> CLUSTER OBS [medium]: The corpus has no rule for when an amendment becomes a new ADR, and the cluster shows both outcomes side by side. ADR-0010 was properly superseded by ADR-0009 with reciprocal, dated pointers and an explicit record of what was *not* built. ADR-0009 then twice reversed its own Decision in place, and a third reversal shipped with no amendment at all. Same maintainers, same month, opposite discipline …
   EV: Good: docs/adr/0009:4 ↔ docs/adr/0010-file-operation-engine-module.md:3,:5,:7 (reciprocal, dated, with a 'what was not built' note). Bad: docs/adr/0009:116 'The "does not become a generic command runner" and "does not replace safe internal subprocess APIs" constraints (Decision §17, Initial Scope §33) were intentionall…

>> CLUSTER OBS [medium]: CONTEXT.md — described in project instructions as the machine-greppable domain canon — drifted in lockstep with the ADRs rather than acting as a check on them. It carries eight live glossary entries for retired SDK-era modules, and it also restates an ADR-0009 invariant that the code has since broken. An agent grepping CONTEXT.md for the current dispatch seam gets 2026-05 answers.
   EV: CONTEXT.md:29 'Dispatch Policy Module', :43 'Query Runtime Context Module', :46 'Native Dispatch Adapter Module', :49 'Query CLI Output Module', :70 'Command Topology Module', plus Query Execution Policy / Query Subprocess Adapter / Query Command Resolution — none of which exist in src/ or gsd-core/bin/lib/. Only CONTE…

>> CLUSTER OBS [medium]: Seam ADRs in this cluster consistently decide *ownership* and consistently omit *composition* — who calls the seam, in what order, and what the entry-point must do before delegating. That gap is where the concrete defects in this cluster live.
   EV: ADR-0004 makes planning-workspace authoritative for `.planning` resolution but the worktree-root resolve-then-compose step lives in gsd-core/bin/gsd-tools.cjs:2379-2387 with an undocumented monorepo escape hatch ('if cwd/.planning exists, skip worktree resolution'); the Module's `planningDir` (src/planning-workspace.ct…

>> CLUSTER OBS [low]: Two cluster members are not decision-shaped. ADR-0001 is a changelog (one decision paragraph plus two amendment blocks, one of which is a bare list of five deleted filenames) with no Context, alternatives, or Consequences. ADR-0009 is a design doc in proposal voice (an Interface sketch, a line-numbered migration inventory, four Open Questions) carrying an Accepted status. Neither records alternati…
   EV: docs/adr/0001-dispatch-policy-module.md is 42 lines: :6 decision, :8-29 and :31-41 amendments, :20-28 a five-filename deletion list; no `## Context`, `## Alternatives`, or `## Consequences`. docs/adr/0009:7 'We propose introducing...', :64-89 'Interface sketch', :36-62 'Migration Inventory' with bin/install.js line num…

>> NO-ADR: Runtime Hooks Surface Module (src/runtime-hooks-surface.cts, 2358 lines) — ownership of managed-hook lifecycle across Cline, Cursor, Copilot, and Codex
   WHY: This module owns exactly the surfaces ADR-0009 decided belonged to the Shell Command Projection Module — managed hook commands in settings.json, managed hook commands in runtime config JSON/TOML, and generated shim/wrapper script text (ADR-0009:12-16, Initial Scope items 1-6). A … | EV: src/runtime-hooks-surface.cts:3-18 header: 'Runtime Hooks Surface Module — hook-surface writer functions extracted from bin/install.js (ADR-857 phase 5f-1) ... Shared: buildHookCommand, rewriteLegacyManagedNodeHookCommands' and :19 'BEHAVIO…

>> NO-ADR: In-process Hub dispatch vs. subprocess re-entry into gsd-tools.cjs (`dispatchGsdCommand`)
   WHY: ADR-0174 §4-§5 decided that `CommandRoutingHub.dispatch<T>()` is the dispatch seam, owning four cross-cutting concerns including the uniform error contract and DispatchEvent/traceId observability, and made adding a Result variant an ADR-amendable act. A shipped code path now bypa… | EV: src/shell-command-projection.cts:588-596 docblock, and :567-580 the `DispatchGsdCommandResult` shape (`{ok, stdout, stderr, code, timedOut}`) versus ADR-0174 §5's `Result<T>` discriminated union in src/command-routing-hub.cts. docs/adr/0174…

>> NO-ADR: Multi-project `.planning/<project>` scoping (`GSD_PROJECT`) — whether it is a feature, and who composes the path
   WHY: This is a documented, user-facing environment variable that changes where every planning artifact lives, and it has no ADR, no writer, and no consistent reader. ADR-0006 states the shape in one line as an SDK detail and is itself defunct. Exactly one site honours it; two other pl… | EV: docs/CONFIGURATION.md:1646 '`GSD_PROJECT` | Override project root for multi-project workspace support (v1.32)' (also zh-CN:1264, pt-BR:1295). Read at exactly one site: src/planning-workspace.cts:123, validated at :129; set nowhere in src/, …

>> NO-ADR: The seam-Module vocabulary itself — what counts as a Module vs. an Adapter vs. an Interface, when a Module requires an ADR, and what retires one
   WHY: Seven ADRs in this cluster and roughly a dozen CONTEXT.md glossary entries are written in a specific vocabulary ('Module owns X', 'thin Adapters', 'Interface', 'depth/leverage/locality') that no document defines. The nearest thing to a definition was ADR-0005, which ADR-0174 supe… | EV: docs/adr/0174-retire-gsd-sdk-package-boundary.md Supersedes table on ADR-0005: 'the seam-Module vocabulary survives intact under a single `src/` tree' — with no restatement of what the vocabulary requires. docs/adr/README.md:212 'Orientatio…

>> NO-ADR: Subprocess-execution bypass policy — when a module may call spawnSync/execFileSync directly instead of the projection seam's execGit/execNpm/execTool
   WHY: ADR-0009's 2026-05-13 Update claims subprocess dispatch as owned scope and CONTEXT.md elevates that to an invariant ('Single seam for platform-conditional logic'), but 23 direct call sites in 8 modules say otherwise and nothing anywhere states whether they are violations, grandfa… | EV: CONTEXT.md:841 and :844 state the single-seam invariant. `grep -rn 'spawnSync|execFileSync|execSync' src/*.cts | grep -v shell-command-projection | wc -l` → 23, across src/capability-source.cts, src/check-command-router.cts, src/gate-predic…

==============================================================================
## AGENT: a1d2c6ca4b5a165d8
==============================================================================

--- docs/adr/857-capability-system.md [ACCURATE] Accepted — ratified 2026-07-17 (originally Proposed 2026-06-08)
    [high|lacuna] The 12 Loop Extension Point names are declared a stability contract, but nothing decides — or checks — that a declared point is actually *queried* by the host. `execute:pre` is fully declared and completely dead: a capability registering a hook there would sil…
        EV: `execute:pre` is in the canonical list (scripts/gen-loop-host-contract.cjs:43), declared by the workflow marker (gsd-core/workflows/execute-phase.md:3 `points: execute:pre, execute:wave:pre, execute:wave:post, execute:post`), and indexed in the registry (`byLoopPoint['execute:pre'] = {steps:0, contr…
        REC: Decide and record the point-liveness rule: either every canonical point MUST have a host call site (add a CI gate in gen-loop-host-contract's --check comparing declared `points:` against actual `loop render-hooks <point>` call sites in the …
    [medium|contradiction] The ratification evidence asserts a fact that is false in the tree: 'All 12 named points … have live render-hook call sites in the host loop.'
        EV: ADR-857:19 (quoted). Actual call-site census over gsd-core/workflows/: discuss:pre 1, discuss:post 1, plan:pre 5, plan:post 1, execute:wave:pre 1, execute:wave:post 1, execute:post 7, verify:pre 1, verify:post 6, ship:pre 1, ship:post 1 — **execute:pre 0**.
        REC: Correct the ratification bullet to '11 of 12 points have live call sites; execute:pre is declared-but-unqueried' and link the decision from the lacuna above. A ratification section whose evidence is checkable and wrong undermines the whole …
    [medium|contradiction] §'What this ratification does not settle' asserts ADR-959 'stays Proposed' — but ADR-959 was graduated to Accepted on the same date by ADR-2346, and issue #2346 is closed.
        EV: ADR-857:36 'ADR-959 (command contribution) stays `Proposed`: issue #2346 … is OPEN and maintainer-approved'. Against: docs/adr/959-capability-command-contribution.md:3 '**Status:** Accepted (graduated from Proposed by ADR-2346, 2026-07-17)'; docs/adr/2346-command-dispatch-completion.md:7 records the…
        REC: Replace the paragraph with a pointer to ADR-2346 as the capstone that graduated 959.
    [medium|relation-asymmetry] ADR-1244 D8 states 'ADR-857 is updated to mark D7 and D8 "extended by ADR-1244"'. That update was never made — the pointer is one-way.
        EV: docs/adr/1244-capability-ecosystem.md:147 (D8, quoted) and its own self-acknowledgement at :27 ('the D8 cross-reference promised back into ADR-857 … was never written'). `grep -n 1244 docs/adr/857-capability-system.md` returns exactly one hit, at line 23, an incidental quotation of 1244's capability…
        REC: Add `- **Extended by:** [ADR-1244] (D7 third-party code-loading, D8 third-party runtime loading — deferral now delivered behind a trust gate)` to the 857 header and annotate Decisions 7 and 8 inline.
    [medium|lacuna] Decision 6's default-resilient failure ('a non-gate hook that errors is skipped with a warning') is decided with no observability owner: nothing decides where the warning goes, whether skips are recorded, or how a user learns a feature silently did not run.
        EV: ADR-857:69 and :155 name the 'small "silent skip" risk' as an accepted cost but no consequence, rollout step, or open question assigns it a surface. `src/loop-resolver.cts` resolves and filters hooks; no ledger/telemetry of skipped hooks appears in the capability modules (grep of capability-*.cts fo…
        REC: Record a decision on skip observability (e.g. `loop render-hooks` returns a `skipped[]` the orchestrator must render), or explicitly accept silence and say so.
    (+2 low)

--- docs/adr/894-capability-declaration-format.md [ACCURATE] Accepted — ratified 2026-07-17 (originally Proposed 2026-06-08)
    [high|lacuna] §2 fixes the capability.json schema but never decides whether the schema is CLOSED (unknown top-level keys rejected) or OPEN. It is open — and an executable surface with the largest blast radius, `mcpServers`, therefore reaches shared runtime config without ev…
        EV: `grep -n 'unknown field|unrecognized|KNOWN_TOP_LEVEL|ALLOWED_KEYS|unknown key' gsd-core/bin/lib/capability-validator.cjs` → 0 matches; `grep -rn mcpServers gsd-core/bin/lib/capability-validator.cjs scripts/gen-capability-registry.cjs` → 0 matches. Yet src/capability-lifecycle.cts:593 reads `manifest…
        REC: Decide it explicitly: either close the schema (reject unknown top-level keys, and add `mcpServers` as a validated field with a shape) or record why it is deliberately open and what compensating control covers unvalidated executable declarat…
    [medium|contradiction] §3's load-bearing claim — 'The contract is generated from the workflows, not hand-authored — so it cannot drift into a lie' — overstates what the generator proves. The marker block is hand-authored prose inside the workflow, and the generator only checks it ag…
        EV: scripts/gen-loop-host-contract.cjs:38-51 hardcodes `CANONICAL_POINTS`; :54-60 hardcodes `EXPECTED_POINTS_BY_STEP`; :95-107 parses the hand-written `<!-- gsd:loop-host -->` block. The only cross-check performed is agent-role→agent-reference (`ROLE_TO_AGENT`, :65-71). Live proof of the residual drift:…
        REC: Either add the call-site cross-check (the generator already reads the file, so this is cheap) or soften §3 to 'the contract is generated from a declaration co-located with the workflow', which is what it actually is.
    [medium|consequence-drift] §4's '`tier` is the source of profile/cluster membership … Install profiles and surface clusters are **generated** from capability tier + the requires-closure' is only half-delivered: surface clusters remain hand-authored and the registry contribution is addit…
        EV: `src/clusters.cts` still exports a hand-authored frozen `CLUSTERS` map (header: 'Skill cluster definitions … Behaviour is preserved byte-for-behaviour from the prior hand-written .cjs'; `export const CLUSTERS: ClusterMap = Object.freeze({…})`). src/install-profiles.cts:204-262 shows `_capabilitySkil…
        REC: Amend §4 to state the actual model (registry-derived membership unioned with a retained hand-authored cluster map) and either schedule the collapse or record it as accepted.
    (+2 low)

--- docs/adr/959-capability-command-contribution.md [ACCURATE] Accepted (graduated from Proposed by ADR-2346, 2026-07-17)
    [medium|consequence-drift] §5's 'first-party only; third-party deferred' rule — '`module` is confined to `gsd-core/bin/lib/` and `require`d in-process' — has been superseded in the tree by an overlay dispatch path that requires from a third-party install root, and this ADR records no am…
        EV: gsd-core/bin/gsd-tools.cjs:2685 exports `{ dispatchCapabilityCommand, dispatchOverlayCapabilityCommand, defaultRequireFromInstallRoot, dispatchHostCommand, HOST_COMMAND_ROUTERS }`. `defaultRequireFromInstallRoot` is precisely the out-of-tree loader §5 defers. ADR-1244 D7 ('Registry-driven dispatch ……
        REC: Add a reciprocal amendment: 959 gains `- **Extended by:** [ADR-1244] D7 (third-party overlay dispatch — §5's first-party-only confinement now applies to the first-party layer only)`, and 1244 D7 cites 959 as the mechanism it extends.
    (+2 low)

--- docs/adr/1016-runtime-capability-descriptor.md [ACCURATE] Accepted
    [high|consequence-drift] The ADR's core principle is that the vocabulary is CLOSED and that widening it is a reviewed first-party act. The vocabulary has widened four times in code with no amendment here — so the ADR is no longer the record of the closed sets it decides.
        EV: Validator vs ADR: `VALID_CONFIG_HOME_KINDS` (capability-validator.cjs:709) includes `'none'` — D1 lists 4 kinds, not 5 (used by vscode). `VALID_HOOKS_SURFACES` (:711) includes `'kimi-hooks-toml'` and `'windsurf-hooks-json'` — D5 declares a 6-value enum containing neither. `VALID_INSTALL_SURFACES` (:…
        REC: Either add an amendment log to this ADR (the way ADR-894 handles #1634) and a CI check that the validator enum sets match a table in the ADR, or explicitly demote the ADR from 'source of the closed vocabulary' to 'rationale for closing it',…
    [high|consequence-drift] Gemini was removed as a supported runtime, but this ADR still makes gemini-specific decisions across four sections and names a converter function that no longer exists.
        EV: `git log --oneline -- capabilities/gemini` → `8f2ebbe9 feat(#1928): remove sunset Gemini CLI runtime, redirect to Antigravity (#1996)`; there is no `capabilities/gemini/`. Yet ADR-1016:27 lists gemini among 'GSD installs into 16 host CLIs'; :88 names `convertClaudeCommandToGeminiToml` as one of '15 …
        REC: Add a dated amendment: gemini retired (#1928/#1996); the `hookEvents: 'gemini'` dialect now has exactly one consumer (antigravity — confirmed in its descriptor) and the Gemini agent-event trio has zero. Refresh the runtime roster to 19.
    [medium|consequence-drift] D5's per-runtime assignments are wrong for two runtimes: windsurf and kimi are listed as registering zero managed lifecycle hooks; both now carry dedicated hook surfaces.
        EV: ADR-1016:104 '`none` = windsurf, trae, kimi, kilo, opencode — these five runtimes register **zero** managed lifecycle hooks today'. Actual descriptors: windsurf `hooksSurface: windsurf-hooks-json`, kimi `hooksSurface: kimi-hooks-toml` (and kimi additionally carries `extendedHookEvents: [SubagentStop…
        REC: Update the D5 roster, or replace per-runtime rosters in the ADR with a pointer to the generated `docs/reference/capability-matrix.md`, which is drift-guarded (tests/capability-matrix-sync.test.cjs) and this prose is not.
    [medium|consequence-drift] D8's escape hatch for the `CODEX_AGENT_SANDBOX` registration-tax map points at an issue that has since closed COMPLETED without removing the map — so the ADR's only tracking vehicle for a named residue is now dead.
        EV: ADR-1016:171 'full removal of that registration-tax map is tracked under #1138 (phase-6 descriptor-residue removal)'. `gh issue view 1138` → {state: CLOSED, stateReason: COMPLETED, title: 'enhancement: phase 6 remove runtime descriptor residue — ADR-857'}. The map is still at bin/install.js:330 (`co…
        REC: Either re-file the residue removal or amend D8 to record that `CODEX_AGENT_SANDBOX` is permanently GSD agent policy rather than descriptor residue — the ADR already argues the latter, so this may just be a matter of deleting the stale track…
    [medium|contradiction] The Out-of-scope section delegates the structural 'off means off' rule to ADR-894, stating it is 'recorded there'. It is not recorded there — or anywhere.
        EV: ADR-1016:203 'The structural "off means off" rule (the host derives shared outputs from the active hook set; hooks add or are counted, never mutate host source) — an ADR-894 contribution-model concern, recorded there, not in this ADR.' `grep -in 'off means off' docs/adr/894-capability-declaration-fo…
        REC: Write the rule down — in ADR-894's contribution model, per 1016's own routing — or delete the false cross-reference. As it stands a reader chasing a named invariant reaches a dead end.
    [high|lacuna] D9 names the Connected Capability contract as 'a named, tracked gap' with #956 as its design vehicle. The vehicle is closed, no ADR of any number uses the term, and the archetypal Connected Capability shipped as an ordinary feature capability — so the gap is n…
        EV: ADR-1016:180, :201. `gh issue view 956` → {state: CLOSED, stateReason: COMPLETED, title: 'Pre-Proposal: first-party MemPalace plugin capability (PRD/ADR draft)'}. `grep -rln 'Connected Capability' docs/adr/` → only 1016-runtime-capability-descriptor.md. `capabilities/mempalace/capability.json` keys …
        REC: Re-open or re-file the Connected Capability contract as an approved-* issue, or record in this ADR that the concept was dissolved (MemPalace absorbed as a declarative feature capability) so the term stops implying an owned, pending design.
    (+1 low)

--- docs/adr/1213-capability-state-writer.md [STALE_PROPOSED] Proposed
    STATUS-EV: Every acceptance signal is green except a documentation edit the ADR itself specifies. `setCapabilityState` at src/capability-writer.cts:140, `cmdCapabilitySet` at :451; landing commit `bf634b95 feat(#1213): Capability State Writer — write-side inverse of the resolver (#1225)` confirmed ancestor of HEAD (`git merge-bas…
    [high|contradiction] The Decision section still specifies a two-field return and a warnings-only divergence channel; the shipped module has three fields and a hard-failure channel that exits non-zero. The ADR as written misdescribes the interface it decided.
        EV: ADR-1213:39 '`-> { capabilities: CapabilityStateEntry[]; warnings: string[] }`' and :49 'divergence … is returned as `warnings`, not silently swallowed'. Shipped: src/capability-writer.cts:113 `interface SetCapabilityStateResult {` with `errors` documented at :104 ('operation-not-applied messages; t…
        REC: Perform the ADR's own stated unblock: revise the signature and item 4, add `- **Amended by:** [ADR-1411]`, then ratify. This is the cheapest high-value fix in the cluster.
    [medium|relation-asymmetry] ADR-1411 amended this ADR's decided interface, but neither file records the relation in its header and ADR-1411 does not mention ADR-1213 at all.
        EV: ADR-1213:13 correctly cites 'ADR-1411 … records the mutation-verb shape as `{ capabilities, warnings, errors }` (ADR-1411 lines 81, 86)' — verified: docs/adr/1411-resolution-provenance.md:81 ('`capability-writer`'s `errors[]` (operation-not-applied) is load-bearing and cannot fold into `warnings[]`'…
        REC: Add `- **Amends:** [ADR-1213] (the mutation-verb return shape)` to ADR-1411 and the reciprocal `Amended by` to ADR-1213. A superseding interface decision recorded only in the superseded ADR's prose body is invisible to the index generator (…
    [medium|lacuna] Both Open questions remain open and unowned, and one of them is the invariant this ADR exists to guarantee.
        EV: ADR-1213:81-82: (a) whether `enabled: true` below the install floor auto-adds to `explicitAdds` or warn-and-refuses — the code chose refuse (src/capability-writer.cts:231 `its skills are not in the install profile` pushed to `errors`, aborting the write) but the ADR never records the resolution; (b)…
        REC: Close (a) by recording the shipped choice (refuse) — it is a user-visible behaviour discoverable only from an error string. Decide (b) explicitly; 'the resolver is the writer's test surface' (item 4) is only an invariant if something assert…
    (+2 low)

--- docs/adr/1244-capability-ecosystem.md [ACCURATE] Accepted — ratified 2026-07-17 (originally Proposed 2026-06-14)
    [high|consequence-drift] D5 rule 8 binds project consent to 'the bundle integrity + disclosure signature'. The shipped consent store deliberately binds to NEITHER — it binds to a recomputed full-bundle content hash — and the code's own header explains that the ADR's binding is insecur…
        EV: ADR-1244:130 '…keyed by `(realpath(projectRoot), id)` and **bound to the bundle integrity + disclosure signature**.' Against src/capability-consent.cts:12-22: 'The consent record is bound to a RECOMPUTED full-bundle content hash (`bundleContentHash`), **NOT** to the ledger `integrity` (which is `''`…
        REC: Amend D5 rule 8 to the shipped binding (`bundleContentHash` recomputed over manifest + artifacts + identity at load) and record the two attacks the ADR's original binding permitted. Leaving the ADR stating a weaker rule than the code enforc…
    [medium|missing-artifact] D3 decides five source adapters; only four exist. The `<name>@<registry>` adapter is unimplemented, and the ratification block reports 'the four adapters' without flagging that a decided fifth is missing.
        EV: ADR-1244:88 table row '`<name>@<registry>` | registry'; :19 ratification text 'backed by the four adapters `resolveLocal` (861), `resolveGit` (892), `resolveNpm` (944), `resolveTarball` (1017)'. In the tree the kind is parsed (`type SpecKind = 'registry' | 'git' | 'npm' | 'tarball' | 'local'` at src…
        REC: Record it: either D3 is amended to four adapters with `registry` deferred (and D9's 'whether GSD operates a central registry is TBD' is the reason), or the adapter is scheduled. A user reading D3 will write a `<name>@<registry>` spec and ge…
    [high|lacuna] D5 rule 2 makes `mcpServers` a first-class disclosed executable surface, but no ADR ever adds it to the capability manifest schema — so the highest-risk third-party surface is written into the user's shared settings.json after passing a disclosure routine but …
        EV: ADR-1244:124 names `hooks`, `mcpServers`, and command modules as the executable surfaces requiring install-time consent including each server's `env`/`cwd`. `grep -rn mcpServers gsd-core/bin/lib/capability-validator.cjs` → 0 matches; the validator has no unknown-top-level-key rejection either (`grep…
        REC: Add `mcpServers` to the ADR-894 §2 schema with a validated shape (command/args/env/cwd), and decide the unknown-key policy. This is the concrete content of the OPEN epic #1900 ('MCP arg/cwd confinement') and is currently unscheduled.
    [medium|relation-asymmetry] D8's central relational act — annotating ADR-857's Decisions 7 and 8 as 'extended by ADR-1244' — was never performed. The ADR ratified with the gap acknowledged but unfixed.
        EV: ADR-1244:147 (D8) and its own ratification caveat at :27 ('the D8 cross-reference promised back into ADR-857 … was never written — `docs/adr/857-capability-system.md` has no mention of ADR-1244'). Independently confirmed: `grep -n 1244 docs/adr/857-capability-system.md` → one hit at :23, an incident…
        REC: Write the annotation. D8 is not an aspiration — it is a decision whose entire content is a documentation edit, so an unwritten D8 is an unimplemented decision.
    [medium|quality] The ratification block hands off the highest-risk residual — the security hardening of the surface this ADR created — to an epic whose three children were all closed NOT_PLANNED, with no owner and no schedule.
        EV: ADR-1244:27 'epic #1900 (ADR-1244 edge hardening: MCP arg/cwd confinement, tarball/registry SSRF denylist, duplicated injection patterns) remains OPEN with all three of its filed children (#1901, #1902, #1903) closed `NOT_PLANNED` … the hardening itself is not yet scheduled.' Verified: `gh issue vie…
        REC: Either assign the epic or record in the ADR the risk-acceptance decision (with the reasoning that the pipeline is fail-closed). An open epic with all children NOT_PLANNED is indistinguishable from an abandoned one.
    (+1 low)

>> CLUSTER OBS [high]: The cluster's founding promise — ADR-857's 'One resolved capability state replaces three contradicting toggle systems; "off" means off' — is only partially delivered, and the corpus disagrees with itself about it. ADR-894 §4 declares clusters and profiles are *generated* from `tier`; ADR-1213 D5 quietly records that they are not; ADR-894 was never reconciled.
   EV: `src/clusters.cts` still exports a hand-authored `Object.freeze({...})` CLUSTERS map (header: 'Behaviour is preserved byte-for-behaviour from the prior hand-written .cjs'). src/install-profiles.cts:262 unions registry-derived skills into a manifest-derived profile (`const capSkills = registry ? _capabilitySkillsForMode…

>> CLUSTER OBS [medium]: The cluster's ratification blocks (857, 894, 1244, and 1213's audit section) are built on file:line citations with no drift guard, and every one of them has already decayed within ~7 days of being written. The convention the README advertises as the corpus's honesty mechanism is unmaintainable as practised.
   EV: 894:26 cites validator '2,346 lines' (now 2368) and the gate discriminator 'around lines 1566–1573' (validateGate now at :1576); 894:30 cites registry byLoopPoint/configKeys/requiresClosure at 3033/3578/5779 (exported at 6074/6075/6081); 1213:11 cites gsd-tools.cjs:1941/:2314 for CLI wiring that ADR-2346 P2 relocated t…

>> CLUSTER OBS [high]: Two of the three 'closed vocabulary' claims in this cluster are enforced in code but not recorded in the ADR that decides them — enum growth is annotated with issue numbers in the validator and never flows back. There is no policy for who amends an ADR when a closed set it owns is widened.
   EV: gsd-core/bin/lib/capability-validator.cjs:707 ('// 'none' added #2103'), :722 ('// 'none' added #2103'), :725 ('// 'antigravity' added #2096 Phase B Upgrade 1'), :727 ('// SubagentStart added #2092 Phase B Upgrade 2') — four documented widenings of ADR-1016 D1/D5/D7a enums, none reflected in ADR-1016. The validator eve…

>> CLUSTER OBS [high]: Third-party trust is decided across three ADRs with no single owner, and the seams between them contradict: ADR-894 fixes a schema that turns out to be open, ADR-959 §5 confines modules to `gsd-core/bin/lib/` while the tree ships an out-of-tree overlay require, and ADR-1244 D5 states a consent binding the code explicitly refuses as insecure. None of the three carries a relation pointer to the othe…
   EV: ADR-894 §2 schema + `grep 'unknown key' capability-validator.cjs` → 0. ADR-959:89 '`module` is confined to `gsd-core/bin/lib/` and `require`d in-process' vs `defaultRequireFromInstallRoot` exported at gsd-core/bin/gsd-tools.cjs:2685. ADR-1244:130 consent binding vs src/capability-consent.cts:12-22. Relation fields: 959…

>> CLUSTER OBS [low]: README.md's own 'lessons' prose is now stale in the same way it warns against, citing ADR-959 as a live example of an unratified ADR after ADR-2346 ratified it.
   EV: docs/adr/README.md:98 '…and [ADR-959] has an approved issue planning its graduation as its own ADR. Verify the *decision*, not just the code.' But README.md:149 in the generated table already shows ADR-959 as `Accepted`, and `gh issue view 2346` → CLOSED/COMPLETED. The generated table and the hand-written prose above i…

>> NO-ADR: Loop Extension Point liveness — must a declared point have a host call site?
   WHY: A real decision with alternatives and consequences was made by omission. The point set is a declared stability contract (ADR-857 Consequences) validated three separate ways — workflow marker, generator canonical list, registry index — none of which checks the host ever asks for t… | EV: `execute:pre` declared at gsd-core/workflows/execute-phase.md:3, canonicalized at scripts/gen-loop-host-contract.cjs:43 and :57, indexed at registry `byLoopPoint['execute:pre']`, matrix'd at scripts/gen-capability-matrix.cjs:38, resolver-te…

>> NO-ADR: Openness of the capability manifest schema (unknown top-level keys) and the status of `mcpServers`
   WHY: This is the trust boundary for third-party manifests and it was settled silently in the permissive direction. ADR-894 §2 presents the schema as the enforceable contract; ADR-1244 D5 treats `mcpServers` as a first-class executable surface requiring consent. Neither notices that th… | EV: `grep -rn mcpServers gsd-core/bin/lib/capability-validator.cjs scripts/gen-capability-registry.cjs` → 0 matches; no unknown-key rejection in the validator; src/capability-trust.cts:204 documents the gap in a code comment; src/capability-lif…

>> NO-ADR: Where the capability system's OWN CLI lives — host command vs capability family
   WHY: ADR-2346 makes the host/capability boundary 'the load-bearing distinction' (a core host command must never be a toggleable feature, a leaf verb must never be a fake capability), but the capability subsystem sits astride it with no record. `capability` is a HOST router; `profile-p… | EV: gsd-core/bin/lib/capability-command-router.cjs:1-10 ('Owns the capability lifecycle CLI (state/list/install/upgrade/remove/consent/trust) … Dispatched via HOST_COMMAND_ROUTERS.capability'); registry `commandFamilies` contains `extract-messa…

>> NO-ADR: Newly-authored hand-written `.cjs` inside the compiled output directory
   WHY: ADR-457/ADR-3524 establish `src/*.cts` → `gsd-core/bin/lib/*.cjs` as the single source direction, and tsconfig.build.json's own header frames hand-written bin/lib files as legacy being migrated *out*. But ADR-1244 D2's validator extraction created a brand-new 2368-line hand-writt… | EV: tsconfig.build.json '//' key: 'As modules migrate, they move from hand-written bin/lib/*.cjs into src/*.cts here.' Files in gsd-core/bin/lib with no src counterpart and not generated: capability-validator.cjs (2368 lines), capability-comman…

>> NO-ADR: Native capability version + `engines.gsd` bump policy
   WHY: ADR-1244 D1 makes `version` and `engines.gsd` hard gates at install AND load, and D6 makes them the substrate for upgrade and graceful downgrade — but only for third-party. For the 39 native capabilities, `version` is stamped uniformly by a release sweep while `engines.gsd` is ha… | EV: All 39 capabilities carry `version: 1.8.0` (uniform, stamped by scripts/sync-manifest-versions.cjs per package.json `version` script) while `engines.gsd` splits: `>=1.6.0` for 28 and `>=1.7.0` for the newer set (broken-windows, claude-orche…

==============================================================================
## AGENT: a45856373216f9ea7
==============================================================================

--- docs/adr/1239-gsd-embeddable-orchestration-engine.md [ACCURATE] Accepted
    [high|missing-artifact] Phase E's "published SDK" — the stated reason the ADR flipped Proposed to Accepted — has no importable entry point, and the smoke test that was supposed to prove an external author can use it deep-requires internals instead.
        EV: ADR-1239:90 — "Status flipped from `Proposed` once Phase E shipped the published SDK". docs/how-to/author-a-host-plugin.md:39 — `const SDK = require('@opengsd/gsd-core/sdk'); // the published entry`; docs/tutorials/embed-gsd-in-a-new-host.md:26 repeats it. But package.json has NO `exports` field (ve…
        REC: Either add an exports map with a ./sdk subpath pointing at gsd-core/bin/lib/host-integration-sdk.cjs, or amend the ADR + docs to state the real deep-require path. Make tests/sdk-smoke.test.cjs resolve through the documented specifier so the…
    [medium|consequence-drift] The closed vocabulary gained a `subagentToolkit: 'built-in-only'` member that can never appear in a negotiated result — a declared, validated capability value that is silently unreachable, contradicting the ADR's own `effective ⊆ host-declared ∩ engine-known` …
        EV: src/host-integration.cts:47 — `subagentToolkit: Object.freeze(['full', 'read-only', 'built-in-only'] as const)` (added by commit d579daa3, #2505 Phase 6). capabilities/kimi-code/capability.json declares `dispatch.subagentToolkit: "built-in-only"`. But negotiation collapses it: src/host-integration.c…
        REC: Either widen SubagentToolkit + the negotiation ladder so 'built-in-only' propagates (and degradationFor grades it), or remove it from the closed vocabulary and keep namedDispatch as the sole signal. Amend ADR-1239 with whichever is chosen —…
    [medium|lacuna] `runtime.hostBehaviors` is a second, parallel per-runtime capability table with 58 distinct keys and ZERO schema validation, sitting beside the 9 closed, validated `runtime.hostIntegration` axes. No ADR decides the boundary between them — and it is a hole in A…
        EV: 58 distinct hostBehaviors keys are declared across capabilities/*/capability.json (counted programmatically). `grep -n hostBehaviors gsd-core/bin/lib/capability-validator.cjs` returns 0 matches, while the same validator enforces closed vocabularies for every hostIntegration axis (VALID_EFFORT_SURFAC…
        REC: Decide and record: is hostBehaviors part of the negotiated interface (closed vocabulary + validator parity guard, like hostIntegration) or explicitly first-party-only and rejected on third-party descriptors? Today it is neither.
    [medium|lacuna] The #2481 amendment establishes the principle that per-host data belongs in the descriptor rather than a core table, and the very next feature violates it: kimi-code declares `dispatch.builtInSubagents` that no ADR describes and that the engine does not read —…
        EV: capabilities/kimi-code/capability.json: `dispatch.builtInSubagents: ["coder","explore","plan"]`. src/host-integration.cts:579-604 — DISPATCH_TYPE_SUFFIX_MAP hard-codes the role→built-in regexes and resolveDispatchType returns literal 'coder'/'explore'/'plan'; builtInSubagents is never read (no consu…
        REC: Either wire resolveDispatchType to read builtInSubagents from the negotiated descriptor, or delete the field. Record the role→built-in mapping heuristic as a decision — it has a real failure mode: a gsd-*-writer agent silently becomes 'code…
    [medium|status-drift] The Phase A host lists and the #2481 correction to them are both stale again at HEAD; and kimi-code violates the amendment's own rule that unresearched hosts must carry the explicit `undocumented` sentinel.
        EV: ADR-1239:99 claims "all 16 installed CLIs" / "22 such markers"; ADR-1239:141 corrects to "18 descriptors … 28 `undocumented` sentinels". Live tree at HEAD aa0f7dee: 19 descriptors declare runtime.hostIntegration, carrying 43 exact-value `undocumented` sentinels (enumerated over capabilities/*/capabi…
        REC: Stop carrying census numbers in an append-only ADR; replace the lists with a pointer to the generated matrix (the ADR half-says this at :141). Separately decide whether omission or the sentinel is required for effortSurface, and make the va…
    (+1 low)

--- docs/adr/1143-claude-orchestration-capability.md [ACCURATE] Proposed
    [medium|consequence-drift] Both halves of the 2026-07-06 amendment's loop-registration statement are now false: the capability registers at `execute:wave:pre` (not `wave:post`), and `execute:wave:pre` IS wired.
        EV: ADR-1143:116-118 — "Loop registration is at the two **wired** points the loop host contract actually renders: `execute:wave:post into:executor` … `execute:wave:pre` and `execute:pre` are declared in the contract but **not wired** today, so the capability registers at `wave:post`". Live descriptor: c…
        REC: Append a dated amendment recording that #2285 wired execute:wave:pre and that the contribution moved there. As written, a reader debugging why the Workflow guidance does or doesn't render will look at the wrong extension point.
    [medium|lacuna] The ADR declares a byte-for-byte parity regression test a "release gate, not a nicety" — the property that makes the capability safe to be default-off — and no such gate exists or is tracked.
        EV: ADR-1143:80 — "A parity regression test asserting this on a runtime without the Workflow tool is a release gate, not a nicety — it is what lets the capability be default-off and low-risk." No test in tests/claude-orchestration.test.cjs, tests/claude-orchestration-command-router.test.cjs, or tests/fi…
        REC: File the parity gate as a tracked issue and reference it from the ADR, or downgrade the language from "release gate" to what actually ships. A safety property with no artifact and no owner is the failure mode the ADR was written to avoid.
    (+2 low)

--- docs/adr/1235-descriptor-driven-agent-conversion-migration.md [ASPIRATIONAL_ACCEPTED] Accepted
    STATUS-EV: The design gate is sound and the parity harness shipped (tests/issue-1575-agent-descriptor-parity.test.cjs, 8.0K, exists as claimed). But the decided mechanism is bypassed in production: convertedAgentsKind — whose inertness the ADR's Context named as the problem — is still inert on the real install path at HEAD, per a…
    [high|contradiction] The ADR reports "install-path cutover complete" for 7 runtimes; the layout module states in-tree that bin/install.js's agent loop does not consume convertedAgentsKind at all and "remains authoritative for the real install" — i.e. the exact inertness the ADR's …
        EV: ADR-1235:86-87 — "Step 1 (trivial converters): cursor, windsurf, augment, trae, codebuddy — install-path cutover complete (PR #1438)"; "Step 2 (scope-aware): copilot and antigravity — cutover complete (#1575)". src/runtime-artifact-layout.cts:224-238 — "What IS still deferred is wiring `resolveRunti…
        REC: Amend the progress log to distinguish "descriptor declaration landed" from "install path cut over". Success criterion 3 ("convertedAgentsKind is live for every converting runtime") is unmet for every runtime on the install path.
    [medium|status-drift] The progress log's "Remaining: steps 3–6" is stale in the other direction too — step 4's qwen half has landed (a converter the ADR says does not exist now exists, and qwen + kimi are in the descriptor-agents set).
        EV: ADR-1235:50 — behavior (h) "Qwen/Hermes branding swaps … **no converter function exists today**"; ADR-1235:79 step 4 — "add real convertClaudeAgentTo{Qwen,Hermes}Agent converters". Live: convertClaudeAgentToQwenAgent exists at src/runtime-artifact-conversion.cts:2215 (exported :2932); capabilities/q…
        REC: Refresh the cutover table from the tree (_DESCRIPTOR_AGENTS_RUNTIMES + each capability.json artifactLayout) rather than from PR memory.
    [medium|lacuna] A second, unrecorded gating condition for the cutover was invented in a source comment, and the ADR sets no owner, no issue, and no expiry for the dual-path period it authorises.
        EV: ADR-1235 gates each cutover on golden parity alone (:27, :75). But src/runtime-artifact-layout.cts:230-236 introduces a different gate: "That duplication is deliberate until the second `layout.kinds` consumer — `applySurface` / `/gsd:surface` / `--materialize` (`src/surface.cts`) — mirrors the legac…
        REC: Record the surface-parity precondition in the ADR, file a tracked issue owning it, and add a guard that fails when a descriptor's declared agents converter disagrees with the _hostBehaviors dispatch bin/install.js actually performs.
    (+1 low)

--- docs/adr/58-runtime-install-policy-module.md [ACCURATE] Accepted
    [medium|consequence-drift] Both amendments cite the wrong file:line for the seam they exist to defend — src/runtime-artifact-install-plan.cts:82 is a different, homonymous InstallPlan type belonging to a different module.
        EV: ADR-58:7 and ADR-58:23 both assert "this seam remains live at `src/runtime-artifact-install-plan.cts:82`". Verified: src/runtime-artifact-install-plan.cts:82 is `interface InstallPlan { items: PlanItem[]; cleanupDirs: string[] }` — the artifact copy plan (PlanItem = {kind, sourceDir, destDir}, :76-8…
        REC: Correct both citations to src/runtime-config-adapter-registry.cts:79/:179. The current pointer sends a reader to a module that does not implement this ADR.
    [medium|lacuna] Two distinct types named InstallPlan now exist in the engine and nothing decides which is "the" install plan or how they compose — precisely the composition question this ADR was written to own.
        EV: src/runtime-config-adapter-registry.cts:79 (InstallPlan extends RuntimeConfigIntent — config intentions) vs src/runtime-artifact-install-plan.cts:82 (InstallPlan { items, cleanupDirs } — artifact placements). ADR-58:34 states the problem it solves: "no ADR owns composing those — placements + command…
        REC: Rename one of the two types, or amend this ADR to declare the two-plan model explicitly (config-intent plan plus artifact plan) and state which module composes them.
    (+2 low)

--- docs/adr/3660-runtime-artifact-layout-module.md [ACCURATE] Accepted
    [high|missing-artifact] The Qoder addendum asserts, in the present tense, a runtime registration that does not exist anywhere in the tree and never landed — the issue is still open and the PR was closed unmerged.
        EV: ADR-3660:181-188 — "### Qoder (issue #860 / PR #1021) — first runtime recorded under this addendum … Qoder qualifies on all three criteria and **is added** as a layout-table entry", listing `case 'qoder' → [ skillsKind('skills','gsd-','convertClaudeCommandToQoderSkill','qoder',configDir) ]`, "'qoder…
        REC: Rewrite the Qoder section as a worked example of the addendum criteria rather than a record of a landed change, or delete it and keep the criteria. As written it is the corpus's most concrete false positive.
    [high|consequence-drift] The Decision's core claim — this module is "the single owner of the per-runtime artifact-placement table" — is no longer true. The table moved into capability descriptors and the module became a dispatcher, with no amendment recording the inversion.
        EV: ADR-3660:27 — "Add a Runtime Artifact Layout Module … as the single owner of the per-runtime artifact-placement table"; :33-35 show per-runtime case-style entries. Live: src/runtime-artifact-layout.cts:532-534 — "ADR-857 phase 5d: driven by the capability-registry artifactLayout descriptor instead o…
        REC: Amend ADR-3660 to record the switch→descriptor inversion and point at ADR-1016 as the table's new owner; fix the misleading error string at src/runtime-artifact-layout.cts:566.
    [medium|consequence-drift] The addendum's own three qualifying criteria — the test it exists to make explicit — have been breached twice without the "fuller amendment here" the addendum requires.
        EV: ADR-3660:169-175 — criteria include "(2) No new artifact kind — the layout entry is composed only of the existing commands/agents/skills kinds … no new ArtifactKind shape", and "A runtime failing any of the three needs a fuller amendment here (or a new ADR)". Breach (a): a fourth kind exists — src/r…
        REC: Amend with the kimi-agents kind and the nesting/recursive fields, or restate the criteria to match what the project actually treats as addendum-eligible. An explicit test that is silently overridden is worse than no test.
    [medium|status-drift] Both per-runtime exemplars in the Decision are now factually wrong: Cline no longer resolves to zero kinds, and Gemini's descriptor was deleted outright.
        EV: ADR-3660:34 — "Cline: kinds: [] — Cline resolves to zero kinds in Phase 1 (no commands kind)", repeated at :156. Live: capabilities/cline/capability.json artifactLayout.global = [{"kind":"skills","destSubpath":"skills","prefix":"gsd-","nesting":"nested","converter":"convertClaudeCommandToClineSkill"…
        REC: Amend the exemplars — they are load-bearing: the ADR uses them to define what a 'kind absent' case looks like, and tests/runtime-artifact-layout-edge-cases.test.cjs was scoped to them.
    (+2 low)

--- docs/adr/1508-runtime-artifact-conversion-module.md [ACCURATE] Accepted
    [medium|consequence-drift] The Decision's one named Exception no longer holds — the opencode/kilo pre-conversion step is no longer bin/install.js-owned, voiding the exception's stated rationale.
        EV: ADR-1508:17 — "**Exception:** opencode and kilo path-prefix rewriting remains a deliberate `bin/install.js`-owned pre-conversion step (see `applyOpencodeFamilyPathPrefix`); this is intentional per #784 and is not a violation of the single-owner rule." Live: bin/install.js:7434 — "applyOpencodeFamily…
        REC: Amend to state where the exception now lives and whether it is still an exception — an install-engine-owned pre-conversion step is a different single-owner question than an install.js-owned one.
    [medium|consequence-drift] The stated purpose of relocating getDirName — keeping capability-registry.cjs out of the conversion module's dependency graph — is defeated: the module now imports capability-registry directly.
        EV: ADR-1508:24 — "`getDirName(runtime)` relocates to `src/runtime-name-policy.cts` (a clean fs/path-only leaf), **so the conversion module can consume it without dragging in `capability-registry.cjs`** (which runtime-homes.cjs requires)." Live: src/runtime-artifact-conversion.cts:26 — `import capabilit…
        REC: Either amend the rationale (the dependency was accepted deliberately for hostBehaviors) or note the cost — the conversion module is no longer the leaf the ADR designed it to be, which matters for the "imports nothing upward, only deeper lea…
    (+3 low)

>> CLUSTER OBS [medium]: Relation integrity for ADR-1239's four declared Subsumes targets is now CLEAN — the corpus repaired the failure it documents. But the repair was scoped to the literal word 'Subsumes', leaving two adjacent one-way pointers in this cluster.
   EV: All four targets record the reciprocal in their header: 1016-runtime-capability-descriptor.md:10, 58-runtime-install-policy-module.md:6, 3660-runtime-artifact-layout-module.md:7, 894-capability-declaration-format.md:8 — each with a dated 2026-07-16 amendment whose closing line is 'Recorded because ADR-1239 declared thi…

>> CLUSTER OBS [medium]: Append-only amendment discipline produces ADRs that self-correct and are still wrong. Census facts (host counts, sentinel counts, per-runtime kind tables) drift faster than the amendment cadence, and the corpus keeps re-recording them by hand.
   EV: ADR-1239:99 says '16 installed CLIs' / '22 markers'; ADR-1239:141 corrects to '18 descriptors' / '28 sentinels' and warns 'enumerate the descriptors from the tree, not from those lists'; the live tree gives 19 descriptors and 43 sentinels. ADR-1239:95's profile counts (9/7/none) are now 11/7/1. ADR-3660:34-35's Cline a…

>> CLUSTER OBS [low]: Three ADRs in this cluster justify their seams partly by predicting bin/install.js will shrink. It has grown ~10% since the most recent of them, even though the extractions themselves landed.
   EV: ADR-3660:73/:131 predict a ~250-line reduction. ADR-1508:9 describes 'the 12,289-line hand-authored bin/install.js' on 2026-06-20. At HEAD aa0f7dee, `wc -l bin/install.js` = 13,486. The extractions are real (src/install-engine.cts:719/:1278 own the layout-driven install/uninstall loops; src/runtime-artifact-install-pla…

>> CLUSTER OBS [medium]: The generated gsd-core/bin/lib/*.cjs tree is gitignored and can be materially stale, so any reader (human or agent) who consults it instead of src/*.cts reads a different architecture than the one on next.
   EV: `git check-ignore -v gsd-core/bin/lib/host-integration.cjs` → '.gitignore:72'. `grep -c effortSurface gsd-core/bin/lib/host-integration.cjs` → 0, while src/host-integration.cts:56 defines the axis and src/commands.cts:597/:634 consume it. Loading the built module at HEAD reports 8 axes with no effortSurface; the source…

>> CLUSTER OBS [medium]: Two homonymous InstallPlan types coexist in the engine, and the ambiguity has already produced repeated citation errors inside the ADR corpus itself.
   EV: src/runtime-config-adapter-registry.cts:79 `interface InstallPlan extends RuntimeConfigIntent` (ADR-58's config-intent projection, returned by resolveInstallPlan at :179) vs src/runtime-artifact-install-plan.cts:82 `interface InstallPlan { items: PlanItem[]; cleanupDirs: string[] }` (the artifact copy plan, CONTEXT.md:…

>> CLUSTER OBS [high]: ADR-1239's negotiated interface is closed, validated and parity-guarded; the second per-runtime table that actually drives install branching is open, 58 keys wide, triplicated, and entirely unvalidated. The two live side by side in the same descriptor file with no ADR drawing the line.
   EV: runtime.hostIntegration: 9 closed-vocabulary axes, validated in gsd-core/bin/lib/capability-validator.cjs (VALID_EFFORT_SURFACES:741, per-axis checks :1200-1226) with a parity guard per ADR-1239:92. runtime.hostBehaviors: 58 distinct keys declared across capabilities/*/capability.json; `grep -n hostBehaviors gsd-core/b…

>> NO-ADR: Epic #2505 — runtime-aware subagent dispatch for built-in-only hosts (Kimi Code split)
   WHY: A real decision with a named, rejected alternative and a permanent consequence was made entirely in source comments and changesets. It added a member to ADR-1239's closed subagentToolkit vocabulary, a new descriptor field (dispatch.builtInSubagents), a new engine entry point (gsd… | EV: src/host-integration.cts:549-604 — the whole design is a 40-line comment block: 'This is Option A of the Phase 4 design (per-workflow runtime detection via `gsd_run query resolve-dispatch-type`), not Option B (PreToolUse mutation) — Kimi Co…

>> NO-ADR: The runtime.hostBehaviors descriptor surface — what belongs in it vs. the negotiated hostIntegration axes
   WHY: This is the largest per-runtime capability surface in the codebase and it has no owner, no closed vocabulary, no validator, and no stated relationship to the interface ADR-1239 exists to be. Adding a key is a silent, unreviewed extension of the host contract; a third-party descri… | EV: 58 distinct keys across capabilities/*/capability.json (enumerated programmatically), spanning install branching (pluginOnlyInstall, globalDirResolver, localInstallStyle, settingsFileByScope), security-adjacent config (permissionsSchema, ma…

>> NO-ADR: Runtime sunset / removal policy
   WHY: ADR-3660's addendum carefully decides how a runtime is ADDED (three qualifying criteria, addendum-vs-new-ADR test) but nothing decides how one is REMOVED — and a removal has already happened, leaving open questions with real user impact: what becomes of artifacts already installe… | EV: capabilities/gemini/ deleted by commit 8f2ebbe9b (#1928, PR #1996) per ADR-1239:131. Consequences left unrecorded: ADR-3660:35 still declares Gemini's layout entry as current; ADR-1239:95 still lists gemini in the declarative-cli profile (t…

>> NO-ADR: Public-surface publication policy after the sdk/ package boundary was retired
   WHY: ADR-0174 removed the package boundary that used to define 'public'. ADR-1239 Phase E then promised a published SDK. Nothing in between decides HOW a public surface is published or enforced now: package.json has no exports map, so every internal module is equally importable and th… | EV: package.json (verified dump) has no exports key; main is '.opencode/plugins/gsd-core.js'; files ships 'gsd-core' wholesale. docs/how-to/author-a-host-plugin.md:39 and docs/tutorials/embed-gsd-in-a-new-host.md:26 both instruct `require('@ope…

>> NO-ADR: The dual-path agent-install period and its exit condition
   WHY: ADR-1235 decided the exit condition is per-runtime golden parity. A source comment substitutes a different, larger exit condition (surface-path feature parity with the legacy pipeline) and authorises an indefinite duplication in the meantime. Because the duplicated mapping is spl… | EV: src/runtime-artifact-layout.cts:230-238 — 'That duplication is deliberate until the second `layout.kinds` consumer — `applySurface` / `/gsd:surface` / `--materialize` (`src/surface.cts`) — mirrors the legacy agent pipeline … Until that foll…

==============================================================================
## AGENT: a8c27cf7be32baf69
==============================================================================

--- docs/adr/0011-skill-surface-budget-module.md [ACCURATE] Accepted
    [high|consequence-drift] The Decision bullet "Allow profile composition: `--profile=core,audit` resolves to `union(closure(core), closure(audit))`" is not implemented, and the actual failure mode is fail-open to `full` — the exact state the ADR exists to prevent.
        EV: `src/install-profiles.cts:245` filters modes to `Object.prototype.hasOwnProperty.call(profiles, mode)` where `profiles` is `PROFILES` (core/standard/full only), then `:246-248` returns `{ name: 'full', skills: '*' }` with the comment "Invalid/corrupt marker fallback: avoid empty installs by defaulti…
        REC: Either implement capability/cluster names as composable profile components (threading `registry.profileMembership` into the `validModes` check), or strike the composition bullet from the ADR and the `--help` text. Separately, an unrecognise…
    [high|missing-artifact] The stated cluster-coverage invariant is neither enforced nor true: the test named as its enforcer does not exist, and one shipped skill stem belongs to no cluster.
        EV: ADR line 41: "every installed skill stem must appear in at least one cluster (enforced by `tests/surface-clusters.test.cjs`)". `tests/surface-clusters.test.cjs` does not exist (`ls tests/ | grep surface` → only `surface-empty-manifest-agents.test.cjs`, `surface-md-paths.regression.test.cjs`, `runtim…
        REC: Add the bijection test the ADR already claims exists (stems in `commands/gsd/*.md` ⊆ union(CLUSTERS)), and either place `onboard` in a cluster or record the deliberate exemption.
    [medium|missing-artifact] All nine test files listed in the "Status — Phase 1 shipped" / "Phase 2 shipped" sections are absent from the tree.
        EV: Checked individually: MISSING `tests/surface-state.test.cjs`, `tests/surface-clusters.test.cjs`, `tests/surface-resolve.test.cjs`, `tests/surface-apply.test.cjs`, `tests/surface-list.test.cjs`, `tests/install-profiles-manifest.test.cjs`, `tests/install-profiles-marker.test.cjs`, `tests/install-profi…
        REC: Rewrite the shipped-artifact lists to cite the surviving consolidated test files, or drop file-level test enumeration in favour of naming the invariants.
    [medium|lacuna] The ADR never decides what happens to a skill that belongs to no cluster, nor who owns the cluster map once ADR-857 introduced a second, generated cluster source.
        EV: `src/surface.cts:224-243` implements a merge of hand-authored `CLUSTERS` with `registry.capabilityClusters`, including a divergence precedence rule ("if the values DIFFER (future drift bypassing the gate), prefer the hand-authored value"), a malformed-entry skip ("FIX 5: skip malformed (non-array) e…
        REC: Add an amendment (here or in ADR-857) recording the cluster-map precedence contract and the unclustered-skill policy.
    (+1 low)

--- docs/adr/0011-review-default-reviewers.md [ACCURATE] Accepted — ratified 2026-07-17 (originally Proposed 2026-05-13)
    [medium|relation-asymmetry] ADR-1517 materially amended this ADR's unknown-slug consequence, but neither ADR records the amendment relation — and this ADR was re-opened and ratified on 2026-07-17, three weeks AFTER 1517 landed, without noticing.
        EV: This ADR, line 34: "Unknown slugs in the key emit a warning and are dropped" (repeated in the pseudocode at line 129: `# warn on each invalid slug`). Shipped behaviour, `src/review-reviewer-selection.cts:272-284`: when `instancesConfigured` is true an undefined entry is pushed onto `errors` — a HARD…
        REC: Add a reciprocal pair: an `Amended by ADR-1517` line here (with a one-line note that unknown entries are now hard errors when instances are configured) and an `Amends ADR-0011 (0011-review-default-reviewers.md)` line in ADR-1517.
    (+2 low)

--- docs/adr/1517-reviewer-instances-config-surface.md [ACCURATE] Accepted
    [medium|lacuna] The shared-adapter caveat — whose stated purpose is that "consensus is never silently overstated" — does not fire for the mixed case where a built-in slug and an instance share the same adapter.
        EV: `src/review-reviewer-selection.cts:312-318`: `for (const r of resolvedInstances) { if (r.kind === 'instance') { cliCounts[r.cli] = … } }` — only `kind === 'instance'` reviewers are counted. With `default_reviewers: ["opencode", "opencode-deepseek"]`, two reviewers both run the OpenCode adapter, `cli…
        REC: Count every `resolvedInstances` entry by `cli` regardless of `kind`, and amend rule 5 to say "≥2 selected reviewer identities sharing a base cli".
    [medium|lacuna] `KNOWN_REVIEWER_SLUGS` is a *derived*, growable set, so a previously-valid instance name can be retroactively invalidated by adding an unrelated runtime — and the ADR treats the set as stable while declaring the field names a Hyrum's-Law contract.
        EV: `src/review-reviewer-selection.cts:44-55`: `KNOWN_REVIEWER_SLUGS = [...deriveRuntimeReviewerSlugs(), ...NON_RUNTIME_REVIEWER_SLUGS]` where `deriveRuntimeReviewerSlugs()` filters the runtime registry on `runtimes[id]?.runtime?.hostBehaviors?.reviewerCli === true`. Flipping `reviewerCli` on for a new …
        REC: Record the derivation and a collision policy (e.g. reserve an instance-name prefix, or make new built-in slugs check the shipped instance namespace) as an amendment; at minimum state that reviewer-slug additions are a breaking-change class.
    (+3 low)

--- docs/adr/1593-skill-mapping-converter-methodology.md [ACCURATE] Accepted
    [high|consequence-drift] The ADR's headline runtime inventory is wrong in both directions: it counts a runtime that was deleted (gemini) and omits runtimes that were added (kimi-code, zcode).
        EV: ADR line 12: "GSD installs skills into 16 host CLIs (claude, codex, gemini, opencode, …)"; line 63: "All 15 skill-bearing runtimes use `prefix: "gsd-"`. (Gemini is commands-only — no skills kind.)" Reality: `tests/gemini-runtime-removed.test.cjs:1-8` — "#1928 — Gemini CLI runtime removal… Google sun…
        REC: Amend the ADR to reference the descriptor-derived count rather than a frozen number, and add matrix rows for `kimi-code` and `zcode` (and a documented N/A for `pi`/`vscode`, whose `artifactLayout` is empty).
    [high|consequence-drift] Decision 2's converter-enum arithmetic is stale — the enum has 27 members, not the 24 (15+9) the ADR states, and the cited enforcement-test line points at an unrelated test.
        EV: Live enumeration of `require('./gsd-core/bin/lib/capability-validator.cjs').VALID_CONVERTER_NAMES` → 27 entries: 17 command/skill/workflow converters (the ADR's 15 plus `convertClaudeCommandToKimiCodeSkill` and `convertClaudeCommandToWindsurfWorkflow`) and 10 agent converters (the ADR's 9 plus `conv…
        REC: Replace the hard counts with a pointer to the enum + its size test, and fix the two line citations (or drop line numbers, which rot on every generated-file rebuild).
    [medium|consequence-drift] Decision 4's three "structural facts" are all now false; the companion matrix was updated and the ADR was not, so the two authorities now disagree.
        EV: ADR line 64: "Six runtimes nest under `gsd-ns-*` routers (cline, qwen, hermes, augment, trae, antigravity)" vs `docs/reference/skill-mapping-matrix.md:46` "**Five runtimes nest** (cline, qwen, hermes, augment, trae)" — antigravity was flattened by #1614 and the matrix's own antigravity row records "…
        REC: Delete the duplicated structural facts from the ADR body and let the matrix own them (the ADR already declares the matrix the maintainable projection) — duplicating them in an immutable decision record guarantees this drift.
    [medium|relation-asymmetry] Three of the four ADR relations this ADR declares are one-way pointers; notably ADR-1016 carries no record of the status correction ADR-1593 claims to have applied to it.
        EV: `grep -n '1593' docs/adr/3660-runtime-artifact-layout-module.md docs/adr/1508-runtime-artifact-conversion-module.md docs/adr/1235-descriptor-driven-agent-conversion-migration.md docs/adr/1016-runtime-capability-descriptor.md` → 0 matches in all four. Only `docs/adr/766-claude-code-plugin-manifest-mo…
        REC: Add `Extended by ADR-1593` to ADR-3660, `Sibling: ADR-1593` to ADR-1508, and a provenance line to ADR-1016 mirroring the ADR-0010 pattern.
    [medium|lacuna] The ADR names "transform order is load-bearing for byte-parity" as a hard constraint but assigns it no enforcement and no owner — and then explicitly notes the categories are not type-enforced, leaving the entire methodology unguarded.
        EV: ADR line 55: "**Transform order is load-bearing for byte-parity** (cf. ADR-1235 §0)… A converter that silently inherits another's ordering breaks byte-for-byte parity without a test signal." The ADR states the failure mode and then leaves it. Its own Consequences (line 88) concede: "The eight transf…
        REC: Either cite the golden-parity suite as the byte-parity guard (closing the stated hole) or record explicitly that it is an accepted uncovered risk with a named owner.
    (+1 low)

--- docs/adr/1866-agent-skills-dual-injection-contract.md [ACCURATE] Accepted
    [medium|lacuna] The Drift guard section names three surfaces but the test enforces a bijection over only two — the workflow-side `query agent-skills <type>` call set has no guard at all.
        EV: ADR line 47: "Three surfaces share the consumer-agent set: the `CONSUMER_AGENTS` list in `tests/agent-skills.test.cjs`, the agents carrying the bootstrap, and the `query agent-skills <type>` calls in workflows. `tests/agent-skills-bootstrap.test.cjs` asserts the bijection between `CONSUMER_AGENTS` a…
        REC: Extend the bijection to the third surface: scan `gsd-core/workflows/*.md` for `query agent-skills <type>` tokens and assert the type set is a subset of `CONSUMER_AGENTS` (or an exact match, with documented exemptions).
    (+1 low)

--- docs/adr/766-claude-code-plugin-manifest-module.md [ACCURATE] Accepted
    [medium|consequence-drift] The ADR's "What stays OUTSIDE this Module" boundary has been breached without amendment: marketplace listing is now an in-repo artifact.
        EV: ADR line 45: "**Marketplace listing.** Publishing gsd-core to a marketplace registry is an external, out-of-repo act." Reality: `.claude-plugin/marketplace.json` is committed (588 bytes) declaring `owner: open-gsd` and a `plugins[0]` entry with `"source": "./"` and `"version": "1.8.0"`; `tests/issue…
        REC: Amend ADR-766 to bring `marketplace.json` inside the Module (it is a third projection of the same identity surface), or file a sibling ADR for the self-hosted-marketplace decision. Leaving the exclusion sentence in place makes the record ac…
    [medium|consequence-drift] The enumerated always-on hook set is stale — `hooks/hooks.json` now carries two hooks and three lifecycle events the ADR never lists, and the drift-guard test was updated while the ADR was not.
        EV: ADR line 33 enumerates a closed set: "`gsd-check-update` (SessionStart), `gsd-context-monitor` (PostToolUse), and the security guards `gsd-prompt-guard` / `gsd-read-guard` / `gsd-worktree-path-guard` / `gsd-read-injection-scanner`." Live `hooks/hooks.json` also carries `gsd-ensure-canonical-path.js`…
        REC: Amend the hook-projection paragraph to the current set (or replace the enumeration with a pointer to the drift-guard test's expected map, which is the real single source).
    [medium|lacuna] The ADR names the plugin path a "parallel Adapter" but never decides what happens when a user has BOTH installations active — a real, reachable state with a visible collision surface.
        EV: The file-copy install writes `/gsd-<cmd>` skills into `~/.claude/skills/` (`docs/reference/skill-mapping-matrix.md` claude row: `skills/`, prefix `gsd-`, flat) while the plugin ships the identical 71 stems as `gsd-<stem>/SKILL.md` under `skills/` namespaced `/gsd-core:<command>` (`.claude-plugin/plu…
        REC: Decide and record: does the plugin path honour the profile marker, does the installer detect an active plugin and warn, or is double-surfacing accepted? At minimum add a consequence line naming the token cost.
    (+1 low)

>> CLUSTER OBS [high]: This cluster's ADRs are systematically stale in exactly one way: they hard-code counts and file paths that the code then outgrew, while the *generated or tested* projections of the same facts stayed correct. In every case the test or reference page is right and the ADR is wrong.
   EV: ADR-1593:35 says the converter enum holds "24 names" while `tests/capability-registry.test.cjs:4024` asserts "exactly 27 entries (17 command/skill/workflow + 10 agent converters)" and the live Set has 27. ADR-1593:64 says "Six runtimes nest" while its own companion `docs/reference/skill-mapping-matrix.md:46` says "Five…

>> CLUSTER OBS [high]: Relation reciprocity is enforced for the two ADRs whose relations sit in the *header* fields, and absent for every relation expressed in prose. The gate appears to read header fields only.
   EV: Header-declared: ADR-0011-skill-surface's `Supersedes: ADR-0010` is reciprocated at `docs/adr/0010-skill-surface-budget-module.md:3` (`Superseded by ADR-0011`), and its `Subsumed by: ADR-857` is reciprocated at `docs/adr/857-capability-system.md:6` (`Subsumes (generalizes)`). Prose-declared: ADR-1593's `Extends: ADR-36…

>> CLUSTER OBS [high]: Two ADRs in this cluster each declare a bijection/closure drift-guard as the mechanism that keeps their contract honest, and in both cases the guard covers strictly less than the ADR claims — the uncovered surface is the one that actually fails silently.
   EV: ADR-0011-skill-surface:41 claims `tests/surface-clusters.test.cjs` enforces "every installed skill stem must appear in at least one cluster" — the file does not exist, and `onboard` is in fact unclustered (71 stems, 70 clustered). ADR-1866:47 names three surfaces sharing the consumer-agent set and its guard (`tests/age…

>> CLUSTER OBS [medium]: The surface-budget intent and the projection surfaces are not reconciled anywhere: three independent mechanisms can each re-expand the eager skill surface that ADR-0011 exists to bound, and no ADR owns the interaction.
   EV: (1) `src/install-profiles.cts:246-248` fails open to `full` on any unrecognised profile component. (2) `.claude-plugin/plugin.json` `"skills": "./skills/"` ships all 71 stems regardless of `.gsd-profile` (71 dirs in `skills/` = 71 files in `commands/gsd/`), so a plugin install restores the full surface for a `--profile…

>> CLUSTER OBS [low]: Numbering collisions at 0010/0011 are known and documented but still produce ambiguous prose references inside the ADRs themselves.
   EV: `docs/adr/README.md:36`: "Because `0010-*` and `0011-*` each resolve to more than one file, a bare cross-reference is ambiguous", and `:64` mandates `[ADR-0011](0011-skill-surface-budget-module.md)` over bare `ADR-0011`. Yet `docs/adr/0011-skill-surface-budget-module.md:80-82` closes with bare "See `0008-installer-migr…

>> NO-ADR: Self-hosted Claude plugin marketplace (`.claude-plugin/marketplace.json`)
   WHY: GSD now publishes its own marketplace registry pointing at itself (`"source": "./"`). That is a distribution-channel decision with live alternatives (list on a third-party marketplace; npm-only; git-clone `@skills-dir` only) and real consequences: a third identity/version project… | EV: `.claude-plugin/marketplace.json` (committed, 588B, `"version": "1.8.0"`, `"source": "./"`); `scripts/sync-manifest-versions.cjs:37` stamps `plugins.0.version`; `tests/issue-1855-marketplace-manifest.test.cjs`; `gh issue view 1855` → CLOSED…

>> NO-ADR: Fail-open-to-`full` policy for unrecognised install profiles
   WHY: A deliberate choice was made — with an explicit rationale in the code comment — that a corrupt or unknown `--profile=` value installs EVERY skill rather than erroring or falling back to `core`. The alternatives are obvious and the consequences are opposite in kind (empty install … | EV: `src/install-profiles.cts:245-248` — `validModes` filter followed by `// Invalid/corrupt marker fallback: avoid empty installs by defaulting to full.` returning `{name:'full', skills:'*'}`; reproduced live: `resolveProfile({modes:['bogus']}…

>> NO-ADR: Precedence contract between hand-authored `CLUSTERS` and generated `registry.capabilityClusters`
   WHY: Two sources of truth for the same taxonomy were introduced by the capability system, and a non-obvious precedence rule was chosen (hand-authored wins when values diverge), plus a fail-soft rule (malformed registry entries are skipped, never thrown) and a prototype-pollution allow… | EV: `src/surface.cts:213-243` (the merge, with `// FIX 4: … the hand-authored value wins` and `// FIX 5: skip malformed (non-array) entries — never throw on bad registry`); guarded by `tests/capability-consumption.test.cjs:612` ("FIX-4 divergen…

>> NO-ADR: Derivation of the reviewer-identity namespace from the runtime registry
   WHY: `KNOWN_REVIEWER_SLUGS` — the allow-list that ADR-1517 calls "the trust boundary" and the namespace user-chosen instance names must avoid — is not a literal list but a projection of `hostBehaviors.reviewerCli` over the runtime registry, unioned with a hard-coded non-runtime list. … | EV: `src/review-reviewer-selection.cts:44-55` (`deriveRuntimeReviewerSlugs()` filtering `runtimes[id]?.runtime?.hostBehaviors?.reviewerCli === true`, then `KNOWN_REVIEWER_SLUGS = [...derived, ...NON_RUNTIME_REVIEWER_SLUGS]`); collision check at…

>> NO-ADR: Coexistence of the file-copy install and the plugin install on one host
   WHY: ADR-766 calls the plugin a "parallel Adapter" and stops there. Both adapters can be active simultaneously and both enumerate the same 71 skills into the same eager `<available_skills>` budget under different namespaces (`/gsd-<cmd>` vs `/gsd-core:<command>`), which is the exact c… | EV: `ls skills/ | wc -l` = 71 = `ls commands/gsd/*.md | wc -l`; `.claude-plugin/plugin.json` `"skills": "./skills/"` (generated by `scripts/gen-plugin-skills.cjs`, unconditioned on any profile marker); `src/install-profiles.cts:923` `PROFILE_MA…

==============================================================================
## AGENT: a777d58e0f7376017
==============================================================================

--- docs/adr/1769-state-md-transition-module.md [ACCURATE] Accepted (Phase 7 closeout — all 10 lifecycle/maintenance transitions migrated)
    [high|consequence-drift] The header's first `Supersedes:` claim — "the policy portions of `syncStateFrontmatter`" — was never realized. Encoding 1 of the three drifting preservation encodings the ADR's Context names is fully intact and consults nothing from the field-classification ta…
        EV: `syncStateFrontmatter` lives at src/state.cts:1731 and still hand-rolls its entire policy: status-preserve-when-derived-is-unknown at :1740-1742; the #948/#2135 milestone_name placeholder guard at :1753-1774; the #905 `stopped_at`/`paused_at`/`current_phase`/`current_phase_name` empty-derive fallbac…
        REC: Either amend the ADR to narrow the Supersedes claim to the RMW post-sync block only (what #1796 actually delivered), or open the follow-on that routes `syncStateFrontmatter`'s four preserve rules through `getFieldClassification`. The header…
    [medium|contradiction] The #1796 amendment and the Neutral section contradict each other inside the same file about how many preservation encodings survive.
        EV: Amendment line 240 claims the change "routes the `#1264` preservation policy through the single field-classification table (one policy source, not three drifting encodings)." But the Neutral section at lines 185-186 says `cmdStateBuildFrontmatter` (Encoding 3) "stays as the read path; future work ma…
        REC: Reword the amendment to "one policy source for the write path's post-sync block" and state plainly that the read path (Encoding 3) and `syncStateFrontmatter` (Encoding 1) remain separate encodings pending follow-on work.
    [medium|lacuna] §6 excludes append-only transitions on the stated ground that "they don't touch curated fields," but records no invariant and installs no enforcement that keeps that true — and the surviving raw-RMW count already exceeds the ADR's own budget.
        EV: §Neutral (line 184) budgets "the 7 append-only callbacks that aren't migrating." `readModifyWriteStateMd` closures still exist at src/state.cts:430, :465, :524, :555, :728, :787, :855, :946, :1031, :1078, :2265, :2526, :2887 — 13 sites. `cmdStateAddDecision` (:765), `cmdStateAddBlocker` (:836), `cmd…
        REC: Add the missing invariant explicitly ("a raw RMW closure MUST NOT write a field whose classification is `curated`") and back it with a test that enumerates RMW call sites, or migrate the remaining sites. Otherwise the #1743/#1695 bug class …
    [medium|lacuna] Nothing decides — or enforces — that a new STATE.md frontmatter key must get a FIELD_CLASSIFICATION row. The table's completeness is maintained by hand and validated only at runtime.
        EV: The table docstring states the field set was "verified against `buildStateFrontmatter` (state.cts:1474)" — a one-time manual check (src/state-transition.cts:68-69), and the cited symbol has since moved to src/state.cts:1510. Unknown keys throw at runtime from four separate sites (src/state-transitio…
        REC: Add a parity test asserting `Object.keys(buildStateFrontmatter(...)) ⊆ Object.keys(FIELD_CLASSIFICATION)`, and record it in the ADR as the mechanism that keeps §4's "adding a new field = one table row" honest.
    (+2 low)

--- docs/adr/1817-state-md-rebuild-derivability-contract.md [ACCURATE] Accepted (Phase 0 — ADR + CONTEXT.md update; lands ahead of Phases 1–2)
    [high|consequence-drift] §2's section taxonomy — the ADR's central decision artifact — names STATE.md sections that do not exist. Four of its seven rows have no counterpart in the shipped template, and the implementation had to silently remap them.
        EV: gsd-core/templates/state.md contains exactly `## Project Reference` (:23), `## Current Position` (:30), `## Performance Metrics` (:39), `## Accumulated Context` (:58), `## Deferred Items` (:80), `## Session Continuity` (:88). The taxonomy's `## By-Phase Progress`, `## Session`, `## Decisions`, and `…
        REC: Amend §2 with the real section names. The ADR's own Negative consequence says "any future STATE.md body section must declare its taxonomy class" — that obligation currently points at a table a contributor cannot match to the file they are e…
    [high|consequence-drift] §3's locked `kind` enum for the audit log does not match the shipped enum: 1 of 6 locked names survives, and three locked kinds have no implementation at all — meaning three of the Context's motivating drift signatures are neither reconciled nor logged.
        EV: §3 locks `orphaned-row | placeholder-removed | archive-deduplicated | wrong-section-source | milestone-count-stale | section-rewritten`. Shipped: `type RebuildLogEntryKind = 'placeholder-removed' | 'current-position-reconciled' | 'by-phase-table-reconciled' | 'session-archive-deduplicated'` (src/sta…
        REC: Amend §3 to the shipped enum and explicitly move the three unimplemented drift classes to a named follow-on issue — right now the ADR reads as if rebuild handles six classes when it handles three.
    [medium|consequence-drift] §2 says rebuild re-derives "each prose field" of `## Current Position` from frontmatter; the implementation deliberately reconciles only two fields and explicitly excludes the `Phase:` prose line that the Context names as a motivating drift signature.
        EV: `reconcileCurrentPosition` touches only `Current Phase` (src/state-transition.cts:1772-1790) and `Current Phase Name` (:1793-1811). The code comment at :1767-1771 states the exclusion: "The body `Phase:` prose line (e.g. \"Phase: 3 of 12 (Test Phase)\") is owned by other transitions (beginPhase / co…
        REC: Either extend `reconcileCurrentPosition` to the `Phase:` prose line or amend §2 and the Context to state that this signature is out of rebuild's scope and who owns it.
    [medium|consequence-drift] §2's archive retention is specified as "configurable; default 3" and as dropping duplicates; the shipped behavior is a hard-coded count with no duplicate detection.
        EV: `const DEFAULT_MAX_SESSION_ARCHIVES = 3;` at src/state-transition.cts:1960, referenced directly in `deduplicateSessionArchive` with no options parameter. No `session_archive`/`max_session` key exists in gsd-core/bin/shared/config-defaults.manifest.json or src/config.cts. The drop rule is positional …
        REC: Drop "configurable" from §2 (or add the config key), and rename the operation to retention rather than deduplication so the Context's duplicate-blocks signature isn't presumed covered.
    [high|lacuna] The ADR decides rebuild is manual but never decides who invokes it or how a drifted project discovers it should be run. The capstone transition has no surface in the product's own instruction layer.
        EV: §5 specifies the trigger as "Manual (`gsd-tools state rebuild`)". Searching gsd-core/workflows/, agents/, and commands/ for `state rebuild` / `state-rebuild` returns zero matches. In GSD's architecture the runtime only issues gsd-tools subcommands that a workflow instructs it to issue (see gsd-core/…
        REC: Decide the discovery path: a `verify` warning that fires when rebuild would mutate (rebuild is already pure and dry-runnable, so this is cheap), or a workflow reference in progress/health. Without one, the derivability contract is a guarant…
    [medium|lacuna] §3 declares `## Rebuild Log` append-only and preserved, defers retention to "a separate prune step (out of scope here)", and that step does not exist — an unbounded-growth failure mode with an explicit non-owner inside the very file whose drift the epic exists…
        EV: `appendRebuildLogSection` only ever appends (src/state-transition.cts:2057-2077). `REBUILD_LOG_TRUNCATION_LIMIT = 512` (:1677) bounds per-entry text but not entry count. `pruneCore` has no knowledge of `REBUILD_LOG_SECTION`; grep for `Rebuild Log` in src/ matches only state-transition.cts and two co…
        REC: Decide retention now (count cap, age cap, or archive-on-prune) — a deferred owner-less policy on an append-only section is how the drift class that motivated this ADR started.
    (+4 low)

--- docs/adr/2207-status-field-lifecycle-ownership.md [ACCURATE] Accepted
    [medium|consequence-drift] The safety net Decision 3 leans on does not cover the value Decision 1 introduced. `verify.cts` was named as a required consumer update and was not updated, so the ship gate no longer fires on the new intermediate state.
        EV: Decision 3: "its correctness is carried by ... the `verify.cts` ship gate that already errors when STATE claims milestone-complete while phases are unstarted." The gate is `const isMarkedComplete = /milestone complete|archived/.test(stateStatus)` (src/verify.cts:2053), reading a lowercased status. `…
        REC: Either widen the gate to `/(milestone|all phases) complete|archived/` or amend Decision 3 to name a different carrier — as written, the decision cites a guard that its own change disabled for the wrong-phase-flip scenario (#2199/#2200) it w…
    [medium|lacuna] The ADR establishes a four-value lifecycle as ubiquitous language but decides no enforcement — every value is a bare string literal at its write site, so a fifth value can be introduced with nothing failing. That is exactly how `Milestone complete` became over…
        EV: `'Ready to plan'` / `'All phases complete'` are inline literals at src/state-transition.cts:951 and src/gsd2-import.cts:351; `${version} milestone complete` is template-built at :1331; `Awaiting next milestone` is emitted as prose inside a section body at :1361. There is no `VALID_STATE_STATUSES` en…
        REC: Add a frozen `STATE_STATUS` constant in state-transition.cts consumed by all four write sites, plus a test asserting every written status is a member. Cheap, and it makes the "ubiquitous language" section enforceable rather than aspirationa…
    (+1 low)

--- docs/adr/2121-phase-identifier-parsing-consolidation.md [ACCURATE] Accepted (Phase 0 — ADR only; locks the contract Phases 1–4 execute against. No production…
    [high|consequence-drift] Decision 3's locked contract for `parsePhaseFromProse` does not describe the shipped function. The ADR locks `Phase`-anchoring; the code ships start-anchoring with the `Phase` label optional, so it still mines a leading numeral out of arbitrary prose.
        EV: ADR Decision 3: "It extracts a phase identifier **only** from a genuine phase reference — the literal token `Phase <id>`" and "absence of a `Phase` anchor yields `{ phase: null }`, not a guessed number." Shipped regex, src/phase-id.cts:312: `/^\s*(?:Phase\s+)?(?:[A-Z][A-Z0-9_]*-)?(\d+[A-Z]?(?:\.\d+)…
        REC: Amend Decision 3 to state the real invariant (start-anchored on the post-label field VALUE) with the boundary examples. Phases 2–4 were told to "consume [these contracts] verbatim"; a locked contract that overstates its own tightness is wor…
    [high|consequence-drift] Decision 5's delegation — the specific thing #2114 requested — did not ship. `roadmap.cts`'s CLI siblings still re-scan content with their own matcher instead of delegating to the single impure resolver, so the two ROADMAP content matchers did not collapse.
        EV: Decision 5: "`roadmap.cts`'s CLI siblings (`cmdRoadmapGetPhase`, `getRoadmapPhaseWithFallback`) **delegate to it** rather than re-scanning content, collapsing the `searchPhaseInContent` vs `findRoadmapPhaseInContent` duplication onto one resolution path — this is precisely the delegation #2114 reque…
        REC: Either finish the delegation or amend Decision 5 and the Consequences to record that source-list sharing was the delivered scope and matcher collapse remains open, with an issue number. Two live matchers over the same file is the divergence…
    [medium|consequence-drift] Decision 4 names `stripConfiguredProjectCodePrefix` as "the path" for resolving caller-supplied queries. It has zero consumers — the init resolution path still strips config-blind.
        EV: `grep -rn stripConfiguredProjectCodePrefix src/ --include=*.cts` matches only its definition (src/phase-id.cts:338) and its export (:399). src/init.cts:77 destructures `stripProjectCodePrefix` (the config-BLIND stripper) alongside `isForeignPrefixedPhaseQuery`; the guard at src/init.cts:132 and :144…
        REC: Either wire it at the init/roadmap query boundary as Decision 4 specifies, or amend Decision 4 to say the resolution path is `isForeignPrefixedPhaseQuery`-rejection plus blind stripping, and mark `stripConfiguredProjectCodePrefix` as provid…
    [medium|consequence-drift] Decision 7's guard was narrowed to one of three locked grammar shapes, leaving the project-code-prefix shape and the heading-builder shape unguarded — including a divergent site the ADR itself inventories.
        EV: Decision 7: the scanner "flags phase-ID-shaped regex literals (`\d+[A-Z]?(?:\.\d+)*`, `[A-Z][A-Z0-9_]*-`, and `Phase\s+…:` heading builders)". Shipped `TOKEN_DRIFT_RE` (scripts/lint-phase-id-drift.cjs:52) matches only the first; the script's own docstring (:23-30) states the exclusion: "Bare `\d+` p…
        REC: Amend Decision 7 to the shipped detection scope and note explicitly which inventoried sites fall outside it, so "re-divergence is a CI failure" isn't read as covering more than it does.
    [medium|lacuna] The `// phase-id-owner:` allowlist has no expiry, no owner, and no visibility. The ADR calls a stale entry "a small, visible debt" but nothing makes it visible.
        EV: Sanctions are matched by `OWNER_RE = /^\s*\/\/.*phase-id-owner:/` and silently `continue`d (scripts/lint-phase-id-drift.cjs:74-77). On success `main()` prints only "ok phase-id-drift: no unsanctioned phase-token re-derivations outside phase-id.cts" (:130) — no sanction count, no listing. Decision 7 …
        REC: Have the scanner print sanctioned sites (count and locations) on the success path, or add a test that pins the sanction inventory so adding one is a reviewed diff.
    [medium|lacuna] "Single canonical owner" is scoped to `src/**` by implementation but stated without qualification in the decision, and phase-ID grammar demonstrably lives outside that boundary.
        EV: Decision 1: "Every function in that set lives in `phase-id.cts` ... **no consumer defines a phase-identifier regex locally.**" The guard scans only `SCAN_DIRS = ['src']` with `SCAN_EXT = {.cts,.ts,.mts}` (scripts/lint-phase-id-drift.cjs:83-85). Phase-ID grammar also lives in `hooks/gsd-statusline.js…
        REC: State the ownership boundary explicitly in Decision 1 (`src/*.cts` authored source only) and record hooks/markdown as an out-of-scope surface with a named owner — otherwise ADR-612's PR-5/PR-6 will introduce grammar the guard structurally c…
    (+2 low)

--- docs/adr/612-bracket-phase-id-convention.md [ACCURATE] Proposed (PR-0 — ADR only; locks the contract PR-1…PR-6 execute against. No production cod…
    [high|contradiction] Decision 4's `parsePhaseId` is the exact function shape ADR-2121 Decision 3 explicitly rejected, and ADR-612 — which cites ADR-2121 as its single-owner authority four times — never acknowledges, distinguishes, or supersedes that rejection.
        EV: ADR-612 Decision 4: `function parsePhaseId(input: string): PhaseId; // accepts display OR dir OR bare arg` (612-*.md:105), landing "**inside `src/phase-id.cts`**, the single canonical owner established by ADR-2121" (:94). ADR-2121 Decision 3, rejected option (C): "a single mega-parser `parsePhaseId(…
        REC: Resolve before PR-1 opens. Either split into `parseDisplayId` / `parseDirId` (per-surface, matching 2121's stated principle), or state in ADR-612 that it revises ADR-2121 Decision 3 and record the reciprocal in 2121. Right now a PR-1 author…
    [high|contradiction] Decision 6 requires either changing an existing `phase-id.cts` export's behavior or reading config inside it — both are barred by locked ADR-2121 constraints — and ADR-612 explicitly defers the choice rather than resolving the conflict.
        EV: ADR-612 Decision 6: "`getMilestoneFromPhaseId` ... gains a bracket path ... gated on `phase_id_convention === 'bracket'`. ... Gating requires either a new parameter or a config read at the call boundary — that is a PR-1 design choice, not decided here" (612-*.md:132). ADR-2121 Decision 2 (locked): "…
        REC: Decide it in this ADR, not in PR-1. If the answer is a new parameter, say so and state that ADR-612 is the "own ADR" ADR-2121 Decision 2 demands. If it is a config read, say explicitly that phase-id.cts's purity constraint is being relaxed …
    [medium|contradiction] Decisions-to-ratify #1 offers making `normalizePhaseName('02-04')` throw as a live option without disclosing that ADR-2121 Decision 2 bars behavior changes to that export.
        EV: 612-*.md:186: "Options: (i) throw a disambiguation error ... The throw is a *new* behavior — `normalizePhaseName('02-04')` returns `'02-04'` today, it does not truncate — so it is not merely a fix. Not committed here." ADR-2121 Decision 2 locks against exactly this, on a CRITICAL blast radius the AD…
        REC: Annotate option (i) with the ADR-2121 Decision 2 constraint so whoever ratifies it knows it requires overriding a sibling Accepted ADR, not just picking an option.
    [high|lacuna] The W021 disposition is decided against a premise that is already false: W021 is not a single check. Two unrelated verify checks emit it today, and there is no W-code registry to allocate against.
        EV: 612-*.md:26: "W021 is pinned by shipped `tests/milestone-prefixed-convention.test.cjs` and cannot be renumbered." Line 191 resolves the question as "W021 renumber → **void** ... kept, message-disambiguated." But src/verify.cts emits `'W021'` at TWO sites: :1982 (milestone-prefix mismatch, inside the…
        REC: Fix the collision (renumber the ship-gate check) before ADR-612 builds a message-disambiguation decision on top of it, and record the W/E-code namespace as owned somewhere — a code that means two things cannot be "message-disambiguated" int…
    [medium|lacuna] The ADR gates every bracket path on a config value but never decides what happens when the on-disk layout and the config value disagree — the mismatched-state failure mode has no owner.
        EV: Decision 2 gates on `config.phase_id_convention === 'bracket'` and Decision 6 defers how the pure leaf learns it. Decision 7's only refusal rule is HARD-REFUSE when `project_code` is absent (612-*.md:147). Nothing addresses: bracket-shaped dirs on disk with `phase_id_convention: null`, or `'bracket'…
        REC: Add a coherence decision: what reads authoritative (config or disk), and what the read path does on mismatch (refuse / warn / trust disk). The ADR already concedes in Consequences that bracket introduces "a second milestone authority ... wh…
    [medium|consequence-drift] The verify.cts and roadmap-parser.cts anchors that PR-1…PR-6 authors are told to navigate to are off by hundreds of lines.
        EV: ADR cites "the W021 read-path check in `src/verify.cts:1724` (`phase_id_convention === 'milestone-prefixed'` branch, warning at `:1733`)" — actual: branch at src/verify.cts:1973, warning at :1982 (~250 lines off). "`src/roadmap-parser.cts:430` (`getMilestonePhaseFilter`, warning body at `:441-445`)"…
        REC: Refresh before PR-1 opens. An ADR whose stated job is to "lock the contract PR-1…PR-6 execute against" is navigated by exactly these anchors.
    (+1 low)

--- docs/adr/1787-gsd-next-smart-entry.md [ACCURATE] Accepted
    [medium|missing-artifact] The guard test the ADR names twice — once in Context as the reason re-introducing a `next` entry point is safe, once in References — no longer exists at the cited path.
        EV: ADR line 23: "`tests/bug-3054-stale-gsd-next-references.test.cjs` guards against user-facing surfaces re-referencing the removed flat command." Line 120 repeats it under References. `ls tests/ | grep 3054` returns nothing. `git log --diff-filter=D -- 'tests/bug-3054*'` shows 697cbb1f ("test(#1977): …
        REC: Repoint both references to tests/repo-invariants.test.cjs. The guard is real; only the pointer rotted, and this ADR's safety argument rests on a reader being able to find it.
    [medium|lacuna] The headline guarantee — "Route 0 / Gates 1–3 cannot be bypassed through the new front door" — is true today only because the current menu happens not to emit `--force`. The engine's documented total-bypass flag is never mentioned, and no decision or test forb…
        EV: gsd-core/workflows/next.md:41: "If `--force` flag was passed, skip all gates, Route 0, and the prior-phase completeness prompt." Reaffirmed at :43, :143, and in the acceptance checklist at :336 ("`--force`: everything skipped (Gates, Route 0, prior_phase_completeness)"). ADR Consequences/Positive: "…
        REC: Add the invariant explicitly ("no smart-entry action may emit `--force`") and assert it over the full action list, not just `[0]`. Alternatively, record `--force` as a deliberate escape hatch and say who may use it — as written the ADR guar…
    [medium|lacuna] The ADR accepts two detection paths but never decides which wins when they disagree — there is no reconciliation, no warning, and no test for the divergence it explicitly permits.
        EV: Negative/trade-offs: "detection logic exists in two places. Accepted: they share parsing primitives and only the *routing decision* is centralized (in `--next`), which is where divergence would actually harm the user." But smart-entry can classify `executing` from STATE.md while Route 0 resolves a *…
        REC: Either have the classifier surface a "resume pending" signal so the menu label matches what `--next` will do, or state the accepted user-visible behavior (label may not match destination; the engine wins) so it is a known consequence rather…

>> CLUSTER OBS [high]: ADR-612 and ADR-2121 collide head-on inside the same module, and only one side knows it. ADR-612 lands a multi-surface sniffing `parsePhaseId` and a config-gated `getMilestoneFromPhaseId` inside `src/phase-id.cts` — the exact function shape ADR-2121 Decision 3 rejected on Postel's-Law grounds, and a change to one of the twelve exports ADR-2121 Decision 2 locks against, in a module ADR-2121 require…
   EV: 612-*.md:94-108 (`parsePhaseId` inside phase-id.cts, "accepts display OR dir OR bare arg") and :132 (config-gated `getMilestoneFromPhaseId`, "a PR-1 design choice, not decided here") vs 2121-*.md:83 (rejected option C: "a discriminated god-function hides that and widens the accept surface"), :60 ("may **not** change th…

>> CLUSTER OBS [high]: ADR-1769 and ADR-1817 both claim `current_phase_name`, with opposite policies, and neither notices. ADR-1769's table marks it `curated`/`preserve-always` specifically so nothing overwrites it (#1743/#1695); ADR-1817 §2 then declares the whole `## Current Position` block re-derivable on the stated ground that frontmatter "already derives correctly from disk + ROADMAP" — which is false for exactly t…
   EV: src/state-transition.cts:88 — `current_phase_name: { source: 'curated', preservation: 'preserve-always' } // #1743, #1695`. ADR-1817 §2 taxonomy row 1 lists source of truth as "Frontmatter (which `buildStateFrontmatter` already derives correctly from disk + ROADMAP)". `reconcileCurrentPosition` rewrites the body field …

>> CLUSTER OBS [medium]: The corpus applies its own anti-drift discipline unevenly to two structurally identical single-source claims — and the unguarded one has in fact drifted. ADR-2121's "one seam owns phase-ID parsing" got a CI drift scanner, an npm script, and a reference-identity test. ADR-1769's "one policy source, not three drifting encodings" got nothing, and `syncStateFrontmatter` still hand-rolls the whole poli…
   EV: Guarded: scripts/lint-phase-id-drift.cjs + `check:phase-id-drift` in package.json + tests/phase-id-drift-guard.test.cjs:118 (`scanRepo` zero) and :129-159 (identity guard). Unguarded: `applyStatePreservation` has one call site (src/state.cts:2167) while src/state.cts:1731-1800 re-encodes status/milestone_name/stopped_a…

>> CLUSTER OBS [medium]: Three ADRs in this cluster carry stale "Phase 0 / no production code" status qualifiers while their phases are fully shipped, and the corpus's own honesty machinery structurally cannot see it. The README's ratification bar and the `--check` gate both operate on the leading status token only, so a shipped ADR reading "Accepted (Phase 0 — ADR only ... No production code lands in this PR)" passes CI …
   EV: 1817-*.md:3 and 2121-*.md:3 both carry Phase-0-only qualifiers; Phases 1–2 and 1–4 respectively are shipped (see per-ADR status_evidence). `scripts/gen-adr-index.cjs:241`: `const statusToken = statusRaw ? (statusRaw.match(/^([A-Za-z]+)/) || [])[1] : null;`. `node scripts/gen-adr-index.cjs --check` → "docs/adr/README.md…

>> CLUSTER OBS [medium]: ADR-2207's retained coupling rests on a safety gate that ADR-2207's own change disabled. Decision 3 keeps `isLastPhase` on the phase-completion path and names the verify.cts ship gate as the carrier of its correctness; Decision 1 changes the written value to one that gate does not match.
   EV: 2207-*.md:21 ("the `verify.cts` ship gate that already errors when STATE claims milestone-complete while phases are unstarted") and :33 (verify.cts listed among consumers that "must recognize `All phases complete`"). src/state-transition.cts:951 writes `'All phases complete'`; src/verify.cts:2053 gates on `/milestone c…

>> CLUSTER OBS [medium]: The cluster contains both entry-point extremes from the same year, with nothing deciding what makes a gsd-tools subcommand reachable. ADR-1787 correctly reasons that a front door has value only if the runtime is told to route through it, and wires command → workflow → engine end to end. ADR-1817 shipped the capstone state transition with a CLI alias, docs, and tests but zero references in any work…
   EV: 1787: commands/gsd/next.md → gsd-core/workflows/smart-entry.md → `gsd-tools smart-entry` (gsd-core/bin/gsd-tools.cjs:2100). 1817: `state rebuild` registered at src/command-aliases.cts:174-178 and documented at docs/COMMANDS.md:1684, but `grep -rn "state rebuild\|state-rebuild" gsd-core/workflows/ agents/ commands/ hook…

>> NO-ADR: The STATE.md read-path preservation policy (`syncStateFrontmatter` + `buildStateFrontmatter`)
   WHY: This is the surviving half of the exact bug class ADR-1769 exists to kill, and ADR-1769 disowns it twice — its header claims `syncStateFrontmatter`'s policy portions are superseded (they are not), and its Neutral section explicitly scopes the read-path copy out ("future work may … | EV: src/state.cts:1731-1800 (`syncStateFrontmatter`: status-when-unknown :1740-1742, #948/#2135 milestone_name guard :1753-1774, #905 fallbacks :1791-1800) and src/state.cts:1510+ (`buildStateFrontmatter`), neither calling `getFieldClassificati…

>> NO-ADR: The `--force` escape hatch on `workflows/next.md` (total bypass of Route 0 and Gates 1–3)
   WHY: One flag disables the #160 resume-incomplete-phase invariant and all three safety gates — the exact invariants ADR-1787 builds its entire "one advancement engine" guarantee on. That is a genuine decision with alternatives (no bypass at all / bypass gates but never Route 0 / requi… | EV: gsd-core/workflows/next.md:41-43 ("If `--force` flag was passed, skip all gates, Route 0, and the prior-phase completeness prompt"), :143, :333-336. ADR-1787 Consequences: "Route 0 / Gates 1–3 cannot be bypassed through the new front door."

>> NO-ADR: The STATE.md body-section canon (which `##` sections exist and what a consumer may assume about them)
   WHY: Three modules pattern-match STATE.md sections by literal name, and no record says why those names, what may be added or renamed, or what a consumer may rely on. That ADR-1817 tried to legislate a taxonomy over this surface and shipped with four of seven rows naming sections that … | EV: gsd-core/templates/state.md defines `## Project Reference`, `## Current Position`, `## Performance Metrics`, `## Accumulated Context`, `## Deferred Items`, `## Session Continuity`. Literal-name matching: `deduplicateSessionArchive` matches …

>> NO-ADR: `normalizeStateStatus` as the human-string ↔ machine-enum boundary
   WHY: This one projection is the load-bearing reason ADR-2207 could introduce a new lifecycle value "without code changes" for statusline, workstream inventory, and smart-entry — ADR-2207 says so explicitly and then treats it as a given. A substring-collapse projection over a human-aut… | EV: src/state-document.cts:123 (`normalizeStateStatus`). ADR-2207 line 33: "`normalizeStateStatus` already maps any status containing \"complete\" → `completed`, so it needs no change." CONTEXT.md:59 records the resulting carve-out: "`isComplet…

>> NO-ADR: The `verify` issue-code (W###/E###) namespace
   WHY: ADR-612 makes a decision that depends on W021 being one stable identity ("pinned ... cannot be renumbered", disposition: "kept, message-disambiguated"), and W021 is already two unrelated checks. There is no registry, no allocation rule, and no uniqueness test — while shipped test… | EV: src/verify.cts:1982 (milestone-prefix mismatch, inside the `phase_id_convention === 'milestone-prefixed'` branch at :1973) and src/verify.cts:2086 ("STATE says milestone complete but ROADMAP lists N unstarted phase(s)") both `addIssue('warn…

>> NO-ADR: `## Rebuild Log` retention
   WHY: ADR-1817 §3 makes the audit log a hard contract ("the drop is observable, the audit trail is the contract"), declares it append-only and preserved, and then defers retention to "a separate prune step (out of scope here)" that does not exist and has no owner. An unbounded-count ap… | EV: src/state-transition.cts:2057-2077 (`appendRebuildLogSection` appends only); `REBUILD_LOG_TRUNCATION_LIMIT = 512` (:1677) bounds per-entry text, not entry count; `pruneCore` contains no reference to `REBUILD_LOG_SECTION`. ADR-1817 §3: "Rebu…

==============================================================================
## AGENT: aaeffef7fa9e6c3f3
==============================================================================

--- docs/adr/550-spec-phase-probe-contract.md [ACCURATE] Accepted
    [medium|contradiction] Decision 7e names a compiled `prohibition-probe.cjs` bin that was never built and whose non-existence is now the deliberate design — nothing in ADR-550 reconciles the two.
        EV: D7e (550:48): "Each probe ships its own compiled bin (`edge-probe.cjs`, `prohibition-probe.cjs`) calling a shared `runProbeCli(...)`." There is no `src/prohibition-probe.cts` (`ls src/ | grep probe` → edge-probe.cts, probe-core.cts, ui-consideration-probe.cts) and no `gsd-core/bin/lib/prohibition-pr…
        REC: Amend D7e in place (or add a one-line note) to record that only probes with a deterministic propose stage ship a bin; the prohibition probe is prose-recall per D7b.
    [medium|consequence-drift] CONTEXT.md — which this ADR names as the propagation target for its glossary and 'the decision also lives in' claims — carries a stale description of the enforcement contract that #1279 and #1906 superseded.
        EV: CONTEXT.md:347 still reads "...and `failFirst` stays caller-attested (machine-proof is #1279)", but #1279 landed the machine prover (`defaultProveFailFirst`, src/prohibition-enforcement.cts:560+) and #1906 made the causation control mandatory (src/prohibition-enforcement.cts:619-626), with `failFirs…
        REC: Refresh the Prohibition Probe Module and Probe Core Module predicates in CONTEXT.md to the post-#1906 contract; they are the greppable canon agents read at runtime.
    [medium|lacuna] D7's own justification ('one adapter is hypothetical; two is real') is now a three-adapter fact, and neither the seam's adapter-registration policy nor D7e's deferred dispatcher CLI was ever revisited.
        EV: `src/ui-consideration-probe.cts` (16.0K) is a third `probe-core` adapter — it imports `runProbeCli` (:32) and invokes it (:310) — with its own reference (gsd-core/references/ui-consideration-probe.md), tests (tests/ui-consideration-probe.test.cjs), and a CONTEXT.md predicate (CONTEXT.md:416: `PROBE.…
        REC: Either extend ADR-550 with a short 'third adapter' addendum fixing the adapter-onboarding contract (tier vocabulary, gate placement, bin-or-prose rule), or write a small ADR for the ui-consideration probe.
    [medium|lacuna] The #1154 addendum names model-tier coupling as a known failure mode but decides no policy for it — the budget tier is an escape hatch with no owner.
        EV: 550:160: "abstention is reliable on the default `gsd-verifier` tier (`sonnet`+); the budget tier (`haiku`) heeds the tag only inconsistently and degrades toward current behavior — captured as a documented cost (and a test) so a tier regression is caught". Nothing in the ADR decides whether a project…
        REC: Record a decision: either the honest-verifier abstention path emits an explicit degraded-tier warning when the resolved verifier model is a budget tier, or the ADR states the degradation is accepted and names who owns it.
    (+2 low)

--- docs/adr/1606-prohibition-enforcement-verify-seam.md [STALE_PROPOSED] Proposed
    STATUS-EV: All seven decisions are shipped and verified at the exact lines the ADR cites: `runProhibitionEnforcement` at src/prohibition-enforcement.cts:654 (ADR says 654-732 — correct); `dispositionForProhibition` at src/probe-core.cts:470 (ADR says 470-516 — correct); `isNonVacuousNodeTestRed` at src/prohibition-enforcement.cts…
    [high|status-drift] A fully-shipped, CI-tested, issue-closed subsystem is documented by an ADR that declares itself non-binding, so the enforcement mechanism has no binding architecture-of-record.
        EV: 1606:38: "Until that is agreed, 550's addenda remain authoritative and this ADR is non-binding." Meanwhile `gh issue view 1606` → {"state":"CLOSED","stateReason":"COMPLETED"}, and every decision point is live code (see status_evidence). docs/adr/README.md:188 lists it as Proposed.
        REC: Execute the dedup the ADR names (collapse 550's 2026-06-12/#1279/#1346/#1278 addenda to a pointer) and flip Status to Accepted in the same PR, or — if the dedup is not wanted — accept the ADR and delete its self-imposed precondition.
    [high|contradiction] Circular authority: ADR-550 delegates the #1906 decision-of-record to ADR-1606, while ADR-1606 declares itself non-binding until ADR-550 is edited. The mandatory-cleanFixture rule that ships in code is therefore recorded only in a document that disclaims autho…
        EV: 550:130: "The decision-of-record lives in **ADR-1606's 2026-07-03 (#1906) addendum**; this note supersedes the 'Why opt-in, not required' paragraph immediately above." 1606:38: "Until that is agreed, 550's addenda remain authoritative and this ADR is non-binding." The rule itself is live: src/prohib…
        REC: Break the loop by accepting 1606 first (the #1906 decision exists nowhere else), then perform the 550 dedup as a follow-up. Note that the dedup as originally framed is now unsafe: 1606 is no longer purely duplicative of 550.
    [medium|relation-asymmetry] ADR-1820 (Accepted) names ADR-1606 as the consuming seam, but ADR-1606's Cross-references section does not point back.
        EV: 1820:57: "**ADR-1606** — the verify-time enforcement seam that consumes the authored predicates." 1606:255-265 (Cross-references) lists ADR-550, ADR-857, gsd-core/references/prohibition-probe.md, docs/how-to/resolve-prohibition-findings.md, code paths, and issues #644/#1259/#1278/#1279/#1346/#1906 —…
        REC: Add ADR-1820 to 1606's Cross-references as the spec-optional producer whose descriptor-less predicates this seam hard-gates.
    [medium|lacuna] The #1906 mandatory-control decision is justified entirely by 'zero in-tree node-test consumers', but nothing owns or triggers a re-evaluation when the first consumer appears.
        EV: 1606:239-241: "Per this ADR's own Consequences ('Open conventions … no in-tree `node-test` consumer'), **there are none** … So the mandatory control hard-gates **zero** real checks today." Still true at HEAD — the only dogfood is the lint-rule `local/no-source-grep`; `GSD_PROHIB_SUBJECT` has no in-t…
        REC: Add a cheap guard (a test or lint that fails when a `check_kind: node-test` prohibition appears outside tests/) so the Hyrum's-Law escape hatch closes deliberately rather than silently.
    (+1 low)

--- docs/adr/1820-spec-optional-predicate-rail.md [ACCURATE] Accepted
    [medium|lacuna] Decision 3 — the precedence and authoring contract, the part the ADR calls the reason this is ADR-worthy — has no CI surface at all, in a family whose governing rule is 'the contract is the CI surface'.
        EV: The ADR pins only detection: "The contract is pinned by `tests/spec-section.test.cjs`" (1820:28). That file's 10 tests are all `countSectionDataRows`/`specSectionStatus`/CLI cases. `grep -rln 'specless\|spec-less' tests/` returns no test file (only install-tree JSON fixtures matched on unrelated sub…
        REC: Add a contract test over the deterministic half: (a) a fixture where `supplied === true` proves the fallback does not fire, (b) an assertion that fallback-authored prohibitions carry no `check_*` scalar after `projectProhibitions`.
    [medium|consequence-drift] `workflow.specless_probe_fallback` is declared 'default-ON' but is absent from the defaults manifest — the default is realized only by a shell rule in one reference file, so any other reader of the key gets undefined rather than `true`.
        EV: The key appears in gsd-core/bin/shared/config-schema.manifest.json:35 (validKeys) but a full walk of gsd-core/bin/shared/config-defaults.manifest.json finds no `workflow.specless_probe_fallback` entry (the only plan-related defaults are `plan_review.source_grounding = True` and `plan_review.source_g…
        REC: Register the key in config-defaults.manifest.json (the pattern ADR-22's keys follow) so `/gsd-settings`, `/gsd-config`, and any future consumer see the same default the workflow assumes.
    (+3 low)

--- docs/adr/22-plan-drift-guard.md [ACCURATE] Accepted — ratified 2026-07-17 (originally Proposed 2026-05-29)
    [high|lacuna] The reserved authority rungs are accepted by config validation and hard-block at rung>=3, but no rung>=2 adapter exists — so setting `source_grounding_authority: lsp` yields grep-quality resolution with LSP-authority hard-blocking. The ADR never decides how re…
        EV: src/config.cts:838 `const VALID_SOURCE_GROUNDING_AUTHORITIES = ['grep', 'intel', 'treesitter', 'lsp', 'scip'];` — all five are settable. src/plan-drift-guard.cts:134-137 returns `{ severity: 'HIGH', hardBlock: true }` for MISSING whenever `rung >= HARD_BLOCK_RUNG_THRESHOLD` (=3), with no check that …
        REC: Either reject rungs 2-4 at config-set until an adapter ships, or have `getEffectiveAuthority` clamp unimplemented rungs down to the highest available adapter. Record whichever in the ADR.
    [medium|lacuna] Rung 2 (treesitter) severity is undecided in the Decision text; the code silently chose it, and the 2026-07-17 Ratification quietly restated the decision to cover the gap.
        EV: Decision locked sub-decision (22:59): "`MISSING` from rung 0–1 -> `needs-acknowledgement` … Hard block (HIGH) is reserved for rung >=3". Rung 2 is in neither clause. src/plan-drift-guard.cts:49 `const HARD_BLOCK_RUNG_THRESHOLD = 3;` resolves it to needs-acknowledgement. The Ratification section (22:…
        REC: State the rung-2 disposition in the Decision (not only in Ratification prose) with its rationale — tree-sitter can prove a declaration's kind but not exportedness, which is the actual argument.
    [medium|consequence-drift] The 'Cadence' locked sub-decision was never implemented or written into the workflow that implements every other sub-decision.
        EV: 22:63: "**Cadence.** Resolve once per unique symbol per convergence cycle (cache within the cycle); run the pass every cycle so cycle-N fixes are re-verified in cycle N+1." `grep -n 'once per unique\|cache within\|per convergence cycle' gsd-core/workflows/plan-review-convergence.md` → 0 matches. The…
        REC: Add the dedupe/caching instruction to the workflow step, or drop the sub-decision and remove the 'bounded by the cadence rule' claim from Consequences.
    (+3 low)

--- docs/adr/0656-research-module-seam.md [ACCURATE] Accepted
    [high|consequence-drift] The ADR's central positive consequence — one module change propagating to all seven researcher agents — is materially false: five of the seven never call the seam, and the drift guard built by the follow-up issue codifies that non-adoption as correct.
        EV: Seam-call counts per agent: gsd-phase-researcher.md (research-plan=2, research-store=1, package-legitimacy=2) and gsd-project-researcher.md (research-plan=2, research-store=1) are the only adopters. gsd-advisor-researcher.md, gsd-ai-researcher.md, gsd-domain-researcher.md, gsd-ui-researcher.md, gsd-…
        REC: Either extend the profile drift guard to require the seam calls in every agent that performs discovery, or amend the ADR's Consequences to state which agents are in scope (the honest claim is 'propagates to the two agents that fetch').
    [high|lacuna] The ADR introduces a single-source-of-truth provider module but decides no adoption policy — nothing says which agents must consume it, by when, or what enforces it. That is why the drift it was written to kill is still live.
        EV: The Decision section (0656:16-30) describes the modules and ends with "All three modules are reachable via `gsd-tools query research-plan | research-store | package-legitimacy`." It never states a consumption obligation. The only mechanical guard, scripts/gen-research-agents.cjs, is a `--check` over…
        REC: Record the adoption rule as a decision (e.g. 'any agent that performs live provider discovery MUST route through `research-plan`; prose waterfalls are banned') and back it with a lint over agents/gsd-*researcher*.md.
    [medium|consequence-drift] The 'Deferred to #657' block is stale and overstates what landed: #657 closed as COMPLETED but delivered a frontmatter drift guard, not generated-from-profiles agents.
        EV: 0656:40-42 defers "Collapsing the seven researcher agent `.md` files into generated-from-profiles agents (the prose waterfall duplication in those files is the primary `DEFECT.RESEARCH-PROVIDER-PROSE-DRIFT` site)". `gh issue view 657` → CLOSED/COMPLETED. What shipped is scripts/gen-research-agents.c…
        REC: Update the Deferred block to record what #657 actually delivered and re-open the prose-drift half as its own tracked item, or state that prose duplication is accepted.
    [medium|quality] No 'Alternatives considered' section and no Issue link — a real architectural choice (L2-hybrid: code owns policy, MCP owns fetch) is recorded with no rejected alternatives, so a re-proposal meets no recorded reason.
        EV: docs/adr/0656-research-module-seam.md has exactly four sections: Context, Decision, Consequences (Positive / Deferred to #657 / Known constraint). Compare the sibling ADRs in this cluster: ADR-22 has a five-entry Alternatives section, ADR-550 added one by addendum, ADR-1606 has four entries with re-…
        REC: Add an Alternatives section recording at least: pure-prose provider policy (status quo), in-process HTTP fetch instead of the MCP seam, and keeping `slopcheck` as the install gate — each with the standing reason and a re-open condition.
    (+1 low)

>> CLUSTER OBS [high]: The cluster's consolidation attempt has deadlocked into a state where the currently-shipped enforcement rule has no binding architecture-of-record. ADR-1606 was written to replace ADR-550's addenda, declared itself non-binding until that replacement happens, and the replacement never happened — but in the interim the #1906 mandatory-cleanFixture decision was made *inside ADR-1606* and shipped, and…
   EV: 550:130: "The decision-of-record lives in **ADR-1606's 2026-07-03 (#1906) addendum**". 1606:38: "Until that is agreed, 550's addenda remain authoritative and this ADR is non-binding." The rule is live at src/prohibition-enforcement.cts:619-620. `gh issue view 1606` → CLOSED/COMPLETED; docs/adr/README.md:188 still lists…

>> CLUSTER OBS [high]: Answering the cluster question directly: ADR-1606 is NOT redundant with what ADR-550 and ADR-1820 already ship, and the dedup as originally framed is now unsafe. Decisions 1-3 and 5-7 of ADR-1606 are faithful re-statements of ADR-550's addenda (verified line-by-line against src/prohibition-enforcement.cts and src/probe-core.cts). Decision 4 as amended by the #1906 addendum is a genuinely new decis…
   EV: 1606 D1/D2/D3/D5/D6/D7 map 1:1 onto 550's 2026-06-12, #1279, #1278 addenda. The #1906 mandatory-node-test-control ruling (1606:219-253) has no counterpart in 550 other than the pointer at 550:130. Collapsing 550's addenda to a pointer at ADR-1606 while ADR-1606 remains non-binding would leave the whole enforcement subs…

>> CLUSTER OBS [medium]: No contradiction found between the SPEC-optional rail (ADR-1820) and the probe contract (ADR-550/1606) — the composition is coherent and the risky interaction is explicitly handled in the workflow. But it is handled only in prose, one layer below any ADR.
   EV: ADR-1820 D3 reuses `projectProhibitions` unchanged and inherits the fail-closed disposition; the concrete rule that makes this safe is in gsd-core/references/specless-probe-fallback.md §B — "author each kept prohibition as **flagged-unverified with NO wired-check descriptor**. NEVER write `check_kind` / `check_target` …

>> CLUSTER OBS [medium]: Three of the five ADRs in this cluster declare a mechanical CI/config surface that turns out to live one layer lower than claimed — the family's own governing rule ('the CI-testable surface is the contract') is applied unevenly.
   EV: ADR-1820's precedence contract has no test (`grep -rln 'specless\|spec-less' tests/` → no test file) and its 'default-ON' toggle is absent from config-defaults.manifest.json. ADR-22's 'Cadence' sub-decision appears in no workflow. ADR-0656's 'propagates to all researcher agents' is enforced by a drift guard derived ver…

>> CLUSTER OBS [low]: Cross-ADR pointers in this cluster are consistently one-way, downhill from newer to older. The generated index does not record relations for any of the five, so nothing catches it.
   EV: 1820 → 550, 1606, 857, 457; `grep -n '1820'` returns 0 matches in both 550 and 1606. 1606 → 550, 857; 550 → 1606 exists (550:130, 550:169-170) so that pair is symmetric, but 1606 → 1820 does not. docs/adr/README.md shows '—' in the relations column for ADR-22, ADR-550, ADR-0656, ADR-1820, and ADR-1606.

>> CLUSTER OBS [low]: Every ADR in this cluster is decision-shaped rather than a spec in disguise, with one exception: ADR-550 has crossed into being a running changelog of an implementation, and ADR-0656 is thin on the decision half (no alternatives).
   EV: ADR-550 is 42.9KB across six dated addenda that revise D3/D4 three times without ever restating them; ADR-0656 is 4.9KB with no Alternatives section and no Issue header. ADR-22, ADR-1820, and ADR-1606 all carry Context/Decision/Consequences/Alternatives with re-open conditions and are well-formed.

>> NO-ADR: The ui-consideration probe as the third `probe-core` adapter (#1867)
   WHY: ADR-550 D7 justified extracting `probe-core` with "one adapter is hypothetical; two is real" and deferred a dispatcher CLI on that two-adapter basis. A third adapter has since shipped with its own verification-tier vocabulary, its own soft-gate placement (ui-phase, not spec-phase… | EV: src/ui-consideration-probe.cts (16.0K) imports `runProbeCli` (:32) and invokes it (:310); gsd-core/references/ui-consideration-probe.md; tests/ui-consideration-probe.test.cjs (18.0K) + tests/ui-consideration-probe-docs-fixtures.test.cjs; CO…

>> NO-ADR: Runtime resolution and availability policy for compiled `bin/lib/*.cjs` artifacts invoked from workflow bash (the `_gsd_lib()` idiom)
   WHY: A real decision with a real alternative was made silently: when a generated artifact is missing at runtime the workflow hard-fails the whole planning/spec run rather than degrading to no-probe. It also encodes a five-location search order and a 'build only inside a source checkou… | EV: gsd-core/workflows/spec-phase.md:198-237 (5-path search, `[ -f "$_GSD_RT/tsconfig.build.json" ] && [ -f "$_GSD_RT/src/edge-probe.cts" ]` build gate, "edge-probe.cjs not found — reinstall GSD or run `npm run build:lib`"); gsd-core/references…

>> NO-ADR: Mandatory-consumption policy for the research seam across the seven researcher agents
   WHY: ADR-0656 created a single-source-of-truth module for provider policy; the follow-up then built a drift guard that pins five of the seven agents as legitimately not calling it. Whether the seam is mandatory or advisory is a genuine decision — it determines whether `DEFECT.RESEARCH… | EV: scripts/research-profiles.cjs:75,91,108 `requiredSeamCalls: []` for advisor/ai/domain researchers; :124,139 only `'gsd_run query commit'` for ui-researcher and synthesizer. Seam-call counts: only gsd-phase-researcher.md and gsd-project-rese…

>> NO-ADR: Policy for config enum values that validate but have no implementation behind them ('reserved' rungs)
   WHY: `plan_review.source_grounding_authority` accepts `treesitter|lsp|scip` today, the docs say they have no effect, and the severity seam hard-blocks at those rungs. That triple is a fail-dangerous configuration reachable by any user, and it generalizes: nothing in the repo decides w… | EV: src/config.cts:838 `VALID_SOURCE_GROUNDING_AUTHORITIES = ['grep','intel','treesitter','lsp','scip']` — all settable; src/plan-drift-guard.cts:49 `HARD_BLOCK_RUNG_THRESHOLD = 3` and :134-137 return `{severity:'HIGH', hardBlock:true}` with no…

>> NO-ADR: The config-default registration seam: which keys get an entry in `config-defaults.manifest.json` versus being defaulted at the call site
   WHY: Two keys in this cluster take opposite paths with no stated rule. `plan_review.source_grounding` is registered as a manifest default and is therefore visible to settings UI, `config-get`, and any future consumer; `workflow.specless_probe_fallback` is validKeys-only and its 'defau… | EV: gsd-core/bin/shared/config-defaults.manifest.json contains `plan_review.source_grounding = True` but a full recursive walk finds no `workflow.specless_probe_fallback`; gsd-core/bin/shared/config-schema.manifest.json:35 registers the key in …

==============================================================================
## AGENT: a3761810350562e21
==============================================================================

--- docs/adr/452-eslint-lint-harness.md [ASPIRATIONAL_ACCEPTED] Accepted
    STATUS-EV: The harness itself is real and gating: eslint.config.mjs (451 lines) exists, package.json `lint` = `eslint . --cache`, `lint:ci` composes it, and .github/workflows/test.yml:112 runs `npm run lint:ci`. Owning issue #452 is CLOSED/COMPLETED (`gh issue view 452`). BUT four of the six numbered Decision clauses (4, 5, 6, an…
    [high|consequence-drift] Decision 4's two `no-restricted-syntax` bans — described as `error` from day one — do not exist in the config.
        EV: ADR 452:43-46 specifies `CallExpression[callee.name='setTimeout']` inside `Program > ExpressionStatement` and `MemberExpression[property.name='only'][object.name=/^(test|it|describe)$/]`. The only `no-restricted-syntax` block in the repo is eslint.config.mjs:370-380, whose two selectors are `AwaitEx…
        REC: Either add the two selectors or amend Decision 4 to record what actually shipped and why the `.only` backstop was judged redundant with `eslint-plugin-no-only-tests`.
    [high|consequence-drift] `local/no-elapsed-assertion` does not detect the shapes the ADR says it bans, and is still `warn`.
        EV: ADR 452:37 says the rule 'bans `assert` on elapsed-time values (e.g., `Date.now() - start > N`, `process.hrtime`, `performance.now()` comparisons in assertions)'. eslint-rules/no-elapsed-assertion.cjs:27 defines `TIMING_PROPS = /^(elapsed|duration|took|ms)$/` and `containsTimingRef` (lines 29-62) ma…
        REC: Extend the rule to recognize `Date.now()`, `process.hrtime(.bigint)`, and `performance.now()` operands, or amend the ADR to state the rule is name-heuristic only.
    [high|consequence-drift] `local/no-source-grep` implements only the first half of its ADR spec, and decides source-path-ness by regex over raw text — the exact technique the ADR's Context rejected.
        EV: ADR 452:35 specifies two clauses: (i) readFileSync-then-text-method, and (ii) 'also bans `assert.match/doesNotMatch` on `.stdout/.stderr` without JSON round-trip'. `grep -n 'doesNotMatch|assert.match|stdout|stderr' eslint-rules/no-source-grep.cjs` returns 0 matches — clause (ii) was never built. Sep…
        REC: Implement the `.stdout/.stderr` clause (it is the only mechanism ADR-456's typed-surface mandate has) and replace `looksLikeSourcePath` with literal/template-node analysis.
    [high|status-drift] The warn→error promotion gate the ADR names (#453) closed as COMPLETED without performing the promotion, and no successor owner exists.
        EV: ADR 452:6,39,58,82 all designate '#453' as the follow-up that 'flips all three to error'. `gh issue view 453 --repo open-gsd/gsd-core` returns `CLOSED COMPLETED fix(tests): deterministic concurrency via injectable clock seam; delete flaky racing tests` — a different work item entirely. Today `local/…
        REC: Open a real promotion issue, or amend the ADR to record which rules stay at `warn` permanently and why.
    [medium|consequence-drift] The ADR's type-aware mechanism (`tsconfig.lint.json` project reference) no longer exists; hand-written `.cjs` gets no type-aware linting at all.
        EV: ADR 452:31 says typescript-eslint runs with 'type-aware rules enabled via `tsconfig.lint.json` project reference'; ADR 452:22,86 treat `tsconfig.lint.json` as the stopgap. `ls tsconfig.lint.json` → No such file. Type-aware linting is now wired at eslint.config.mjs:239-249 to `./tsconfig.build.json` …
        REC: Amend the ADR to point at tsconfig.build.json + the src/**/*.cts scope, and state explicitly that remaining hand-written .cjs surfaces (gsd-tools.cjs, scripts/, eslint-rules/) are un-typechecked.
    [medium|missing-artifact] Decision 5's `eslint-plugin-n` rules are not enabled.
        EV: ADR 452:47-49 names `n/no-missing-require` ('catches import-graph drift for hand-written CJS files') and `n/no-unsupported-features/es-syntax` against `engines.node`. eslint.config.mjs:322-324 enables only `n/no-process-exit: 'error'` and `n/no-path-concat: 'error'`. Neither ADR-named rule appears i…
        REC: Enable both rules or drop the import-graph claim from the ADR summary.
    [medium|lacuna] `warn` is a permanent state by construction and the ADR sets no expiry, owner, or promotion criterion.
        EV: package.json `"lint": "eslint . --cache --cache-location node_modules/.cache/eslint/"` carries no `--max-warnings`, and `lint:ci` composes it unchanged — so a rule at `warn` can never fail CI. ADR 452:39 says 'The CI lint gate (`npm run lint`) does not fail on warnings' as an intentional transient, …
        REC: Record a warn-severity policy: every `warn` rule carries a promotion issue and a date, or `--max-warnings 0` with an explicit per-rule downgrade list.
    (+3 low)

--- docs/adr/456-test-rigor-architecture.md [ASPIRATIONAL_ACCEPTED] Accepted
    STATUS-EV: Owning issue #456 is CLOSED/COMPLETED and the named docs exist (TESTING-STANDARDS.md at root, docs/TESTING-SUITES.md, 21.7K). fast-check ^4.8.0 is a devDependency and appears in 60 test files; Stryker ^9.6.1 runs PR-gating via .github/workflows/mutation.yml. But three of the four policies are substantially not as-built…
    [high|contradiction] §(d)'s delete-bad-tests sweep did not happen; the exemption mechanism was instead institutionalized in an allowlist ratchet — which is Rejected Alternative (a).
        EV: ADR 456:105 lists 'Permanent `allow-test-rule` … in place for more than one release cycle without a tracked cleanup issue' as a DELETE category, and ADR 456:135 requires the sweep to 'complete before the three ESLint rules flip to error (i.e., before #453 merges)'. #453 is CLOSED/COMPLETED. Live sta…
        REC: Either schedule the burn-down with a deadline and an owner, or amend §(d) to record that grandfathering-with-a-ratchet was adopted in place of deletion, and state the criterion for retiring an entry.
    [high|consequence-drift] §(b)'s Stryker description is materially wrong: no `--since origin/next`, no 80% policy threshold, no 30-minute timeout, and gating covers only 7 hand-registered modules.
        EV: ADR 456:67,122 state '--since origin/next', 'default mutation score threshold is 80% (killed/total)', 'the job has a 30-minute timeout'. As-built .github/workflows/mutation.yml:139 runs `npx stryker run --incremental --mutate "${{ matrix.mutate }}"` (no --since); :102 sets `timeout-minutes: 15`; :13…
        REC: Rewrite §(b) to describe the per-module ratchet, or record 80% as an aspirational TARGET (which scripts/mutation-matrix.cjs:130 already calls it) distinct from the enforced floor.
    [high|lacuna] The ADR never decides which modules are subject to mutation gating or how a module enters that set — so an unregistered module has zero mutation gating forever and nothing notices.
        EV: ADR 456 §(b) speaks as if mutation testing applies to the codebase ('PRs touching modules with surviving Stryker mutants…'). The as-built answer is a hand-maintained map with a documented human protocol living only in a code comment: scripts/mutation-matrix.cjs:80-106 ('every covered module declares…
        REC: Add a decision (here or in a new ADR) covering entry criteria for COVERED, who raises floors, and what happens to unregistered modules.
    [medium|consequence-drift] §(a)'s normative mechanism — `node:test` `mock.timers` — is used nowhere; a different clock seam shipped and the ADR was never amended.
        EV: ADR 456:31 mandates 'an injectable clock seam backed by `node:test` `mock.timers`', and the code block at ADR 456:43-49 shows `t.mock.timers.enable(['Date'])` / `.setTime(0)` / `.tick(...)`. `grep -rIn 'mock\.timers' tests/` → 0 files. What actually shipped is a module seam: src/clock.cts (4.6K) con…
        REC: Amend §(a) to name src/clock.cts + tests/helpers/clock.cjs as the canonical seam; the intent is satisfied but the written API is not the one in use.
    [medium|consequence-drift] §(c)'s typed-surface mandate has no enforcement and is broadly violated.
        EV: ADR 456:73-75 forbids asserting on 'Rendered CLI text (stdout/stderr string content beyond exit-code and parse-able JSON)', with an explicit BAD example `assert(stdout.includes('Phase 1.1'), stdout)` at ADR 456:89. `grep -rIn 'stdout\.includes(|stderr\.includes(|assert\.match(stdout' tests/ --includ…
        REC: Ship the missing rule clause, or downgrade §(c) from a mandate to a documented convention with a named review owner.
    [medium|consequence-drift] The `allow-test-rule` escape the ADR depends on is a whole-file opt-out — the exact shape ADR-1703 condemned and removed elsewhere.
        EV: eslint-rules/no-source-grep.cjs:33-37: `const comments = sourceCode.getAllComments(); const hasAllowAnnotation = comments.some((c) => /allow-test-rule:\s*\S/.test(c.value)); if (hasAllowAnnotation) return {};` — one matching comment anywhere in a file disables the rule for the whole file. ADR 1703:3…
        REC: Narrow the opt-out to line scope, or record in the ADR that whole-file suppression is accepted for this rule and why.
    [medium|lacuna] §(d)'s ban on untracked permanent skips has no enforcement, unlike its sibling ban on untracked exemptions.
        EV: ADR 456:109: 'Tests must not be … marked with a skip that has no associated tracking issue and deadline.' `grep -rIn 'it.skip|test.skip|describe.skip|{ skip: true' tests/ --include=*.test.cjs` → 16 sites. There is a CI gate for the annotation case (scripts/lint-allow-test-rule-refs.cjs, wired into p…
        REC: Add a skip-requires-issue lint mirroring lint-allow-test-rule-refs.cjs, or state that skips are review-enforced.
    (+2 low)

--- docs/adr/457-generated-cjs-single-source.md [ACCURATE] Accepted
    [high|consequence-drift] Nine tsc-emitted modules are committed to git — the rejected model 1 — with no parity test and no drift gate.
        EV: Decision 1 (ADR 457:104-107) chose 'model 2 (build at publish): TS source is canonical, `.cjs` is a gitignored artifact', and Rejected Alternative (b) (ADR 457:145-147) rejects checking in both because it 'imports a drift invariant, parity tests, and dual commits for zero runtime benefit'. `git ls-f…
        REC: `git rm --cached` the nine, add them to .gitignore, and add a CI assertion that no path matching the eslint tsc-generated ignore set is tracked.
    [high|lacuna] 'Which .cjs is generated' is answered by three uncoordinated registries with no invariant tying them together, and they already disagree in both directions.
        EV: Programmatic diff: .gitignore holds 160 `/gsd-core/bin/lib/*` entries; the `ignores` array in eslint.config.mjs holds 164; tsconfig.build.json defines the real answer implicitly via `include: ["src/**/*.cts"]`. Seven paths are eslint-ignored but not gitignored (listed above); three are gitignored bu…
        REC: Derive both lists from tsconfig.build.json at check time (a `--check` script in lint:generated-sync), or stamp an `@generated` header in the emit and assert it.
    [medium|consequence-drift] Decision 5's precondition was violated: tsconfig.lint.json was retired while hand-written .cjs remain, leaving those surfaces with no type checking at all.
        EV: ADR 457:118-119: 'Wire type-aware linting to the real `tsconfig.json` as modules become TS; retire `tsconfig.lint.json` only when the last hand-written `.cjs` is gone.' `ls tsconfig.lint.json` → No such file. Hand-written `.cjs` that remain in gsd-core/bin/lib with no `src/*.cts` and no `@generated`…
        REC: Either restore a lint-only tsconfig for the residual .cjs surface or amend Decision 5 to record that those surfaces are deliberately un-typechecked and name the migration issue.
    (+2 low)

--- docs/adr/1703-portability-enforcement-architecture.md [ACCURATE] Accepted
    [medium|lacuna] 'Zero escape hatches' is enforced only against inline disable directives; config-level de-scoping is an unguarded and much cheaper escape.
        EV: ADR 1703:130-145 enumerates exactly two things that must hold — rules must be precise, and 'the disable directive is itself banned for these rules' — and the shipped guard (tests/portability-rule-disable-ban.test.cjs) scans files for `eslint-disable*` directives. Nothing guards the alternative: addi…
        REC: Extend the disable-ban test (or add a sibling) to assert that no path under tests/, src/, or the named production files is excluded by the ignores array or by glob narrowing.
    [medium|lacuna] No policy governs how a new top-level production file enters the portability globs; one already entered without appearing in the ADR.
        EV: ADR 1703:122-126 (§Architecture/Wiring) names the glob expansion targets as 'bin/install.js and the build/install scripts'. The shipped block at eslint.config.mjs:278 is `files: ['bin/install.js', 'bin/gsd-mcp-server.js', 'scripts/build-hooks.js']` — `bin/gsd-mcp-server.js` is not mentioned anywhere…
        REC: Record the entry rule for the production glob (e.g. 'every top-level bin/*.js is covered by default') and add gsd-mcp-server.js to the as-built note.
    (+1 low)

--- docs/adr/1610-workflow-agent-size-budget-ratchet.md [ACCURATE] Accepted
    [medium|lacuna] Decision 4's anti-gaming rule is prose-only: nothing measures the thing the byte count is a proxy for.
        EV: ADR 1610:79-87 states 'The real target is total context loaded at runtime' and that eager `@~/.claude/gsd-core/references/...` extraction is proxy-gaming. The only mechanised signal is the per-file byte count that the same paragraph says can be gamed by relocation; there is no guard computing the tr…
        REC: Either add a transitive @-closure byte metric alongside the per-file baseline, or state explicitly in the ADR that anti-gaming is review-enforced and name the reviewing role.
    (+2 low)

--- docs/adr/227-input-validation-shape-not-just-type.md [ACCURATE] Accepted
    [high|lacuna] The ADR states a repo-wide MUST for 'any trust boundary' but never enumerates the boundaries, assigns no owner, and ships no enforcement — leaving it a convention doc shaped like a mandate.
        EV: ADR 227:43 'Defensive normalization at any trust boundary MUST validate two layers'. There is no lint rule, registry, or test for it: package.json `lint:ci` composes lint, lint:skill-deps, lint:generated-sync, lint-test-file-count, lint-command-contract, lint-pr-check-project-dir, lint:legacy-name, …
        REC: Either add a registry-style guard mirroring lint-resolution-provenance.cjs for trust-boundary fields, or restate the Decision as a review convention and say so in Consequences.
    [medium|missing-artifact] The ADR's only stated mitigation for its acknowledged downside was never built.
        EV: ADR 227:66: 'Silent coercion hides invalid inputs from callers … Mitigate with an opt-in debug log (`process.env.GSD_DEBUG`) that surfaces the coercion without affecting production behavior.' `grep -rn 'GSD_DEBUG' src/ gsd-core/ scripts/ tests/` → zero hits repo-wide. src/observability/event.cts:14-…
        REC: Implement the GSD_DEBUG coercion warn (a one-line guard in isValidParentTraceId's caller), or strike the mitigation and record that the tradeoff is accepted unmitigated.
    [medium|lacuna] The coerce-vs-throw boundary is an escape hatch with no policy and no recorder.
        EV: ADR 227:48 'Throw only if the surrounding codebase treats throws as a normal-flow signal (it usually does not…)' and ADR 227:104 'Fields where an invalid value is genuinely fatal (not just malformed) may throw — that is a per-field decision, not the general rule.' No criterion distinguishes 'malform…
        REC: Give the hatch a shape: a named criterion plus a requirement that throwing fields be listed somewhere greppable (CONTEXT.md predicate or the module's doc block).
    (+1 low)

--- docs/adr/1411-resolution-provenance.md [ASPIRATIONAL_ACCEPTED] Accepted
    STATUS-EV: Real machinery shipped: src/resolution.cts (2.6K) exists; loadConfigResolved + `interface ConfigResolution { config, source, degraded }` at src/config-loader.cts:419-445 with the full branch→provenance table documented at :437-443; the P4 CI guard scripts/lint-resolution-provenance.cjs exists and RUNS (package.json `li…
    [high|consequence-drift] Decision sub-rule 1 ('Resolution MUST NOT depend on an arbitrary descendant cwd') is inverted at the very seam it binds — the MUST became a caller obligation for ~30 call sites.
        EV: ADR 1411:29: 'Deterministic anchoring. Resolve the project root through one walk-up module. Resolution MUST NOT depend on an arbitrary descendant cwd.' src/config-loader.cts:446-449: 'NOTE: loadConfigResolved resolves from cwd AS-IS (no walk-up). Callers that need ancestor-anchoring (e.g. cmdAgentSk…
        REC: Amend the Decision to state the as-built contract (resolver is cwd-faithful; anchoring is the caller's responsibility) and add a guard listing which callers must anchor — otherwise the invariant is unowned.
    [high|consequence-drift] The recurrence-prevention claim rests on a CI guard that self-documents as unable to detect the class, with a registry of exactly one verb.
        EV: ADR 1411:89 (Amendment): 'Recurrence prevention does not depend on a shared envelope — it is delivered by P4's CI guard (a configured input resolving empty must carry a `reason`).' scripts/lint-resolution-provenance.cjs:9-11 states the opposite: 'This is a REGRESSION-LOCK and REGISTRATION RATCHET, N…
        REC: Amend the Amendment: state that recurrence prevention is a manual registration ratchet, and either add registration to a PR checklist/template or build a heuristic detector with an allowlist.
    [medium|consequence-drift] The stated ~18-verb migration did not happen and no descope is recorded.
        EV: ADR 1411:52: 'One envelope to learn; ~18 verbs migrate onto it across phases P3–P4.' `grep -rn 'loadConfigResolved' src/**/*.cts` shows exactly one non-definition caller: src/init.cts:2259 `const { config, source, degraded } = loadConfigResolved(projectRoot);` (plus the import at src/init.cts:66). F…
        REC: Record the actual adoption state and either name a follow-up issue for the remaining verbs or mark P3–P4 as descoped-by-design.
    [medium|lacuna] The 'grandfather burn-down' the ADR invokes has no deadline, owner, or shrink criterion in the ADR.
        EV: ADR 1411:39: 'A configured input that resolves empty without a reason is a CI-guarded regression (grandfather burn-down, mirroring the `no-adhoc-markdown-parsing` rule).' The mechanism exists — scripts/lint-resolution-provenance.cjs:26-29 documents 'add the verb to scripts/lint-resolution-provenance…
        REC: Lift the registration protocol into the ADR's Decision and attach a burn-down owner.
    (+1 low)

>> CLUSTER OBS [high]: The cluster splits cleanly into ADRs written BEFORE implementation (452, 456, 227, 1411) — all of which drifted and none of which was amended — and ADRs written AT closeout or retroactively WITH verification (1703, 1610) — both of which verify clean. The two clean ones are the only two that carry an explicit as-built reconciliation section. This is a repeatable, mechanisable lesson, not a coincide…
   EV: ADR 1703:189-235 ('Phase 7 — as-built / acceptance') names four deviations from its own Phase-0 text and reconciles each; every artifact it names was verified present. ADR 1610:16-29 ('Ratification (2026-07-17): Proposed → Accepted') lists five falsifiable evidence lines with file:line citations; all five re-verified a…

>> CLUSTER OBS [high]: Grandfathering is simultaneously banned and standard practice, with no ADR saying when a ratchet is legitimate. The same primitive that one ADR in this cluster exists to destroy is the sanctioned enforcement mechanism for three neighbouring gates, two of them governed by ADRs in this same cluster.
   EV: ADR 1703:52-53: 'Hard-fail, no ratchet, no grandfathering. There is no KNOWN_OFFENDERS allowlist. Every existing and currently-grandfathered violation is fixed, not registered.' Meanwhile scripts/lib/allowlist-ratchet.cjs survives and is consumed by tests/workflow-size-budget.test.cjs and tests/agent-size-budget.test.c…

>> CLUSTER OBS [high]: `warn` severity is a permanent, non-blocking state by construction, and two ADRs' central consequence clause depends on a promotion event that already happened under a different name and did not perform the promotion.
   EV: package.json `"lint": "eslint . --cache --cache-location node_modules/.cache/eslint/"` carries no `--max-warnings`; `lint:ci` composes it unchanged, so nothing at `warn` can fail CI. ADR 452:6,39,58,82 and ADR 456:69,115,124,135 both designate #453 as the flip-to-error gate. `gh issue view 453 --repo open-gsd/gsd-core`…

>> CLUSTER OBS [high]: Three uncoordinated registries answer 'which .cjs is a generated artifact', and their disagreement is what allowed nine tsc-emitted modules to stay committed under an ADR that explicitly rejected checking them in.
   EV: Programmatic diff at HEAD: .gitignore holds 160 `/gsd-core/bin/lib/*` entries; the `ignores` array in eslint.config.mjs holds 164; tsconfig.build.json defines the real answer via `include: ["src/**/*.cts"]` + `outDir: gsd-core/bin/lib`. Seven paths are eslint-ignored-as-generated but absent from .gitignore (claude-orch…

>> CLUSTER OBS [low]: The two oldest ADRs in the cluster link their governing issues at a repository that no longer exists, which is what made the #453 misattribution invisible.
   EV: ADR 452:81-82 and ADR 456:147,149 use `https://github.com/open-gsd/get-shit-done-redux/issues/{452,453,456}`. The live repo is `open-gsd/gsd-core` (used by ADR 1703:5-6, ADR 1610, ADR 457:170). Following the stale links does not surface the fact that #453's actual title is unrelated to the warn→error promotion both ADR…

>> CLUSTER OBS [medium]: Two independent enforcement mechanisms in this cluster both use a whole-file comment opt-out; one ADR removed that design as unsound while the other still depends on it.
   EV: ADR 1703:32-33 rejects `// windows-portability-ok:` because it is 'matched by a whole-source regex, coarse enough that a single occurrence anywhere in a file can disable that file's check' — and the sweep confirmed zero live occurrences remain. eslint-rules/no-source-grep.cjs:33-37 implements the identical pattern for …

>> NO-ADR: The allowlist-ratchet primitive (`scripts/lib/allowlist-ratchet.cjs`) — when grandfathering is a legitimate enforcement strategy vs. an anti-pattern
   WHY: A real decision with a real alternative was made repeatedly and silently, and the repo now holds two contradictory positions with no arbiter. ADR-1703 bans ratchets categorically for its domain ('no ratchet, no grandfathering… Every existing and currently-grandfathered violation … | EV: scripts/lib/allowlist-ratchet.cjs (10.3K) exports assertFileBaseline (used by ADR-1610's tests/workflow-size-budget.test.cjs and tests/agent-size-budget.test.cjs) and assertWithinAllowlist (used by scripts/lint-resolution-provenance.cjs:34 …

>> NO-ADR: Which modules are mutation-gated at all, and at what score — the `COVERED` registry + per-module minScore floors in scripts/mutation-matrix.cjs
   WHY: This silently replaced ADR-456's written policy (one 80% threshold, incremental --since across the codebase) with a fundamentally different one (a hand-curated 7-module set, each with its own floor between 52 and 80, and a trivially-green gate when nothing covered changed). That … | EV: scripts/mutation-matrix.cjs:122 `const COVERED = {` with 7 entries (context-utilization, prompt-budget, frontmatter, adr-parser, config-schema, active-workstream-store, core-utils); minScore values 80, 66, 62, 68, 52, 80, 75 at lines 131-18…

>> NO-ADR: CI test-scope selection — `scripts/ci-test-scope.cjs` + `ci-prepare-test-scope.cjs` decide which tests a PR actually runs
   WHY: This determines what 'green CI' means for every PR in the repo, and it is a hand-maintained path→test mapping. The alternatives are substantive (always-full matrix vs. path-mapped subset vs. dependency-graph-derived selection) and the failure mode is silent and severe: a path tha… | EV: .github/workflows/test.yml:195,199 — `node scripts/ci-prepare-test-scope.cjs` then `node scripts/run-tests.cjs --files-from .ci-selected-tests.txt`. scripts/ci-test-scope.cjs holds hand-written RULES entries, e.g. :231-240 ('workflow prompt…

>> NO-ADR: The 'no external dependencies in core' rule — its scope, what 'core' means, and how it reconciles with the two runtime dependencies the package now ships
   WHY: This unwritten rule is doing load-bearing work as a hard constraint in at least three ADRs' rejected-alternatives sections, deciding real technology choices (no Zod, no remark/markdown-it). But 'core' is never defined, and package.json now declares two runtime dependencies plus o… | EV: ADR 227:100 rejects Zod because 'Adding a runtime dependency to the core library violates the project's no-external-dependencies policy for `gsd-tools.cjs`'. ADR 1372:31,90-91 calls it 'a hard rule' ('no external dependencies in core') to r…

>> NO-ADR: The build-ordering contract — a raw checkout is non-functional until `tsc` runs (prepare / prepack / pretest / CI build-before-lint)
   WHY: ADR-457 raised this as an explicit Open Question ('Does any consumer rely on `bin/lib/*.cjs` being present in a raw (un-built) checkout? If so, build-at-publish needs a prepare-script bridge') and it was answered in code across four npm lifecycle hooks and a CI step — but never w… | EV: package.json: `"prepare": "npm run build:lib"`, `"prepack": "npm run build:lib"`, `"pretest": "npm run build:lib && npm run lint:skill-deps"`, `"pretest:coverage"` likewise, `"prepublishOnly": "npm run build:lib && npm run build:hooks"`. .g…

==============================================================================
## AGENT: ab9b567b5525bfbb9
==============================================================================

--- docs/adr/230-introduce-next-integration-branch.md [AMBIGUOUS] Proposed
    STATUS-EV: The architectural core is shipped and load-bearing at HEAD aa0f7dee: `gh api repos/open-gsd/gsd-core --jq .default_branch` → `next`; auto-backmerge.yml:41 `if: true`; pr-target-validator.yml:48 `WARN_ONLY: 'false'`; auto-branch.yml:79 `ref: 'heads/next'` with a heads/main fallback at :86. Issue #230 is CLOSED/COMPLETED…
    [high|contradiction] The ADR's own named remediation tool, `scripts/setup-branch-protection.sh`, would revert an Accepted ADR if run — it sets `next`'s `required_status_checks.strict` to `false` and `enforce_admins` to `true`, both of which ADR-415 decided the other way.
        EV: scripts/setup-branch-protection.sh:108 `require_up_to_date=false` for the `next` case, fed into the payload at :134 `strict: $require_up_to_date`; header comment at :38 still reads 'require branches to be up to date before merging (OFF — this is the whole point)'. ADR-415 Decision 1 (Accepted 2026-0…
        REC: Either update the script's `next` knobs to `require_up_to_date=true` / `enforce_admins=false` and amend ADR-230's Decision table to match ADR-415, or amend ADR-415. Do not leave the ADR corpus and the apply script disagreeing on the same tw…
    [high|lacuna] The ADR decides a branch-protection posture but never decides who owns it, how it is applied durably, or how drift from it is detected. Its entire enforcement story is a bash script a human may or may not run.
        EV: No repo artifact reads back live protection: `grep -rl setup-branch-protection tests/ scripts/ .github/ docs/` returns only the script itself, docs/branching.md, and this ADR — no test, no workflow. The repo has ten in-tree drift `--check` generators chained under `lint:generated-sync` (package.json…
        REC: Decide the protection substrate (classic protection applied by CI, or promote the Rulesets to `active` with literal `ref_name` conditions) and add a scheduled workflow that diffs live protection against the intended posture and fails. Name …
    [medium|missing-artifact] `.github/workflows/hotfix.yml` — named in the Mechanical-changes table and the subject of two Phase-3 patch blocks — does not exist. Its logic was folded into release.yml.
        EV: `test -f .github/workflows/hotfix.yml` → absent; `git log --diff-filter=D -- .github/workflows/hotfix.yml` → e14218a4 'fix(#676): consolidate hotfix into release.yml (delete standalone hotfix workflow) (#678)'. The equivalent steps now live in release.yml's `create` job: 'Create hotfix branch from b…
        REC: Update the Mechanical-changes table and Phase 3/4 sections to point at release.yml's `create` job, and cross-reference #678.
    [medium|consequence-drift] 'Branch from `next`, not `main`' is enforced by a repo *setting*, not by release.yml. The Phase-3 create patch was never applied; the workflow branches from whatever `actions/checkout` defaulted to.
        EV: release.yml:147 is bare `git checkout -b "$BRANCH"`, with no ref argument and no preceding `git fetch origin next:next`. The `create` job's checkout (release.yml:114-116) passes no `ref:`, so it lands on the repo default branch. The ADR's Phase-3 diff specified `git checkout -b "$BRANCH" next 2>/dev…
        REC: Either apply the Phase-3 create patch (explicit `ref: next` on the checkout, or an explicit base in `git checkout -b`) or record in the ADR that 'branch from next' is delegated to the default-branch setting and is therefore coupled to it.
    [medium|consequence-drift] The script's `REQUIRED_CHECKS_NEXT` list no longer matches the live required contexts, so the 'idempotent' script is not idempotent — running it would drop two live required checks and add one that is never reported.
        EV: scripts/setup-branch-protection.sh:67-73 lists `test`, `Validate Branch Name / check-branch`, `Changeset Required / changeset-lint`, `Docs Required / docs-lint`, `PR Target Validator / validate-target`. Live: `gh api repos/open-gsd/gsd-core/branches/next --jq .protection.required_status_checks.conte…
        REC: Regenerate the two REQUIRED_CHECKS arrays from live protection and add a check-mode to the script so CI can assert no drift.
    (+2 low)

--- docs/adr/660-release-from-next-head.md [AMBIGUOUS] Proposed
    STATUS-EV: Re-verified at aa0f7dee: the ADR's own blocker is still accurate. §3 (immutable tags) is shipped — release.yml:415-431 (rc) and :614-630 (finalize) hard-error if the tag exists on a different commit and never force-move; `git ls-remote --tags` shows no duplicated release tags. §4 (@next as RC surface) is shipped — `npm…
    [high|consequence-drift] Decision §1 — recreate/hard-reset an ephemeral `release/<version>` from `origin/next` HEAD at the start of each rc/finalize — is not implemented at HEAD. The ADR's self-audit is confirmed accurate, not stale.
        EV: `grep -n 'git reset\|git fetch origin next\|origin/next' .github/workflows/release.yml` returns only :19 (an input description), :178, :192, :193 — all inside the hotfix cherry-pick step of the `create` job. The `rc` job checkout is release.yml:328-331 `ref: ${{ needs.validate-version.outputs.branch…
        REC: Either add the reset step to `rc` and `finalize` and ratify, or amend §1 to record that the re-cut is a documented manual pre-step and move the mechanism to a tracked issue.
    [high|consequence-drift] The 2026-06-12 Amendment's guarantee that `next` is 'synced automatically by the release pipeline for every release type' is best-effort, not guaranteed — both sync steps are `continue-on-error: true` — and release.yml's own comment records that the Amendment'…
        EV: release.yml:474-479 ('Sync next branch to the published pre-release') and :722-728 ('Sync next branch to the published release') both carry `continue-on-error: true`. The comment immediately above the finalize step (release.yml ~:717-721) reads: '`next` (and feature branches cut from it) reports the…
        REC: Amend the ADR to record the finalize-side sync as a third path and the observed failure of the back-merge-folded sync; decide whether a failed sync should be tolerated (`continue-on-error`) or should fail the release.
    [high|lacuna] The ADR decides '@next IS the RC surface' but never decides what `@next` means between RC cycles. Today the documented tester install command hands out a build older than `@latest`.
        EV: `npm view @opengsd/gsd-core dist-tags --json` → `{"next":"1.7.0-rc.6","latest":"1.8.0"}`. Decision §4 says testers run `npm i -g @opengsd/gsd-core@next` and that '`@next` remains the prerelease channel managed exclusively by the `rc` job; `finalize` does not repoint it.' 1.7.0-rc.6 precedes 1.8.0, s…
        REC: Decide the between-cycles semantics: repoint `@next` to the released version on finalize, or publish a `-dev`/`-next` snapshot, or document that `@next` is intentionally stale outside an RC window and add a check that surfaces it.
    [medium|lacuna] The release branch is decided to be 'ephemeral' / 'a scratch staging area', but no deletion step or lifecycle owner is specified, and stale release branches persist on the remote.
        EV: `git ls-remote --heads origin` shows `refs/heads/release/1.01.0`, `refs/heads/release/1.03.0`, `refs/heads/release/1.1.0`, `refs/heads/release/1.2.0` still present. release.yml contains no branch-delete step. `.github/workflows/branch-cleanup.yml` deletes merged PR head branches (:22-48) and sweeps …
        REC: Add an explicit release-branch delete step to `finalize` (or a release/* rule to branch-cleanup.yml), and say so in the Decision so 'ephemeral' has a mechanism.
    [medium|lacuna] Tag immutability is asserted by workflow convention only. The GitHub Ruleset that would actually enforce it has been in dry-run since creation, and the ADR never decides to enable it despite citing GitHub Immutable Releases in its Context.
        EV: `gh api repos/open-gsd/gsd-core/rulesets` → ruleset id 16752570 `tag-immutability`, `target: "tag"`, `enforcement: "evaluate"`, `created_at`/`updated_at` 2026-05-22. The Context cites 'GitHub shipped *Immutable Releases* (GA 2025) specifically to stop this', but the Decision names no platform-level …
        REC: Promote the tag-immutability ruleset to `enforcement: "active"` (or enable Immutable Releases) and record it as part of Decision §3; a decision whose only guard is 'the workflow does not do the bad thing' does not survive a manual operator.
    (+1 low)

--- docs/adr/218-release-version-validation.md [ACCURATE] Accepted (2026-05-24)
    [high|consequence-drift] The 'Recovery sequence' — written as a completed, recorded recovery of production state — was never executed. Every artifact it says to create still does not exist, and every artifact it says to delete still does.
        EV: `git ls-remote --tags origin` → `refs/tags/v1.01.0` and `refs/tags/v1.03.0` are BOTH still present (Recovery §2 and §3 prescribe `git push origin :refs/tags/v1.01.0` / `:refs/tags/v1.03.0`); `refs/tags/v1.1.0` does NOT exist (Recovery §2 prescribes creating it). `gh release view v1.01.0 --repo open-…
        REC: Either run the recovery and note the date, or rewrite the section as 'Recommended recovery (NOT executed — the orphan tags/branches and the mis-targeted v1.1.0 release remain live)'. An Accepted ADR asserting a recovery that did not happen …
    [medium|consequence-drift] The Recovery §4 constraint 'the workflow only allows `.0` patch-component versions' is no longer true — the validator now accepts X.Y.Z with Z>0 for hotfixes — and nothing marks the sentence historical.
        EV: release.yml:68 `elif echo "$VERSION" | grep -qE '^(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)\.[1-9][0-9]*$'` → sets `IS_HOTFIX=true`, `BRANCH="hotfix/${VERSION}"`, and derives a semver-correct `BASE_TAG` (:76-85). This arrived with the hotfix consolidation (e14218a4, #678).
        REC: Add a one-line note that the `.0`-only constraint was lifted by #678, or scope the sentence to 2026-05.
    [medium|lacuna] The ADR hardens the version *input* but never decides an authority for the invariant the incident actually broke: that npm version, git tag, release branch, and GitHub Release tagName stay consistent. That hole is precisely why the v1.1.0/v1.01.0 divergence su…
        EV: No consistency guard exists: `grep -rl 'gh release list\|refs/tags' scripts/ tests/` returns only capability-source.test.cjs and capability-lifecycle.test.cjs (unrelated). `scripts/verify-npm-publish.cjs` verifies the version is live and, at :220, only emits `::warning::` on a dist-tag mismatch — it…
        REC: Add a post-release (or scheduled) check asserting that for each published npm version there is a matching annotated git tag and a GitHub Release whose `tagName` equals it, and record the owner of that invariant in the ADR.
    (+2 low)

--- docs/adr/415-prevent-stale-base-token-reintroduction.md [ASPIRATIONAL_ACCEPTED] Accepted (2026-05-28)
    STATUS-EV: Decisions 2 and 3 are verifiably in force: `gsd-core/workflows/_runtime-launcher.snippet.sh` (4.4K), `scripts/sync-runtime-launcher.cjs` (14.1K), and `tests/runtime-launcher-parity.test.cjs` (76.8K) all exist; the parity test's assertion (A) is at :156-164 ('(A) no GSD_SDK token in any workflow .md file', `if (content.…
    [high|contradiction] Decision 1 ('`required_status_checks.strict = true` on `next`') is directly contradicted by the repo's only branch-protection applier, which sets it to `false` — and that script predates this ADR and has not been touched since.
        EV: scripts/setup-branch-protection.sh:106-108 — the `next)` case sets `require_up_to_date=false`; :134 feeds it as `strict: $require_up_to_date`; the header at :38 documents the intent as 'require branches to be up to date before merging (OFF — this is the whole point)'. `git log --oneline -- scripts/s…
        REC: Change `require_up_to_date=true` for `next` in the script and update its :38 comment to cite ADR-415, so the decision has an in-repo representation that matches it.
    [high|contradiction] Decision 4 and Alternatives (b) both require `enforce_admins = false`; the same script hard-codes `enforce_admins: true` unconditionally for both branches — i.e. it applies the alternative this ADR explicitly rejected.
        EV: scripts/setup-branch-protection.sh:137 `enforce_admins: true,` sits outside the per-branch `case`, so it applies to `main` and `next` alike. ADR-415 Consequences: '`--admin` can still bypass `strict` (`enforce_admins = false`)'. Alternatives (b): '`enforce_admins = true` — rejected. Too heavy for a …
        REC: Set `enforce_admins: false` (or make it per-branch) and add a comment citing ADR-415 Decision 4; otherwise a single run of the documented procedure breaks the maintainer's routine flow and silently converts Decision 4 from process into hard…
    [high|lacuna] This ADR's only mechanism lives entirely in mutable remote GitHub configuration with no owner, no drift detector, and no re-verification cadence. One API call can silently undo it and nothing in the repo would notice.
        EV: Decision 1's parenthetical is '(already applied)' — the ADR never specifies where the setting is asserted or how it is kept. `grep -rl 'setup-branch-protection' tests/ scripts/ .github/ docs/` → only the script, docs/branching.md, and docs/adr/230-*.md; no test, no workflow. By contrast the repo run…
        REC: Add a scheduled workflow that reads `required_status_checks.strict` (and `enforce_admins`) on `next` and fails if they drift from the ADR's values; name the owner in the ADR.
    [medium|lacuna] Decision 4 names the exact failure mode but assigns it only to human discipline, and stops short of the cheap automated backstop it itself describes.
        EV: Decision 4: 'When batch-merging, merge structural-rename/propagation PRs **last**, or re-run the parity gate on the integration branch after the batch.' Nothing in `.github/workflows/` runs `runtime-launcher-parity` on push to `next` after a merge — the Consequences concede 'admin merges rely on the…
        REC: Add a post-merge `push: branches: [next]` job that runs the parity gate (and fails loudly). It costs one workflow trigger and converts the residual risk from discipline into detection, without touching `enforce_admins`.

--- docs/adr/2264-golden-parity-redesign.md [AMBIGUOUS] Proposed
    STATUS-EV: Phase 1 is genuinely shipped and clean: `tests/helpers/install-shared.cjs` holds `VOLATILE_FILES` (:105), `HOOK_CONFIG_FILES` (:125), `HOOK_CONFIG_RELATIVE_PATHS` (:143), `EXCLUDED_PREFIXES` (:155), `buildParityManifest` (:187), `buildInstallTree` (:234), all exported at :401-408; `scripts/gen-golden-install-parity-zco…
    [high|contradiction] The Decision (§§2-4) and Acceptance Criteria (1, 3, 4) describe a design the same-day Amendment overturned and were never edited. A reader taking the document top-down gets the wrong architecture; only the final section is true.
        EV: AC3 names `tests/fixtures/converted-artifacts/<runtime>.json` — the directory does not exist (`test -d tests/fixtures/converted-artifacts` → ABSENT). AC4 names 'the copy-parity property test' — `find . -iname '*copy-parity*'` returns nothing repo-wide. AC1 ('editing a copied shipped file requires **…
        REC: Do exactly what the ADR's own Unblock condition says: rewrite AC1/AC3/AC4 to the delivered design, mark §§3-4 superseded by the Amendment in place (not only at the bottom), then flip Status. No code work required.
    [medium|relation-asymmetry] The claimed relation to ADR-1239 is one-way AND misattributed — ADR-1239 has no reciprocal reference, and its Phase B is a different thing entirely.
        EV: This ADR's header: '**Supersedes:** nothing; amends the ADR-1239 Phase-B safety-net harness' and 'Evolves `tests/golden-install-parity.test.cjs` (ADR-1239 Phase B).' `grep -n '2264\|golden-install-parity' docs/adr/1239-gsd-embeddable-orchestration-engine.md` → zero hits for 2264 and zero for golden-…
        REC: Fix the attribution (name the ADR/issue that actually created the harness) and add a reciprocal 'Amended by ADR-2264' line to ADR-1239 if the relation is real.
    [medium|lacuna] With §4 dropped, no one decided who blesses a golden diff. The retained content-hash golden's failure messages actively instruct one-command regeneration, and the 'never auto-regenerated / must be blessed by a human' rationale went away with the section that c…
        EV: §4 said the converted-artifact gate 'stays a **hard failure** and is never auto-regenerated/auto-committed ... exactly the changes that *should* require a human to bless.' The Amendment retains the monolithic golden 'as-is' and adds `npm run gen:golden` as 'the one-command fix'. Live, `tests/golden-…
        REC: Decide and record a bless policy: e.g. require the regenerating commit to state which source edit caused the hash churn, or split the failure message by whether the changed paths are inside a transform surface.
    [medium|lacuna] The anti-staleness guard — the Amendment's main safety claim — is a hand-maintained path allowlist whose correctness depends on a human keeping it in sync with what the installer actually emits. The ADR does not name this residual risk or an owner, even though…
        EV: scripts/ci-test-scope.cjs:170-173 enumerates literal prefixes (`hooks/`, `commands/`, `agents/`, `skills/`, four `gsd-core/` subtrees, `scripts/changeset/`, `scripts/lib/`) plus three named generator files, with the comment at :169 'Verify against Object.keys(golden fixture) grouped by gsd-core/<sub…
        REC: Add a test that derives the emitted top-level source prefixes from a golden manifest and asserts the ci-test-scope rule covers them, or soften the 'impossible' claim in the Amendment.
    (+1 low)

--- docs/adr/2008-command-exit-zero-gate.md [ACCURATE] Accepted
    [high|consequence-drift] The antipattern this ADR was written to end — a hard-coded `capId ==` prose branch standing in for a real gate — grew rather than shrank after acceptance. A second such branch was added at `ship:pre` fifteen days later.
        EV: The ADR's Context: 'The security capability's `threats_open == 0` enforcement worked only because `ship.md` hard-codes a `capId == "security"` prose branch ... There was no extension path for a third-party capability's gate to actually fire.' Live `gsd-core/workflows/ship.md` now hard-codes TWO: :98…
        REC: Either register `artifact-frontmatter-equals` in KIND_TABLE and wire `ship:pre` to the generic path (retiring both prose branches), or amend the ADR to state explicitly that `ship:pre` is exempt and that per-capId prose branches remain the …
    [high|lacuna] `ship:pre` is the one gate point NOT wired to the generic predicate path, and the ADR never says so — yet 100% of the `check.predicate` declarations that actually ship live at `ship:pre`. The extension path's reach excludes every existing user of the feature.
        EV: The ADR enumerates 'The three generic workflow gate-dispatch sites — `execute:wave:post`, `execute:post`, `plan:post`'. Live there are four: gsd-core/workflows/execute-phase.md:930 and :1197, plan-phase.md:1523, and verify-work.md:76 (`verify:pre`, unmentioned by the ADR). `grep -rn 'check predicate…
        REC: State the gate-point coverage explicitly in the Decision (which points dispatch generically, which do not, and why), and add `verify:pre` to the enumeration. Coverage that is decided per-point ad hoc should be written down as a policy.
    [medium|lacuna] The blast radius of a declared-but-unregistered predicate kind is never decided. Two shipped built-in capabilities declare `artifact-frontmatter-equals` with `onError: "halt"`, and the ADR's own fail-closed mapping would turn that into a workflow HALT if it ev…
        EV: `EVALUATOR_KINDS = Object.freeze(['command-exit-zero'])` (src/gate-predicate-evaluator.cts:42) and `KIND_TABLE` (:154-156) register only that kind; the ADR's Out-of-scope section confirms 'the kind table is structured for it but it is not registered'. The ADR's fail-closed mapping: an unknown kind t…
        REC: Decide and record the policy for declared-but-unregistered kinds: either make the registry validator reject an unregistered kind at generation time (fail at build, not at ship), or make the evaluator return a non-blocking 'unsupported kind'…
    [medium|relation-asymmetry] The header's '**Amends** ADR-0894' is a one-way pointer — the target records nothing — and the generated ADR index has no column that could surface it.
        EV: `grep -n '2008\|command-exit-zero\|gate-predicate' docs/adr/894-capability-declaration-format.md` → zero hits. Same grep against docs/adr/857-capability-system.md (the ADR's other named reference) → zero hits. `docs/adr/README.md:168` renders ADR-2008's row with '—' in the relation column; that colu…
        REC: Add an 'Amended by: ADR-2008 (`check.predicate` evaluation path)' line to ADR-894, and teach `scripts/gen-adr-index.cjs` to emit Amends/Amended-by so one-way pointers become visible in the index.

>> CLUSTER OBS [high]: The load-bearing mechanism of three ADRs in this cluster (230, 415, 660) is remote GitHub or npm configuration that no repo artifact ever reads back. The repo has an elaborate in-tree drift-detection culture and zero out-of-tree config drift detection — a structural blind spot every one of these ADRs quietly assumes away.
   EV: `npm run lint:generated-sync` (package.json:114) chains TEN `--check` generators for in-tree artifacts. Against that: `grep -rl setup-branch-protection tests/ scripts/ .github/` finds no test and no workflow; all three GitHub Rulesets (16752567 main-protection, 16752568 release-branches, 16752570 tag-immutability) are …

>> CLUSTER OBS [high]: One file, `scripts/setup-branch-protection.sh`, simultaneously contradicts ADR-415 Decision 1, ADR-415 Decision 4 / Alternatives (b), and ADR-230's own Unblock condition — and it is the artifact both ADRs tell an operator to run. It has not been edited since before ADR-415 existed.
   EV: Line 108 `require_up_to_date=false` for `next` (→ `strict: false` at :134) vs ADR-415 D1 'strict = true'. Line 137 `enforce_admins: true` for both branches vs ADR-415 D4 'remains false' and Alt (b) 'rejected'. Lines 67-73 REQUIRED_CHECKS_NEXT (5 contexts, wrong naming form) vs the 7 live contexts on `next`. Line 47 sti…

>> CLUSTER OBS [high]: Release-history integrity is nobody's job across the whole cluster: ADR-218 recorded a recovery that never ran, ADR-660 makes tags immutable by workflow convention while the platform control stays dry-run, and no artifact reconciles npm dist-tags with git tags with GitHub Releases.
   EV: Live at aa0f7dee: `refs/tags/v1.01.0` and `refs/tags/v1.03.0` still exist while `refs/tags/v1.1.0` does not; `gh release view v1.01.0 --json tagName,name` → `{"name":"v1.1.0","tagName":"v1.01.0"}`; `refs/heads/release/{1.01.0,1.03.0,1.1.0,1.2.0}` all still on origin; `npm view @opengsd/gsd-core dist-tags` → `{next: 1.7…

>> CLUSTER OBS [medium]: Both cross-ADR relation claims in this cluster are one-way, and the generated index cannot surface either, because `scripts/gen-adr-index.cjs`'s table has no Amends/Amended-by column.
   EV: ADR-2008 header 'Amends ADR-0894' → `grep '2008\|command-exit-zero' docs/adr/894-capability-declaration-format.md` returns nothing. ADR-2264 'amends the ADR-1239 Phase-B safety-net harness' → `grep '2264\|golden-install-parity' docs/adr/1239-*.md` returns nothing, and ADR-1239's Phase B (:81) is the engine↔host boundar…

>> CLUSTER OBS [medium]: The 'Why this is still `Proposed` (audited 2026-07-17)' convention used by 230, 660 and 2264 is working as a truth-telling device — all three self-audits re-verified accurate at aa0f7dee, seven days later — but it has produced zero follow-through, because none of the three Unblock conditions names an owner, a tracking issue, or a re-check cadence.
   EV: Each block ends with an 'Unblock condition' paragraph and (for 230 and 2264) verification commands, but no assignee and no open issue: issues #230, #660, #2264 are all CLOSED/COMPLETED (`gh issue view --json state,stateReason`), so there is no live work item behind any of the three. ADR-2264's blocker is a pure documen…

>> CLUSTER OBS [medium]: Two ADRs in this cluster describe `.github/workflows/hotfix.yml` as a live file to patch, but it was deleted and folded into release.yml — and the consolidation itself, which introduced real new machinery, has no ADR.
   EV: `test -f .github/workflows/hotfix.yml` → absent; `git log --diff-filter=D` → e14218a4 'fix(#676): consolidate hotfix into release.yml (delete standalone hotfix workflow) (#678)'. ADR-230's Mechanical-changes table and two Phase-3 diffs target hotfix.yml; ADR-660's Amendment says 'finalize / hotfix (these push `main`)'.…

>> NO-ADR: Repository configuration as code — branch protection, rulesets, required-check contexts, default branch, merge strategy
   WHY: A real decision with real alternatives was made silently and is now the single point of failure for ADR-415 and half of ADR-230. The chosen shape is 'classic branch protection, applied by a hand-run bash script, plus three GitHub Rulesets left permanently in dry-run.' The alterna… | EV: scripts/setup-branch-protection.sh:100-152 (per-branch knobs + payload) with `git log` showing no protection-related edit since 1bc7d612; rulesets 16752567/16752568/16752570 all `enforcement: "evaluate"`, `updated_at` 2026-05-22; `gh api re…

>> NO-ADR: npm dist-tag lifecycle — what `@next` means when no RC is in flight, and who owns the gap between `@next` and `@latest`
   WHY: ADR-660 §4 decides '@next IS the RC surface' and that finalize does not repoint it, but the between-cycles state was decided by omission. The consequence is live and user-facing: the install command the project gives RC testers currently hands them an older artifact than the stab… | EV: `npm view @opengsd/gsd-core dist-tags --json` → `{"next":"1.7.0-rc.6","latest":"1.8.0"}` at HEAD; release.yml:437 publishes `--tag next` in `rc` only; :636 publishes stable with no `--tag`; scripts/verify-npm-publish.cjs:220 emits only a `:…

>> NO-ADR: The hotfix release model — consolidating hotfix.yml into release.yml's `create` job
   WHY: This was a genuine architectural change with alternatives and durable consequences, executed as a `fix:` commit with no decision record. It made a single `create` job serve two structurally different release shapes (minor/major cut from the trunk vs. patch cut from a prior tag), … | EV: e14218a4 'fix(#676): consolidate hotfix into release.yml (delete standalone hotfix workflow) (#678)'; release.yml:59-89 (hotfix branch/base-tag derivation, `sort -V` + awk 'highest tag strictly below target'), :163-176 (skeleton push ration…

>> NO-ADR: Gate-point coverage policy for capability gates — which loop extension points get generic data-driven dispatch vs. a hard-coded per-capId prose branch
   WHY: The coverage is being decided ad hoc, one point at a time, and the pattern is diverging from the ADR-2008 direction rather than converging on it. Four points now dispatch generically; `ship:pre` — the point where every real `check.predicate` declaration actually lives — has accum… | EV: Generic dispatch at gsd-core/workflows/execute-phase.md:930 and :1197, plan-phase.md:1523, verify-work.md:76; hard-coded branches at ship.md:98 (security) and :115-148 (broken-windows, added d16a6647 on 2026-07-19); the only two `check.pred…

>> NO-ADR: Golden-fixture bless policy — when a hash-churn diff may be regenerated versus when it must be reviewed
   WHY: ADR-2264 §4 articulated the policy ('hard failure, never auto-regenerated, exactly the changes that should require a human to bless') and then its Amendment deleted §4 and replaced the bless story with a one-keystroke regenerator — in a deferral paragraph, not a decision. What re… | EV: package.json:95 `"gen:golden": "node scripts/gen-golden-install-parity-zcode.cjs && node scripts/gen-install-tree-fixtures.cjs"`; tests/golden-install-parity.test.cjs:93, :111, :149, :162 all print 'Run "npm run gen:golden"'; 19 fixtures to…

==============================================================================
## AGENT: ae25ce4fda5741597
==============================================================================

--- docs/adr/1372-markdown-sectionizer-seam.md [ACCURATE] Accepted
    [medium|relation-asymmetry] ADR-2143 declares itself "the second half of #1372" and lists ADR-1372 under References, but ADR-1372 carries no reciprocal pointer. A reader landing on ADR-1372 cannot learn that its stated closed set (fences/headings/sections/bullets) was widened to six oper…
        EV: Scanning all `docs/adr/*.md` for `ADR-1372`/`1372-markdown` returns `1372-markdown-sectionizer-seam.md` (itself), `1769-state-md-transition-module.md`, `2143-markdown-table-and-mutation-consolidation.md`, `README.md`. Scanning for `ADR-2143`/`2143-markdown` returns only `2143-...md` (itself) and `RE…
        REC: Add `- **Extended by:** ADR-2143` to ADR-1372's header and note in §1 that the closed set was widened by ADR-2143 §1.
    [high|lacuna] The prohibition's scope is never decided. §2 says the rule flags ad-hoc markdown scanning "in `src/*.cts`" and the rule hard-codes that filename guard — so `hooks/*.js`, `scripts/*.cjs`, and the hand-written `gsd-core/bin/gsd-tools.cjs` are entirely unpoliced.…
        EV: `eslint-rules/no-adhoc-markdown-parsing.cjs:108`: `if (!/(?:^|\/)src\/[^/]+\.cts$/.test(filename.replace(/\\/g, '/'))) { return {}; }`. Live violation outside scope: `hooks/gsd-statusline.js:143-164` `function parseStateMd(content)` hand-rolls a STATE.md frontmatter parser — `content.match(/^---\n([…
        REC: Either extend the rule's filename guard to `hooks/`, `scripts/`, and `gsd-core/bin/*.cjs` with a fresh grandfather list, or record in the ADR why those trees are exempt and what stops divergence there.
    (+1 low)

--- docs/adr/2143-markdown-table-and-mutation-consolidation.md [ACCURATE] Accepted (Phase 0 — ADR only; locks the contract Phases 1–4 execute against. No production…
    [medium|lacuna] The ADR extends the prohibition to `.replace()` mutation but never decides the detection contract. The shipped rule matches on receiver-identifier-name heuristics AND a regex-shape fingerprint — a name-based guard any rename (`doc`, `md`, `body`, `text`) silen…
        EV: `eslint-rules/no-adhoc-markdown-parsing.cjs:269` `const REPLACE_RECEIVER_RE = /roadmap|state|reqContent|content/i;` and `:318` `return isTableRegexSource(src) || isSectionCollectRegexSource(src);` — both must hold. The rule's own header at `:70-71` concedes: "nor does `.replace()` on a receiver whos…
        REC: Record the known false-negative class in §7 and pair it with a positive-side guard — e.g. extend the table-schema drift lint to assert every reader of a canonical table imports from the seam.
    [medium|lacuna] §5 decides seam parses return `Result<T>` "never a bare null", but says nothing about what a caller must do with `{ok:false}`. A new silent-failure surface appears one level up: a caller may branch on `!result.ok` and skip its write exactly as the old null pat…
        EV: `src/milestone.cts:60-97` documents the recurrence verbatim: `updateTableCell` bound to the wrong table "fails with `{ok:false, reason:'unknown column: Status'}`, and the real Traceability row is never flipped, while the checkbox surface still flips and the command reports success (the #2140 silent-…
        REC: Add a decided caller obligation: a command receiving `{ok:false}` from a required write must surface the reason and set its top-level success flag false; lint for `if (!result.ok) return` patterns that drop the reason.
    (+2 low)

--- docs/adr/1577-untrusted-input-boundary-and-injection-blocking.md [ASPIRATIONAL_ACCEPTED] Accepted — ratified 2026-07-17 (originally Proposed 2026-06-25)
    STATUS-EV: The prompt-level half is fully real: `gsd-core/references/untrusted-input-boundary.md` (1854 bytes) carries the four decided controls and is @-included by exactly the 10 named ingest agents. Config key registered end-to-end: `gsd-core/bin/shared/config-schema.manifest.json:109`, `config-defaults.manifest.json:103` (fal…
    [high|consequence-drift] The ADR's first Decision bullet — "Extend the scanner to match `Read | WebFetch | WebSearch`" — is realized only in the Claude Code plugin manifest. The `npx @opengsd/gsd-core --<runtime>` installer, the primary distribution path for all 15 runtimes, writes a …
        EV: `src/runtime-hooks-surface.cts:1701-1711`: `settings.hooks[postToolEvent].push({ matcher: 'Read', hooks: [{ type:'command', command: readInjectionScannerCommand, timeout: 5 }] });` — no WebFetch/WebSearch; the surrounding comment at `:1693-1695` still says it scans "content returned by the Read tool…
        REC: Change `src/runtime-hooks-surface.cts:1703` to `matcher: 'Read|WebFetch|WebSearch'` (and Kimi `:2133` to its tool vocabulary), update the two install tests, and add a parity assertion that `hooks/hooks.json`'s matcher for a hook equals the …
    [high|quality] The Ratification block's own evidence is what hides the drift. It cites the scanner's internal `SCANNED_TOOLS` set and the plugin-manifest matcher as proof the gap is closed, and cites SCAN-WF-01/02/03 + SCAN-WS-01 as proof of real detections — but those tests…
        EV: `tests/read-injection-scanner.security.test.cjs:26` `const HOOK_PATH = ... 'hooks/gsd-read-injection-scanner.js'`; `:31` `execFileSync(process.execPath, [HOOK_PATH], ...)`; `:225` `return { tool_name: 'WebFetch', tool_input: { url }, tool_response: content };`. The file documents the real matcher el…
        REC: Add an install-level assertion (in the `tests/install*.test.cjs` family, which already spawns the real `install()`) that written settings register the scanner against WebFetch and WebSearch.
    [high|lacuna] The ADR scopes the prompt-level boundary — which it names "the primary control" — to a hard-coded list of 10 ingest agents, and never states the rule that keeps that list in sync with which agents can actually fetch untrusted content. The set has already drift…
        EV: Enumerating `agents/*.md` for agents granting `WebFetch|WebSearch|mcp__*` without an `untrusted-input-boundary` include yields 4: `agents/gsd-planner.md:4` `tools: Read, Write, Edit, Bash, Glob, Grep, Skill, WebFetch, mcp__context7__*, mcp__plugin_context7_context7__*`; `agents/gsd-debugger.md:4` `.…
        REC: Decide and lint the invariant: any agent whose `tools:` grants WebFetch/WebSearch/`mcp__*` MUST @-include `untrusted-input-boundary.md` — mechanically checkable in `scripts/lint-skill-deps.cjs`-style tooling.
    [high|lacuna] Hook-level channel coverage is decided by omission. `Bash` — where `curl`/`wget`/`gh api` deliver arbitrary remote content — and all `mcp__*` tool results are never scanned, even though the ADR's own prompt-level reference classifies MCP output as untrusted. T…
        EV: `hooks/hooks.json:47` registers `gsd-context-monitor.js` for `Bash|Edit|Write|MultiEdit|Agent|Task` — the only PostToolUse hook seeing Bash output, and it is a context-headroom monitor, not a scanner. `hooks/hooks.json:34` is the scanner's only registration and lists no MCP tool and no Bash. `hooks/…
        REC: Add a channel-coverage table (channel → scanned? → why/why not) so an unscanned channel is a recorded, revisitable decision. At minimum decide explicitly whether MCP tool results are in hook-level scope.
    [medium|lacuna] `isExcludedPath` is a security-relevant allowlist with no ADR, no stated policy, and no owner. It blanket-excludes the entire `.planning/` tree — exactly where research agents write fetched external content and where later phases re-read it. An injection fetch…
        EV: `hooks/gsd-read-injection-scanner.js:95-106` excludes `/.planning/`, `.planning/`, `REVIEW.md`, any basename matching `/CHECKPOINT/i`, `/(?:^|\/)(?:security|techsec|injection)[/\\.]/i`, `security.cjs`, `/.claude/hooks/`; applied unconditionally at `:172` `if (isExcludedPath(source)) process.exit(0);…
        REC: Record the exclusion set and its rationale in the ADR, and separate 'GSD's own artifacts' from 'artifacts containing externally-fetched text' — the latter should not be blanket-excluded.
    [medium|lacuna] The circuit-breaker's failure semantics are undecided. Every error path fails open silently — a 5s stdin timeout exits 0, a malformed payload exits 0, the outer catch exits 0 — with no log, counter, or user signal that a scan was skipped. Defensible for an adv…
        EV: `hooks/gsd-read-injection-scanner.js:153` `const stdinTimeout = setTimeout(() => process.exit(0), 5000);`; `:236` exit 0 on no findings; `:268-269` `} catch (e) {` / `// Silent fail — never block tool execution` / `process.exit(0)`. Host-side `"timeout": 5` at `hooks/hooks.json:36`. `:239` `const se…
        REC: Decide (a) whether `injection_blocking: true` fails closed on hook error/timeout, and (b) whether any single high-confidence pattern is HIGH on its own. Both are currently implicit implementation properties.
    (+2 low)

--- docs/adr/1990-existing-code-onboarding.md [ACCURATE] Accepted — ratified 2026-07-17 (originally Proposed 2026-07-06)
    [high|lacuna] Open question 1 — "Should codebase-map completeness be sourced from a single shared predicate ... so the two cannot drift?" — is not merely open; the drift vector is live and unguarded. The canonical `.planning/codebase/` artifact set is stated three times in …
        EV: Consumer/gate: `src/onboard-projection.cts:31-34` `const REQUIRED_CODEBASE_MAP_FILES = ['STACK.md','ARCHITECTURE.md','STRUCTURE.md','CONVENTIONS.md','TESTING.md','INTEGRATIONS.md','CONCERNS.md']` and `:36-38` `FAST_CODEBASE_MAP_FILES = ['STACK.md','INTEGRATIONS.md','ARCHITECTURE.md','STRUCTURE.md']`…
        REC: Promote the artifact list to one exported constant consumed by the workflow generator and both readers, guarded by a drift lint in `lint:ci` — the pattern ADR-2143 §3/§7 already ships for tables.
    [medium|lacuna] Two of the four 'Safety invariants' — idempotency/no-silent-overwrite and 'SUMMARY.md is a trailing artifact' — are properties of code explicitly outside the Module. The projection is pure and read-only by construction, so it cannot violate them; the actual wr…
        EV: ADR: "Onboarding never mutates existing tracked `.planning/` artifacts; re-running leaves them byte-unchanged" and Consequences: "The projection is unit-tested (`tests/onboard-command.test.cjs`) for ... idempotency/no-mutation". The actual write is `gsd-core/workflows/onboard.md:259` (`gsd_run --cwd…
        REC: Move the invariant statement to name the workflow as its owner and add a workflow-level regression test, or downgrade it from a Module invariant to an end-to-end acceptance criterion with an explicit owner.
    (+3 low)

--- docs/adr/2164-statusline-scope-boundary.md [ACCURATE] Accepted
    [medium|lacuna] The tier table has no row for the case the statusline already ships: network-derived data fetched by a different hook and rendered from a local cache. The `⬆ /gsd:update` segment displays a value ultimately obtained from the npm registry via `~/.cache/gsd/<pkg…
        EV: `hooks/gsd-statusline.js:12-13` imports `isSemverNewer` and `updateCacheFileName`; `:614` `const cacheFile = path.join(homeDir, '.cache', 'gsd', updateCacheFileName);`; `:617-620` parses it and emits `'\x1b[33m⬆ /gsd:update\x1b[0m │ '`. The cache is written by `hooks/gsd-check-update.js`, which spaw…
        REC: Add a Tier 1.5 row — 'network data fetched by a separate, already-approved hook and read from a local cache' — with an explicit rule, otherwise the tier table is satisfiable by indirection.
    (+2 low)

--- docs/adr/2346-command-dispatch-completion.md [ACCURATE] Accepted
    [high|consequence-drift] Decision §3 (`parseFamilyArgs`) never shipped. The symbol does not exist anywhere in the tree, and the four duplicated arg-parsers it was decided to delete are all alive — `capFlagValue` and `capRepeatedFlag` were merely relocated into the extracted capability…
        EV: A search of `src/`, `gsd-core/bin/`, `scripts/` for `parseFamilyArgs` returns nothing — absent from `src/cjs-command-router-adapter.cts` where §3 placed it 'beside `routeHubCommandFamily`' (`routeHubCommandFamily` at `:89`, exported `:180`; no sibling helper). Survivors: `gsd-core/bin/lib/capability…
        REC: Land `parseFamilyArgs` with its first consumer as §3 specifies, or amend §3 and the Consequences bullet to record that the consolidation was dropped and why.
    [medium|consequence-drift] Decision §4's two named consolidations did not land. `capability-cli.cts` does not exist, so there is no separation between the thin router and the CLI wiring the ADR decided to split out; and `readStrictKnownRegistries` does not exist, so `capReadStrict` and …
        EV: `ls src/capability-cli.cts gsd-core/bin/lib/capability-cli.cjs` → no match (`src/` holds only capability-{activation,consent,ledger,lifecycle,loader,lock,source,state,trust,writer}.cts). `readStrictKnownRegistries` returns zero hits repo-wide. The un-consolidated read survives at `gsd-core/bin/lib/c…
        REC: Complete the §4 consolidations or amend §4 to record what was actually extracted versus what was deferred.
    [medium|consequence-drift] Decision §2's code-organization half did not land. The ADR states '~40 remaining verbs rehome into ~4 themed leaf modules'; instead every leaf verb became a module-scope `route*` function inside `gsd-core/bin/gsd-tools.cjs` itself. The dispatch table was achie…
        EV: Leaf handlers defined in `gsd-tools.cjs`, not themed modules: `:874` `function routeAgent(`, `:878` `routeSmartEntry`, `:882` `routeCheck`, `:940` `routeTemplate`, `:1229` `routeAgentSkills` — all referenced from the table at `:2099-2123`. `ls src/ | grep -E 'leaf|-cli\.cts'` returns nothing. Size t…
        REC: Land the themed leaf modules or amend §2 to state leaves stay in-file, and record the resulting size-ratchet obligation on `gsd-tools.cjs`, which has no per-file size guard unlike `agents/` and `workflows/`.
    [medium|lacuna] The extraction's destination is undecided, and it landed where the repo's build model does not contemplate. `capability-command-router.cjs` is 733 lines of hand-authored, git-tracked CommonJS inside `gsd-core/bin/lib/` — the directory otherwise populated by `s…
        EV: `git ls-files gsd-core/bin/lib/capability-command-router.cjs` → tracked; absent from the `/gsd-core/bin/lib/*.cjs` ignore block in `.gitignore:71-230` that covers its 158 generated siblings (including `capability-trust.cjs`, `capability-lifecycle.cjs`, `markdown-table.cjs`). `wc -l` → 733. `eslint-r…
        REC: Decide the rule: hand-authored engine modules live in `src/*.cts`; `gsd-core/bin/lib/` holds build output only. If this router is a deliberate exception, record why and bring it under lint explicitly.
    (+2 low)

--- docs/adr/443-opus48-unified-effort-and-fast-mode-routing.md [ACCURATE] Proposed (2026-05-28)
    [high|lacuna] Decision item 2 says 'a per-runtime renderer maps it to each runtime's wire parameter', but the renderer table covers exactly two runtimes out of the 15+ GSD ships to. Every other runtime silently renders nothing. The ADR names this nowhere — the fall-through …
        EV: `src/model-catalog.cts:198` `export const EFFORT_RENDERING: Record<string, EffortSpec> = { claude: {...}, codex: {...} };` — two keys, closing at `:217`. The undecided fall-through is documented only in code: `:234-236` 'A host absent from this table renders null, so an undeclared or undocumented ho…
        REC: Amend Decision item 2 to state the closed set of runtimes with an effort channel and the explicit policy for the rest, so 'gemini has no effort control' is a recorded decision rather than an emergent property of a two-entry table.
    [medium|lacuna] The unblock condition is now under-specified. The 2026-07-21 amendment chose path (a) and then delivered a third mechanism, leaving the stated flip condition ('item 1 gains a live caller and a test exercises that path through a workflow') attached to a mechani…
        EV: ADR amendment table row 1: 'Decision item 1 — `resolveEffortInternal`'s **invocation-override** step (`--effort`) | **Still no live caller.** No workflow, reference, or agent passes `--effort` to `resolve-execution`. This change does not add one.' The amendment closes two deferrals explicitly ('reso…
        REC: Make the item-1 call the way item 6's was made: name the intended consumer with an issue, or amend Decision item 1 to drop the orchestrator invocation-override layer now that the argv channel exists — then the ADR can be ratified.
    (+1 low)

--- docs/adr/1671-dynamic-context-management-platform.md [ACCURATE] Proposed
    [medium|consequence-drift] One of the Context's two named motivating mechanisms no longer exists. The ADR states 'The one true emission-time cap, Windsurf's 12,000-byte limit (`src/runtime-artifact-conversion.cts`), is a hard `throw` with no graceful fallback', and Decision item 2 promi…
        EV: A repo-wide search for `12000` / `12,000` / `12_000` across `*.cts`, `*.cjs`, `*.js`, `*.md` (excluding `node_modules` and `test-timings.json`) returns zero hits. `src/runtime-artifact-conversion.cts`'s Windsurf section (`:1012-1048`) contains only string-rewrite converters — no size check, no throw…
        REC: Re-ground the Context before acting on this ADR — the 'graceful auto-trim replaces a hard throw' selling point for Decision item 2 currently has no referent.
    [medium|lacuna] The ADR names 'trimming a load-bearing fragment is a correctness hazard' as its top risk and proposes three mitigations, but never decides what *makes* a fragment load-bearing. The three examples given are illustrative, not a predicate. Without a decided class…
        EV: ADR 'Architecture and contracts' bullet 2: "`flexReserve`-style floors for load-bearing fragments (`META.RULE` citation rules, contribution gates, closing-keyword rules)" — parenthetical examples only. 'Negative / risks' bullet 1 concedes the history: 'paraphrased `META.RULE` → agent violations'. Op…
        REC: Add a fourth open question or a decided rule for how a fragment's priority/floor is assigned and audited — this is the decision that determines whether the platform is safe, and it is the only major axis left unnamed.
    (+2 low)

--- docs/adr/15-autonomous-cross-ai-convergence.md [ACCURATE] Accepted — ratified 2026-07-17 (originally Proposed 2026-05-24)
    (+2 low)

>> CLUSTER OBS [high]: Two Accepted ADRs in this cluster (1577, 2346) were ratified or accepted on evidence that cannot detect the specific way the code drifted, because the evidence was gathered at the wrong layer. ADR-1577's ratification cites the scanner's internal tool set and unit tests that spawn the hook directly with synthetic tool names — neither can fail when the matcher wiring omits WebFetch. ADR-2346 was acc…
   EV: ADR-1577 ratification bullets cite `hooks/gsd-read-injection-scanner.js:118` (SCANNED_TOOLS) and `tests/read-injection-scanner.security.test.cjs` SCAN-WF-01/02/03; the actual wiring gap is at `src/runtime-hooks-surface.cts:1703` (`matcher: 'Read'`) — a file the ratification never opened, whose own tests (`tests/install…

>> CLUSTER OBS [medium]: The 1372→2143 consolidation is the strongest work in this cluster and the clearest template the repo has for ending a recurring bug class: a seam, a lint with teeth, a per-phase grandfather burndown, and a drift lint in `lint:ci`. It fully landed. But its own scope decision — 'police `src/*.cts`' — was never revisited, and the same bug class is alive one directory over in code that is arguably mor…
   EV: `eslint-rules/no-adhoc-markdown-parsing.cjs:108` hard-codes `/(?:^|\/)src\/[^/]+\.cts$/`. Live violation outside scope: `hooks/gsd-statusline.js:143-164` hand-rolls STATE.md frontmatter parsing (`content.match(/^---\n([\s\S]*?)\n---/)` + per-line `/^(\w+):\s*(.+)/`), duplicating both `src/frontmatter.cts` and the secti…

>> CLUSTER OBS [high]: Three ADRs in this cluster independently re-encode a canonical list that has no single source and no parity assertion — the exact 'Generative Fix Divergence' failure ADR-2143 §3 solved for tables. The pattern is a repo-wide gap, not a per-ADR wart: ADR-2143 built the machinery (`scripts/lint-table-schema-drift.cjs`, `TABLE_SCHEMAS`) but nobody generalized it beyond pipe tables.
   EV: (1) `.planning/codebase/` artifact set: prose in `gsd-core/workflows/map-codebase.md:313-323`, hard-coded array in `src/onboard-projection.cts:31-38`, independent path reads in `src/profile-output.cts:452,469` — ADR-1990's own Open question 1, still open. (2) Injection-scanner tool matcher: `hooks/hooks.json:34` (`Read…

>> CLUSTER OBS [medium]: The two Proposed ADRs in this cluster (443, 1671) are unusually honest — 443 carries a dated self-audit plus a follow-up amendment that explicitly corrects its own stale grep; 1671 ships a labelled non-shipping prototype and lists real open questions. Neither is stale-Proposed. But both have degraded premises nobody re-checks, and neither carries a revisit trigger or owner, so the honesty is a sna…
   EV: ADR-1671's cited Windsurf 12,000-byte throw no longer exists anywhere in the repo (zero hits for `12000`); two of its three named duplicate predicate IDs are gone from `CONTEXT.md`; and its worst-case headroom figure degraded from a cited 293 bytes to 12 (`agents/gsd-verifier.md` = 49,140 vs `LARGE_CAP = 49152` at `tes…

>> CLUSTER OBS [medium]: 1577 is the corpus's only security ADR and it is scoped to one hook plus one prompt-level reference. Everything else security-shaped in the tree — the prompt guard, the read guard, the workflow guard, the worktree path guard, the scan-exclusion allowlist, the fail-open convention, the `security.cts` pattern registry — is undecided in the ADR corpus. The consequence is that 1577 reads as 'GSD's sec…
   EV: `hooks/hooks.json` registers `gsd-prompt-guard.js` (`:20` matcher `Write|Edit`), `gsd-read-guard.js`, `gsd-workflow-guard.js`, `gsd-worktree-path-guard.js`, and `gsd-read-injection-scanner.js` (`:34`) — only the last has an ADR. `src/security.cts:482` notes 'gsd-read-injection-scanner.js) inline their own pattern subse…

>> NO-ADR: Per-runtime hook registration surface (which tool names each hook is matched against, per host)
   WHY: A real decision with alternatives and a security consequence, made three times in three places with three different answers and no parity test. The alternatives are genuine — one canonical matcher list translated per-runtime through the existing tool-vocabulary maps, versus a han… | EV: Three sources of truth for one fact: `hooks/hooks.json:34` `"matcher": "Read|WebFetch|WebSearch"`; `src/runtime-hooks-surface.cts:1703` `matcher: 'Read'`; `:2133` `matcher: 'ReadFile'`. The unifying translation table already exists at `src/…

>> NO-ADR: Fail-open as the universal hook convention (what a security-relevant hook must do on error, timeout, or oversized payload)
   WHY: Every GSD hook silently exits 0 on any failure. For advisory hooks that is obviously right; for a hook shipping an opt-in blocking mode it is a decision with a real alternative (fail closed when `injection_blocking: true`) and a real consequence (an attacker-inducible bypass). Th… | EV: `hooks/gsd-read-injection-scanner.js:153` `const stdinTimeout = setTimeout(() => process.exit(0), 5000);`; `:268-269` `} catch (e) { /* Silent fail — never block tool execution */ process.exit(0); }`; host-side `"timeout": 5` at `hooks/hook…

>> NO-ADR: When an engine module may be hand-authored directly in `gsd-core/bin/lib/` instead of `src/*.cts`
   WHY: ADR-457 decided build-at-publish generation for `bin/lib/*.cjs`, and the ground rule everywhere else is authored-in-`src`. A 733-line hand-written, git-tracked router now lives in that directory, which silently exempts it from every `src/*.cts`-scoped ESLint rule (including the A… | EV: `git ls-files gsd-core/bin/lib/capability-command-router.cjs` → tracked; absent from the `/gsd-core/bin/lib/*.cjs` ignore block in `.gitignore:71-230` that covers its 158 generated siblings (including `capability-trust.cjs`, `capability-lif…

>> NO-ADR: The canonical `.planning/codebase/` artifact contract (what a 'complete' vs 'fast' codebase map is, and who owns the list)
   WHY: This list is a load-bearing gate — `onboard` refuses to route a repo to `/gsd:new-project` until it is satisfied — and it is stated three times in three formats with no shared constant and no parity assertion. The alternatives (registry + drift lint, as ADR-2143 §3 already does f… | EV: Producer prose: `gsd-core/workflows/map-codebase.md:313-323`. Consumer arrays: `src/onboard-projection.cts:31-34` (`REQUIRED_CODEBASE_MAP_FILES`, 7 entries) and `:36-38` (`FAST_CODEBASE_MAP_FILES`, 4 entries), gating at `:205,332-342`. Thir…

>> NO-ADR: Agent tool-grant ↔ security-reference coupling (which @-includes an agent's `tools:` frontmatter obligates)
   WHY: ADR-1577 makes the prompt-level boundary 'the primary control' and binds it to a hand-maintained list of 10 agent files. The general rule — a capability grant implies a matching guidance include — is mechanically checkable (the repo already lints skill `requires:` closure the sam… | EV: Four agents grant fetch tools without the boundary include: `agents/gsd-planner.md:4` (`WebFetch`, `mcp__context7__*`), `agents/gsd-debugger.md:4` (`WebSearch`), `agents/gsd-framework-selector.md:4` (`WebSearch`), `agents/gsd-executor.md:4`…

>> NO-ADR: The injection scanner's path-exclusion allowlist (`isExcludedPath`)
   WHY: A security scanner's exclusion list is a trust assertion: 'content under these paths is not adversary-controlled.' The list blanket-excludes the entire `.planning/` tree — where research agents persist externally-fetched text — collapsing two different justifications (avoid self-… | EV: `hooks/gsd-read-injection-scanner.js:95-106` excludes `/.planning/`, `.planning/`, `/(?:^|\/)REVIEW\.md$/i`, any basename matching `/CHECKPOINT/i`, `/(?:security|techsec|injection)[/\\.]/i`, `security.cjs`, `/.claude/hooks/`; applied uncond…
