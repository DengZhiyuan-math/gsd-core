
### A1  [CONFIRMED] severity=high
  WHY: I tried hard to refute this and instead found corroborating evidence the finder missed. (1) DISPATCH ORDER — exact, verbatim at the cited lines of gsd-core/bin/gsd-tools.cjs: :2641 `if (dispatchCapabilityCommand({ command, args, cwd, raw, error })) break;`, :2646 `if (dispatchOverlayCapabilityCommand({ command, args, cwd, raw, error })) break;`, :2652 `if (await dispatchHostCommand({ command, args, cwd, raw, error, defaultValue, workstreamContext })) break;`. Each is followed by `break`, so a `true` from the overlay layer terminates dispatch. `runCommand` at :2101 is now `switch (command) { default: { ... } }` — the switch really is dissolved; there are no surviving `case` arms. (2) THE GATE
  EV: gsd-core/bin/gsd-tools.cjs:2101 `switch (command) {` / `default: {`; :2641 `if (dispatchCapabilityCommand({ command, args, cwd, raw, error })) break;`; :2646 `if (dispatchOverlayCapabilityCommand({ command, args, cwd, raw, error })) break;`; :2652 `if (await dispatchHostCommand({ command, args, cwd, raw, error, defaultValue, workstreamContext })) break;`; :2061 `const HOST_COMMAND_ROUTERS = {` incl. 'agent','check','

### A2  [CONFIRMED] severity=low
  CORRECTED: The two code facts are exactly right and no ADR records a rationale — but this is a documentation gap, not a defect. The asymmetry is deliberate (capability/overlay routers go through the synchronous CommandRoutingHub contract, ADR-0174:70 "Dispatch is synchronous: dispatch<T>(req: DispatchRequest): Result<T>"; the host `await` exists because the extracted `capability` router performs async install/upgrade I/O), it f
  WHY: Both cited sites verified verbatim and at the exact line numbers claimed. gsd-core/bin/gsd-tools.cjs:446-451 (inside dispatchCapabilityCommand) and :560-565 (inside dispatchOverlayCapabilityCommand) are byte-identical blocks: `if (_result && typeof _result.then === 'function') { error('capability command "' + command + '" router "' + entry.router + '" in module "' + entry.module + '" must be synchronous (returned a Promise); async capability routers are not supported.', ERROR_REASON.SDK_FAIL_FAST); }`. Confirmed only two occurrences of "must be synchronous" exist in the file, at :448 and :562. gsd-core/bin/gsd-tools.cjs:2175-2177 confirmed: `// \`await\` so async host routers (e.g. capabilit
  EV: gsd-core/bin/gsd-tools.cjs:446-451 and :558-565 (both blocks read in full; `grep -n "must be synchronous"` returns exactly 2 hits, at :448 and :562). :2162 `async function dispatchHostCommand({ command, args, cwd, raw, error, defaultValue, workstreamContext }) {`; :2173-2177 `const router = HOST_COMMAND_ROUTERS[command]; if (typeof router !== 'function') return false; // \`await\` so async host routers (e.g. capabili

### B1  [CONFIRMED] severity=medium
  CORRECTED: Two termination mechanisms coexist with opposite flush semantics — io.cts:218 `process.exit(1)` inside error() vs cli-exit.cts ExitError→`process.exitCode`. Empirically, an error() reached inside the captureStdoutSyncWrites wrapper silently discards ALL already-emitted stdout (exit 1, empty stdout); the ExitError path preserves it. The capability-writer.cts:29 comment asserting ExitError 'is how every gsd-tools comma
  WHY: I tried to refute this and could not on the mechanism: every cited line says what the finding says, and a direct execution of the real dispatcher proves stdout loss on the error() path and preservation on the ExitError path. The claim as literally written (mechanism + comment falsity + consequence) holds. I downgrade severity to medium rather than high because the consequence is not currently exercised by any shipped command I could locate — the finding is a real invariant gap that nothing enforces, not an observed data-loss defect. Publish it framed as latent.
  EV: src/io.cts:211-219 verbatim: `function error(message, reason = ERROR_REASON.UNKNOWN): never { … writeAllSync(2, 'Error: ' + message + '\n'); } process.exit(1); }`. src/cli-exit.cts:31-54 runMain sets `process.exitCode` and never calls process.exit. src/capability-writer.cts:29 verbatim: `// ExitError (NOT process.exit) is how every gsd-tools command signals a non-zero exit: runMain`. gsd-core/bin/gsd-tools.cjs:2561-2

### B2  [WEAKENED] severity=low
  CORRECTED: `--pick <missing-field>` returns an empty string with exit 0, and `--pick` over non-JSON output silently passes the raw capture through with exit 0. Both behaviours are real and reproducible — but the missing-field case is an INTENTIONAL, regression-tested contract, not an oversight: tests/pick-flag.test.cjs:27-30 asserts exactly `success === true` and `output === ''` for a missing field, the flag is documented as a 
  WHY: The code behaviour is exactly as described and I reproduced it three ways. But the finding presents it as an unguarded fail-open, and there is a committed regression test locking the semantics in — which flips it from 'bug the team should fix' to 'contract the team chose'. Publishing it as a defect without that test citation would be a false positive in framing. The second half (unparseable → silent raw passthrough) is confirmed but is likewise the documented passthrough shape and its dangerous sub-case (@file: marker leaking to a caller) is unreachable.
  EV: gsd-core/bin/gsd-tools.cjs:2534-2548 verbatim: `const resolved = resolveAtFileOutput(captured); try { const obj = JSON.parse(resolved); const value = extractField(obj, pickField); const result = value === null || value === undefined ? '' : String(value); fs.writeSync(1, result); } catch { fs.writeSync(1, captured); } return;` — no error(), no non-zero exit. EMPIRICAL (built tree at aa0f7dee): `state --pick config.mod

### B3  [WEAKENED] severity=low
  CORRECTED: Only one half survives: `--raw` with a defined rawValue does short-circuit before the >50000 size check (src/io.cts:129-131 precedes the check at :136), so a large raw payload is never spilled. The rest is wrong. (a) The spill is NOT 'via a global fs.writeSync monkeypatch' — it is `platformWriteSync(tmpPath, json)` in io.cts:140; the monkeypatch at gsd-tools.cjs:2564-2578 is a separate stdout-capture. (b) The claimed
  WHY: I set out to check the ordering claim and it held, but the surrounding framing collapses under execution. The finding attributes the spill to the wrong mechanism, and its impact story — that --raw uniquely escapes a size guard — is refuted by measurement: both paths put the full payload on stdout because the CLI transparently undoes the spill. What remains is a low-severity code-hygiene note (a dead protection plus dead workflow shims), not the defect described.
  EV: src/io.cts:127-151 verbatim: `if (raw && rawValue !== undefined) { data = String(rawValue); } else { const json = JSON.stringify(result, null, 2); … if (json.length > 50000) { … platformWriteSync(tmpPath, json); data = '@file:' + tmpPath; } … }` — the --raw short-circuit does precede the size check. gsd-core/bin/gsd-tools.cjs:2555-2558: `const captured = await captureStdoutSyncWrites(…); fs.writeSync(1, resolveAtFile

### C1  [WEAKENED] severity=low
  CORRECTED: In the `loadConfigResolved` runtime path, `~/.gsd/defaults.json` is consulted ONLY when no `.planning/` directory exists (src/config-loader.cts:687 / :704). That exclusivity is intentional, documented (docs/CONFIGURATION.md:1659 — global defaults are a SEED merged into a new project's config.json by `buildNewProjectConfig`, which uses a generic `...userDefaults` spread with no allow-list, src/config.cts:328-333) and 
  WHY: Both halves of the mechanical claim hold, but the framing as a defect does not, and two cited line ranges are slightly off. VERIFIED TRUE: src/config-loader.cts:687 is `if (fs.existsSync(planningDir(cwd, ws))) {` and the `~/.gsd/defaults.json` read at :704 sits below it, so the global path is reachable only when no `.planning/` dir exists. src/config-loader.cts:541 is `? (_deepMergeConfig(rootParsed, fileData) as ParsedConfig ?? fileData)` — root↔workstream IS deep-merged. The #2069 comment is verbatim: 'Without these entries, ~/.gsd/defaults.json silently drops them — model_policy/provider/budget etc. are honored when set in a project but ignored when set globally.' CITATION CORRECTIONS: th
  EV: src/config-loader.cts:687 `if (fs.existsSync(planningDir(cwd, ws))) {`; :704 `const globalDefaultsPath = path.join(home, '.gsd', 'defaults.json');`; :708-742 `_globalBaseCfg` literal (counted 25 explicit keys: model_profile, commit_docs, research, plan_checker, verifier, nyquist_validation, post_planning_gaps, parallelization, text_mode, resolve_model_ids, context_window, subagent_timeout, model_overrides, models, gr

### C2  [CONFIRMED] severity=low
  WHY: I could not find any consumer that compares, gates on, or branches on `gsd_state_version`, after searching every non-test source location in the repo with several spellings. The claim is accurate. The complete non-test, non-doc population of the identifier is five sites: two writes, one classification-table row, two comments. src/state.cts:1706 `const fm: Record<string, unknown> = { gsd_state_version: '1.0' };` — hardcoded, unconditional, inside `buildStateFrontmatter`, so the value can never diverge from '1.0' through the normal sync path. src/state-transition.cts:1199 `gsd_state_version: existingFm['gsd_state_version'] || '1.0',` — the only READ of the field anywhere, and it is a copy-forw
  EV: Repo-wide grep for `gsd_state_version` across *.cjs/*.cts/*.js/*.sh/*.md/*.json excluding .claude/worktrees, node_modules, tests/, docs/ — exactly 5 code sites: src/state-transition.cts:80, :1134 (comment), :1197 (comment), :1199; src/state.cts:1706; plus their generated mirrors gsd-core/bin/lib/state-transition.cjs:51/846/901/903 and gsd-core/bin/lib/state.cjs:1363, the template gsd-core/templates/state.md:11, .plan

### C3  [WEAKENED] severity=low
  CORRECTED: No ADR mentions `gsd_state_version` (confirmed). But it is not true that no ADR covers `.planning/` forward/backward compatibility: ADR-1817 (docs/adr/1817-state-md-rebuild-derivability-contract.md:215-218) explicitly reasons about cross-version STATE.md reads, and ADR-1817's derivability contract plus `state rebuild` is the repo's actual compatibility strategy for STATE.md — reconstruct from canonical sources rather
  WHY: The first half survives; the second half is a classic absence claim that a single additional grep overturns. SUB-CLAIM 1 — 'No ADR mentions gsd_state_version': CONFIRMED. `grep -rln gsd_state_version docs/` returns only the five localized copies of docs/reference/state-md.md (en, ja-JP, ko-KR, pt-BR, zh-CN). Zero hits under docs/adr/ across all 66 ADR files + README. SUB-CLAIM 2 — 'no ADR covers .planning/ forward/backward compatibility': REFUTED as stated. docs/adr/1817-state-md-rebuild-derivability-contract.md:215-218, under Consequences → Negative, reads verbatim: 'A new body section (`## Rebuild Log`) is added to STATE.md. Older GSD versions reading the file ignore the section (preserved
  EV: `ls docs/adr | wc -l` → 67 entries (66 ADRs + README.md), matching ground truth. `grep -rln gsd_state_version docs/` → docs/{,zh-CN/,pt-BR/,ko-KR/,ja-JP/}reference/state-md.md only; no docs/adr/ hit. Synonym hunt across docs/adr/*.md with `grep -rniE 'backward.?compat|forward.?compat|schema version|artifact version|version.{0,15}(migration|upgrade)|migrat'` (~60 hits reviewed) plus `grep -rn '\.planning/' docs/adr/*.

### D1  [WEAKENED] severity=low
  CORRECTED: Uninstall removes GSD artifacts by `gsd-` filename-prefix match and never consults the install manifest for ownership evidence — `bin/install.js:8835` only *deletes* `gsd-file-manifest.json`, and `installerMigrations.readInstallManifest`/`classifyArtifact` appear nowhere in the uninstall path (they exist only in the install-side `_migrateLegacyOpencodeCommandDir`, src/install-engine.cts:1173/1185). Consequence is nar
  WHY: Three sub-claims, three different fates. (1) 'prefix match, not the manifest' — CONFIRMED; I could not find any manifest consultation anywhere in the uninstall path across src/install-engine.cts and bin/install.js. (2) 'for one layout kind rmSync's the whole namespace dir' — technically true of the source text but INERT: no shipped capability descriptor sets prefix='', hermes (the historical prefix='' case, #3664) now ships 'gsd-', and the code's own comment states it is a defensive guard for a runtime that does not exist. Publishing this as a live destructive path would be a false positive. (3) The ADR-0008 contradiction does not hold as cited: the two cited lines say something else entirel
  EV: src/install-engine.cts:1278-1294 (uninstallRuntimeArtifacts → _removeGsdEntries per layout kind, no manifest read); :486-512 _removeGsdEntries — line 503 `if (kind.prefix === '')` then line 506 `fs.rmSync(destDir, { recursive: true, force: true });`, immediately preceded by the doc comment at :483 'No current runtime uses prefix='' after #947 reversed Hermes; kept as a defensive guard for future runtimes.' Verified e

### D2  [CONFIRMED] severity=medium
  CORRECTED: stripGsdFromCodexConfig truncates config.toml from the GSD marker to EOF with no end-marker and no backup, so user TOML added below the marker is silently destroyed on uninstall. Nuance the finder missed: the *install* path (mergeCodexConfig, bin/install.js:6438-6449) performs the same marker-to-EOF truncation, so this is a repo-wide 'GSD owns everything below the marker' convention, not an uninstall-only bug — which
  WHY: I opened the function end-to-end, confirmed there is no end-marker to look for, confirmed no backup is written on the uninstall write path, and reproduced the data loss by actually invoking the shipped function. The only mitigations present are partial and confirm the maintainers know about the hazard: bin/install.js:5661-5664 deliberately inserts migrated user hooks ABOVE the marker 'so the migrated user hooks stay in the "user" portion of the file and are not swept away when stripGsdFromCodexConfig strips from the marker to EOF', and line 4572 recovers only [agents] scalars. I am downgrading the impact framing rather than the finding: because mergeCodexConfig already truncates marker-to-EO
  EV: bin/install.js:4562-4586. Line 4568 comment verbatim: '// Has GSD marker — remove everything from marker to EOF.' Line 4573: `let before = content.substring(0, markerIndex);` — the only thing recovered from below the marker is `extractCodexUserAgentsScalars(content.slice(markerIndex))` (line 4572), i.e. bare `[agents]` scalars; everything else after markerIndex is discarded. No end-marker exists: `grep -n 'GSD_CODEX_

### D3  [WEAKENED] severity=low
  CORRECTED: The *broad* partial-install rollback — the snapshot/restore of skills/gsd-*, agents/gsd-*, gsd-core/VERSION, config.toml and hooks.json — is gated on `tomlConfigInstall`, which exactly one runtime (Codex) declares, so no other runtime gets file-tree restore after a mid-install throw. But non-Codex runtimes are NOT rollback-less: `rollbackInstallerMigrations()` runs for every runtime on an early-install throw (bin/ins
  WHY: The two mechanical sub-claims hold exactly as cited: tomlConfigInstall is declared by precisely one descriptor, and both :10152 and :10206 gate on it. But the load-bearing part of the finding is the absence claim ('rollback exists only for Codex'), and that is refuted by the very catch block the rollback lambda feeds into — the line immediately above `if (_codexPreConfigRollback)` calls rollbackInstallerMigrations() unconditionally, and its comment says in so many words that migration rollback covers every runtime while Codex layers a broader snapshot on top. The finder appears to have read the Codex-gated lambda and not the call site. What genuinely remains is a coverage asymmetry — a throw
  EV: Global grep `tomlConfigInstall` across *.json/*.cts/*.js/*.cjs/*.md (node_modules excluded): the only source-of-truth declaration is capabilities/codex/capability.json:79 `"tomlConfigInstall": true,` (plus its build outputs in gsd-core/bin/lib/capability-registry.cjs:949/4654, a worktree copy, and the assertion in tests/codex-declarative-reference.test.cjs:124). Four consumers, all in bin/install.js: :8067 (uninstall

### E1  [WEAKENED] severity=medium
  CORRECTED: On every runtime whose hooks surface registers it (the 6 settings-json runtimes — claude, qwen, hermes, augment, antigravity, codebuddy — plus codex and kimi/kimi-code), GSD registers a SessionStart hook that unconditionally spawns a detached background worker which runs `npm view <pkg> version` against the npm registry on every session start. There is no env-var, config-key, TTL, or install-flag opt-out anywhere in 
  WHY: MECHANISM CONFIRMED. hooks/gsd-check-update.js:54-66 is `const child = spawn(process.execPath, [workerPath], { stdio:'ignore', windowsHide:true, detached:true, env:{...} }); child.unref();` at top level — no guard of any kind above it (only an fs.mkdirSync of the cache dir at :45-47). It never stats or reads the cache file it computes at :38, so there is no TTL skip. hooks/gsd-check-update-worker.js:91-95 calls `checkLatestVersion()` unconditionally; gsd-core/bin/check-latest-version.cjs:96-105 → `execNpm(['view', PACKAGE_NAME, 'version'], {timeout:15_000})` → src/shell-command-projection.cts:536 `childProcess.spawnSync('npm', args, ...)`. That is a real registry call on every invocation. OP
  EV: Read in full: hooks/gsd-check-update.js (67 lines), hooks/gsd-check-update-worker.js (109 lines), gsd-core/bin/check-latest-version.cjs (162 lines). Key lines quoted: check-update.js:54-66 `const child = spawn(process.execPath, [workerPath], {stdio:'ignore', windowsHide:true, detached:true, ...}); child.unref();`; worker:93 `const lv = checkLatestVersion();`; check-latest-version.cjs:97 `const r = execNpm(buildViewAr

### E2  [CONFIRMED] severity=medium
  CORRECTED: Managed-hook ownership is decided by per-basename regex over FOUR hand-maintained allowlists (three in src/shell-command-projection.cts, one in hooks/managed-hooks-registry.cjs) with no generator and no lint/test tying the projection sets to the ship list. They diverge today: gsd-worktree-path-guard.js, gsd-graphify-update.sh and gsd-ensure-canonical-path.js are installer-registered/shipped hooks that both isManagedH
  WHY: STRUCTURE CONFIRMED at the cited lines. src/shell-command-projection.cts:196 `const MANAGED_HOOK_BASENAMES_BY_SURFACE: Record<string, Set<string>> = {` (9 entries for 'settings-json', 1 for 'codex-toml'); :213 `const MANAGED_HOOK_COMMAND_BASENAMES_BY_SURFACE: ... = {` (12 for 'settings-json', 1 for 'codex-toml', 4 for 'codex-hooks-json'); :244 `const LEGACY_MANAGED_HOOK_ALIASES_BY_SURFACE` (a FOURTH list the claim omits — undercount, not overclaim); hooks/managed-hooks-registry.cjs:18-43 `const MANAGED_HOOKS = [...]` (24 entries). All four are literal, hand-typed `new Set([...])` / array bodies — NOT generated from any source, so the 'REFUTE if generated from one source' escape does not appl
  EV: Opened src/shell-command-projection.cts:185-320 (all four list definitions + isManagedHookBasename :257 + isManagedHookCommand :273 + the per-basename `new RegExp` at :300-303) and hooks/managed-hooks-registry.cjs in full (24 entries, :18-43). Ran `node -e` against gsd-core/bin/lib/shell-command-projection.cjs over 15 registered hook basenames; verbatim output rows: `gsd-worktree-path-guard.js cmdManaged=false baseMa

### F1  [CONFIRMED] severity=low
  WHY: Both halves hold. docs/adr/857-capability-system.md:1 is `# ADR-857: Capability system — five-step loop as core, features as plug-ins behind Loop Extension Points [Proposed]` while :3 is `- **Status:** Accepted — ratified 2026-07-17 (originally Proposed 2026-06-08)`. scripts/gen-adr-index.cjs:213 is `.replace(/\s*\[(?:Proposed|Accepted|Superseded|Legacy|Retired)\]\s*$/i, '')` inside parseAdr's title derivation — it strips the bracket unconditionally, and I read validate() at :270-383 in full: it checks statusToken vocabulary, declaredId-vs-filename, Superseded successor links, and relation symmetry, but never compares the H1 bracket to the Status field. So the mismatch is silently normalized
  EV: docs/adr/857-capability-system.md:1 `# ADR-857: ... [Proposed]`; :3 `- **Status:** Accepted — ratified 2026-07-17 (originally Proposed 2026-06-08); see "Ratification" below`; scripts/gen-adr-index.cjs:207-214 (title derivation, incl. :213 strip regex); scripts/gen-adr-index.cjs:270-383 (full validate(), no H1-bracket check); node scan over all 66 ADRs → sole mismatch `857-capability-system.md | H1=[Proposed] | Status

### F2  [WEAKENED] severity=low
  CORRECTED: Three bare, unlinked ADR-0010/ADR-0011 references are live in docs/adr/ and pass CI — but all three sit outside the checker's declared contract (one in a non-relation `Builds on:` header field, two in body prose, none in a relation field), and each carries inline prose that disambiguates which file is meant. The gap is that the checker's ambiguity rule is scoped to supersedes/subsumes fields only, not that ambiguous 
  WHY: I ran the grep myself and the hits are genuinely bare — no markdown link on any of them: 1213-capability-state-writer.md:7 `- **Builds on:** Capability declaration format (ADR-894), Capability command contribution (ADR-959), Skill Surface Budget Module (ADR-0011)`; 1610:121 `...install-time skill-surface budget** of ADR-0010/0011`; 1610:131 `- Related but distinct: **ADR-0010/0011** (skill-surface budget — install-time, not file size)`. Ambiguity of the raw ids is real: canonicalId collapses 0011→11 and `ls docs/adr/` shows 0010-file-operation-engine-module.md, 0010-skill-surface-budget-module.md, 0011-review-default-reviewers-prd.md, 0011-review-default-reviewers.md, 0011-skill-surface-budg
  EV: grep -n 'ADR-0010|ADR-0011' over the two files (3 hits, quoted above, none inside `[...](...)`); `ls docs/adr/ | grep -E '^(0*10|0*11)-'` → 5 files; scripts/gen-adr-index.cjs:73-78 RELATION_FIELDS map; :109-112 canonicalId; :300-341 bare-id and ambiguity error paths; `node scripts/gen-adr-index.cjs --check` → exit 0.

### F3  [CONFIRMED] severity=low
  WHY: Opened the line. docs/adr/1239-gsd-embeddable-orchestration-engine.md:99 contains ``[`docs/reference/host-integration-capability-matrix.md`](reference/host-integration-capability-matrix.md)`` — link TEXT says docs/reference/..., href is the relative `reference/...`. From docs/adr/ that resolves to docs/adr/reference/host-integration-capability-matrix.md. I checked: `ls docs/adr/reference` → 'No such file or directory'; the real target exists at docs/reference/host-integration-capability-matrix.md (104.7K). So the link is dead; correct href is `../reference/...`. I also confirmed it is un-gated: grep for check-links / markdown-link / lychee / linkcheck across .github/workflows/, package.json 
  EV: docs/adr/1239-gsd-embeddable-orchestration-engine.md:99 (full paragraph read); `ls -la docs/reference/host-integration-capability-matrix.md` → exists, 104.7K; `ls docs/adr/reference` → No such file or directory; `grep -rn '](reference/' docs/adr/` → 1 hit (this line); `grep -rn 'check-links|markdown-link|lychee|linkcheck' .github/workflows/ package.json scripts/` → no output.

### F4  [CONFIRMED] severity=low
  WHY: src/plan-drift-guard.cts:5 reads ` * ADR-22 (docs/adr/0022-source-grounding-drift-guard.md).` The real file is docs/adr/22-plan-drift-guard.md (H1: 'Plan-vs-codebase drift guard: defaults and symbol-resolver seam', Status Accepted). I tested the 'never existed' absence claim rather than assuming it: `git log --all --oneline --diff-filter=A -- 'docs/adr/0022*'` returns nothing, and `git log --all --follow --name-status -- docs/adr/22-plan-drift-guard.md` shows it was ADDED under its current name at d2ff4ac0 and only ever M-modified since (3bb2f8f1, 67a9243c) — no rename from the cited path. `grep -rln 'source-grounding' docs/` hits only docs/issueevidence/1192-adr-test-audit-2026-06-13.md, ne
  EV: src/plan-drift-guard.cts:1-12 (comment quoted); docs/adr/22-plan-drift-guard.md:1-5; `ls docs/adr/ | grep -E '^0*22-'` → 22-plan-drift-guard.md only; `git log --all --diff-filter=A -- 'docs/adr/0022*'` → empty; `git log --all --follow --name-status -- docs/adr/22-plan-drift-guard.md` → A at d2ff4ac0 under current name; scan of every `docs/adr/*.md` path literal in src/ against the filesystem → single MISSING: docs/ad

### F5  [WEAKENED] severity=low
  CORRECTED: CONTRIBUTING.md:105 presents a fabricated worked example as history — 'Issue #3485 was opened, approved, and its number became the prefix: docs/adr/3485-adr-prd-naming-convention.md' — but issue/PR #3485 does not exist in open-gsd/gsd-core (highest number is #2620) and neither does the file. docs/contributor-standards.md:99 and :103 reuse the same invented number. These are illustrative example filenames in backticks
  WHY: Both cited lines are exactly where claimed and the file genuinely does not exist (`ls docs/adr/ | grep 3485` → empty). I went further and checked the issue: `gh issue view 3485 --repo open-gsd/gsd-core` → 'Could not resolve to an issue or pull request with the number of 3485' (that query covers PRs too), and `gh issue list --state all --limit 1` → #2620 is the highest number in the repo, so #3485 is ~865 above anything that exists. But the finding's framing — 'cite ... which does not exist' — implies a dangling reference, and that consequence does not follow. Neither site is a link: CONTRIBUTING.md:105 is `**Example:** Issue #3485 was opened, approved, and its number became the prefix: \`doc
  EV: CONTRIBUTING.md:100-106; docs/contributor-standards.md:95-105 (incl. :99 and :103); `ls docs/adr/ | grep 3485` → empty; `gh issue view 3485 --repo open-gsd/gsd-core` → GraphQL 'Could not resolve to an issue or pull request with the number of 3485'; `gh issue list --repo open-gsd/gsd-core --state all --limit 1 --json number` → [{"number":2620}]; repo-wide grep for 3485 → also CLAUDE.md:187, .planning/codebase/CONVENTI

### F6  [REFUTED] severity=none
  CORRECTED: The behavior is real and reproduces exactly as described, but it is deliberate, documented in-source, and pinned by a dedicated regression test — it is not a hole. --check (the CI gate) still exits 1 on the same corpus.
  WHY: I built the violating corpus as instructed: copied docs/ and scripts/ to a temp tree, added docs/adr/9999-bogus-verifier-probe.md with `- **Status:** Bananas` and `- **Supersedes:** [ADR-4242](4242-nope.md)`. `--check` → exit 1 with both violations named. `--write` → 'Wrote ADR index into .../README.md (67 ADRs)', then '2 lifecycle violation(s) remain — --check will fail:' on stderr, and WRITE_EXIT=0. So the mechanical claim is precisely right, and the cited lines are right: :491 is `if (errors.length > 0 && flag !== '--write') {` and :517-520 is the write-path `if (errors.length > 0) { process.stderr.write(...) }` with no throw. But the implied defect is refuted on three independent grounds
  EV: scripts/gen-adr-index.cjs:485-524 (main(), incl. :491 and :517-520) and :23-25 (usage doc); constructed temp corpus at scratchpad/adrtest with an invalid-status + dangling-supersedes ADR; `node scripts/gen-adr-index.cjs --check` → exit 1, 2 violations named; `node scripts/gen-adr-index.cjs --write` → stdout 'Wrote ADR index ... (67 ADRs)', stderr '2 lifecycle violation(s) remain', WRITE_EXIT=0; tests/adr-index-gate.t

### F7  [WEAKENED] severity=low
  CORRECTED: gen-adr-index.cjs --check is 9th of 10 links in the lint:generated-sync && chain (package.json:114), which is itself the 3rd link of the 13-link lint:ci chain (package.json:106). An earlier failure does defer it, but the gate is never bypassed — CI stays red until every link passes. The cost is slower feedback (serial red cycles), a property shared by all 10 links and by lint:ci as a whole, not a defect specific to t
  WHY: The positional facts are exactly right — I split the script programmatically rather than eyeballing it: package.json:114 lint:generated-sync has TOTAL 10 links and gen-adr-index.cjs --check is #9 (check-glossary-refs.cjs --check is #10). Where the finding overreaches is the consequence. 'Short-circuits it' is true per-run but does not mean the ADR gate can be evaded: && failure is a non-zero exit, CI fails, and the ADR check simply runs on the next attempt once the earlier link is fixed. Nothing merges past it. The claim also singles out the ADR gate for a property that is uniform across the chain — link #10 (check-glossary-refs) is strictly worse off by the same logic, and lint:ci at :106 p
  EV: package.json:114 split on '&&' via node → 10 entries, #9 = `node scripts/gen-adr-index.cjs --check`, #10 = `node scripts/check-glossary-refs.cjs --check`; package.json:106 lint:ci = 13-link chain with lint:generated-sync 3rd; grep for gen-adr-index across .github/, tests/, scripts/, .githooks/ → only the lint chain plus tests/adr-index-gate.test.cjs; that test file's makeRepo()/run() harness at :21-72 operates on tem

### G1  [WEAKENED] severity=low
  CORRECTED: No ADR governs the agent return contract / completion markers, but a live, workflow-loaded registry does: `gsd-core/references/agent-contracts.md` enumerates every agent's markers, the marker rules, and the Planner→Executor / Executor→Verifier handoff schemas. The gap is that this doc is explicitly descriptive ("describes what IS, not what should be") and unratified — casing/format inconsistencies are recorded rather
  WHY: The ADR half survives my hunt: I grepped docs/adr/ for 'COMPLETE', '## PLANNING/PLAN/EXECUTION/VERIFICATION/RESEARCH COMPLETE', 'completion marker', 'return contract', 'structured return', 'agent return', 'sentinel', 'handoff', 'marker', 'agent contract', 'subagent contract', 'agent output' — the only 'marker' hits are unrelated (`.gsd-profile` marker files, `undocumented` descriptor sentinels, `gsd:loop-host` HTML comments, `pending #2143` grandfather markers). No ADR title in docs/adr/README.md's index is about agent return. I also checked the two nearest candidates: ADR-1866 is about `agent_skills` config injection at spawn time, ADR-2346 is about `gsd-tools` command dispatch registry — n
  EV: docs/adr/ multi-spelling grep: zero hits for completion-marker/return-contract terms. gsd-core/references/agent-contracts.md L1-3: '# Agent Contracts / Completion markers and handoff schemas for all GSD agents. Workflows use these markers to detect agent completion and route accordingly.' L5: 'This doc describes what IS, not what should be. Casing inconsistencies are documented as they appear in agent source files.' 

### G2  [WEAKENED] severity=low
  CORRECTED: No ADR establishes a behavioural eval harness for GSD's own prompts — but ADR-550 Decision 5 explicitly *decides* the policy that LLM-behaviour stages are out of deterministic CI scope and are 'validated by offline batteries', and no such battery is committed to the repo. The gap is an unbuilt/uncommitted harness under an existing decision, not an ungoverned area.
  WHY: I hunted docs/adr/ for eval, evals, harness, golden, corpus, rubric, judge, regression suite, behavio*, ablation, A/B, LLM-as-judge, prompt eval, prompt regression, golden transcript. ADR-550 §D5 is a direct hit and squarely on-topic: it decides what CI may and may not claim about LLM behaviour and rejects LLM-judgment tests as vacuous. ADR-550 §Decision 5-on-N17 additionally records measured behavioural properties (100%→17% false-pass) with explicit evidence-honesty caveats ('n=27, 1 rep — direction-finding, not powered') and mandates a test so a model-tier regression is caught. ADR-456(c) further constrains the test surface. So 'no ADR governs prompt/behavioural evaluation' is too strong. 
  EV: docs/adr/550-spec-phase-probe-contract.md:39: 'The CI-testable surface is the contract, not the classifier. The recall/precision/tier-assignment stages are LLM behavior and are **not** claimed as deterministic CI coverage (validated by offline batteries...). ... A test that asserts the LLM's *judgment* is vacuous and is rejected per RULESET.TESTS.delete-bad-tests.' ADR-550:160: 'Evidence honesty: N17 is n=27, 1 rep —

### G3  [REFUTED] severity=low
  CORRECTED: `withPlanningLock` is named as authoritative Module surface in Accepted ADR-0004 and its ownership is explicitly re-decided in ADR-0010's supersession note. What no ADR does is *specify* the concurrency model (stale-steal, timeout, multi-writer semantics) — ADR-0010 leaves 'whether lock semantics should be one shared policy for installer + planning' as an open question.
  WHY: This is the claim I most expected to survive and it did not. ADR-0004 (Accepted, 2026-05-08) lists 'planning lock semantics (`withPlanningLock`)' by name in its authoritative-surface bullet list — so 'withPlanningLock was built with no ADR' is directly contradicted by an Accepted ADR naming the function. ADR-0010 goes further and makes a concrete ownership decision about the lock's file-lifecycle semantics versus atomic-write semantics. ADR-1213 §3 relies on `withPlanningLock` as its atomicity mechanism. ADR-456 policy (a) governs how lock concurrency is *tested* (injectable clock seam, after a documented 40% flake rate on a real-race lock test), and src/clock.cts's docstring names `acquireS
  EV: docs/adr/0004-worktree-workstream-seam-module.md:14: '- planning lock semantics (`withPlanningLock`)' under 'The Planning Workspace Module Interface is authoritative for:'; Status line: 'Accepted, 2026-05-08'. docs/adr/0010-file-operation-engine-module.md:7: 'Lock-file lifecycle (Track B item 3) remains owned by `withPlanningLock` in `planning-workspace.cjs` because its `{ flag: wx }` exclusive-create semantics diffe

### G4  [WEAKENED] severity=low
  CORRECTED: No ADR decides telemetry, and all five quoted deferrals exist — but they are five FILES, not five live decisions: two are Superseded, one is a frozen Legacy PRD, and the skill-surface-budget pair is the same module renumbered. Only two Accepted ADRs carry a live deferral, covering two distinct decisions. Separately, none of the five is about network egress at all — every hit is about *usage/product metrics*; 'egress'
  WHY: I verified each quote at the cited file and each is real, so the deferral half holds, and I found no ADR that decides telemetry (grep for telemetry|egress|analytics|phone.home|no network|outbound|does not send across docs/adr/, docs/, CONTRIBUTING.md, SECURITY.md, README.md). But the framing inflates the evidence twice over. (1) Status headers: ADR-0010-file-operation-engine is 'Superseded by ADR-0009'; ADR-0010-skill-surface-budget is 'Superseded by ADR-0011 ... originally Proposed'; ADR-0011-review-default-reviewers-prd is 'Legacy — frozen historical record; not a pattern to follow'. That leaves ADR-0011-skill-surface-budget and ADR-0011-review-default-reviewers as the only live Accepted d
  EV: All five quotes opened and confirmed: docs/adr/0010-file-operation-engine-module.md:101 'Whether file mutation telemetry (per-op reason codes and rollback events) should be required for all engine adapters.' (header L3: 'Superseded by ADR-0009'); docs/adr/0010-skill-surface-budget-module.md:139 'Whether telemetry (opt-in) is worth proposing ... Track separately; not a blocker.' (header L3: 'Superseded by ADR-0011 ...

### G5  [CONFIRMED] severity=low
  WHY: Verified two independent ways and the claim is exactly right. What I could not confirm is the implied severity. Root already carries per-runtime context files — CLAUDE.md (27.3K), GEMINI.md (2.5K, explicitly 'the context Antigravity reads automatically'), .clinerules (1.2K) — so agents on this repo are not context-less; the missing file is specifically the AGENTS.md convention (Codex/OpenCode/Cursor). The mild sting is dogfooding: GSD ships Codex as a supported runtime and its own installer writes AGENTS.md into *consumer* Codex projects (changeset fix-3163-codex-agents-md: 'when config.runtime is codex ... the handler overrides the output target to AGENTS.md'), yet the repo does not carry o
  EV: `ls -la AGENTS.md` → '/usr/bin/ls: cannot access AGENTS.md: No such file or directory'. `git ls-files | grep -i AGENTS.md` → only .changeset/archived/fix-3163-codex-agents-md.md, docs/AGENTS.md, docs/ja-JP/AGENTS.md, docs/ko-KR/AGENTS.md, docs/pt-BR/AGENTS.md — no root entry. `grep -n -i agents.md .gitignore` → no match (exit 1), so it is not a generated/ignored file. Root listing shows CLAUDE.md, GEMINI.md, .clineru

### G6  [REFUTED] severity=low
  CORRECTED: Two enforced markdown link checkers exist. What is missing is a *general-purpose* checker for ordinary relative `[text](path.md)` links in prose docs — and that gap is empirically real: 13 broken relative links exist in tracked .md files today, including one inside docs/adr/ that the ADR checker's narrow scope does not cover.
  WHY: The absence claim fails on its own terms. (1) scripts/gen-adr-index.cjs parses sibling-ADR markdown links and hard-fails on unresolvable targets, and it is wired into CI via the `lint:generated-sync` npm script (`node scripts/gen-adr-index.cjs --check`), with behavioural tests at tests/adr-index-gate.test.cjs. (2) tests/command-contract.test.cjs asserts every `execution_context` @-ref resolves on disk — the exact class of failure CONTEXT.md:831 records as DEFECT.WORKFLOW-DELEGATION-TARGET-NOT-INSTALLED. (3) tests/no-dead-sdk-refs.test.cjs is a third, narrower path-reference guard. So 'no markdown link checker exists' is refuted. I then ran the scan myself to see whether the *narrowed* versio
  EV: scripts/gen-adr-index.cjs:176-182 `linkedAdrFiles` regex, and the validator: 'if (!byFile.has(l)) add(a.file, `"${rel.field}" links "${l}", which does not exist in docs/adr/.`)'. package.json:114 `"lint:generated-sync": "... && node scripts/gen-adr-index.cjs --check && node scripts/check-glossary-refs.cjs --check"`. tests/command-contract.test.cjs:91-97 `assert.ok(fs.existsSync(path.join(GSD_ROOT, normalized)), `${na

### H1  [WEAKENED] severity=low
  CORRECTED: Locale docs are inconsistently maintained rather than uniformly "drifted 98%". The cited numbers are exact (docs/pt-BR/FEATURES.md = 3,935 B vs docs/FEATURES.md = 180,999 B = 2.17%), but that file self-declares as a condensed Portuguese overview linking to the canonical English doc, and CONTRIBUTING.md:277 states translations are community-maintained and need not be updated per-PR. Byte ratio is not a valid drift met
  WHY: The arithmetic is right but the framing is wrong in three ways. (1) pt-BR/FEATURES.md is not a drifted translation — its own second line says it is a condensed view pointing to English as canonical, so 2.2% is by design; pt-BR/AGENTS.md (2,304 B) carries the identical pointer. (2) CONTRIBUTING.md has an explicit 'Language policy' section declaring translations community-maintained and exempt from per-PR updates, so the drift is accepted policy, not an unnoticed defect. (3) Byte-count comparison across scripts is unsound: ja-JP/FEATURES.md is 9% LARGER than English, and ko-KR/FEATURES.md at 93,219 B (51%) reflects Hangul density as much as content loss. I searched for staleness banners across
  EV: wc -c across all four locales for 9 root docs. Exact: FEATURES.md en=180999 pt-BR=3935 ja-JP=197550 ko-KR=93219 zh-CN=139291; CONFIGURATION.md en=121729 ja-JP=18973 ko-KR=16912; AGENTS.md en=35365 pt-BR=2304 zh-CN=MISSING. docs/pt-BR/FEATURES.md:3 — 'Visão em Português dos recursos centrais do GSD. Para catálogo completo e detalhamento exaustivo, consulte [FEATURES.md em inglês](../FEATURES.md).' CONTRIBUTING.md:277 

### H2  [WEAKENED] severity=low
  CORRECTED: 44 of 91 workflows (not 47) carry an actual response_language directive; 47 merely contain the string, of which 3 only list it as a JSON output field with no honoring instruction. Of the 44 with no mention at all, roughly half are non-user-facing utility workflows (list-workspaces.md, sync-skills.md, node-repair.md, reapply-patches.md, stats.md, list-seeds.md) where the directive is inapplicable, and four more (progr
  WHY: The raw grep count reproduces (47/91) but is wrong in both directions, which is exactly the artifact the task asked me to check for. Splitting mention-vs-directive drops the honoring count to 44. On the other side, the 44 'gaps' include workflows where response_language is meaningless (a workspace lister emits no prose) and four that receive the field through the init JSON seam. So neither 47 nor 44 is the number a maintainer should act on. The verified actionable set is ~7 user-facing workflows. I am reporting the agents/ finding as the stronger adjacent defect: the reference doc makes a claim about agent behavior that no agent file implements — the directive is propagated only by workflow 
  EV: node -e count over gsd-core/workflows (bypassing grep filtering): 'total workflows 91 mention 47 with-directive 44'. Directive pattern = /If `response_language` is set|execute-phase-response-language/. comm -13 listed all 44 non-mentioning files. Per-file init-call check: explore.md 0, help.md 0, fast.md 0, review.md 0; progress.md 3 (init context, init todos), verify-phase.md 1, code-review.md 1, pause-work.md 1. sr

### H3  [CONFIRMED] severity=medium
  WHY: Both halves hold, and the removal half is stronger than a monotonic count would prove. I did not rely on the counts alone — a net +4 could hide removals — so I ran the set difference: `comm -23` of v1.0.0's command file list against v1.8.0's returned EMPTY, meaning literally every command shipped at v1.0.0 is still shipped at v1.8.0. The four additions are commands/gsd/{mempalace-capture,mempalace-recall,next,onboard}.md. Note the growth is stepwise (67 through v1.4.x, 69 at v1.5.0-v1.6.x, 71 at v1.7.0-v1.8.0), not smooth, but it is strictly non-decreasing. This is load-bearing because ADR-0011:10 identifies the skill-listing budget as the actual constraint: 'With 66 skills and 33 agents, GS
  EV: git ls-tree -r --name-only <tag> -- commands/gsd/ | grep -c '\.md$' for v1.0.0=67, v1.2.0=67, v1.3.0=67, v1.4.0=67, v1.5.0=69, v1.6.0=69, v1.7.0=71, v1.8.0=71. comm -23 <(v1.0.0 list) <(v1.8.0 list) = empty (zero removals). comm -13 = mempalace-capture.md, mempalace-recall.md, next.md, onboard.md. ls commands/gsd/*.md at HEAD aa0f7dee = 71.

### H4  [CONFIRMED] severity=low
  CORRECTED: Same substance, corrected line anchors and one caveat: the constants are at scripts/lint-docs-required.cjs:33 (TRIGGERING_TYPES) and :35 (DOCS_PREFIX); the predicate body is line 42 (`return file.startsWith(DOCS_PREFIX);`), not 43 (which is the closing brace); and the actual proxy decision is line 98 (`if (changedFiles.some(isDocsFile))` → OK_DOCS_UPDATED). Caveat worth stating alongside: .github/workflows/docs-requi
  WHY: I read the script end to end including both escape hatches and the CI wrapper. The predicate is a bare prefix test with no relationship whatsoever to what the changeset describes: touching docs/adr/, docs/audit/, docs/research/, a locale file, or a one-character typo in docs/README.md all satisfy a triggering Added/Changed/Deprecated/Removed fragment. So 'a proxy, not a truth check' is literally accurate. I checked the three ways this could be inert and none apply: the gate is wired into a real CI job (docs-required.yml:41 runs the script), it fails closed on malformed fragments and on partial per-fragment exemption, and the two opt-outs (`no-docs` label, `<!-- docs-exempt: <reason> -->` wit
  EV: Full read of scripts/lint-docs-required.cjs. Line 33: `const TRIGGERING_TYPES = new Set(['Added', 'Changed', 'Deprecated', 'Removed']);`. Line 35: `const DOCS_PREFIX = 'docs/';`. Lines 41-43: `function isDocsFile(file) { / return file.startsWith(DOCS_PREFIX); / }`. Line 98-99: `if (changedFiles.some(isDocsFile)) { return { ok: true, reason: LINT_REASON.OK_DOCS_UPDATED, ... } }`. Escape hatches: isExemptFragment (line

### H5  [CONFIRMED] severity=medium
  CORRECTED: Substance holds exactly; fix the citation. ADR-0010 is Superseded by ADR-0011 (Accepted), so the rationale should be cited as ADR-0011:17 ('`full` (all skills — the previous default)') with ADR-0010:15 noted as the superseded draft that stated it plainly. Also worth adding: ADR-0010:20 proposed keeping an interactive install picker, and no such picker exists in bin/install.js — the full surface is handed over with no
  WHY: I did not take the code comment on trust — I executed resolveEffectiveProfile against a fresh empty temp dir with no flag and no marker and it returned 'full', and resolveProfile([]) returned {name:'full', skills:'*'}. The '*' sentinel means no staging filter, and `ls commands/gsd/*.md` at HEAD is exactly 71, so '71 skill descriptions' is precise. The ADR-0010:15 quote is verbatim: 'Default install (no flag) remains `full` for back-compat.' The one real defect in the finding is the citation: ADR-0010 line 3 marks it 'Superseded by ADR-0011', and ADR-0010 itself carries a 2026-07-16 provenance note explaining that its status was stale and corrected — citing it as the live rationale would be e
  EV: node -e against gsd-core/bin/lib/install-profiles.cjs on a fresh mkdtemp: 'no flag, no marker -> full' and 'resolveProfile([]) -> {"name":"full","skills":"*"}'. src/install-profiles.cts:994 comment '3. Else → full (back-compat for fresh non-interactive installs)' and :1003 `return 'full';`. docs/adr/0010-skill-surface-budget-module.md:3 '**Status:** Superseded by [ADR-0011]' and :15 'Default install (no flag) remains

### I1  [WEAKENED] severity=low
  CORRECTED: GSD's 66 ADRs use the Michael Nygard template (Context / Decision / Consequences, 49/61/62 files) rather than MADR. None of MADR's distinctive headings (Context and Problem Statement, Decision Drivers, Considered Options, Decision Outcome, Pros and Cons of the Options) appear anywhere in the corpus, and no ADR has a MADR-style 'Confirmation' / fitness-function section. Alternatives are recorded in 28/66 ADRs under fo
  WHY: MADR half is CONFIRMED verbatim. GSD half is overstated in a way that inverts the finding's implication. (a) MADR's distinctive headings are genuinely absent from GSD: grep -ain -E 'Context and Problem|Considered Options|Decision Outcome|Decision Drivers|Pros and Cons' over all 66 docs/adr/*.md returns ZERO hits. (b) BUT '0/66 use any MADR heading' is literally false: '## Consequences' is a named MADR element and appears in 62/66 GSD ADRs. More importantly, GSD's actual H2 histogram is Consequences 62 / Decision 61 / Context 49 — that is verbatim the Michael Nygard decision-record template (Title, Status, Context, Decision, Consequences), the single most-cited ADR prior art. So 'GSD follows 
  EV: Fetched https://raw.githubusercontent.com/adr/madr/develop/template/adr-template.md AND the 4.0.0 tag. Verbatim: '## Context and Problem Statement', '## Considered Options', '## Decision Outcome' carry NO optional marker; '### Confirmation' is preceded by '<!-- This is an optional element. Feel free to remove. -->' and reads '{Describe how the implementation / compliance of the ADR can/will be confirmed. Is there any

### I2  [WEAKENED] severity=low
  CORRECTED: AWS Prescriptive Guidance — one widely cited ADR process, not an industry consensus — holds that an accepted ADR is immutable and that any change requires a new, separately reviewed ADR. GSD instead declares ADRs 'append-only' with dated in-file amendments (docs/adr/README.md:5), and 21/66 ADRs carry such a section; at least one (ADR-0009) revises the original Decision in place ('...constraints ... were intentionally
  WHY: Both quoted facts are real and I confirmed them verbatim; the framing 'mainstream guidance' is the overreach, and the implied severity does not follow. (a) AWS text CONFIRMED twice on the fetched page: 'When the team accepts an ADR, it becomes immutable. If new insights require a different decision, the team proposes a new ADR. When the team accepts the new ADR, it supersedes the previous ADR.' and, under 'ADR review process': 'The team should treat ADRs as immutable documents after the team accepts or rejects them. Changes to an existing ADR requires creating a new ADR, establishing a review process for the new ADR, and approving the ADR.' (b) docs/adr/README.md:5 CONFIRMED verbatim: 'ADRs 
  EV: Fetched https://docs.aws.amazon.com/prescriptive-guidance/latest/architectural-decision-records/adr-process.html — both immutability sentences quoted above appear verbatim. `sed -n '5p' docs/adr/README.md` -> the append-only sentence, exactly as cited. `grep -ail -E '^#+ *(Amendment|Amendments|Addendum|Addenda|Update)' docs/adr/*.md | grep -v README | wc -l` -> 21. `grep -ail -E '^#+ .*Ratification' docs/adr/*.md | w

### I3  [CONFIRMED] severity=low
  WHY: Both halves hold on direct inspection. Half 1 — 'Rejected' is first-class in all three cited sources plus Nygard: MADR 4.0.0 frontmatter reads status: "{proposed | rejected | accepted | deprecated | … | superseded by ADR-0123}"; Cosmos SDK docs/architecture/PROCESS.md has REJECTED in the consensus state diagram (DRAFT -> PROPOSED -> LAST CALL -> ACCEPTED | REJECTED -> SUPERSEDED) with the gloss 'ADR can go from PROPOSED or ACCEPTED to rejected if the consensus among project stakeholders will decide so'; AWS Prescriptive Guidance: 'The team can also decide to reject the ADR. In this case, the ADR owner adds a reason for the rejection... The owner changes the ADR state to Rejected.' Nygard's S
  EV: MADR 4.0.0 template frontmatter (fetched, verbatim above). https://raw.githubusercontent.com/cosmos/cosmos-sdk/main/docs/architecture/PROCESS.md — full status list with REJECTED quoted. AWS adr-process.html — rejection paragraph quoted. `grep -n STATUSES scripts/gen-adr-index.cjs` -> line 55 (declaration) and line 287 (enforcement); `grep -rn 'Rejected\b' scripts/gen-adr-index.cjs docs/adr/README.md` -> 0 hits. src/a

### I4  [REFUTED] severity=none
  CORRECTED: GSD's Subsumes/Subsumed-by relation is NOT novel — it reproduces Kruchten (2004) §5.4 'Subsumes' with essentially the same definition, and Kruchten's relation set also contains 'Comprises (is made of, decomposes into)'. Dated, evidence-carrying status promotion likewise has prior art (Kruchten §4.4 state machine + §4.5 timestamped state history; MADR's optional 'Confirmation' fitness-function element). What is plausi
  WHY: The 'Subsumes' half is flatly wrong, and the 'ratification' half has close, citable prior art. SUBSUMES: Philippe Kruchten, 'An Ontology of Architectural Design Decisions in Software-Intensive Systems' (2004) — the foundational ADR-relations paper — §5.4 is literally titled 'Subsumes' and defines it as 'A is a design decision that is wider, more encompassing than B. Examples: "All subsystems are coded in Java" subsumes "Subsystem XYZ is coded in Java".' That is GSD's definition almost word for word: scripts/gen-adr-index.cjs comments 'subsumes — the target decision still HOLDS, but a broader ADR now frames it', and docs/adr/README.md:73 'the target still holds, but a broader ADR now frames i
  EV: Fetched https://philippe.kruchten.com/wp-content/uploads/2009/07/kruchten-2004-design-decisions.pdf, extracted text with PyMuPDF (WebFetch could not read the binary), grepped and read §4.4 State (lines 254-300 of extract) and §5.1-5.9 relations (lines 400-525). Verbatim §5.4 quoted above. MADR 4.0.0 Confirmation element fetched. AWS adr-process.html conformance paragraph read. GSD side: scripts/gen-adr-index.cjs RELA

### I5  [CONFIRMED] severity=none
  WHY: Both halves verified on the primary source and independently corroborated. Governance: agents.md states 'AGENTS.md is now stewarded by the Agentic AI Foundation under the Linux Foundation', with the format's origin credited to collaboration among OpenAI Codex, Amp, Jules (Google), Cursor and Factory. I did not take the fetch on trust — the Linux Foundation's own press release ('Linux Foundation Announces the Formation of the Agentic AI Foundation (AAIF), Anchored by New Project Contributions Including Model Context Protocol (MCP), goose and AGENTS.md') and OpenAI's 'OpenAI co-founds the Agentic AI Foundation under the Linux Foundation' independently confirm AGENTS.md is an AAIF anchor projec
  EV: Fetched https://agents.md/ — stewardship sentence, root-placement requirement, and the nearest-file-wins sentence all returned as direct quotes. Cross-checked with a web search returning linuxfoundation.org/press/linux-foundation-announces-the-formation-of-the-agentic-ai-foundation and openai.com/index/agentic-ai-foundation/, both confirming AAIF formation under the Linux Foundation with AGENTS.md as an anchor projec

### J1  [WEAKENED] severity=medium
  CORRECTED: Anthropic's Agent Skills best practices (500-line SKILL.md body, references one level deep, TOC for reference files >100 lines) are correctly stated, and all seven line counts are exact. But GSD's SKILL.md bodies are `commands/gsd/*.md` — 71 files, largest 204 lines (graphify.md), all far under 500. The four cited `agents/*.md` files are subagent system prompts loaded into a fresh subagent context; no command file @-
  WHY: GUIDANCE — CONFIRMED. Verified against a local mirror of Anthropic's Agent Skills best-practices doc at /home/dave/.claude/plugins/cache/claude-plugins-official/superpowers/5.1.0/skills/writing-skills/anthropic-best-practices.md: line 241 'Keep SKILL.md body under 500 lines for optimal performance'; line 357 '**Keep references one level deep from SKILL.md**. All reference files should link directly from SKILL.md to ensure Claude reads complete files when needed.'; line 385 'For reference files longer than 100 lines, include a table of contents at the top.' (Note Anthropic's own skill-creator plugin says '>300 lines' for the TOC threshold — the vendor's materials disagree with themselves on t
  EV: wc -l on all 7 cited files (exact match); wc -l commands/gsd/*.md (max 204); grep -rn 'agents/' commands/gsd/*.md (no hits); cat commands/gsd/execute-phase.md (65 lines, <execution_context> @-include of the 1679-line workflow); grep for @~ includes inside gsd-core/references/*.md (9 edges, 8 distinct targets, listed above); anthropic-best-practices.md lines 241/357/385; docs/ARCHITECTURE.md lines 178, 201-204 (eager 

### J2  [WEAKENED] severity=low
  CORRECTED: Every factual component is true: Anthropic's guidance says the description should state what the Skill does AND when to use it, with a 1024-char maximum; GSD hard-caps at 100 chars (scripts/lint-descriptions.cjs:20); and only 8 of 71 descriptions contain any when/before/after/trigger word (mean length 73.2 chars). But the implied remedy — lengthen descriptions to carry trigger terms — is already refuted inside the re
  WHY: GUIDANCE — CONFIRMED on both halves. anthropic-best-practices.md:150 '`description` - One-line description of what the Skill does and when to use it (1024 characters maximum)'; :187 'The `description` field enables Skill discovery and should include both what the Skill does and when to use it.'; :197 '**Be specific and include key terms**. Include both what the Skill does and specific triggers/contexts for when to use it.'; :1095 'requires `name` (64 characters max) and `description` (1024 characters max)'. GSD CAP — CONFIRMED at the exact cited line. `grep -n 'MAX_LENGTH = 100' scripts/lint-descriptions.cjs` → `scripts/lint-descriptions.cjs:20:const MAX_LENGTH = 100;`. Corroborated by docs/
  EV: scripts/lint-descriptions.cjs:20 `const MAX_LENGTH = 100;` (grep -n, exact); anthropic-best-practices.md lines 150, 187, 197, 1095, 1108; extracted and measured all 71 commands/gsd/*.md description fields (n=71, mean=73.2, max 99); listed the 8 with trigger words; docs/adr/0010-skill-surface-budget-module.md:8 and :147; docs/research/2026-05-12-skill-surface-budget.md:23,34,37; docs/FEATURES.md:1871; docs/COMMANDS.md

### J3  [CONFIRMED] severity=low
  CORRECTED: Accurate as stated. Adding the caveat the claim itself asks for: the catalog is a defaults table, not a validated enum, and claude-opus-4-8 remains an active non-deprecated model — so this is Opus-tier defaults lagging one generation, not a broken reference.
  WHY: FILE CONTENTS — CONFIRMED at every cited line. `claude-opus-4-8` appears at gsd-core/bin/shared/model-catalog.json lines 11 (runtimeTierDefaults.claude.opus), 26 (opencode, as `anthropic/claude-opus-4-8`), 31 (copilot), 36 (hermes, prefixed), 41 (kilo, prefixed), 96 (pi), and again inside providerPresets at 103 and 108. Lines 103-104 are the `anthropic` preset rows and 107-109 the `anthropic-fable` rows, matching the cited 103-108 range. COMPLETE MODEL-ID INVENTORY — I enumerated every `model` value programmatically rather than trusting a grep: anthropic/claude-haiku-4-5, anthropic/claude-opus-4-8, anthropic/claude-sonnet-5, claude-fable-5, claude-haiku-4-5, claude-opus-4-5, claude-opus-4-8,
  EV: grep -n on gsd-core/bin/shared/model-catalog.json confirming claude-opus-4-8 at lines 11, 26, 31, 36, 41, 96 and providerPresets at 103/108; node one-liner walking the parsed JSON to emit every distinct `model` value (17 ids, listed above, no claude-opus-5); head -130 of the raw file to read runtimeTierDefaults and providerPresets structure; bundled claude-api skill Current Models table (claude-opus-5 current; claude

### J4  [CONFIRMED] severity=low
  CORRECTED: Both sub-claims hold: ADR-1610 states the 32,768-byte Codex cap with no citation, and the underlying fact is correct. Severity is low because (a) the assertion is true, so the missing citation misleads no one, (b) 33 of the repo's 67 ADRs contain no external link at all, making uncited external anchors the local norm rather than an outlier, and (c) the ADR explicitly hedges its reliance: 'We adopt the **unit**, not t
  WHY: ADR SAYS IT UNCITED — CONFIRMED. docs/adr/1610-workflow-agent-size-budget-ratchet.md:54 reads '...they are also the unit vendors bound on (Codex caps instruction docs at 32,768 bytes, `project_doc_max_bytes`, and truncates past it). We adopt the **unit**, not the exact number.' Line 133 reads '- External anchors: Codex `project_doc_max_bytes` (32 KiB); Anthropic "effective context engineering for AI agents"...'. `grep -n http` on the file returns zero matches — no URL, no doc version, no retrieval date, for either external anchor. UNDERLYING FACT — CONFIRMED against a primary source, not a blog. I fetched OpenAI's own Codex documentation (developers.openai.com/codex/guides/agents-md, which 3
  EV: docs/adr/1610-workflow-agent-size-budget-ratchet.md lines 27, 54, 77, 133 read directly; grep -n http on that file → 0 matches; grep -rl http docs/adr/*.md → 34 of 67 files; WebFetch of https://learn.chatgpt.com/docs/agent-configuration/agents-md (via 308 from developers.openai.com/codex/guides/agents-md) returning the verbatim project_doc_max_bytes sentence and the combined-chain scope; WebSearch corroboration; test


=== TALLY === {"CONFIRMED":17,"WEAKENED":19,"REFUTED":4} total 40
