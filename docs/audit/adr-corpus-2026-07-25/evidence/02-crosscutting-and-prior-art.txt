
==============================================================================
## AGENT: a840767fb7a25fa71
==============================================================================

[high] 1. Agent completion detection: side-effect spot-check is authoritative, markers are advisory
   DECISION: GSD decided that an orchestrator determines subagent completion from filesystem/git side effects (SUMMARY.md exists, commits visible), NOT from the agent's textual completion marker — and that a missing marker must never block. The rejected alternative is the obvious one: make the `## X COMPLETE` H2 marker the normative return contract that orchestrators regex-match and that agents are linted to emit. The corpus inst…
   EVIDENCE: gsd-core/references/agent-contracts.md:5 — "This doc describes what IS, not what should be. Casing inconsistencies are documented as they appear in agent source files." | agent-contracts.md:79 — "`## PLAN COMPLETE` is the gsd-executor's completion marker but execute-phase.md does not regex-match it. Instead, it detects executor completion via spot-checks (SUMMARY.md existence, …
   WHY ADR: This is the single most consequential contract in the product and it is documented as a descriptive snapshot rather than a decision. A contributor adding an agent reads the registry, sees `## PLANNING COMPLETE` listed as "the standard convention" (agent-contracts.md:38), and reasonably concludes the orchestrator matches it — then wires an…
   OVERLAP: NONE. Zero hits across all 66 ADRs for "completion marker", "PLANNING COMPLETE", or "return contract". ADR-1866 (agent_skills dual injection) governs what goes INTO an agent prompt, never what comes out.

[high] 2. @-include path convention: three coexisting forms, Claude-anchored source, install-time rewrite
   DECISION: Prompt-layer files are authored with paths hardcoded to the Claude home (`@~/.claude/gsd-core/...`) even though GSD ships to 15+ runtimes, and the installer rewrites the anchor per runtime. Three incompatible reference forms coexist with no stated rule for choosing one. The rejected alternative is a runtime-neutral token (e.g. `@$GSD_HOME/...` or `@gsd-core/...` everywhere) resolved at read time instead of rewritten …
   EVIDENCE: Form counts across gsd-core/workflows + agents + commands + skills: `@~/.claude/gsd-core/references` 226 occurrences, `@~/.claude/gsd-core/workflows` 163, `@~/.claude/gsd-core/templates` 20, `@~/.claude/agents` 1 — 411 total anchored-form hits. | Second form: bare `@gsd-core/references/...` — 27 occurrences across 26 files (gsd-core/workflows/execute-phase.md:18, verify-work.md…
   WHY ADR: An author who writes `@~/.gemini/gsd-core/...` (seemingly more correct for a Gemini fix), or who prettifies an anchored path across a line break, or who moves a prose @-ref into `<execution_context>` thinking it was already loading, silently breaks context loading on some or all runtimes — and the failure is a quiet degradation (missing r…
   OVERLAP: ADR-1610 (docs/adr/1610-workflow-agent-size-budget-ratchet.md:80-87) is the ONLY ADR that mentions `@~/` at all, and only to assert that "`@~/.claude/gsd-core/references/...` imports are loaded **eagerly**" as an anti-gaming rule for the byte budget. Residual …

[high] 3. Gates taxonomy is the canonical vocabulary for workflow control flow but reaches 3 of 91 workflows
   DECISION: GSD decided every validation checkpoint in every workflow maps to exactly one of four gate types (Pre-flight / Revision / Escalation / Abort), with fixed semantics for blocking, looping, escalating, and terminating — and then chose to distribute that vocabulary by voluntary @-include rather than by a lint, a schema, or an engine-side gate primitive. The rejected alternative is enforcing gate identity structurally (a …
   EVIDENCE: gsd-core/references/gates.md:3 — "Canonical gate types used across GSD workflows. Every validation checkpoint maps to one of these four types." Four types defined at :9, :18, :26, :35, with a Gate Matrix at :46-57 naming specific workflow/step pairs (plan-phase Step 12 = Revision, max 3; next Entry = Abort). | Reach: `grep -l 'references/gates.md' gsd-core/workflows/*.md` = 3 o…
   WHY ADR: The gate matrix at gates.md:50-57 is a hardcoded snapshot of eight specific workflow steps. When a workflow's gate moves or a new workflow adds one, nothing detects the matrix going stale, and the 88 workflows that never load the reference have no shared vocabulary to be consistent with — so new gates get invented ad hoc (halt? warn? retr…
   OVERLAP: NONE for the taxonomy. ADR-2008 (2008-command-exit-zero-gate.md) defines a *generic gate-predicate evaluator* (`command-exit-zero`) — an engine-side predicate primitive, which is a different object from the four-type control-flow vocabulary; it does not refere…

[high] 4. gsd_run shim resolution precedence: project-local payload wins over the installed global
   DECISION: Every workflow's bash preamble resolves the CLI engine through a 20-branch precedence chain that tries the *project-local* `gsd-core/bin/gsd-tools.cjs` first, then project `.claude/` and `.codex/`, then `gsd-tools` on PATH, then each runtime's global config home in a fixed order. The rejected alternative is global-first (the installed, version-known payload wins) or version-pinned resolution. Project-first means a st…
   EVIDENCE: gsd-core/workflows/_runtime-launcher.snippet.sh:1 — single-line chain: `_GSD_RUNTIME_ROOT="${RUNTIME_DIR:-$(git rev-parse --show-toplevel …)}"; GSD_TOOLS="${_GSD_RUNTIME_ROOT}/gsd-core/bin/gsd-tools.cjs"; if [ -f "$GSD_TOOLS" ] … elif [ -f "${_GSD_RUNTIME_ROOT}/.claude/gsd-core/bin/…" ] … elif command -v gsd-tools … elif [ -f "${CLAUDE_CONFIG_DIR:-$HOME/.claude}/…" ] … elif HER…
   WHY ADR: This chain is the entry point for 81 of 91 workflows and it is documented nowhere — the CLAUDE.md section reserved for it is literally empty. The project-first ordering means a developer with a checked-in or leftover `gsd-core/` in their repo runs that engine, not the one they just upgraded — a debugging trap that costs hours because the …
   OVERLAP: NONE for the precedence order. Seven ADRs mention `gsd_run` (1990, 415, 1671, 1143, 2008, 959, 894) but only as a caller; `grep -i "resolution order" docs/adr/*.md` returns no hit on shim resolution. ADR-3660 (runtime-artifact-layout-module) governs where the …

[high] 5. Context-budget policy: a mandatory orchestrator discipline that 1 of 91 workflows actually loads
   DECISION: GSD decided orchestrator context discipline is a *prompt-delivered* policy — a reference document stating hard rules (never read agents/*.md, never inline files into subagent prompts, read-depth branches on a `context_window` config value, four degradation tiers with per-tier behavior) — rather than an engine-enforced budget or a per-workflow contract. The rejected alternative is enforcing it where it is checkable: i…
   EVIDENCE: gsd-core/references/context-budget.md:11 — "Every workflow that spawns agents or reads significant content must follow these rules:" followed by 5 numbered universal rules (:13-19), a read-depth table keyed on `context_window` (:23-28), and 4 degradation tiers PEAK/GOOD/DEGRADING/POOR with trigger actions (:34-39). | Actual reach: `grep -l 'references/context-budget.md' gsd-cor…
   WHY ADR: A document that opens "Every workflow ... must follow these rules" while being loaded by one workflow is a false contract. A contributor writing a new agent-spawning workflow gets none of it — so they inline a file into a subagent prompt or read a full SUMMARY.md, exactly the failures rules 2 and 3 exist to prevent, and the orchestrator d…
   OVERLAP: PARTIAL / adjacent only. ADR-0011 (0011-skill-surface-budget-module.md) and ADR-1213 are the two ADRs matching "context budget", but both govern *install-time skill listing* token cost (what the host injects into the system prompt), a different budget from *ru…

[high] 6. Degradation when the host has no usable subagent tool: capability-probed, per-runtime, and asymmetric
   DECISION: GSD decided subagent absence is handled by *runtime-capability probing at prompt-execution time* with three distinct behaviors — (a) probe actual tool availability, explicitly not runtime name; (b) a named per-runtime carve-out where the tool exists but its completion signal is untrustworthy (Copilot defaults to inline); (c) an asymmetric authorization rule that inline execution is FORBIDDEN when Agent is available. …
   EVIDENCE: gsd-core/workflows/execute-phase.md:20-31 (<runtime_compatibility> block) — "**Copilot:** Subagent spawning does not reliably return completion signals. **Default to sequential inline execution**: read and follow execute-plan.md directly for each plan instead of spawning parallel agents. Only attempt parallel spawning if the user explicitly requests it"; and "**Other runtimes:*…
   WHY ADR: Multi-runtime support is the product's differentiator and this is the rule that keeps it honest. It currently exists as prose in one workflow. A contributor adding runtime #16, or fixing a hang on an existing one, has no written policy — so they branch on runtime name (explicitly forbidden at execute-phase.md:31), or apply the Copilot inl…
   OVERLAP: PARTIAL. ADR-1866 (agent-skills-dual-injection-contract) covers persona delivery when orchestrator-side injection can't run, and gsd-core/references/agent-skills-bootstrap.md:3-10 covers the Cursor case where Skill() delegation doesn't execute bash. ADR-1239 (…

[medium] 7. Bounded revision loop: cap of 3, stall detection, and escalate-never-fail
   DECISION: GSD decided the producer→checker→revise loop terminates on three independent conditions — a hard cap of 3 iterations, an early break when the issue count fails to *decrease* between consecutive iterations, and an INFO-severity floor that never triggers revision — and that exhaustion escalates to a human yes/no gate offering "Proceed anyway" rather than failing the workflow. Also decided: each iteration is a fresh age…
   EVIDENCE: gsd-core/references/revision-loop.md:7 — "## Pattern: Check-Revise-Escalate (max 3 iterations)". Flow pseudocode :17-35 including `if iteration > 3: Escalate` and `if issue_count >= prev_issue_count: Escalate to user: "Revision loop stalled (issue count not decreasing)"`. Exhaustion behavior :68-80 — gate offering "Proceed anyway" / "Adjust approach", and "If 'Proceed anyway': …
   WHY ADR: The cap of 3 and the fail-open-to-human escalation are the load-bearing safety property of every quality loop GSD runs (plan-phase, execute-phase, discuss-phase per revision-loop.md:86-88): they are what prevents an unbounded agent-vs-agent spend loop while also preventing a checker from permanently blocking a user. Both numbers are magic…
   OVERLAP: NONE. Zero ADR hits for "revision loop", "max 3", or "iteration cap" across all 66 ADRs. ADR-22 (plan-drift-guard) and ADR-550 (spec-phase probe contract) govern adjacent plan-quality machinery but neither addresses loop bounding or the escalation semantics.

[medium] 8. Progressive disclosure: lazy step-file extraction is mandated, but its path form and resolution are unspecified
   DECISION: GSD decided oversized workflows are decomposed into subdirectory step/mode files that are `Read` on demand at the step that needs them — explicitly NOT @-imported, because @-imports are eager and would defeat the purpose. But it also decided these lazy files are addressed by *bare, unanchored relative paths*, unlike every other cross-file reference in the layer. The rejected alternative is the anchored `@~/.claude/gs…
   EVIDENCE: Structure: `find gsd-core/workflows -mindepth 1 -type d` = discuss-phase/{modes:9, templates:2}, execute-phase/steps:5, plan-phase/steps:3, help/modes:4. | Bare-path addressing: gsd-core/workflows/execute-phase.md:558 "Read and execute `gsd-core/workflows/execute-phase/steps/per-plan-worktree-gate.md`", :884 "Read and execute `gsd-core/workflows/execute-phase/steps/post-merge-g…
   WHY ADR: A path form that is neither anchored nor rewritten is a live portability hazard: the reader is an LLM in a project cwd being told to Read `gsd-core/workflows/execute-phase/steps/regression-gate.md`, a path that exists under the runtime home, not the project. It works today by inference, which is exactly the kind of thing that stops workin…
   OVERLAP: ADR-1610 half-covers it: it names the pattern and mandates laziness as the anti-Goodhart rule for the byte budget, and cites discuss-phase/modes/ as the exemplar. Residual gap: ADR-1610 says nothing about (a) which path form a lazy reference must use, (b) how …

[medium] 9. Reference / template / workflow-step layering: three prompt-fragment kinds with no placement rule
   DECISION: GSD decided prompt content is split across three distinct kinds — 98 `references/*.md` (eagerly @-included guidance), 45 `templates/*.md` (artifact skeletons), and per-workflow `steps/`+`modes/` files (lazily Read) — plus a `contexts/` dir, with the choice of kind determining eager-vs-lazy loading and therefore per-invocation token cost. No rule states which kind new content belongs in. The rejected alternative is a …
   EVIDENCE: Inventory: gsd-core/references = 98 .md files (plus edge-probe-fixtures/, few-shot-examples/, prohibition-probe-fixtures/ subdirs); gsd-core/templates = 45; gsd-core/workflows = 114 .md across root + 9 subdirs; gsd-core/contexts/ exists as a fourth bucket. | Kind determines cost: references are reached by `@~/.claude/gsd-core/references/...` — 226 occurrences, eager per docs/ad…
   WHY ADR: Placement silently determines whether content costs tokens on every invocation of every consumer or only at the step that needs it — the exact cost dimension ADR-1610 was written to ratchet. With 98 references and no rule, the cheapest move for a contributor adding guidance is a new references/*.md plus an @-include, which is the eager pa…
   OVERLAP: PARTIAL. ADR-1610 governs per-file byte budgets and eager-vs-lazy extraction but treats the existing directory taxonomy as given. ADR-0010/0011 (skill-surface-budget-module) govern the install-time *skill* surface, not the reference/template split. Residual ga…

[medium] 10. The prompt layer is the larger half of the codebase and holds ~1/6 of the ADR corpus
   DECISION: The ADR process was scoped, in practice, to Node engine module seams. The dominant ADR shape is literally "<X> Module owns <Y>" — a code-seam ownership statement. Markdown-as-code, which is where product behaviour actually lives, was left to a reference-doc convention with no decision record. The rejected alternative is treating a workflow/agent/reference contract as an ADR-worthy architectural surface on equal footi…
   EVIDENCE: Corpus split by cited artifact: 40 of 66 ADRs cite an engine path (`src/*.cts` or `bin/lib/*.cjs`); 23 of 66 cite any prompt-layer markdown path (gsd-core/{workflows,references,templates}/, agents/gsd-*.md, commands/gsd/*.md). Of those 23, only 9 cite prompt-layer paths more than once, and only 3 do so substantively — 15-autonomous-cross-ai-convergence.md (8 hits), 3660-runtime…
   WHY ADR: This is the root cause of the other candidates. Because "architecture" was operationally defined as "a .cts module seam", every markdown contract — return protocol, gates, revision bounds, path convention, degradation ladder — landed in a reference doc that describes rather than decides, that no lint guards, and that most consumers never …
   OVERLAP: NONE — this is a meta-gap about corpus scope. docs/adr/README.md:7 ("Reading this corpus") and :44 ("Lifecycle rules") govern ADR status vocabulary, naming, and supersession, but never say what kinds of artifact warrant an ADR.

NOTES: SCOPE + METHOD. Slice audited: gsd-core/workflows (114 .md incl. 9 subdirs), agents/ (34), commands/gsd/ (71), gsd-core/references/ (98), gsd-core/templates/ (45), skills/ (71). Absence was checked against all 66 ADRs (67 files minus README.md) by keyword grep before every "NONE" claim; the exact grep is quoted in each candidate's evidence field. HEADLINE COUNTS (reproducible): - ADRs citing an engine path (src/*.cts or bin/lib/*.cjs): 40/66. - ADRs citing any prompt-layer markdown path: 23/66; only ~10 have the prompt layer as their subject. - Prompt layer: 433 .md files / 3.7 MB. Engine src/: 168 .cts / 3.3 MB. The larger half carries ~1/6 of the records. - Zero-hit greps across the whole …

==============================================================================
## AGENT: a3fa415f82e9e793a
==============================================================================

[high] 1. Command dispatch precedence: third-party overlay capabilities outrank core host commands
   DECISION: Dispatch is a THREE-layer ladder — first-party capability registry → installed THIRD-PARTY overlay capability → core host router table — so a consented third-party capability that declares `commands: [{family:"state"}]` shadows the core `state` router. The rejected alternative is the obvious one: put core host commands FIRST (or maintain a HOST_RESERVED_COMMANDS deny-list checked at load/build time), so first-party c…
   EVIDENCE: gsd-core/bin/gsd-tools.cjs:2641 (dispatchCapabilityCommand) → :2646 (dispatchOverlayCapabilityCommand) → :2652 (dispatchHostCommand) — third-party runs BEFORE core. HOST_COMMAND_ROUTERS contains `state`, `phase`, `check`, `commit`, `verification`, `milestone`, `progress` etc. at gsd-core/bin/gsd-tools.cjs:2100-2156. The overlay admission gate only rejects collisions against FIR…
   WHY ADR: ADR-959:112 explicitly REJECTED a `HOST_RESERVED_COMMANDS` gate, and its stated reason was "the `default`-case placement makes shadowing harmless at runtime — the hardcoded `case` always wins." ADR-2346 then dissolved that switch entirely (§1 'two-layer dispatch'), voiding the premise, without revisiting the rejection. So the corpus now r…
   OVERLAP: ADR-959 (959-capability-command-contribution.md:112,119) — rejects the reserved list on a now-void premise. ADR-1244 (1244-capability-ecosystem.md:75,128) — 'first-party always wins' but scoped to ids / skill stems / agent stems / config keys, NOT command fami…

[high] 2. Process-termination contract and exit-code taxonomy
   DECISION: The engine ships TWO independent termination mechanisms with opposite semantics: `io.error()` performs a hard `process.exit(1)` (immediate, event loop torn down), while `ExitError` + `runMain()` set `process.exitCode` and let the loop drain. Which one a call site uses is by convention only. The rejected alternative — a single mechanism (all non-zero exits throw ExitError; `error()` throws instead of exiting) — is wha…
   EVIDENCE: src/io.cts:211-219 — `function error(...): never { ... process.exit(1); }`. src/cli-exit.cts:11-56 — `ExitError` + `runMain()` which maps to `process.exitCode` precisely so "output flushes and process.on('exit') cleanup still fires". src/capability-writer.cts:29 asserts the contrary: "ExitError (NOT process.exit) is how every gsd-tools command signals a non-zero exit". Conseque…
   WHY ADR: A contributor adding a new verb has no way to know which mechanism to use, and the one inline comment that addresses it (capability-writer.cts:29) is wrong. The failure mode is silent output truncation, not a crash. Downstream workflow bash shims already branch on numeric exit codes (`-eq 124`), so the taxonomy is a real consumed contract…
   OVERLAP: ADR-0012 (0012-command-routing-hub.md:24) states the *hub* never calls process.exit — scoped to the hub only, and the hub is not on the CLI termination path. ADR-0174 mentions process.exit in the SDK-retirement context. No ADR mentions `ExitError`, `runMain`, …

[high] 3. Config layering: global defaults are an exclusive fallback, not a precedence layer
   DECISION: `~/.gsd/defaults.json` is consulted ONLY when the project has no `.planning/` directory at all — it is an either/or branch, not a low-precedence layer merged under project config. And the keys it is allowed to carry are a hand-maintained field-by-field enumeration rather than a merge. The rejected alternative is a uniform cascade (global → root → workstream, deep-merged at every level), which is exactly what the root…
   EVIDENCE: src/config-loader.cts:687 — the global branch is reachable only inside `if (fs.existsSync(planningDir(cwd, ws)))`'s ELSE path; :702-742 builds `_globalBaseCfg` by naming ~22 keys one at a time (`model_profile:`, `commit_docs:`, `research:` …). Contrast src/config-loader.cts:540-542 where workstream config IS deep-merged over root: `rootParsed ? (_deepMergeConfig(rootParsed, fil…
   WHY ADR: Every user-visible global setting added after this code was written silently no-ops from `~/.gsd/defaults.json` until someone hand-adds a line — #2069 is the proof this already shipped broken once, and nothing structurally prevents the next one. A contributor adding a config key has no signal that a second, non-obvious edit is required. I…
   OVERLAP: ADR-1411 (1411-resolution-provenance.md) — names `'global-defaults'` as one of four `ConfigResolution.source` values and mandates that fallback be *visible*, but never states the layering RULE (exclusive vs cascading) nor the allow-list. It also only got one p…

[high] 4. Programmatic re-entry is subprocess-only; the in-process Hub is a throwaway and its observability is dormant
   DECISION: Every embedding surface (MCP server, VS Code/pi host integration, the embeddable-engine path) re-enters GSD by SPAWNING `gsd-tools.cjs` with `--raw --json-errors` rather than dispatching in-process, because no fully-populated hub exists. Correspondingly, every `createHub()` call site constructs a fresh SINGLE-FAMILY hub with no injected logger, so the Hub's observability seam (DispatchEvent, traceId/parentTraceId cor…
   EVIDENCE: src/shell-command-projection.cts:587-607 states it outright: "No fully-populated in-process command-routing hub exists anywhere in the tree — every `createHub()` caller … builds a single-family hub for its own narrow purpose. The ONLY dispatch path that covers the FULL family/subcommand surface is the gsd-tools.cjs CLI itself." Same claim at pi/gsd.cjs:33 and src/mcp-server.cts…
   WHY ADR: Three consequences that a contributor cannot infer: (1) the entire `src/observability/` tree (event.cts, logger.cts, redaction.cts) plus the documented `GSD_AUDIT` / `GSD_AUDIT_ARGS` env contract is dead code in the shipped CLI; (2) any in-process embedding pays a Node process spawn per command; (3) the parentTraceId correlation designed …
   OVERLAP: ADR-1239 (1239-gsd-embeddable-orchestration-engine.md:194,206) leaves this as an explicitly OPEN question ("Decide whether the imperative plugin invokes the engine in-process … or shells out to a Node gsd-tools.cjs") — the code has since decided it, and the AD…

[medium] 5. Clock and determinism seam: opt-in, singleton, env-pinned, and split UTC/local
   DECISION: Determinism is delivered by an opt-in singleton `realClock` module plus an in-process env-var time pin (`GSD_NOW_MS`, honored ONLY when `GSD_TEST_MODE` is set), and modules may still call `Date.now()` / `new Date()` directly — there is no ban. Separately, the clock deliberately exposes TWO calendar days: `today()` (UTC) for internal stamps and `localToday()` (host-local) for operator-facing date fields. Rejected alte…
   EVIDENCE: src/clock.cts:38-56 `_pinnedNowMs()` — `if (!process.env.GSD_TEST_MODE) return null;` then strict-integer parse of `GSD_NOW_MS`. src/clock.cts:92-120 — `today()` returns `this.nowIso().split('T')[0]` (UTC) while `localToday()` builds from `getMonth()/getDate()/getFullYear()`, with the rationale in-comment: operator-facing fields "must never name a day ahead of `last_updated`'s …
   WHY ADR: Two distinct traps. (1) A contributor writing a new date-stamping verb has no rule telling them to use the seam, and no lint catches it — so `.planning/` artifacts acquire non-pinnable timestamps and golden/parity tests fracture. (2) The UTC-vs-local split is invisible: picking `today()` for an operator-facing field produces a date one da…
   OVERLAP: ADR-456 (456-test-rigor-architecture.md:29-54) covers the clock seam ONLY as a test-rigor pattern for concurrency (`{ clock = Date } = {}` parameter injection backed by `mock.timers`) — a different shape from the shipped singleton-plus-env-pin, and silent on p…

[medium] 6. Two parallel closed error enums with a lossy one-way bridge
   DECISION: The engine maintains two independent frozen error taxonomies — `ERROR_REASON` (17 snake_case wire codes, the `--json-errors` payload) and `ERROR_KINDS` (4 Hub Result kinds) — and bridges them only for the `InvalidArgs` variant via an optional `exitReason` field bolted on by amendment #1642. The other three kinds are translated by calling `error(message)` with NO reason, so they all surface on the wire as `reason: 'un…
   EVIDENCE: src/io.cts:163-187 (ERROR_REASON, 17 values, default `UNKNOWN` at :211) vs src/command-routing-hub.cts:50-59 (ERROR_KINDS, 4 values). The bridge: src/command-routing-hub.cts:79-87 `exitReason?: string` on InvalidArgs only, and src/cjs-command-router-adapter.cts:157-169 which forwards it. The loss: :148-155 UnknownCommand is hard-coded to `ERROR_REASON.SDK_UNKNOWN_COMMAND`; :171…
   WHY ADR: CONTRIBUTING mandates typed-reason assertions over raw text matching (src/io.cts:154-157 cites #2974), but the Hub path structurally cannot produce a typed reason for 3 of 4 error kinds — so tests for those paths must either assert `'unknown'` (vacuous) or fall back to text matching (banned). A contributor adding a code has two enums to c…
   OVERLAP: ADR-0012 (:27,:37) closes the ERROR_KINDS enum and requires an ADR amendment to extend it — but still lists the six pre-#175 kinds including the removed SdkLoadFailed/SdkDispatchFailed. ADR-0174 amendment #1642 (0174-…:3,85) reconciles the Result type and reco…

[medium] 7. stdout contract: the >50KB `@file:` indirection, its global fs.writeSync monkeypatch, and the `--raw` carve-out
   DECISION: JSON results larger than 50,000 characters are spilled to a temp file and stdout receives `@file:<path>` instead; the CLI then transparently resolves that by MONKEYPATCHING the process-global `fs.writeSync` for the duration of every command, buffering fd 1 in a string. `--raw` output is exempt — it short-circuits before the size check and streams unbounded. Rejected alternatives: an injected writer/sink passed to han…
   EVIDENCE: src/io.cts:127-151 — the raw branch (`if (raw && rawValue !== undefined) data = String(rawValue)`) returns before the `if (json.length > 50000)` spill at :136, so a large `--raw` payload bypasses the mechanism entirely. gsd-core/bin/gsd-tools.cjs:2561-2601 `captureStdoutSyncWrites` — `fs.writeSync = function patchedWriteSync(fd, data, ...rest)`, restored in both promise arms; :…
   WHY ADR: The 50KB number is a host-specific constant (Claude Code's Bash tool buffer) baked into a cross-runtime engine that ships to 15+ hosts with different limits, and it is a magic literal with no named constant. The monkeypatch means any library the engine loads that writes to fd 1 is silently captured and re-emitted, and it is fundamentally …
   OVERLAP: ADR-1671 (1671-dynamic-context-management-platform.md) is the only ADR mentioning `@file:`, and does so as an assumed-existing facility rather than deciding it. `grep -ril "captureStdout|monkey" docs/adr/` returns nothing. Residual gap: the whole stdout contra…

[medium] 8. `--pick` fails open: missing field and unparseable output are indistinguishable from success
   DECISION: When `--pick <field>` resolves to null/undefined the CLI prints an EMPTY STRING and exits 0; when the captured output is not valid JSON it silently prints the raw capture instead. Rejected alternative: a non-zero exit (or a distinguishable sentinel) for an absent field, matching the `--default` flag's explicit opt-in-to-a-default design.
   EVIDENCE: gsd-core/bin/gsd-tools.cjs:2534-2548 — `const value = extractField(obj, pickField); const result = value === null || value === undefined ? '' : String(value); fs.writeSync(1, result);` inside a `try`, with `catch { fs.writeSync(1, captured); }`. `extractField` (:2612-2629) returns `undefined` for any missing segment. Contrast :2418-2427 where `--default <value>` exists specific…
   WHY ADR: `--pick` is the primary read path workflow bash shims use (`X=$(gsd-tools … --pick foo)`), and this is precisely ADR-1411's named bug class — a configured input resolving empty with no signal, consumed downstream by a planner/verifier that then works without its context. ADR-1411 P3 was narrowed away from `output()` explicitly, so the rul…
   OVERLAP: ADR-1411 (1411-resolution-provenance.md, Decision rule 3 'Visible degradation'; amendment 2026-06-18) — establishes the principle, then narrows P3 away from a shared `output()` envelope, leaving `--pick` uncovered. `grep` of docs/adr/ finds no ADR discussing t…

[medium] 9. Capability routers must be synchronous; host routers may be async
   DECISION: A capability command router (first-party AND third-party) that returns a Promise is a hard error — async capability routers are unsupported — while host routers in HOST_COMMAND_ROUTERS are `await`ed. Rejected alternative: award both layers the same async contract (which the host layer already proves is implementable).
   EVIDENCE: gsd-core/bin/gsd-tools.cjs:446-451 (first-party) and :560-565 (third-party overlay), both: `if (_result && typeof _result.then === 'function') error(… 'must be synchronous (returned a Promise); async capability routers are not supported.', ERROR_REASON.SDK_FAIL_FAST)`. Versus :2175-2177 — "`await` so async host routers (e.g. capability's install/upgrade ops) complete before run…
   WHY ADR: A third-party capability author writing a command that does any network or npm work hits a runtime error with no design rationale available anywhere, and the natural workaround (sync `execSync`/`spawnSync` from inside a third-party router) is strictly worse for the trust model than an awaited promise. The asymmetry also blocks migrating a…
   OVERLAP: NONE — `grep -n -i "async|synchronous|promise" docs/adr/959-capability-command-contribution.md docs/adr/1244-capability-ecosystem.md docs/adr/2346-command-dispatch-completion.md` returns no relevant hit. ADR-1239's negotiated-axis model is the nearest conceptu…

[low] 10. `SKIP_ROOT_RESOLUTION` — a hand-maintained carve-out from deterministic project-root anchoring
   DECISION: Fourteen named commands skip `findProjectRoot(cwd)` entirely and operate against the raw invocation cwd, on the grounds that they are 'pure utility'. Rejected alternative: always anchor (ADR-1411's rule 1) and let the handful of genuinely pre-`.planning/` verbs opt out via an explicit, tested declaration on the router rather than a literal set in the dispatcher.
   EVIDENCE: gsd-core/bin/gsd-tools.cjs:2515-2531 — `const SKIP_ROOT_RESOLUTION = new Set([...])` followed by `if (!SKIP_ROOT_RESOLUTION.has(command)) { cwd = findProjectRoot(cwd); }`. Membership is justified by scattered per-entry comments (#1529 for `project-instruction-file`, #1579 for `eval`) and an explicit coupling warning at :2510-2514 that `loop` and `capability` must be moved toget…
   WHY ADR: ADR-1411 rule 1 states resolution "MUST NOT depend on an arbitrary descendant cwd" and names the Project-Root Resolution Module as the single owner. This set is the unwritten exception to that rule, and it is a plain literal in the entrypoint — so a contributor adding a command silently inherits anchoring, and a contributor adding an entr…
   OVERLAP: ADR-1411 (Decision rule 1, 'Deterministic anchoring') establishes the rule but not the exception mechanism. `grep -ril SKIP_ROOT_RESOLUTION docs/adr/` returns nothing. Residual gap: the criterion for exemption and where it should be declared.

[low] 11. Lint rule-set asymmetry between authored `src/**/*.cts` and generated `gsd-core/bin/**/*.cjs`
   DECISION: The ESLint flat config applies two disjoint rule sets: `src/**/*.cts` gets typescript-eslint type-checked rules plus three local AST rules, while `gsd-core/bin/**/*.cjs` gets `js.configs.recommended` + `eslint-plugin-n` (including `n/no-process-exit: 'error'`) + `local/no-source-grep`. Because each migrated module's emitted `.cjs` is then added to a hand-maintained 164-entry ignore list, migrating a module from hand-…
   EVIDENCE: eslint.config.mjs:239-267 — the `files: ['src/**/*.cts']` block declares only `plugins: { local }` and `extends: [tseslint.configs.recommendedTypeChecked]`; no `n`, no `js.configs.recommended`. eslint.config.mjs:297-328 — the `files: ['gsd-core/bin/**/*.cjs', 'scripts/**/*.cjs']` block declares `plugins: { n, local }`, `...js.configs.recommended.rules`, and `'n/no-process-exit'…
   WHY ADR: The migration to build-at-publish was sold as a strict improvement, and each migration PR is reviewed as behavior-preserving — but each one also quietly removes a rule family from that file, and the ignore list has grown from the 12 entries ADR-457 reasoned about to 164. `n/no-process-exit` is the concrete casualty and it is directly load…
   OVERLAP: ADR-457 (457-generated-cjs-single-source.md:38-44,114-119,124) — anticipated the ignore list, mandated "reconcile the lint config to reality FIRST" for a 12-entry list, and promised "type-aware typescript-eslint rules apply to migrated runtime code" — but neve…

NOTES: Scope worked: `src/*.cts` (156 modules) engine slice — dispatch (`command-routing-hub.cts`, `cjs-command-router-adapter.cts`, `gsd-core/bin/gsd-tools.cjs` main()/runCommand()/dispatch*), config (`config-loader.cts`, `config-schema.cts`, `federated-config.cts`), I/O + error taxonomy (`io.cts`, `cli-exit.cts`), determinism (`clock.cts`), observability (`src/observability/{event,logger,redaction}.cts`, `hook-bus.cts`), plus the capability admission gate in `capability-loader.cts`. Diffed against all 66 ADRs in docs/adr/ via keyword greps. GROUND TRUTH CONFIRMATIONS (task premises verified, not assumed): payload dir is `gsd-core/` (gsd-core/bin/gsd-tools.cjs exists, 2686 lines); `src/*.cts` is a…

==============================================================================
## AGENT: a3cb64084f0caaaf5
==============================================================================

[high] 1. `.planning/` artifact format versioning and forward/backward compatibility across GSD upgrades
   DECISION: GSD stamps a schema version into every user artifact but deliberately never reads it — the de-facto policy is "user artifacts are permanently version-1.0 and every reader must be infinitely tolerant," rejecting the alternative that the installer-migration pattern (ADR-0008) be extended to the user's `.planning/` tree with a version-gated migration runner. The repo simultaneously implements the OPPOSITE policy for one…
   EVIDENCE: src/state.cts:1706 writes `const fm: Record<string, unknown> = { gsd_state_version: '1.0' };` — the literal, never a constant. src/state-transition.cts:1199 only preserves it: `gsd_state_version: existingFm['gsd_state_version'] || '1.0'`, and src/state-transition.cts:80 classifies it `{ source: 'free', preservation: 'derive' }`. `grep -rn "gsd_state_version" src/ | grep -iE "==…
   WHY ADR: Users upgrade GSD continuously (v1.0.0 → v1.8.0 in this tag history) against a `.planning/` tree they have committed to git. Today the compatibility guarantee is entirely accidental — it holds only as long as every parser stays permissive, and there is no signal when it stops. A contributor reading `gsd_state_version` reasonably assumes i…
   OVERLAP: ADR-0008 (installer-migration-module) — covers migrations of the INSTALLED payload in runtime config dirs only; 0 references to `.planning`. ADR-1769 / ADR-1817 (STATE.md transition + rebuild-derivability) govern STATE.md content derivation, not schema evoluti…

[high] 2. Config-key deprecation, renaming, and compatibility policy
   DECISION: Config keys are treated as permanently frozen: the de-facto policy is silent inline self-healing migration performed ad-hoc at one read site, rather than a declared deprecation lifecycle (warn → grace period → remove) or an explicit never-rename freeze. The one rename in GSD's history was implemented as five lines buried in a config loader, and nothing generalizes it.
   EVIDENCE: The config surface is large and federated: `gsd-core/bin/shared/config-schema.manifest.json` → 106 `validKeys`, 1 `runtimeStateKey`, 16 `dynamicKeyPatterns`, plus per-capability federated keys resolved lazily at src/config-schema.cts:36-50 (`_capabilityConfigSchema`). The ONLY deprecation handling anywhere is src/config.cts:245-252: `// Migrate deprecated "depth" key to "granul…
   WHY ADR: 106+ keys with no exit door means every naming mistake is permanent — #2430 is already stuck, and each new capability federates more keys into the same one-way surface. Contributors have no rule to follow, so the next rename will either break users silently (the `depth` precedent: rewrite their file without telling them) or be abandoned. …
   OVERLAP: ADR-1517 (reviewer-instances config surface) and ADR-1244 (capability ecosystem, federated config slices) define how keys are ADDED and validated; ADR-894 mentions deprecation only for the capability declaration format. Residual gap: no ADR covers removing, re…

[high] 3. Slash-command surface budget — how many `/gsd-*` commands is too many, and who says no
   DECISION: GSD chose to solve command-surface cost at INSTALL time (three profiles that hide skills) rather than at CONTRIBUTION time (a cap, a budget, or a placement rule that forces new work into flags/capabilities). No gate limits the number of commands; the profile system absorbs growth indefinitely. The rejected alternative — a hard ceiling or a 'must displace an existing command' rule, analogous to the byte-budget ratchet…
   EVIDENCE: Surface today: 71 files in `commands/gsd/`, 71 skills in `skills/`, 34 agents in `agents/*.md`, 91 workflows in `gsd-core/workflows/*.md`, 39 capabilities in `capabilities/`. Growth is monotonic across every release: `git ls-tree` over tags gives v1.0.0=67, v1.4.0=67, v1.5.0=69, v1.7.0=71, v1.8.0=71 commands — never a single removal. ADR-0011:10 sizes the entire profile design …
   WHY ADR: Every new command is a permanent tax on the runtime's eager system-prompt budget — the exact cost ADR-0011 was written to control — and on the user's ability to find the right entry point among 71 options. Because no rule exists, a contributor proposing a 72nd command has nothing to argue against, and a reviewer has nothing to cite in ref…
   OVERLAP: ADR-0011 (supersedes ADR-0010) — Skill Surface Budget Module: builds profiles/closures to make the surface *installable in slices*, and explicitly leaves the default at maximum (ADR-0010:15 "Default install (no flag) remains `full` for back-compat"). ADR-1610 …

[high] 4. Documentation-freshness enforcement — what makes shipped docs true, not merely touched
   DECISION: Doc correctness is enforced by a proxy — 'did the PR touch any file under docs/' — rather than by any check that the documented claim matches the code. The rejected alternative (executable/greppable doc assertions, generated reference sections, or a code→doc back-reference gate) was never recorded, and the four shipped translation locales were adopted with no sync obligation at all.
   EVIDENCE: scripts/lint-docs-required.cjs is the entire gate. Its verdict logic keys on fragment type and path prefix only: line 33 `const TRIGGERING_TYPES = new Set(['Added','Changed','Deprecated','Removed'])`, line 35 `const DOCS_PREFIX = 'docs/'`, line 43 `function isDocsFile(file) { return file.startsWith(DOCS_PREFIX); }` — touching ANY docs file satisfies it. Two escape hatches: line…
   WHY ADR: Docs are the product for a workflow orchestrator — USER-GUIDE.md and COMMANDS.md ARE how users learn what GSD does. A gate that accepts any docs/ edit means the flagship user document can be wrong at HEAD about where the tool writes files, which is exactly what is happening. Users lose trust in every other line once one is falsified; cont…
   OVERLAP: NONE. `grep -ril "documentation freshness|doc freshness" docs/adr/` → 0 hits. ADR-452 (eslint-lint-harness) governs code lint; ADR-1703 (portability enforcement) governs path portability. No ADR governs doc truth.

[high] 5. Failure-message and error-UX standard for the user-facing surface
   DECISION: GSD built a typed error taxonomy and then scoped it to tests, keeping plain unstructured text as the default and only user-visible form. The rejected alternative — making structured, reason-coded, remediation-bearing errors the DEFAULT and the human text a rendering of it — is visible in the code comment as a conscious choice but was never argued as an architecture decision, and the taxonomy was never extended to the…
   EVIDENCE: src/io.cts:163 defines `ERROR_REASON` with exactly 17 codes. src/io.cts:199-201 states the posture: "Default off so existing callers and human operators keep their plain-text diagnostics. The structured form is opt-in for tooling and tests (#2974)." The human path at src/io.cts:216 is `writeAllSync(2, 'Error: ' + message + '\n')` — no code, no remediation, no next step. Reach i…
   WHY ADR: A workflow orchestrator's failures are its most trust-critical moments, and the pattern the issue tracker keeps reporting is not 'wrong error text' but 'no error at all' — silent drops, empty output indistinguishable from success, false 100% completion. Without a written standard (every failure carries a typed reason; a failed leg is neve…
   OVERLAP: ADR-0012 (command-routing-hub) defines the no-throw Result contract with `UnknownCommand`/`InvalidArgs`/`HandlerRefusal`/`HandlerFailure` kinds — an INTERNAL dispatch contract, not a user-facing message standard. ADR-2008 (command-exit-zero gate) governs exit …

[high] 6. What GSD promises when an agent run is interrupted — the phase-level durability and repair contract
   DECISION: GSD chose 'don't interrupt' as its interruption contract: the engine hardened process-level lock recovery but defined no phase-level durability semantics, so a session killed mid-phase can land in states the pipeline cannot re-enter. The rejected alternative — a declared resumable-checkpoint model where every phase step is idempotent and re-runnable — is implied by the existence of pause-work/resume-work/HANDOFF.json…
   EVIDENCE: The entire user-facing contract is one sentence: docs/USER-GUIDE.md:641 — "GSD subagents run in a separate context window — their work is invisible to the parent session while in progress. Do not interrupt the session. Wait for the result…". The Recovery Quick Reference (docs/USER-GUIDE.md:945-963) offers `/gsd-resume-work`, `git revert`, `/gsd-forensics` — recipes, not a guara…
   WHY ADR: Interruption is not an edge case for an agentic tool — users hit Esc, sessions time out, providers throttle (the CHANGELOG ships provider-escalation for exactly this). If the answer is 'don't', users will still interrupt and land in #2411's dead end with real work stranded and LEARNINGS.md silently never written. Contributors adding pipel…
   OVERLAP: ADR-1787 (gsd-next smart-entry) classifies observable states including `paused` and routes them, but classifies rather than guarantees. ADR-1817 (STATE.md rebuild-derivability contract) guarantees STATE.md can be re-derived from disk — the closest thing, and g…

[medium] 7. Localization scope and enforcement — what `response_language` actually promises
   DECISION: Localization was implemented as per-workflow prose directives rather than at a seam, making coverage a function of which files a contributor remembered to edit; and the shipped-language set was capped at 9 with a silent English fallback rather than an explicit refusal or a pass-through. Both are real choices with obvious rejected alternatives (enforce at the agent-dispatch boundary; fail loudly on an unsupported lang…
   EVIDENCE: `response_language` is a first-class central config key (present in `gsd-core/bin/shared/config-schema.manifest.json` validKeys, confirmed via node). Coverage is roughly half: `ls gsd-core/workflows/*.md | wc -l` → 91; `grep -rl "response_language" gsd-core/workflows/*.md | wc -l` → 47, leaving 44 uncovered — exactly the count in open approved-enhancement #2529 ("44 of 91 workf…
   WHY ADR: A user who sets `response_language: ja` gets a coin-flip: half the workflows honor it, checkpoint frames and report templates do not, and an unlisted language degrades to English with no notice. That is worse than not offering the feature, because it reads as a bug in GSD rather than a scope limit. Contributors adding workflow #92 have no…
   OVERLAP: NONE. `grep -ril "response_language|localization|i18n|translat" docs/adr/` returns only incidental hits — ADR-0174, ADR-1143, ADR-452, ADR-1703, ADR-0012 — none about language output. No ADR mentions `response_language`.

[medium] 8. The first-run contract — what a fresh install owes a first-time user, and why the default install is maximal
   DECISION: The default install ships the FULL surface — every skill and agent — chosen explicitly for back-compat, in the same ADR whose entire purpose is to stop GSD from consuming the runtime's skill-listing budget. The rejected alternative (default to `core`, let users opt up) is named nowhere as a considered-and-declined product decision, and no ADR defines the first-run sequence at all.
   EVIDENCE: ADR-0010:15 records the default and its reason in one clause: "Preserve `--minimal` / `--core-only` as aliases for `--profile=core`. Default install (no flag) remains `full` for back-compat." ADR-0011 (which supersedes it) carries the profile design forward while ADR-0011:10 states the cost being deferred: "With 66 skills and 33 agents, GSD alone consumes roughly 60% of the def…
   WHY ADR: First-run is where adoption is won or lost, and today it is the maximum-surface configuration by inertia: a new user's runtime is handed 71 skill descriptions, which is both the worst first impression and the exact failure (#3408, dropped descriptions when stacking plugins) that the profile system was built to prevent. Because the default…
   OVERLAP: ADR-1990 (existing-code onboarding) governs BROWNFIELD route selection — which primitive runs first on a repo with existing code — and explicitly scopes itself to that. ADR-1787 (gsd-next smart-entry) builds a classifier that handles the `no-project` state and…

[medium] 9. Placement rule — what belongs in a command vs a capability vs a skill vs a core workflow branch
   DECISION: GSD migrated optional features out of the core loop into Capabilities (tdd, security, and others) — a real and consequential re-architecture — but codified only the capability MECHANISM, never the rule for when to use it. The nearest ADR explicitly declines the question. The rejected alternative (a written placement test, e.g. 'anything optional, runtime-specific, or third-party-ownable is a capability; the core loop…
   EVIDENCE: ADR-959:21 draws the boundary and then disclaims half of it: "Two distinct 'command' kinds exist and must not be conflated: **gsd-tools CLI subcommands** (internal, invoked by workflows via `gsd_run`) and **slash-commands/skills** (`commands/gsd/*.md`, agent-facing). This ADR is about the **former**." The migration it leaves ungoverned is recorded only in release prose — CHANGE…
   WHY ADR: With five overlapping mechanisms and no placement test, identical features land in different classes depending on who wrote them — which is how the surface reached 71 commands + 39 capabilities + 91 workflows with no removals ever. A contributor with an approved feature must guess; a reviewer has no basis to redirect them. This is also th…
   OVERLAP: ADR-857 (capability system), ADR-894 (capability declaration format), ADR-959 (capability command contribution), ADR-1244 (capability ecosystem), ADR-1213 (capability state writer) — collectively a thorough specification of HOW capabilities work, plus ADR-0011…

[medium] 10. Default-on vs default-off policy for new gates and behavior changes
   DECISION: Whether a newly shipped gate blocks existing users by default is decided per-feature with no rule — the same release ships one gate on-by-default that can block `/gsd:verify-work`, another blocking gate opt-in, and several behavior changes opt-in. The rejected alternative (a stated policy, e.g. 'anything that can block an existing user's loop ships off for one minor and on thereafter, announced under ### Deprecated/#…
   EVIDENCE: Within the single [1.8.0] section of CHANGELOG.md, mutually inconsistent postures: line 14 — an `api-coverage` gate "blocks `/gsd:verify-work` until the phase produces a `COVERAGE.md`" … "Toggleable via `workflow.api_coverage_gate` (on by default)"; line 31 — the broken-windows ledger "`/gsd:ship` now blocks (when `workflow.windows_enforce=true`, opt-in)" … "enforcement is off …
   WHY ADR: An on-by-default gate can stop an existing user's `verify-work` or `ship` on their next upgrade for a reason unrelated to anything they changed — the single most trust-corrosive upgrade experience a workflow tool can produce. Because the choice is per-author, the risk scales with contribution volume and is invisible in review. A contribut…
   OVERLAP: ADR-2008 (command-exit-zero gate) specifies how a capability predicate gate is EVALUATED and that it fails closed; ADR-857/ADR-1244 define capability activation and consent. ADR-1820 (spec-optional predicate rail) documents one such toggle's default in isolati…

NOTES: METHOD. Ran against HEAD aa0f7dee on `next`. `gh` worked — pulled 60 open + 60 closed issues from open-gsd/gsd-core and used recurring themes as corroboration, not as primary evidence. Every claim below was re-derived from live files; ADR absence was checked with `grep -ril` over docs/adr/ (66 files) before being asserted. MEASURED SURFACE (all commands re-run at HEAD): 71 `commands/gsd/*.md`, 71 `skills/`, 34 `agents/*.md`, 91 `gsd-core/workflows/*.md`, 39 `capabilities/`, 156 `src/*.cts`, 106 central config `validKeys` + 16 dynamic patterns + federated capability keys, 17 `ERROR_REASON` codes, 66 ADRs (50 listed Active in docs/adr/README.md:119). Command count by tag: v1.0.0=67 → v1.8.0=71…

==============================================================================
## AGENT: a91bc1c69dfe157e3
==============================================================================

[high] 1. Uninstall ownership model: gsd- prefix deletion, not the install manifest
   DECISION: Uninstall determines what to remove by PREFIX MATCH on filenames/dirnames (`entry.name.startsWith('gsd-')`), and for one layout kind by deleting the whole namespace dir recursively — deliberately NOT consulting `gsd-file-manifest.json`, which install writes and which the migration framework already uses to classify artifacts as managed-pristine / managed-modified / unknown. Rejected alternative: manifest-driven unins…
   EVIDENCE: src/install-engine.cts:486-503 `_removeGsdEntries` — `if (!entry.name.startsWith(kind.prefix)) continue; fs.rmSync(path.join(destDir, entry.name), { recursive: true, force: true })`; and `if (kind.prefix === '') { fs.rmSync(destDir, { recursive: true, force: true }); return; }`. Contrast the install-side model: src/installer-migrations.cts:22 `const MANIFEST_NAME = 'gsd-file-ma…
   WHY ADR: ADR-0008 states the opposite policy as an accepted decision ("Default ambiguous or unknown files to preserve; destructive changes need managed-file evidence or explicit user choice", docs/adr/0008-installer-migration-module.md:17), and "Treat the existing file manifest as the managed-file ownership baseline" (line 14). A contributor readi…
   OVERLAP: ADR-0008 (installer-migration-module) sets the manifest-as-ownership-baseline policy but scopes it to migrations. ADR-1244 (capability-ecosystem, Accepted 2026-07-17) names the exact defect in its Context — line 41: "The only uninstall surface is whole-product…

[high] 2. Partial-install failure semantics: artifact rollback is Codex-only; multi-runtime install is fail-fast with no cross-runtime unwind
   DECISION: Install is NOT transactional. Content snapshot + rollback of skills/, agents/, and VERSION is built only when the runtime descriptor sets `tomlConfigInstall` — true for exactly one runtime (Codex). For the other 17 selectable runtimes a throw mid-install leaves a partially-written config dir with no restore. Across runtimes, `--all` aborts on the first failure and unwinds only installer MIGRATIONS, not the artifacts …
   EVIDENCE: bin/install.js:10206 `const _codexPreConfigRollback = !_hostBehaviors(runtime).tomlConfigInstall || isMinimalMode(...) ? null : () => {...}`; snapshot capture gated identically at bin/install.js:10152. `tomlConfigInstall` is declared by exactly one descriptor: capabilities/codex/capability.json:79 (grep over capabilities/ returns that single hit). Multi-runtime loop: bin/instal…
   WHY ADR: The code comment at bin/install.js:10195-10201 asserts "the caller is never left in a partially-installed state" — true only for Codex. An agent or contributor extending install (or writing a new runtime adapter) will reasonably assume install is atomic and skip defensive re-entrancy. It isn't: a Claude install that throws after skills/ i…
   OVERLAP: ADR-0008 owns rollback for MIGRATIONS only (docs/installer-migrations.md:399-406 "Rollback" records pre-overwrite bytes for migration actions). ADR-0010 proposed a File Operation Engine owning "rollback bookkeeping" but is Superseded, and its own supersession …

[high] 3. Hook ownership inside host config files is inferred by command-string regex against hand-maintained allowlists, not by an ownership marker
   DECISION: For settings.json / Codex hooks.json / Codex config.toml, GSD decides "is this hook entry mine?" by regex-matching the entry's `command` string against a hardcoded per-surface set of script basenames. Rejected alternative — used by GSD itself for two other runtimes — is writing an explicit ownership sentinel into the entry (`gsd-managed`). Consequence of the chosen model: three separate hand-maintained lists must agr…
   EVIDENCE: Predicate: src/shell-command-projection.cts:273-303 `isManagedHookCommand` — builds a RegExp per basename and tests the normalized command text. Allowlists: src/shell-command-projection.cts:213-227 (`MANAGED_HOOK_COMMAND_BASENAMES_BY_SURFACE['settings-json']`, 12 entries) and 190-211 (`MANAGED_HOOK_BASENAMES_BY_SURFACE`). Third list: hooks/managed-hooks-registry.cjs:19-44 `MANA…
   WHY ADR: Two failure modes, both live. (1) A hook GSD writes but does not list is invisible to its own reconcile/removal paths — it survives uninstall and accumulates as a dangling settings.json entry pointing at a deleted script. (2) A user who wraps a GSD hook in their own launcher script gets it silently deleted on reinstall if the wrapped path…
   OVERLAP: ADR-0010 named this exactly — "Unify managed-hook ownership classification used by install/uninstall/migration hook config rewrites" (docs/adr/0010-file-operation-engine-module.md:26, Initial Scope item 1) — but ADR-0010 is Superseded by ADR-0009, and ADR-0009…

[high] 4. Mutation strategy for host-owned config files is split by file format, and GSD will unlink a host config file it did not create
   DECISION: Two incompatible strategies for editing files a third-party host owns: (a) JSON surfaces are parsed and rewritten WHOLE (`JSON.stringify(parsed, null, 2)`), discarding the user's formatting and key order; (b) TOML surfaces are edited by marker-delimited text surgery with no parser — and Codex's variant removes everything from the GSD marker to EOF, so any user content appended after GSD's block is destroyed. Further,…
   EVIDENCE: JSON whole-file rewrite: src/runtime-hooks-surface.cts:596 `parsed = JSON.parse(raw)` → :671 `const nextContent = `${JSON.stringify(parsed, null, 2)}\n``; same shape at :1076/:1116 and :1277/:1308. TOML marker surgery: bin/install.js:4562-4615 `stripGsdFromCodexConfig` — `const markerIndex = content.indexOf(GSD_CODEX_MARKER); ... let before = content.substring(0, markerIndex)` …
   WHY ADR: A user whose `~/.codex/config.toml` predates GSD, or who appended their own `[mcp_servers]` block below GSD's marker, loses it on uninstall with no backup written anywhere (grep for `backup`/`.bak` in src/runtime-hooks-surface.cts returns zero hits). The registry rule that would have prevented this is scoped to migrations (docs/installer-…
   OVERLAP: docs/installer-migrations.md §Runtime Configuration Contract Registry (lines 346-397) is the nearest artifact and DOES record a per-runtime "Config ownership boundary" column — but it is a reference doc governing migrations, not an ADR, and it does not adjudic…

[high] 5. No version-skew gate between the installed payload and a project's .planning/ artifacts
   DECISION: GSD versions and migrates the INSTALLED PAYLOAD (numbered migration records, a manifest, an applied-migrations state file) but applies no version gate whatsoever to PROJECT data. `.planning/STATE.md` carries a `gsd_state_version` frontmatter field that is written and faithfully preserved across rewrites, yet is never read by any compatibility check. Combined with a single global payload at `~/.claude/gsd-core/` share…
   EVIDENCE: Written: src/state.cts:1706 `const fm: Record<string, unknown> = { gsd_state_version: '1.0' }`. Preserved: src/state-transition.cts:80 `gsd_state_version: { source: 'free', preservation: 'derive' }` and :1199 `gsd_state_version: existingFm['gsd_state_version'] || '1.0'`. Documented as a schema version: docs/reference/state-md.md:58 "| `gsd_state_version` | string (`'1.0'`) | Al…
   WHY ADR: An inert schema-version field is worse than none: a contributor sees `gsd_state_version: '1.0'` and reasonably assumes a compatibility mechanism exists behind it, so ships a breaking `.planning/` shape change without a migration. Meanwhile a user mid-milestone who runs `gsd update` gets new workflow prose reading artifacts produced by the…
   OVERLAP: NONE — no ADR in docs/adr/ mentions `gsd_state_version` (grep -rln returns empty). ADR-1817 (state-md-rebuild-derivability-contract) and ADR-1769 (state-md-transition-module) own STATE.md's shape and derivability but not cross-version compatibility. ADR-0008 o…

[high] 6. Unconditional network egress on every session start, with no opt-out and no written policy
   DECISION: GSD's SessionStart hook spawns a detached background process on every single session in every project that runs `npm view <package> version` against the public registry. There is no environment variable, config key, or install flag to disable it. Rejected alternatives: opt-in update checking; a cache TTL that skips the spawn; or a documented "GSD makes no network calls except X" statement.
   EVIDENCE: hooks/gsd-check-update.js:53-67 — `const child = spawn(process.execPath, [workerPath], { stdio: 'ignore', detached: true, ... }); child.unref();` fired from the SessionStart hook (file header line 3: "Called by SessionStart hook - runs once per session"). Worker: hooks/gsd-check-update-worker.js:21 `const { checkLatestVersion } = require('../gsd-core/bin/check-latest-version.cj…
   WHY ADR: This is the only recurring outbound connection GSD makes, it fires in every project on every session, and it is invisible in SECURITY.md. Users on air-gapped or audited networks, and organizations reviewing GSD for adoption, need a written answer to "what does this tool send, where, and how do I turn it off" — today the answer only exists…
   OVERLAP: ADR-2164 (statusline-scope-boundary) is the nearest — docs/adr/2164-statusline-scope-boundary.md:33 rules a `api.anthropic.com/api/oauth/usage` call out of scope as "Tier 2 (reads OAuth creds + calls ...)", closed wontfix. Residual gap: it draws the boundary f…

[medium] 7. User-modification preservation is a three-way merge (gsd-pristine/ + gsd-local-patches/) with a one-element hardcoded preserve list
   DECISION: When a user edits an installed, generated artifact, GSD neither clobbers it nor refuses to update: it keeps a pristine baseline copy of every managed file plus a patch of the user's diff, and re-applies after update. Separately, the install-time prune preserves user-owned `gsd-`-prefixed skill directories via a hardcoded one-element name list. Rejected alternatives: clobber-and-warn; refuse-to-update-when-modified; o…
   EVIDENCE: bin/install.js:9315 `const PATCHES_DIR_NAME = 'gsd-local-patches'`; :9676-9677 "Backs up modified files to gsd-local-patches/ for reapply after update. Also saves pristine copies (from manifest) to gsd-pristine/"; :9702 `const pristineDir = path.join(configDir, 'gsd-pristine')`; the hash-validated regeneration ladder at :9748-9856 (`sha256(gsd-pristine/X) === originalHash → kee…
   WHY ADR: This is the strongest user-data guarantee GSD makes and it is documented only in inline comments and one runtime's row of docs/installer-migrations.md (line 370, Kimi: "Reinstall/update must preserve locally modified generated Kimi artifacts through manifest-backed `gsd-local-patches/`"). It is also fragile in a specific way: whether a mo…
   OVERLAP: ADR-0008 declares the principle ("Treat user-owned artifacts as a single shared policy consumed by preservation and manifest writing", docs/adr/0008-installer-migration-module.md:15) but names no mechanism. No ADR mentions `gsd-pristine`, `gsd-local-patches`, …

[medium] 8. Per-runtime capability degradation is declared but never enforced at install time
   DECISION: Feature capabilities declare `runtimeCompat: { supported: ['*'], unsupported: [] }` and runtime capabilities declare `supportTier`, but install consults neither. Every feature is materialized identically onto every runtime regardless of whether that runtime can execute it; degradation is left entirely to runtime prose fallbacks inside workflows. Rejected alternative: gate materialization on the declared matrix and em…
   EVIDENCE: Uniform declaration: every capabilities/*/capability.json carries `"unsupported": []` (e.g. capabilities/graphify/capability.json:16, capabilities/claude-orchestration/capability.json:16) with `"supported": ["*"]`. Zero enforcement: `grep -rn runtimeCompat src/*.cts bin/install.js` returns nothing; the only consumers are validation/passthrough in scripts/registry-schema.cjs:179…
   WHY ADR: ADR-1239 makes graceful degradation a first-class contract ("GSD degrades gracefully when a primitive is absent (generalizing #853 into a first-class contract)", docs/adr/1239-gsd-embeddable-orchestration-engine.md:40), and ADR-1016 defines `supportTier` as a coverage axis. A contributor adding a capability that genuinely cannot work on, …
   OVERLAP: ADR-1016 (runtime-capability-descriptor) defines `supportTier` at line 112 ("GSD coverage tier (unchanged: 1 | 2)") and validation at line 33; ADR-894 tabulates it at line 123; ADR-1239 frames negotiate-and-degrade. docs/reference/host-integration-capability-m…

[medium] 9. `--dry-run` previews legacy cleanup only, does not preview install or uninstall, and is absent from --help
   DECISION: The installer's `--dry-run` flag was scoped to a single legacy-artifact cleanup preview and made a terminal branch (print, exit 0) rather than a plan-and-print pass over the actual install or uninstall. It is not listed in the usage block. Rejected alternative: reuse the same planner as apply mode across install/uninstall — the pattern ADR-0008 already mandates for migrations and which `createRuntimeArtifactUninstall…
   EVIDENCE: bin/install.js:13422-13435 — `if (hasDryRun) { if (hasUninstall) { console.log('Note: --dry-run previews legacy get-shit-done-cc cleanup only; it does not preview --uninstall.'); } console.log('Dry run — no files will be modified.\n'); const { plan } = cleanupLegacyGsdCc({ dryRun: true }); ... process.exit(0); }`. The `--help` usage string at bin/install.js:899 enumerates -g/-l…
   WHY ADR: An operator about to run `--uninstall` against a config dir containing hand-edited files will reach for `--dry-run` as the safety net. It exits 0 having previewed something unrelated, and the reassuring line "Dry run — no files will be modified" prints either way. Given candidate #1 (prefix-recursive deletion with no manifest check), this…
   OVERLAP: ADR-0008 decided "Support dry-run output using the same planner used by apply mode" (docs/adr/0008-installer-migration-module.md:20) — satisfied for migrations only. Residual gap: the top-level `--dry-run` flag on bin/install.js shares the name but not the sem…

[medium] 10. Host-Integration Interface dispatch fails OPEN — a registry load failure silently bypasses the adapter for both install and uninstall
   DECISION: When the composed capability registry / imperative adapter cannot be constructed, install and uninstall silently fall back to calling the install engine directly, using the layout resolver instead of the negotiated Host-Integration Interface. Rejected alternative: fail closed (refuse the operation and tell the user the registry is broken), since a registry failure means GSD does not actually know what this runtime's …
   EVIDENCE: bin/install.js:537-543 — `function _runtimeAdapter(runtime) { try { return createImperativeAdapter({ runtime }); } catch { return null; } }` (bare catch, no logging, no warning). Uninstall consumer: bin/install.js:8040-8047 — comment "ADR-1239 / #2086: drive uninstall through the public Host-Integration Interface. Fail-open to the engine directly if the composed-registry adapte…
   WHY ADR: The two postures coexist in the same file with opposite defaults and no stated rule. Fail-open here means a user with a corrupt or partially-generated registry gets a silently different install path than the one every ADR describes, with no diagnostic — and the divergence surfaces later as "GSD installed but nothing works on this runtime"…
   OVERLAP: ADR-1239 (gsd-embeddable-orchestration-engine, Accepted) defines the Host-Integration Interface and the negotiate-then-degrade frame; docs/reference/host-integration-interface.md describes the surface. Residual gap: neither states what happens when the interfa…

NOTES: METHOD. Re-derived from live code at HEAD aa0f7dee on branch `next`. Authored source read from `src/*.cts` (not the generated `gsd-core/bin/lib/*.cjs`), plus `bin/install.js` (13,486 lines, hand-written), `hooks/`, `capabilities/` (39 descriptors), `pi/`, `vscode/`. ADR corpus = 66 files in docs/adr/; absence claims were checked with `grep -rli` per keyword and are cited per candidate. CORRECTIONS TO TASK FRAMING (verified): - `src/host-integration-adapters/` contains exactly one file, `cline-sdk-binding.cts`; the imperative/declarative adapters live at `src/adapter-imperative.cts` / `src/adapter-declarative.cts` and `src/host-integration-adapters/imperative-hook-bus.cts`. - `pi/extension.js…

==============================================================================
## AGENT: a84d28761026a3321
==============================================================================

[high] (enforcement-gap) 1. README rule 2 ('Cross-references to other ADRs are file links, never bare ids') is enforced only inside four named relation fields within the header block, so genuinely ambiguous bare `ADR-0010`/`ADR-0011` references are live inside docs/adr/ itself while `--check` reports the corpus clean.
   EV: Enforcement is scoped by RELATION_FIELDS (scripts/gen-adr-index.cjs:74-79) and headerBlock (:140-144, stops at the first `## `); the bare-id error is emitted at :326-340 for those fields only. Unchecked live violations, all ambiguous because `0010-*` resolves to 2 files and `0011-*` to 3: docs/adr/1213-capability-state-writer.md:7 `- **Builds on:** … Skill Surface Budget Module (ADR-0011)`; docs/a…
   REC: Extend the bare-id scan from relation-field values to (a) every `- **Field:**` / `| **Field** |` line in the header block regardless of field name, and (b) whole-file prose in docs/adr/. Emit the existing ambiguity message ('resolves to N files: …'). Add an allowlist only for the two README passages…

[medium] (enforcement-gap) 2. README:46 asserts the lifecycle rules 'are enforced by scripts/gen-adr-index.cjs', but the ratification bar (README §'Ratifying a stale Proposed', lines 83-99) has zero code behind it — nothing verifies the mechanism exists in tree, the issue closed as completed, or that no material part is unshippe…
   EV: `grep -n 'ratif' scripts/gen-adr-index.cjs` yields only the comment at :348 ('Only a RATIFIED (Accepted) claimant is owed the back-link'). The script never opens a `## Ratification` section, never queries GitHub issue state, and never checks named files/symbols/tests. The only automated effect of ratification is indirect, at :365 (`if (dir === 'out' && a.statusToken !== 'Accepted') continue;`). RE…
   REC: Either scope the README's enforcement claim (state explicitly that the ratification bar is a human procedure, not a gate), or add a machine-checkable slice: require an `## Ratification` section whenever a Status reads `Accepted — ratified`, and validate that its declared evidence paths exist on disk…

[medium] (convention-violation) 3. ADR-857's H1 still carries a stale `[Proposed]` status bracket that contradicts its `Accepted — ratified 2026-07-17` Status field, and the generator hides the contradiction by stripping the bracket rather than flagging it.
   EV: docs/adr/857-capability-system.md:1 `# ADR-857: Capability system — five-step loop as core, features as plug-ins behind Loop Extension Points [Proposed]` vs :3 `- **Status:** Accepted — ratified 2026-07-17 (originally Proposed 2026-06-08); see "Ratification" below`. scripts/gen-adr-index.cjs:213 `.replace(/\s*\[(?:Proposed|Accepted|Superseded|Legacy|Retired)\]\s*$/i, '')` strips it before renderin…
   REC: Fix the H1 in 857-capability-system.md, and add a check in `validate()`: if the H1 ends in a status bracket, it must equal `statusToken`. The parse is already in hand at :213 — capture the bracket instead of discarding it.

[medium] (broken-link) 4. docs/adr/1239 contains a dead relative link — the anchor text says `docs/reference/…` but the href is relative to docs/adr/, so it resolves to the nonexistent docs/adr/reference/.
   EV: docs/adr/1239-gsd-embeddable-orchestration-engine.md:99 — ``[`docs/reference/host-integration-capability-matrix.md`](reference/host-integration-capability-matrix.md)``. `ls docs/adr/reference` → 'No such file or directory'; the real file is `docs/reference/host-integration-capability-matrix.md` (104.7K, exists). Mechanical sweep of every relative markdown link across all 67 files in docs/adr/ foun…
   REC: Change the href to `../reference/host-integration-capability-matrix.md`. Then add a repo-wide relative-markdown-link resolver to the lint suite — no such checker exists today (`ls scripts/ | grep -iE 'link|md|doc'` returns only lint-docs-required.cjs, which checks changeset↔docs coupling, not link t…

[medium] (broken-link) 5. Shipped source cites an ADR path that has never existed — both the number and the slug are wrong.
   EV: src/plan-drift-guard.cts:5 ` * ADR-22 (docs/adr/0022-source-grounding-drift-guard.md).` — no such file. The real ADR is `docs/adr/22-plan-drift-guard.md` ('Plan-vs-codebase drift guard: defaults and symbol-resolver seam', Accepted). This is an authored `.cts` source file, so the stale citation is carried into the generated `gsd-core/bin/lib/*.cjs` payload.
   REC: Correct to `docs/adr/22-plan-drift-guard.md`. A repo-wide `docs/adr/<file>.md` path-existence check is cheap (65 such references across git-tracked files) and would have caught this and the CONTRIBUTING.md case below.

[low] (broken-link) 6. CONTRIBUTING.md and docs/contributor-standards.md both present `docs/adr/3485-adr-prd-naming-convention.md` as a concrete, already-landed example of the naming convention, but no such file exists.
   EV: CONTRIBUTING.md:105 '**Example:** Issue #3485 was opened, approved, and its number became the prefix: `docs/adr/3485-adr-prd-naming-convention.md` on branch `docs/3485-adr-prd-naming-convention`.'; docs/contributor-standards.md:99 'Example: `docs/adr/3485-adr-prd-naming-convention.md`.' Neither resolves (`ls docs/adr | grep 3485` → empty). The repo's own CLAUDE.md repeats the same phantom filename…
   REC: Replace with a real file — `docs/adr/2264-golden-parity-redesign.md` or `docs/adr/1239-gsd-embeddable-orchestration-engine.md`, both already used as examples at docs/adr/README.md:24 — or reword as an illustrative placeholder so it does not read as a claim about disk state.

[low] (convention-violation) 7. bin/install.js labels GitHub epic #2310 as 'ADR-2310' twice, which README rule 2 explicitly forbids ('If you mean an issue, write #857 — not ADR-857') and which the gate cannot see because it only scans docs/adr/ relation fields.
   EV: bin/install.js:4169 '// per-tier embedding below is the ADR-2310 passive-posture epic.)' and :4182 '// (Superseded on the default path by the ADR-2310 passive-posture epic.)'. `ls docs/adr/2310*` → 'No such file or directory'. A repo-wide sweep of `ADR-<n>` tokens in git-tracked files outside docs/adr/ resolved every other id to a real file except test fixtures (tests/adr-index-gate.test.cjs, test…
   REC: Rewrite as '#2310'. If the ADR-vs-issue convention is to hold outside docs/adr/, the check has to run repo-wide — a bare `ADR-<n>` in tracked source that does not resolve to a file in docs/adr/ should fail with the existing 'write #N' message.

[low] (relation-asymmetry) 8. The corpus declares relations with verbs the graph does not model — `Amends`, `Extends`, `Builds on`, `Defers to`, `Relates to` — so those edges are one-way with no reciprocal and no check.
   EV: Mechanical extraction over all 66 files found these unreciprocated edges: docs/adr/959-capability-command-contribution.md:6 '- **Amends:** [ADR-894](894-capability-declaration-format.md)' — 894 records no back-reference to 959 (grep of 894 for 'Amended by' → none); docs/adr/2008-command-exit-zero-gate.md:9 '| **Amends** | ADR-0894 …|' — likewise unrecorded in 894, and bare; docs/adr/1593-skill-map…
   REC: Decide whether `Amends` is a first-class lifecycle relation. If yes, add it to RELATION_FIELDS with an `Amended by` reciprocal and document it in README §3; if no, say so in README §3 so authors stop reaching for it and readers of ADR-894 are not left unaware that two later ADRs amend it.

[medium] (tooling-improvement) 9. The repo has no markdown link checker of any kind, so every relative link outside the narrow ADR-relation path is unverified — which is how the three broken links above survived.
   EV: `ls scripts/ | grep -iE 'link|md|doc'` → only `lint-docs-required.cjs` (7.9K), which enforces that Added/Changed/Removed changesets touch docs/, not that links resolve. scripts/check-glossary-refs.cjs verifies backticked file paths in CONTEXT.md only (its own header, :14-24, scopes itself to TRACKED_PREFIXES). scripts/gen-adr-index.cjs resolves links only at :313-314, only for relation fields, and…
   REC: Add `scripts/lint-doc-links.cjs --check` to the `lint:generated-sync` chain: walk every tracked `.md`, extract relative links, skip http/mailto/pure-anchors, and assert the target exists. The three findings above are its first three hits; the sweep across docs/adr/ alone took under a second.

[low] (tooling-improvement) 10. `gen-adr-index.cjs --check` sits 9th in a 10-link `&&` chain, so any earlier generator failure short-circuits it and the ADR gate silently does not run for that CI attempt.
   EV: package.json:114 `"lint:generated-sync": "node scripts/gen-capability-registry.cjs --check && node scripts/gen-loop-host-contract.cjs --check && node scripts/gen-capability-matrix.cjs --check && node scripts/sync-manifest-versions.cjs --check && node scripts/gen-inventory-manifest.cjs --check && node scripts/generate-package-identity.cjs --check && node scripts/gen-plugin-skills.cjs --check && nod…
   REC: Run the generators unconditionally and aggregate failures — e.g. a small driver that invokes each `--check`, collects non-zero exits, prints all banners, then exits 1. Same total runtime, one round-trip instead of up to ten.

[low] (enforcement-gap) 11. Rule 4 (declared id matches filename) is opt-in: 27 of 66 ADRs declare no id in their H1 and are therefore never checked, so the rule cannot catch the 'rename that never finished' it was written for unless the author happened to write the id.
   EV: scripts/gen-adr-index.cjs:290 `if (a.declaredId && a.declaredId !== a.fileId)` — the `a.declaredId &&` guard exempts a null. Mechanical sweep: 0 mismatches, 27 files with no `ADR-<n>` prefix in the H1 — 0001-dispatch-policy-module.md, 0002, 0003, 0004, 0005, 0006, 0007, 0008, 0009, 0010-file-operation-engine-module.md, 0010-skill-surface-budget-module.md, 0011-review-default-reviewers.md, 0011-rev…
   REC: Require the H1 to declare the id (`# ADR-<prefix>: <title>`) and turn the conditional into an unconditional check. Backfilling 27 H1s is mechanical, and it would make the `0010-*`/`0011-*` files self-identifying — which is the one place in the corpus where the filename alone is not enough.

[low] (enforcement-gap) 12. Relation parsing has two structural blind spots that let a genuine relation declaration go unseen by the symmetry graph: header fields below the first `##` heading, and `## Superseded by` / `## Subsumed by` sections.
   EV: scripts/gen-adr-index.cjs:140-144 `headerBlock` truncates at the first `/^##\s/` line, so any `- **Superseded by:** …` written lower in a file is invisible. Live instance of the seam (harmless in content, since it points at a PR): docs/adr/1411-resolution-provenance.md:75 `- **Supersedes** the tactical fix in PR #1408 (closed)`. Separately, RELATION_SECTION_RE (:155) is `/^##\s+(Supersedes|Subsume…
   REC: Scan relation header fields over the whole file rather than only the leading block (relation fields are unambiguous by name), and extend RELATION_SECTION_RE to include `Superseded by|Subsumed by` mapped to `dir: 'in'`.

[low] (tooling-improvement) 13. `--write` regenerates and commits the index even when lifecycle violations remain, so a contributor following the documented procedure can land a clean-looking index over a corpus the gate will reject.
   EV: scripts/gen-adr-index.cjs:491 `if (errors.length > 0 && flag !== '--write')` — violations abort every mode except `--write`; :513-520 writes the file first and only then prints '`${errors.length} lifecycle violation(s) remain — --check will fail:`' to stderr. README:94 instructs 'run `node scripts/gen-adr-index.cjs --write`' as the ratification procedure's final step, where this ordering is most l…
   REC: Keep the write (it is deliberately useful — the index is still correct for the ADRs that parse), but exit non-zero from `--write` when violations remain so a scripted or agent-driven workflow does not read success from the exit code.

-- ENFORCEMENT MATRIX --
   [YES] Rule 1 — Every ADR declares one status from the canonical vocabulary {Accepted, Proposed, Superseded, Retired, Legacy}; first word of the St… :: scripts/gen-adr-index.cjs:55 `const STATUSES = ['Accepted','Proposed','Superseded','Legacy','Retired'];`; :241 `const statusToken = statusRaw ? (statusRaw.match(/^([A-Za-z]+)/) || [])[1] : null;`; :283-289 `if (!a.status…
   [YES] Rule 1b — A `Superseded` ADR must name its successor as a file link (README:56 obligation column) :: scripts/gen-adr-index.cjs:295-307 — for `statusToken === 'Superseded'`, collects `a.relations.supersedes.in.flatMap(r => r.links)` and errors if empty, with a distinct message when only a bare id is present: 'Bare ids ar…
   [NO] Rule 1c — A `Retired` ADR must 'say what was removed and when' (README:57) :: No occurrence of 'Retired' in scripts/gen-adr-index.cjs outside the STATUSES array (:55) and the GROUPS heading (:398-400). No code inspects a Retired ADR's body for a removal statement or date. Currently vacuous — 0 of …
   [PARTIAL] Rule 2 — Cross-references to other ADRs are file links, never bare ids (README:62-66) :: Enforced ONLY for values of the four fields in RELATION_FIELDS (:74-79 — supersedes / superseded by / subsumes / subsumed by) and ONLY inside `headerBlock(text)` (:140-144, which stops at the first `## ` line). scripts/g…
   [PARTIAL] Rule 2b — 'If you mean an issue, write #857 — not ADR-857' (README:66) :: Only reachable via the same relation-field bare-id path: scripts/gen-adr-index.cjs:331 emits 'If it is an ISSUE number, write "#${b}" — not "ADR-${b}"' when the bare id resolves to no file. Outside relation fields — and …
   [YES] Rule 3 — Supersession and subsumption are symmetric; if A declares either relation toward B, B must record the reciprocal; only an Accepted … :: scripts/gen-adr-index.cjs:354-381. `const OPPOSITE = { out: 'in', in: 'out' };` then for each ADR × kind × dir, `if (dir === 'out' && a.statusToken !== 'Accepted') continue;` (:365 — the Proposed-is-prospective carve-out…
   [PARTIAL] Rule 4 — The declared id matches the filename (README:79-81) :: scripts/gen-adr-index.cjs:216-217 `const declaredIdMatch = h1.match(/^ADR[-\s]?0*(\d+)\b/i); const declaredId = declaredIdMatch ? canonicalId(declaredIdMatch[1]) : null;` then :290-292 `if (a.declaredId && a.declaredId !…
   [NO] Rule 4b — H1 status bracket agrees with the Status field (implied by README:14 'do not leave the label lying') :: scripts/gen-adr-index.cjs:213 `.replace(/\s*\[(?:Proposed|Accepted|Superseded|Legacy|Retired)\]\s*$/i, '')` — the bracket is STRIPPED for display and never compared to the Status field. Live contradiction: docs/adr/857-c…
   [NO] Rule 5 — The ratification bar: mechanism exists in tree; owning issue closed as completed; no material part unshipped; no later ADR supersed… :: No code exists. `grep -n 'Ratification\|ratif' scripts/gen-adr-index.cjs` returns only the symmetry comment at :348 ('Only a RATIFIED (Accepted) claimant is owed the back-link') — the script never reads a `## Ratificatio…
   [YES] (Undocumented but enforced) Filename matches `<issue#>-<kebab-slug>.md` :: scripts/gen-adr-index.cjs:114 `const ADR_FILENAME_RE = /^[0-9]+-[a-z0-9-]+\.md$/;`, partitioned at :132-137, errored at :274-280 with 'filename does not match the `<issue#>-<kebab-slug>.md` convention, so it cannot appea…
   [YES] (Undocumented but enforced) Every relation link resolves to a real file in docs/adr/ :: scripts/gen-adr-index.cjs:313-314 `for (const l of rel.links) { if (!byFile.has(l)) add(a.file, `"${rel.field}" links "${l}", which does not exist in docs/adr/.`); }`. Scope: relation fields only — ordinary body links ar…
   [YES] Index freshness — the committed index equals the generated index (README:101-113) :: scripts/gen-adr-index.cjs:503-512 — `--check` reads README.md, re-splices the rendered index via `spliceIntoReadme`, and `if (expected !== readme) … throw new ExitError(1)` with 'docs/adr/README.md index is stale. Run: n…

SUMMARY: `node scripts/gen-adr-index.cjs --check` passes at HEAD aa0f7dee (`docs/adr/README.md index is up to date (66 ADRs).`, exit=0), and it is genuinely reachable in CI: `package.json:114` `lint:generated-sync` chains it 9th of 10 `&&`-joined generators, `lint:ci` (`package.json:106`) composes that, and `.github/workflows/test.yml:112` runs `npm run lint:ci` in an ungated `lint-tests` job on every push/PR to `next`/`main`. The generator is a real, well-tested gate (31 tests in `tests/adr-index-gate.test.cjs`) — but it is narrower than `docs/adr/README.md:46` claims ("These are enforced by scripts/gen-adr-index.cjs"). Of the five documented lifecycle rules, two are fully enforced (status vocabulary; supersession/subsumption symmetry), two are partial (bare-id ban applies only inside four relation fields in the header block; id-matches-filename is opt-in — 27 of 66 ADRs declare no id in their H1 and are silently exempt), and one — the ratification bar — has zero code behind it. The README's admitted historical failure IS fixed at HEAD: ADR-1239's four subsumption claims all carry reciprocals (`1016:10`, `58:6`, `3660:7`, `894:8`). The residue shows up where the gate does not look: `docs/adr/1213:7` and `docs/adr/1610:121,131` carry genuinely ambiguous bare `ADR-0011`/`ADR-0010` references; ADR-857's H1 still reads `[Proposed]` while its Status says `Accepted — ratified 2026-07-17`, and the generator strips the bracket rather than flagging the contradiction. One dead relative link exists inside docs/adr (`1239:99`), plus two broken inbound `docs/adr/` paths from shipped source and contributor docs — the repo has no markdown link checker at all.

==============================================================================
## AGENT: abb8ea20c94d7b5b3
==============================================================================

[high] (lacuna) 1. No ADR decides how GSD's prompts — its actual product — are evaluated. 34 agent prompts and 91 workflow files ship with zero governing decision on quality measurement, regression detection, or what counts as a prompt improvement.
   EV: `ls agents/*.md | wc -l` = 34; `ls gsd-core/workflows/*.md | wc -l` = 91. Absence greps over docs/adr/*.md: `grep -rilE 'eval(uation)? harness|prompt eval|golden prompt|regression corpus|A/B'` → 0 files. `grep -rilE '\beval\b'` → 2 files (1671, 857), neither about prompt evaluation. An eval subsystem exists in code with no ADR: src/eval.cts (3.2K, header comment 'Deterministic eval scoring verb (#…
   REC: Write the missing ADR: what the eval corpus is, what a prompt regression is, and what gate (if any) a prompt change must pass. The `gsd-eval-auditor`/`gsd-eval-planner` pair and src/eval.cts already encode a de-facto scoring model (coverage_score / infra_score / overall_score with ok|partial|missing…

[high] (lacuna) 2. INTERNAL — ADR-2008 lets a capability ship an arbitrary shell command that runs at a gate point, and ADR-1244 binds capability trust to a disclosure signature that does not include gate commands. Together they imply a decision nobody made: a capability upgrade can add or retarget a `sh -c` command w…
   EV: ADR-2008:52-63 declares `check.predicate.kind: 'command-exit-zero'` with a free-form `command` string (≤4096 chars), executed via `sh -c` inheriting process env (ADR-2008:75-77). ADR-2008:112-117 dismisses the trust question: 'installing one already trusts it to ship skills, agents, and hooks that run arbitrary code. `command-exit-zero` is therefore **not a new trust boundary**.' But ADR-1244 D5 i…
   REC: Either (a) add gate predicate commands to `discloseExecutableSurfaces` + `disclosureSignature` so an added/changed gate command forces re-consent, and amend ADR-2008's trust section to cite ADR-1244 D5 rather than asserting blanket trust; or (b) write the ADR that consciously exempts them. Today the…

[high] (lacuna) 3. There is no compatibility contract for `.planning/` artifacts across GSD versions. A version field exists in the STATE.md template and is faithfully written and preserved — and read by nothing.
   EV: `gsd-core/templates/state.md:11` contains `gsd_state_version: '1.0' # placeholder; syncStateFrontmatter overwrites on first state.* call`. Every consumer is a writer or a preserver, never a comparator: src/state.cts:1706 hardcodes `const fm = { gsd_state_version: '1.0' }`; src/state-transition.cts:1199 preserves `existingFm['gsd_state_version'] || '1.0'`; src/state-transition.cts:80 classifies it …
   REC: Decide the contract before it is forced by an incident: does a newer GSD read an older `.planning/`? Does an older GSD refuse a newer one? Is there a `.planning/` migrator, and does it run at install (ADR-0008's seam) or at first command? ADR-612's bracket migrator is the one ad-hoc precedent — gene…

[high] (lacuna) 4. The concurrency model for `.planning/` is undecided. Sophisticated cross-process locking machinery — pid-liveness probing, lock stealing, a deadman ceiling — was built and revised under audit pressure with no ADR governing it.
   EV: src/planning-workspace.cts implements `withPlanningLock` (:192) over a `.planning/.lock` file with: a module-scope `_heldPlanningLocks` Set for exit cleanup (:32-35), a pid-liveness probe seam added by 'audit M1' (:41-65), a pid-reuse-alias hazard note (:197-200, 'the .lock body has no startTime'), a lock-steal path with a test hook (:270 `_planningLockTestHooks.beforeSteal`), a deadman ceiling mi…
   REC: Write the concurrency ADR. It must answer: may two GSD sessions drive one `.planning/` at all? Is the lock advisory or authoritative? When is stealing correct? What is the guarantee under a killed process, an NFS/DrvFS filesystem, and a Windows host? This is urgent because ADR-0004 also ships *works…

[high] (lacuna) 5. INTERNAL — ADR-1244 keys capability consent on `realpath(projectRoot)` while ADR-0004 makes git worktrees a first-class working mode. Neither ADR says what happens when the same repo is checked out at two paths: consent granted in the main clone silently does not apply in a worktree.
   EV: ADR-1244 D5 item 8 (1244:130): the consent store 'lives at `${GSD_HOME||homedir()}/.gsd/consent.json` — outside any repository — keyed by `(realpath(projectRoot), id)`... Before activating a project-scope overlay (its declarative loop surfaces **and** its command dispatch) the loader requires a matching record on **this machine**; without it the capability is discovered-but-inactive.' Implementati…
   REC: Decide it explicitly. Options: key consent on the worktree's *common* git dir (via ADR-0004's `resolveWorktreeRoot`) so one grant covers all worktrees of a repo; or keep per-path keying and make the discovered-but-inactive state loud rather than silent. The failure mode is quiet and confusing — a ca…

[high] (lacuna) 6. INTERNAL — ADR-1239 (EoS) asserts an embedded host gets 'the same consent guarantees GSD already enforces', but ADR-1244's consent is obtained by an interactive install-time prompt that the embedded path does not have. No ADR says who grants consent when there is no `npx` install.
   EV: ADR-1239:186 describes the imperative embedding path: a thin plugin that 'on init calls the engine's `loadRegistry({ includeInstalled: true })` as a library, composing first-party ∪ installed capability overlays with the **same** precedence, consent, and fail-closed-gate guarantees GSD already enforces.' ADR-1244 D5 item 2 (1244:124) locates that consent at install time and requires it be interact…
   REC: Decide the embedded consent channel before the OpenCode/pi-console plugin ships: does the host prompt on GSD's behalf (and how is the disclosure rendered)? Is a pre-seeded `~/.gsd/consent.json` the supported answer? Or do embedded deployments run first-party-only? ADR-1239's one-line assertion is cu…

[medium] (lacuna) 7. Deprecation policy for commands and config keys has been explicitly deferred by two separate ADRs and never written. Both deferrals name it as owed work; neither has an owner.
   EV: ADR-894:268 (Open Questions): 'Point/role-set deprecation policy once third-party capabilities exist (additive-only holds until then; a rename/removal needs a major bump + deprecation window — deferred with third-party loading per ADR-857).' That precondition has since been met — ADR-1244 shipped third-party loading and is Accepted (README.md index). ADR-1239:210: 'Interface versioning/deprecation…
   REC: Write the policy ADR: how long a deprecated command or config key keeps working, what warning it emits and through which channel (the CLI JSON envelope? a workflow-visible notice?), and what a major bump obligates. ADR-894's own precondition ('once third-party capabilities exist') has been satisfied…

[medium] (lacuna) 8. There is no decision on telemetry, user-data collection, or network egress — despite the question being raised and deferred in five separate ADRs, and despite GSD performing network I/O on the user's behalf in at least two subsystems.
   EV: Five deferrals, no decision: ADR-0010-skill-surface:139 ('Whether telemetry (opt-in) is worth proposing to inform where the `standard` profile line goes. Without it, the cut points are author-intuition. Track separately; not a blocker.'); ADR-0011-skill-surface:65 ('Whether telemetry... should be added to the surface engine or deferred.'); ADR-0010-file-op:101 (per-op telemetry 'should be required…
   REC: Decide the posture even if the decision is 'none, ever' — that is a valuable, citable decision for a tool installed into users' home directories. State what leaves the machine, under whose initiative, and what the `GSD_AUDIT` trace may contain. Five ADRs deferring the same question is the corpus tel…

[medium] (lacuna) 9. The error-taxonomy and exit-code contract is orphaned: the only ADR that closed the error-kind enum is Superseded, and its successor amends one field rather than restating the contract. Nothing governs process exit codes at all.
   EV: ADR-0012:27 established the closed enum and its amendment discipline — 'the six error kinds (UnknownCommand, InvalidArgs, HandlerRefusal, HandlerFailure, SdkLoadFailed, SdkDispatchFailed) are exported as a frozen ERROR_KINDS object... Adding a new error kind requires amending this ADR' (:37: 'requires updating ERROR_KINDS... AND amending this ADR'). ADR-0012 is now Superseded by ADR-0174 (docs/adr…
   REC: Reseat the error-kind enum and its amendment rule in an Accepted ADR (natural home: ADR-0174 as a dated section, or a new one), and decide the CLI exit-code contract that docs/json-errors.md currently documents without a governing decision. Right now the corpus's own rule 'do not cite Superseded ADR…

[medium] (lacuna) 10. No decision governs LLM provider abstraction or model drift: what happens when a model named in the catalog is retired by its provider, how a new provider is onboarded, or how model-version pinning interacts with reproducibility.
   EV: `grep -rilE 'model drift|model retire|provider abstraction'` over docs/adr/*.md → 0 files. ADR-0003 (Model Catalog Module) is 52 lines and decides source-of-truth location only; its sole degradation rule is a fallback shape, not a policy: ':51 — unknown-agent fallback follows profile semantics (quality→opus, budget→haiku, etc.), not a hardcoded sonnet'. There is no rule for an unknown *model*. ADR…
   REC: Write the model/provider-drift ADR: how the catalog is kept fresh, what a user sees when their configured model 404s, whether GSD pins or floats model ids, and what obligation a provider sunset creates. GSD ships across 15+ runtimes and its central value proposition is routing work to models — this …

[medium] (lacuna) 11. Determinism and reproducibility of agent output is not decided anywhere, even though the corpus repeatedly treats determinism as a virtue in the deterministic (JS) layer and once cites it as a benefit of an LLM feature.
   EV: `grep -rilE 'determinism of agent|reproducib.*agent output|non-determinis.*agent'` over docs/adr/*.md → 0 files. The word 'determinis*' appears in 22 files but always about the code layer or test scheduling: ADR-456 makes deterministic scheduling a test-architecture requirement (456:14, injectable clock seams); ADR-1990 decides 'deterministic repo-state detection'; src/eval.cts's own header says '…
   REC: Decide the stance: is GSD's output expected to be reproducible run-to-run, and if not, what does that mean for the verification gates (ADR-1606, ADR-550, ADR-22) that compare agent output against a spec? The corpus has invested heavily in making the deterministic 10% deterministic; it has said nothi…

[medium] (enforcement-gap) 12. Lifecycle rule 2 ('cross-references are file links, never bare ids') is enforced only inside relation fields, so the exact ambiguity the rule exists to prevent — bare ADR-0010 / ADR-0011 — is live in the corpus with CI green.
   EV: `node scripts/gen-adr-index.cjs --check` → 'docs/adr/README.md index is up to date (66 ADRs).' exit=0. Bare-id scanning is scoped to relation-bearing fields only (scripts/gen-adr-index.cjs:238,245,250-252; checked at :318+); body prose is never scanned. Live violations of the ambiguous case, found by `grep -rnE '(^|[^\[])ADR-00?1[01]\b' docs/adr/*.md | grep -v ']('`: docs/adr/1213-capability-state…
   REC: Extend `bareAdrRefs` scanning to the full ADR body, with a targeted first ratchet: fail only on bare ids whose `byId` bucket has more than one candidate (i.e. 0010 and 0011). That catches every genuinely ambiguous reference without a corpus-wide reflow, and it is the subset the rule was actually wri…

[medium] (lacuna) 13. INTERNAL — ADR-1610 and ADR-0011 impose byte and surface budgets on first-party prompt artifacts, and ADR-1244 lets third-party capabilities ship the same artifact kinds. No ADR says whether third-party artifacts are subject to those budgets.
   EV: ADR-1610 sets 'per-file byte baseline + tier hard caps' on workflows and agents and is Accepted; `grep -cin 'capabilit' docs/adr/1610-workflow-agent-size-budget-ratchet.md` → 0. ADR-0011-skill-surface owns 'install-time skill/agent profile staging' and its only capability reference is the subsumption pointer at :7 ('Subsumed by: ADR-857'). ADR-1244 D5 grants third parties full artifact parity: ':4…
   REC: Decide whether the eager-context budget is a *repo hygiene* rule or a *product invariant*. If the latter, the surface engine (`applySurface`, src/surface.cts) needs to account for capability-contributed artifacts at install/surface time, since the whole justification for ADR-0011 is capping eager sy…

[medium] (lacuna) 14. Installer migrations are forward-only by construction and the release model actively encourages version oscillation via an RC dist-tag, but no ADR decides downgrade semantics.
   EV: ADR-0008:6 frames migrations as one-directional: 'Installer upgrade behavior must be represented as versioned migration records that produce a dry-run plan before applying changes'; :56 lists a record's fields as 'description, introduction version, explicit install scopes, destructive status' — an introduction version, no removal or reversal version. `grep -inE 'downgrade|revert|undo' docs/adr/000…
   REC: Decide what `npx @opengsd/gsd-core@1.6.0` does over a tree previously installed by 1.7.0-next: refuse, warn, or best-effort. Pair this with the `.planning/` compatibility finding — the two together are the full 'user reverted' story, and today neither half is decided.

[medium] (lacuna) 15. Supply-chain and dependency policy exists, but outside the ADR corpus, in a Draft v0.1.0 org document whose declared scope is repo CI — not the payload GSD writes into users' home directories.
   EV: `grep -rilE 'supply.?chain'` over docs/adr/*.md → 0 files. The policy lives at docs/security/baseline.md §2.1 'Dependency integrity' (:32-63: 'All third-party dependencies must be pinned via a lock file', `npm audit --audit-level=high`, SLSA L2+ provenance reasoning at :43). Its own header marks it unratified and stale-prone: 'RFC status | Draft', 'Version | 0.1.0', 'Last updated | 2026-05-22' (ba…
   REC: Either promote the dependency-integrity control into an Accepted ADR (so it is citable and lifecycle-governed like every other decision) or add a pointer from the corpus to baseline.md and reconcile the two integrity stories — ADR-1244's SHA/integrity gate and baseline.md's lockfile/audit gate are s…

[medium] (lacuna) 16. No decision governs the agent return contract, even though 8 shipped agents use ALL-CAPS H2 completion markers as the surface orchestrators parse to route the next step.
   EV: `grep -rlE '^## [A-Z ]*COMPLETE' agents/*.md | wc -l` → 8. Corpus absence: `grep -rinE 'PLANNING COMPLETE|completion marker|H2 marker'` over docs/adr/*.md → 0 relevant hits (the 8 raw matches are all the unrelated word 'sentinel': ADR-0008:32 drift sentinel, ADR-1239:99/119/139/141 the `undocumented` axis sentinel, ADR-1671:111 duplicate predicate ids, ADR-2121:21 and ADR-612:132/170 phase-id rese…
   REC: Write the return-contract ADR, and use it to settle whether markers are the contract or a vestigial second mechanism. This is a cheap, high-leverage decision: it is the single interface between the 91 workflows and the 34 agents, and it is currently convention held in prose across 34 files.

[medium] (lacuna) 17. Translation freshness is undecided and the shipped translations have drifted badly — up to 98% missing — while the only written rule covers a different set of files.
   EV: `grep -rilE 'i18n|internationali|locale'` over docs/adr/*.md → 0 files. Four full locale trees ship: docs/ja-JP/, docs/ko-KR/, docs/pt-BR/, docs/zh-CN/, each containing ARCHITECTURE/COMMANDS/CONFIGURATION/FEATURES/USER-GUIDE plus explanation/how-to/reference/tutorials subtrees. Measured drift (bytes, en vs locale): FEATURES.md en=180,999 / pt-BR=3,935 (2.2%) / ko-KR=93,219 (52%); CONFIGURATION.md …
   REC: Decide the policy and make it visible to readers: either add a staleness banner/last-synced marker to each translated page, or scope the shipped translation set down to what is actually maintained. A 2%-complete pt-BR FEATURES.md that looks like documentation is worse for a user than an honest point…

[low] (lacuna) 18. Performance is decided only as a static prompt-byte budget. There is no decision on runtime latency or per-run token cost as an architectural constraint.
   EV: The two budget ADRs are both about bytes-at-rest: ADR-1610 ('per-file byte baseline + tier hard caps' on workflow/agent files) and ADR-0011-skill-surface (install-time surface curation; the cost model is quantified only incidentally, e.g. ADR-3660:21 'roughly 2.7k tokens per session on a measured workstation'). `grep -rinE 'token budget|token-cost budget'` over docs/adr/*.md → 2 hits, both in ADR-…
   REC: Low priority as a standalone ADR, but note the dependency: a latency or token-cost budget cannot be enforced without the measurement decision in the telemetry finding. If telemetry is decided 'never', then say so here too — 'we do not budget runtime cost because we cannot measure it' is a legitimate…

[low] (lacuna) 19. Accessibility of CLI output is entirely undecided for a tool whose primary interface is terminal text across 15+ hosts and three OSes.
   EV: `grep -rilE 'a11y|accessib|NO_COLOR|screen reader|color.?blind'` over docs/adr/*.md → 0 files. (An unanchored `accessib|TTY` grep returns 3 files — 0009, 1143, 3524 — all on `probeTty`/terminal-detection plumbing, not accessibility.) The corpus does decide cross-platform *portability* thoroughly (ADR-1703, AST ESLint rules; ADR-0009, shell command projection) — the machinery to enforce an output-a…
   REC: Cheapest useful version: decide that CLI output must be meaningful without color (honour `NO_COLOR`, never encode state in color alone) and that structured output remains available via the existing `--json` / `--raw` / `--pick` surface. This is a one-paragraph ADR that ADR-1703's existing rule harne…

-- ENFORCEMENT MATRIX --
   [YES] Lifecycle rule 1 — every ADR declares one status from the canonical vocabulary {Accepted, Proposed, Superseded, Retired, Legacy} :: scripts/gen-adr-index.cjs:55 defines STATUSES; validate() adds an error at :283 ('no `- **Status:** <Token>` field found') and :286-288 ('status "X" is not one of ...'). Wired to CI via package.json scripts.lint:generate…
   [YES] Lifecycle rule 1, obligations column — `Superseded` MUST name the successor as a file link :: scripts/gen-adr-index.cjs:292-306: if statusToken === 'Superseded' and relations.supersedes.in has zero links, it errors, and distinguishes the bare-id case ('mentions ADR-N but not as a markdown link').
   [NO] Lifecycle rule 1, obligations column — `Retired` must 'say what was removed and when'; `Legacy` must 'say why it is frozen' :: The Superseded branch at scripts/gen-adr-index.cjs:292 is the only status-conditional obligation in validate(). No branch tests statusToken 'Retired' or 'Legacy'. `grep -n "Retired" scripts/gen-adr-index.cjs` yields only…
   [PARTIAL] Lifecycle rule 2 — cross-references to other ADRs are file links, never bare ids :: Bare-id detection (bareAdrRefs) is applied ONLY inside relation-bearing fields — the Status line and `Supersedes`/`Subsumes`/`Superseded by`/`Subsumed by` fields and sections (scripts/gen-adr-index.cjs:238,245,250-252, c…
   [PARTIAL] Lifecycle rule 3 — Supersedes/Subsumes relations are symmetric; if A declares a relation toward B, B must record the reciprocal (out-directi… :: scripts/gen-adr-index.cjs:355-381 implements the two-direction symmetry check with the documented ratification guard at :365 (`if (dir === 'out' && a.statusToken !== 'Accepted') continue;`). It is correct — but it only s…
   [YES] Lifecycle rule 4 — the id declared in the H1 matches the filename prefix :: scripts/gen-adr-index.cjs:289-291: `if (a.declaredId && a.declaredId !== a.fileId) add(..., 'H1 declares ADR-X but the filename says Y')`. Note the guard `a.declaredId &&` — an H1 with no id at all (e.g. docs/adr/0001-di…
   [YES] Filename convention — new ADRs use `<issue#>-<kebab-slug>.md` :: scripts/gen-adr-index.cjs:127-135 collects non-conforming filenames; validate() :274-279 errors: 'filename does not match the `<issue#>-<kebab-slug>.md` convention, so it cannot appear in the index.'
   [YES] 'The index is generated. Do not hand-edit it.' — index staleness fails the build :: package.json scripts.lint:generated-sync chains `node scripts/gen-adr-index.cjs --check`. Verified live: `node scripts/gen-adr-index.cjs --check` → 'docs/adr/README.md index is up to date (66 ADRs).' exit=0.
   [NO] 'Ratifying a stale Proposed' — all four bars must hold (mechanism exists in tree; owning issue closed as completed; no material part unshipp… :: None of the four bars is machine-checkable and none appears in scripts/gen-adr-index.cjs (no GitHub API call, no tree-existence check; the script's only inputs are the ADR files themselves — see the parse loop at :201-25…

SUMMARY: ## Corpus taxonomy — 66 ADRs at HEAD aa0f7dee (`ls docs/adr/*.md | wc -l` = 67 incl. README; generator reports "66 ADRs") Status split from the generated index (`docs/adr/README.md:119,176,192`): **Active/Accepted 50 · Proposed 9 · Superseded/Retired/Legacy 7**. Each ADR assigned one primary bucket (sums to 66): | # | Category | Count | Members (abbrev.) | |---|---|---|---| | 1 | **Module-ownership / seam** ("Module X owns seam Y") | **14** | 0001, 0002, 0003, 0004, 0005*, 0007*, 0009, 0010-file-op*, 0012*, 0656, 1372, 2143, 2346, 3524* | | 2 | **Capability system & ecosystem** | **9** | 857, 894, 959, 1016, 1143, 1213, 1235, 1244, 2008 | | 3 | **Runtime integration / multi-runtime projection** | **7** | 0174, 766, 1239, 1508, 1593, 1866, 3660 | | 4 | **State & planning artifacts** | **6** | 0006, 612, 1769, 1817, 2121, 2207 | | 5 | **Prompt-layer: spec/probe/verification/context** | **6** | 15, 22, 550, 1606, 1671, 1820 | | 6 | **Quality / lint / test** | **5** | 452, 456, 1610, 1703, 2264 | | 7 | **Product-UX / config surface** | **5** | 0011-review, 0011-review-prd*, 1517, 1787, 1990 | | 8 | **Install / upgrade / surface budget** | **4** | 0008, 0010-skill*, 0011-skill, 58 | | 9 | **Release / CI / branching** | **4** | 218, 230, 415, 660 | | 10 | **Security** | **2** | 1577, 2164 | | 11 | **Cross-cutting principles** | **2** | 227, 1411 | | 12 | **LLM/model routing** | **1** | 443 | | 13 | **Build / codegen** | **1** | 457 | (* = Superseded/Legacy.) **Shape of the corpus.** 30 of 66 (45%) are buckets 1+2 — "who owns this seam" and "how does the capability plug-in system work." Add bucket 3 and it is 37/66 (56%) internal-architecture ownership. This is a corpus written by people refactoring an engine, and it is excellent at that. **What the shape reveals.** The corpus is *inward*-facing. Categories that a mature, widely-installed, multi-runtime developer tool decides for its **users** are thin or empty: security 2, product-UX 5, LLM/model 1, and **zero** for priva…

==============================================================================
## AGENT: a32c5f3d2425d884d
==============================================================================

[high|PRIMARY_VERIFIED] 1. MADR 4.0.0 mandates three sections — Context and Problem Statement, Considered Options, Decision Outcome — and treats Decision Drivers, Consequences, Confirmation, Pros and Cons, and More Information as explicitly optional. Metadata (status, date, decision-mak…
   SRC: https://raw.githubusercontent.com/adr/madr/develop/template/adr-template.md
   QUOTE: From the fetched template verbatim: `status: "{proposed | rejected | accepted | deprecated | … | superseded by ADR-0123}"` / `decision-makers: {list everyone involved in the decision}` / `## Context and Problem Statement` / `## Considered Options` / `## Decision Outcome`, with `<!-- This is an optio…
   GSD NOW: GSD uses none of MADR's headings. Measured across all 66 ADR files in /home/dave/repos/gsd-core/docs/adr/: `## Context and Problem Statement` 0/66, `## Considered Options` 0/66, `## Decision Outcome` 0/66, `## Decision Drivers` 0/66, `## Confirmation` 0/66, `## Pros and Cons` 0/66. GSD's own shape i…
   REC: Do NOT migrate to MADR wholesale — GSD's headings are CI-enforced and MADR's are not, so a migration would trade a checked convention for an unchecked one. Instead: (1) add an `Alternatives considered` (or `Rejected alternatives`) section to the required set and enforce it in scripts/gen-adr-index.cjs validate() the same way Status is enf…

[high|PRIMARY_VERIFIED] 2. MADR's optional `Confirmation` element asks each ADR to state how compliance with the decision will be checked, explicitly naming automated fitness functions. MADR notes it is the optional element most often actually used.
   SRC: https://raw.githubusercontent.com/adr/madr/develop/template/adr-template.md
   QUOTE: "### Confirmation — {Describe how the implementation / compliance of the ADR can/will be confirmed. Is there any automated or manual fitness function? If so, list it and explain how it is applied. … E.g., a design/code review or a test with a library such as ArchUnit can help validate this. Note tha…
   GSD NOW: GSD has NO `Confirmation` heading anywhere (0/66) and only 4 files with any Validation/Acceptance-criteria heading (docs/adr/0011-review-default-reviewers-prd.md:145, docs/adr/2264-golden-parity-redesign.md:62, docs/adr/959-capability-command-contribution.md:105). BUT the underlying practice is pres…
   REC: Add an optional `- **Confirmation:** [tests/adr-NNN-*.test.cjs](…)` header field, teach scripts/gen-adr-index.cjs to parse it (the RELATION_FIELDS machinery at scripts/gen-adr-index.cjs:74 already does exactly this shape of parse), and render a `Confirmed by` column in the Active decisions table. Backfill it for the 31 ADRs that already n…

[medium|PRIMARY_VERIFIED] 3. Nygard's original formulation: five sections (Title, Context, Decision, Status, Consequences); status is `proposed` then `accepted`; a reversed decision keeps its file and is marked `deprecated` or `superseded` with a reference to the replacement; numbering is…
   SRC: https://www.cognitect.com/blog/2011/11/15/documenting-architecture-decisions
   QUOTE: "A decision may be 'proposed' if the project stakeholders haven't agreed with it yet, or 'accepted' once it is agreed." / "If a later ADR changes or reverses a decision, it may be marked as 'deprecated' or 'superseded' with a reference to its replacement." / "If a decision is reversed, we will keep …
   GSD NOW: GSD's status vocabulary (scripts/gen-adr-index.cjs:55: `['Accepted', 'Proposed', 'Superseded', 'Legacy', 'Retired']`) is Nygard's core plus two GSD-specific terms, and the Superseded obligation is CI-enforced (docs/adr/README.md:48-77). GSD deliberately abandons monotonic numbering for issue#-prefix…
   REC: Do not chase a byte cap — GSD's largest ADRs (857, 1239, 550) are genuine multi-part architecture decisions and cutting them would destroy load-bearing content. Instead, apply Azure WAF's documented remedy verbatim ("Break one decision into multiple if an architectural decision is going to result in multiple phases") and Fowler's ("provid…

[low|PRIMARY_VERIFIED] 4. log4brains explicitly rejects sequential ADR file numbering because it causes git merge conflicts — the same failure GSD's issue#-prefix convention was invented to solve.
   SRC: https://raw.githubusercontent.com/thomvaill/log4brains/master/README.md
   QUOTE: "No required file numbering schema (i.e., `adr-0001.md`, `adr-0002.md`...): avoids git merge issues" — and separately "an ADR is immutable. Only its status can change."
   GSD NOW: docs/adr/README.md:16-28 documents the issue#-prefix convention and its rationale: "Two developers computing 'next ADR number' locally against `main` will independently pick the same integer and both ship. The collision is already on disk — `0010-*` exists twice and `0011-*` exists three times. GitH…
   REC: No change needed. This is worth writing up externally — the issue#-prefix convention is a concrete improvement on both Nygard (collides) and log4brains (no citable id), and the ADR corpus already documents the evidence. Consider a short post or an upstream PR to adr.github.io's templates/tooling guidance. If anything, tighten the README t…

[medium|PRIMARY_VERIFIED] 5. adr-tools (Nygard's reference implementation, npryce/adr-tools) provides an authoring and navigation CLI: `adr init`, `adr new` (`adr new -s N` creates a superseding ADR AND flips ADR N's status automatically), `adr link` (arbitrary labeled cross-links), `adr …
   SRC: https://github.com/npryce/adr-tools/tree/master/src
   QUOTE: Fetched src/ listing verbatim: `_adr_add_link`, `_adr_generate_graph`, `_adr_generate_toc`, `_adr_links`, `_adr_remove_status`, `_adr_status`, `adr`, `adr-config`, `adr-generate`, `adr-help`, `adr-init`, `adr-link`, `adr-list`, `adr-new`, `adr-upgrade-repository`. README: "To create a new ADR that s…
   GSD NOW: scripts/gen-adr-index.cjs does index generation (`--write`) and validation (`--check`, wired into `lint:generated-sync` in package.json, which itself runs in the `ci` script). It has NO authoring command (no `--new`, no `--supersede`), and NO graph output. Consequently the two mechanical steps adr-t…
   REC: Add a `--supersede <target-file>` / `--subsume <target-file>` authoring mode to scripts/gen-adr-index.cjs that writes BOTH sides of the relation, mirroring `adr new -s`. This directly prevents the class of bug the corpus already hit and that validate() currently only detects after the fact. Skip `generate graph` for now — with 66 ADRs and…

[high|PRIMARY_VERIFIED] 6. Mainstream ADR guidance is unanimous that an accepted ADR is immutable: changing a decision means writing a NEW record that supersedes the old one, not editing the old one in place.
   SRC: https://docs.aws.amazon.com/prescriptive-guidance/latest/architectural-decision-records/adr-process.html
   QUOTE: AWS: "When the team accepts an ADR, it becomes immutable. If new insights require a different decision, the team proposes a new ADR." and "The team should treat ADRs as immutable documents after the team accepts or rejects them. Changes to an existing ADR requires creating a new ADR…" / Azure WAF: "…
   GSD NOW: GSD runs the opposite convention, and states it as policy: docs/adr/README.md:5 — "ADRs are append-only. Amendments extend existing ADRs with a dated section rather than replacing them." In practice 19 of 66 ADRs carry `## Amendment (date)` / `## Addendum (date)` / `## Update — date` sections, e.g. …
   REC: Two changes. (1) Write a meta-ADR that records the append-in-place convention as a deliberate decision against the AWS/Azure/log4brains consensus, with the agent-readability rationale — right now docs/adr/README.md:5 asserts it with no justification, which is exactly the gap ADRs exist to close. (2) Draw a bright line the convention curre…

[high|PRIMARY_VERIFIED] 7. There is no recognised ADR practice of "ratification" — flipping Proposed→Accepted on the basis of verified shipped evidence. The closest published mechanisms are: Cosmos SDK's time-boxed `LAST CALL yyyy-mm-dd` status (social consensus, not evidence), CometBFT…
   SRC: https://raw.githubusercontent.com/cosmos/cosmos-sdk/main/docs/architecture/PROCESS.md
   QUOTE: Cosmos SDK PROCESS.md status set verbatim: DRAFT → PROPOSED → LAST CALL yyyy-mm-dd → ACCEPTED/REJECTED → SUPERSEDED, with optional ABANDONED; "LAST CALL <date>" = "Signals near-consensus among maintainers; allows community response period"; ACCEPTED = "currently implemented **or to be implemented** …
   GSD NOW: docs/adr/README.md:83-99 defines "Ratifying a stale `Proposed`" with a four-part evidentiary bar: (1) the decided mechanism demonstrably exists in the tree, named files/symbols/tests; (2) the owning issue is closed AS COMPLETED — with an explicit trap that a `stateReason` of not-planned/duplicate me…
   REC: Do not change the bar. Do two things to harden and share it: (1) the bar currently lives only as README prose, so it is unenforceable and re-derivable only by a careful reader — encode it as a `## Ratification` section template with the four criteria as checkboxes, and have scripts/gen-adr-index.cjs require that any ADR whose Status match…

[low|PRIMARY_VERIFIED] 8. GSD's `Subsumes` / `Subsumed by` relation — a broader ADR reframing a still-live narrower one without killing it — has no counterpart in any surveyed template or tool. Every one models only supersession/deprecation.
   SRC: https://adr.github.io/adr-templates/
   QUOTE: MADR status enum verbatim: "{proposed | rejected | accepted | deprecated | … | superseded by ADR-0123}" — no subsumption. Nygard: "marked as 'deprecated' or 'superseded' with a reference to its replacement" — binary. Cosmos SDK: DRAFT/PROPOSED/LAST CALL/ACCEPTED/REJECTED/SUPERSEEDED/ABANDONED — no s…
   GSD NOW: scripts/gen-adr-index.cjs:74 RELATION_FIELDS defines four fields across two distinct relation kinds (`supersedes`/`superseded by`, `subsumes`/`subsumed by`), with the code comment: "Both directions are symmetry-checked, but only `supersedes` implies a status change on the target. Collapsing subsumpt…
   REC: No change to the mechanism. Two small things: (1) `Read first` is a good reader-facing label but it hides the relation name — consider `Read first (subsumed by)` or a footnote, since a reader who has read the ADR literature will look for the word. (2) Like the ratification bar, this is externally novel and worth contributing upstream — an…

[high|PRIMARY_VERIFIED] 9. Published criteria for when a decision warrants an ADR: "architecturally significant" decisions affecting structure, non-functional requirements, dependencies, interfaces, or construction techniques (Richards & Ford, adopted verbatim by AWS Prescriptive Guidan…
   SRC: https://docs.aws.amazon.com/prescriptive-guidance/latest/architectural-decision-records/adr-process.html
   QUOTE: AWS: "Project members should create an ADR for every architecturally significant decision that affects the software project or product, including the following (Richards and Ford 2020): Structure (for example, patterns such as microservices); Non-functional requirements (security, high availability,…
   GSD NOW: GSD has a rigorous PROCESS gate but no SIGNIFICANCE criteria. CONTRIBUTING.md:87-107 defines the full workflow (open issue → wait for approval label → issue# becomes filename prefix → one issue = one ADR = one PR) but its only definition is CONTRIBUTING.md:89 — "An ADR (Architecture Decision Record)…
   REC: Add a "When does a decision warrant an ADR?" section to docs/adr/README.md adopting the AWS/Richards-Ford five categories plus Azure's difficult-to-reverse test, and PROMOTE ADR-3660:163's addendum rule to a corpus-wide granularity rule: a change that rides an established, already-decided seam is an amendment to the owning ADR, not a new …

[medium|PRIMARY_VERIFIED] 10. Cosmos SDK draws an explicit ADR-vs-RFC line: an ADR answers a specific question that can already be articulated; an RFC explores a topic to find out what the questions are, and any decisions it yields must still be recorded separately as ADRs.
   SRC: https://raw.githubusercontent.com/cosmos/cosmos-sdk/main/docs/rfc/README.md
   QUOTE: "if you can articulate a specific question that needs to be answered, write an ADR. If you need to explore the topic and get input from others to know what questions need to be answered, an RFC may be appropriate." and an RFC "may give rise to more specific architectural decisions for the Cosmos SDK…
   GSD NOW: GSD has THREE decision-document surfaces and a routing rule for only one boundary. docs/adr/ (66 files), docs/prd/ (4 files: 1244-capability-ecosystem.md, 209-readme-continuity-release-update.md, 3524-cjs-sdk-hard-seam.md, README.md), and docs/proposals/ (e.g. mempalace-capability-prd-adr.md). CONTR…
   REC: Add a three-line routing rule to docs/adr/README.md and CONTRIBUTING.md:89 adopting Cosmos's test: specific answerable question → ADR; exploration to find the questions → PRD/proposal, which must yield an ADR if it produces a decision. Then state what docs/proposals/ is (or fold it into docs/prd/), and state explicitly which surface wins …

[medium|PRIMARY_VERIFIED] 11. ADR corpus scale and index decay in comparable OSS projects: the index rots when hand-maintained, and Proposed accumulates when nothing forces resolution.
   SRC: https://raw.githubusercontent.com/cosmos/cosmos-sdk/main/docs/architecture/README.md
   QUOTE: Cosmos SDK's index is hand-maintained (PROCESS.md step 4 verbatim: "Update the README entry list") and groups by status: `### Accepted` 16 entries, `### Proposed` 26 entries, `### Draft` 4 entries — i.e. Proposed outnumbers Accepted ~1.6:1. Cosmos also states "ADRs SHOULD NOT be pruned" and that fau…
   GSD NOW: GSD: 66 ADRs — larger than all three verified comparables — grouped 50 Accepted / 9 Proposed / 7 Superseded+Retired+Legacy (docs/adr/README.md index, regenerated). The index is GENERATED and CI-CHECKED: scripts/gen-adr-index.cjs --check runs inside `lint:generated-sync` in package.json, which runs i…
   REC: No structural change. Two maintenance items: (1) the ratification audit was a one-off dated 2026-07-17 (docs/adr/README.md:14) — schedule it as a recurring sweep (Azure WAF's and TechTarget's shared advice is a periodic walk of active ADRs) so the 14% does not drift back up; a `gen-adr-index.cjs --stale` mode reporting Proposed ADRs older…

[medium|PRIMARY_VERIFIED] 12. `Rejected` is a first-class ADR status in every major vocabulary — MADR, Cosmos SDK, CometBFT, AWS Prescriptive Guidance — meaning a decision that was proposed, considered, and turned down, preserved so the debate is not reopened.
   SRC: https://raw.githubusercontent.com/cosmos/cosmos-sdk/main/docs/architecture/PROCESS.md
   QUOTE: MADR front matter: "status: \"{proposed | rejected | accepted | deprecated | … | superseded by ADR-0123}\"". Cosmos: "REJECTED: Consensus determined the approach should not proceed", plus "Faulty ADRs receive merger with *rejected* status rather than deletion" and "ADRs SHOULD NOT be pruned". AWS: "…
   GSD NOW: scripts/gen-adr-index.cjs:55 — `const STATUSES = ['Accepted', 'Proposed', 'Superseded', 'Legacy', 'Retired'];`. No `Rejected`. The three non-live statuses all mean something else, and the file's own doc comment is explicit about it: Superseded = "a specific newer ADR replaced this decision"; Retired…
   REC: Add `Rejected` to STATUSES at scripts/gen-adr-index.cjs:55, add a row to the status table at docs/adr/README.md:48-58 with the obligation "say why it was rejected and link the issue", and add a fourth index group (or fold it into the historical group with a distinct label). Then re-check any ADR currently carrying Legacy/Retired that is a…

[medium|PRIMARY_VERIFIED] 13. GSD's ADR corpus is unreadable to every off-the-shelf ADR tool because the metadata is markdown bullets rather than YAML front matter. MADR decided in its own decision log (0013) to use YAML front matter for metadata, and the entire tooling ecosystem — ADR Man…
   SRC: https://adr.github.io/adr-tooling/
   QUOTE: MADR decisions page: "Decision 0013 mandates using YAML front matter for metadata rather than inline metadata." adr.github.io/adr-tooling lists under "MADR Template": adr-log ("CLI to keep an `index.md` file updated with all ADRs"), ADR Manager ("Web-based UI connecting to GitHub to directly edit AD…
   GSD NOW: GSD parses two hand-rolled shapes — bullet (`- **Status:** Accepted`) and table (`| **Status** | Accepted |`) — via headerFields() in scripts/gen-adr-index.cjs, plus a third path where a relation is declared as a whole `## Supersedes` SECTION (relationSections(), added for ADR-0174's table). Three m…
   REC: Do not migrate wholesale. Do add YAML front matter as the CANONICAL metadata source going forward (status, date, issue, supersedes, superseded-by, subsumes, subsumed-by, confirmation), keep the bullet form as a parsed fallback for the existing 66, and have gen-adr-index.cjs prefer front matter when both exist. This makes the corpus MADR-t…

[medium|PRIMARY_VERIFIED] 14. Empirical work on LLMs checking code against recorded architectural decisions finds they do well on decisions that are explicit and code-inferable, and materially worse on implicit, deployment-oriented, or organizational ones.
   SRC: https://arxiv.org/abs/2602.07609
   QUOTE: From the abstract: the models "achieved substantial agreement and strong accuracy for explicit, code-inferable decisions"; "Accuracy falls short for implicit or deployment-oriented decisions that depend on deployment configuration or organizational knowledge"; "LLMs can meaningfully support validati…
   GSD NOW: GSD's ADR corpus is read primarily by AI agents (the whole product is agent orchestration; CLAUDE.md routes agents to docs/adr/ for architectural decisions). Decision phrasing across the corpus is mixed: some ADRs state code-inferable predicates (docs/adr/2207-status-field-lifecycle-ownership.md nam…
   REC: Tag each ADR as code-enforceable vs process/organizational (a `- **Enforcement:** test | lint | review | manual` header field, parsed alongside the proposed Confirmation field), and surface it in the generated index. Then: for code-enforceable ADRs, require the Confirmation link (a named test); for process ADRs, require a named human owne…

[low|PRIMARY_VERIFIED] 15. Y-statements offer a deliberately lightweight one-sentence decision-record format for decisions that do not justify a full ADR: "In the context of <use case/user story>, facing <concern> we decided for <option> to achieve <quality>, accepting <downside>."
   SRC: https://adr.github.io/adr-templates/
   QUOTE: adr.github.io/adr-templates gives the form verbatim: "In the context of `<use case/user story>`, facing `<concern>` we decided for `<option>` to achieve `<quality>`, accepting `<downside>`." Zimmermann's article: the six parts are Context, Facing, We decided, And neglected, To achieve, Accepting tha…
   GSD NOW: GSD has exactly one tier. All 66 files carry the same lifecycle obligations regardless of weight, yet the corpus is bimodal in practice: 23 of 66 are under 8KB (e.g. docs/adr/2207-status-field-lifecycle-ownership.md at ~3KB, docs/adr/2164-statusline-scope-boundary.md at ~3.2KB, docs/adr/0006-plannin…
   REC: Low priority and optional — resist adding a second document type unless the significance-criteria work (see the AWS/Richards-Ford finding) shows a real backlog of unrecorded small decisions. If it does, the cheaper move is not a new file type but a `Y-statement` one-paragraph summary REQUIRED at the top of every ADR — it gives the index a…

DEAD ENDS: SEARCHED, FOUND NOTHING CREDIBLE — do not re-run these: 1. **A recognised ADR "ratification" practice.** Two targeted searches ("architecture decision record ratification ratified status lifecycle prior art"; "ADR ratify status change to accepted after implementation shipped") plus direct reads of adr.github.io, adr.github.io/adr-templates, MADR 4.0.0 template + MADR decisions log, Nygard 2011, Fowler's bliki, AWS Prescriptive Guidance ADR process, Azure WAF ADR page, Cosmos SDK PROCESS.md, CometBFT architecture README, Backstage ADR docs, and log4brains README. The word "ratification"/"ratify…

==============================================================================
## AGENT: ade7d05ec289c7077
==============================================================================

[high|PRIMARY_VERIFIED] 1. AGENTS.md belongs at the repository ROOT. GSD's own repo has no root AGENTS.md — it has CLAUDE.md, GEMINI.md and .clinerules, and a name-colliding docs/AGENTS.md that is an agent-roster reference doc, not an agent-instruction file.
   SRC: https://agents.md/
   QUOTE: "Create an AGENTS.md file at the root of the repository." ... "AGENTS.md is just standard Markdown. Use any headings you like; the agent simply parses the text you provide." ... "No. AGENTS.md is just standard Markdown." (on required fields)
   GSD NOW: `git ls-files | grep AGENTS.md` returns ONLY `docs/AGENTS.md`, `docs/ja-JP/AGENTS.md`, `docs/ko-KR/AGENTS.md`, `docs/pt-BR/AGENTS.md`. There is no root `AGENTS.md` (confirmed: `ls AGENTS.md` → 'cannot access'). Root agent-instruction files present are `CLAUDE.md` (27.3K), `GEMINI.md` (2.5K), `.cline…
   REC: Two separate actions. (1) Add a root `AGENTS.md` — the highest-value content is the CRITICAL GROUND TRUTH block: payload dir is `gsd-core/` not `get-shit-done/`; `gsd-core/bin/lib/*.cjs` are GENERATED from `src/*.cts`; no `sdk/` tree (ADR-0174); `CONTEXT.md` needs `grep -a`; ADRs in `docs/adr/`. Keep it short and point to CLAUDE.md/CONTRI…  [gsd-allow-legacy-name: quoted ground-truth brief given to agents]

[medium|PRIMARY_VERIFIED] 2. AGENTS.md is no longer a loose community convention — it is a Linux Foundation standard under the Agentic AI Foundation (AAIF), donated by OpenAI alongside Anthropic's MCP and Block's goose.
   SRC: https://agents.md/
   QUOTE: "AGENTS.md is now stewarded by the Agentic AI Foundation under the Linux Foundation."
   GSD NOW: `src/runtime-name-policy.cts:115-116` documents the mapping rationale as: "codex/opencode/kilo/kimi: AGENTS.md is the documented cross-agent instruction file (agentsmd/agents.md convention)." It cites the convention, not the standard or its governing body. The surrounding comment block (lines 95-135…
   REC: Low-effort, high-clarity: update the comment at `src/runtime-name-policy.cts:115` to cite AAIF/Linux Foundation stewardship, matching the primary-source citation discipline the same comment block already applies to copilot and antigravity. More substantively, this justifies writing the multi-runtime instruction-file mapping up as an ADR (…

[low|PRIMARY_VERIFIED] 3. AGENTS.md specifies nested files with nearest-file-wins precedence, so monorepos can give per-package instructions.
   SRC: https://agents.md/
   QUOTE: "Place another AGENTS.md inside each package. Agents automatically read the nearest file in the directory tree, so the closest one takes precedence." ... "The closest AGENTS.md to the edited file wins; explicit user chat prompts override everything."
   GSD NOW: `src/runtime-name-policy.cts:139-147` — `getProjectInstructionFile(runtime)` returns exactly ONE flat path string per runtime ('.claude/CLAUDE.md', '.github/copilot-instructions.md', or 'AGENTS.md'). There is no nesting, no per-package resolution, and no notion of 'nearest file'. Consumed by two sur…
   REC: Do NOT build nested-AGENTS.md emission now — it is speculative until a consumer asks. Instead, record the limitation: when writing the instruction-file-mapping ADR recommended above, add an explicitly-deferred section noting that the convention supports nearest-file-wins nesting and GSD deliberately emits flat, with the trigger condition …

[high|PRIMARY_VERIFIED] 4. GSD's ADR corpus has a machine-enforced status vocabulary and successor-link requirement, gated in CI. This is materially stronger governance than anything in the comparable category — Spec Kit's constitution amendment process is prose-only and unenforced.
   SRC: https://github.com/github/spec-kit/blob/main/spec-driven.md
   QUOTE: Spec Kit constitution amendment process, quoted verbatim: "Modifications to this constitution require: Explicit documentation of the rationale for change, Review and approval by project maintainers, Backwards compatibility assessment." No ADRs are mentioned anywhere in the document, and no tooling e…
   GSD NOW: AHEAD. `scripts/gen-adr-index.cjs:55` defines `const STATUSES = ['Accepted', 'Proposed', 'Superseded', 'Legacy', 'Retired'];`. Line 295: a Superseded ADR must name its successor — line 304 errors with 'status is Superseded but names no successor. Write `Superseded by [ADR-N](N-...)`'. Line 503 handl…
   REC: Write a short ADR that states the architecture-governance model as a deliberate product position, not just an internal hygiene practice: append-only ADRs, five-token status vocabulary, mandatory successor links, CI gate via `gen-adr-index.cjs --check`, and dated ratification-with-evidence for Proposed→Accepted. Cite Spec Kit's prose-only …

[high|PRIMARY_VERIFIED] 5. Spec Kit's upgrade path for an existing project is a destructive force-overwrite that requires the user to manually back up their own customizations first. GSD's versioned installer-migration layer is substantially safer.
   SRC: https://raw.githubusercontent.com/github/spec-kit/main/docs/guides/evolving-specs.md
   QUOTE: Upgrade command: `specify init --here --force --integration <your-agent>`. Warning quoted verbatim: "protect any project-specific material that lives outside `specs/`, especially `.specify/memory/constitution.md` and customized files under `.specify/templates/` or `.specify/scripts/`." And: "Your `s…
   GSD NOW: AHEAD. `docs/installer-migrations.md` (30.8K) defines the layer; its Design Goals open with "1. Protect user data by default." It documents that the installer already handles "backing up locally modified managed files before replacement" and "preserving known user-owned artifacts", and states the la…
   REC: Two things. (1) Surface it — this safety property is currently visible only to contributors reading a 30.8K architecture doc. Add a short user-facing statement to `docs/USER-GUIDE.md` or the upgrade docs: what GSD guarantees on upgrade (managed files replaced, locally-modified managed files backed up, user-owned artifacts preserved) and w…

[high|PRIMARY_VERIFIED] 6. MCP versions its specification with date-based identifiers that increment ONLY on backwards-incompatible change, and pairs this with a published feature-deprecation policy that guarantees a minimum removal window.
   SRC: https://modelcontextprotocol.io/specification/versioning
   QUOTE: "The Model Context Protocol uses string-based version identifiers following the format `YYYY-MM-DD`, to indicate the last date backwards incompatible changes were made." ... "The protocol version will *not* be incremented when the protocol is updated, as long as the changes maintain backwards compat…
   GSD NOW: PARTIAL. Version *declaration* is solid: 39/39 `capabilities/*/capability.json` files carry an `engines` block (verified: `grep -l '"engines"' capabilities/*/capability.json | wc -l` → 39, `ls -d capabilities/*/ | wc -l` → 39). Example `capabilities/ai-integration/capability.json`: `"version": "1.8.…
   REC: Add a Deprecation Policy section to ADR-894 (as a dated amendment, per the corpus's append-only rule) covering: (a) what surfaces are covered by the stability contract — the 12 loop extension points, agent-role vocabulary, capability.json field set; (b) a minimum deprecation window before removal, with MCP's 12-month/90-day-expedited as t…

[medium|PRIMARY_VERIFIED] 7. Claude Code's plugin manifest now natively supports inter-plugin `dependencies` with semver constraints, plus automatic dependency installation and a `claude plugin prune` reaper for orphaned deps.
   SRC: https://code.claude.com/docs/en/plugins-reference
   QUOTE: Manifest schema field, quoted verbatim: "`dependencies` | array | Other plugins this plugin requires, optionally with semver version constraints." Example given: `[{ "name": "secrets-vault", "version": "~2.1.0" }]`. And: "Remove auto-installed plugin dependencies that are no longer required by any i…
   GSD NOW: DIVERGED, arguably deliberately. `.claude-plugin/plugin.json` (verified content) declares `name`, `displayName`, `version` (1.8.0), `description`, `author`, `homepage`, `repository`, `license`, `keywords`, `commands: "./commands/gsd/"`, `skills: "./skills/"`, `hooks: "./hooks/hooks.json"` — and NO `…
   REC: Add a dated amendment to `docs/adr/1244-capability-ecosystem.md` recording the divergence explicitly: Claude Code shipped native plugin `dependencies` + `prune` after ADR-1244's design; GSD does not adopt it because (a) it is single-runtime and GSD ships to 15+, and (b) it lacks the trust/integrity/consent gate that ADR-1244 D5 makes load…

[medium|PRIMARY_VERIFIED] 8. Kiro's steering files support conditional, pattern-triggered context loading via YAML frontmatter — context is loaded only when the agent touches matching files, rather than being eagerly resident.
   SRC: https://kiro.dev/docs/steering/
   QUOTE: "Steering gives Kiro persistent knowledge about your workspace through markdown files." Inclusion modes quoted verbatim: `inclusion: always` ("Loads automatically in every interaction"); `inclusion: fileMatch` with `fileMatchPattern: "components/**/*.tsx"` ("Loads only when working with matching fil…
   GSD NOW: DIFFERENT AXIS. GSD budgets its skill surface at INSTALL time, not load time: `docs/adr/0011-skill-surface-budget-module.md` (Status: Accepted; 'Phase 1 shipped' at line 44) plus `install-profiles.cjs` define three profiles (core/standard/full) that cap how many skills are eagerly present in the sys…
   REC: Before the roadmap commits to prose-trimming as the primary bloat remedy, evaluate conditional activation as an alternative or complement. Concretely: (1) instrument which skills in the `full` profile are actually invoked, per the project's own quick-win-safety constraint — this data is needed either way; (2) check what each target runtim…

[medium|PRIMARY_VERIFIED] 9. Spec Kit publishes a per-agent integration reference documenting the concrete artifact layout for each of 50+ supported agents, including the skills-based vs command-based vs recipe-format distinction.
   SRC: https://github.github.io/spec-kit/reference/integrations.html
   QUOTE: Agents listed include "Amp, Antigravity, Auggie CLI, Claude Code, Cline, CodeBuddy, Codex, Cursor, Devin, Factory Droid, Firebender, Forge, Gemini CLI, GitHub Copilot, Goose, Grok Build, Hermes, IBM Bob, Junie, Kilo Code, Kimi Code, Kiro CLI, Lingma, Mistral Vibe, Oh My Pi, opencode, Pi Coding Agent…
   GSD NOW: PARITY ON MECHANISM, WEAKER ON PUBLICATION. GSD's equivalent machinery exists and is arguably better factored — `src/runtime-artifact-layout.cts` (ADR-3660) resolves commands/agents/skills dirs per runtime, `src/runtime-name-policy.cts` resolves the instruction file, `src/runtime-slash.cts` handles …
   REC: Generate a per-runtime integration reference from the descriptors GSD already has — `runtime-artifact-layout.cts` + `runtime-name-policy.cts` + `runtime-slash.cts` already encode every fact such a page needs, so this is a generator, not new knowledge. Columns: runtime, artifact dirs written, invocation form (`/gsd-<cmd>` vs `$gsd-<cmd>`),…

[medium|PRIMARY_VERIFIED] 10. BMAD-METHOD v6 shipped a breaking restructure — `.bmad` → `_bmad` and IDE artifacts from `.claude/commands/` to `.claude/skills/` — with no documented backward-compatibility statement, relying on installer detection plus manual user cleanup.
   SRC: https://docs.bmad-method.org/how-to/upgrade-to-v6/
   QUOTE: Structural changes: core moved to `_bmad/core/` (previously `_bmad-core`), method to `_bmad/bmm/`, output to `_bmad-output/`, and "IDE skills moved from `.claude/commands/` to `.claude/skills/`". Installer behavior quoted verbatim: "When v4 is detected, you can allow the installer to back up and rem…
   GSD NOW: GSD is on the same trajectory but handling it better. The commands→skills migration is GSD's ADR-1866 (agent-skills dual-injection contract) and the #2505 epic — recent commits confirm active work: `d579daa3 docs(#2505): Phase 6 — migration guide + built-in-only subagent-toolkit enum`, `936a3453 fea…
   REC: Mostly a confirm-and-record finding. (1) Cite BMAD v6's commands→skills move in ADR-1866 as external corroboration that the category is converging on skills — this strengthens the ADR against future 'why did we do this' challenges at zero cost. (2) Note the specific BMAD failure mode in the #2505 migration guide's design rationale: their …

[low|PRIMARY_VERIFIED] 11. OpenSpec exposes a first-class idempotent `openspec update` that regenerates AI guidance and refreshes slash commands in place, separate from `init`.
   SRC: https://raw.githubusercontent.com/Fission-AI/OpenSpec/main/README.md
   QUOTE: "`openspec init`: Sets up the OpenSpec structure in a project directory"; "`openspec update`: Regenerates AI guidance and activates the latest slash commands" — described as: "Run this inside each project to regenerate AI guidance and ensure the latest slash commands are active." Archival: "Archived…
   GSD NOW: PARITY. GSD ships a `gsd-update` skill (present in `skills/`, listed in the available-skills roster) and archival is handled by `gsd-complete-milestone` / `gsd-cleanup`. Both concepts are covered.
   REC: No action beyond a one-line note in whatever competitive-landscape section the audit produces: OpenSpec is the closest peer on update ergonomics and GSD is at parity or better. Do not add work here. Flagging explicitly so a future reader does not re-run this comparison expecting to find a gap.

[low|PRIMARY_VERIFIED] 12. The llms.txt proposal defines a root-level markdown index that gives LLMs a curated, token-budgeted map of a site's documentation, with an Optional section that can be dropped when context is tight. Both Anthropic's Claude Code docs and the MCP spec site serve…
   SRC: https://llmstxt.org/
   QUOTE: "A `/llms.txt` markdown file to provide LLM-friendly content" at the site root. Required structure: "An H1 with the name of the project or site. This is the only required section"; "A blockquote with a short summary of the project"; "Zero or more markdown sections delimited by H2 headers" containing…
   GSD NOW: ABSENT but nearly free. `git ls-files | grep -i 'llms.*txt'` returns nothing — GSD ships no llms.txt. However `docs/README.md` is already a curated Diátaxis index ("Documentation is organised into four quadrants: tutorials... how-to guides... reference... explanation") with H2 sections and markdown-…
   REC: Low priority and do it only as a byproduct. If GSD publishes a docs site, add `/llms.txt` generated from `docs/README.md` — the content already exists, so this is a formatting generator wired into `lint:generated-sync --check` alongside the existing nine. Use the Optional section for the large reference dumps (FEATURES.md, CONFIGURATION.m…

DEAD ENDS: SEARCHED BUT NOT PRIMARY-VERIFIED — do not re-run these expecting a result: 1. **Task Master (eyaltoledano/claude-task-master)** — search surfaced `assets/AGENTS.md` and `docs/configuration.md` on GitHub, and secondary claims about "tagged task lists for multi-context management with full backward compatibility" (v0.16.2) and `task-master rules add windsurf,roo,vscode` profiles. I did NOT fetch the primary files, so I dropped Task Master from findings entirely rather than cite unverified claims. If someone wants this: fetch raw.githubusercontent.com/eyaltoledano/claude-task-master/main/assets/…

==============================================================================
## AGENT: aaa010095c27eb511
==============================================================================

[high|PRIMARY_VERIFIED] 1. Evaluation-driven development for the prompt layer: build evaluations BEFORE writing extensive agent/skill documentation, and treat evals as the source of truth for whether a prompt change helped or hurt.
   SRC: https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices
   QUOTE: "Create evaluations BEFORE writing extensive documentation. This ensures your Skill solves real problems rather than documenting imagined ones." … "Evaluation-driven development: 1. Identify gaps: Run Claude on representative tasks without a Skill. Document specific failures or missing context 2. Cr…
   GSD NOW: NO behavioral eval exists for GSD's own prompts. `ls tests/*.test.cjs | wc -l` → 642 test files; 210 of them carry `// allow-test-rule: source-text-is-the-product` or `runtime-contract-is-the-product`, i.e. they assert on markdown STRING PRESENCE, not model behaviour (e.g. tests/prohibition-probe.pl…
   REC: Create an ADR — call it the prompt-rigor counterpart to ADR-456 — that establishes a behavioral eval tier for the prompt layer. Concretely: (1) pick the 6 core-profile skills from install-profiles.cjs and author ≥3 scenario evals each (task prompt + fixture repo + expected end-state assertions), stored under a new `evals/` tree; (2) requi…

[high|PRIMARY_VERIFIED] 2. Multi-agent systems have emergent behaviour: small edits to an orchestrator prompt unpredictably change downstream subagent behaviour. Evaluate end-state, not turn-by-turn.
   SRC: https://www.anthropic.com/engineering/multi-agent-research-system
   QUOTE: "Multi-agent systems have emergent behaviors, which arise without specific programming. For instance, small changes to the lead agent can unpredictably change how subagents behave." … "Start evaluating immediately with small samples. In early agent development, changes tend to have dramatic impacts …
   GSD NOW: Orchestrator→subagent spawn is the core pattern (CLAUDE.md "Primary Execution Flow"; agents/gsd-planner.md:3 description "Spawned by /gsd:plan-phase orchestrator"). ADR-1610 (docs/adr/1610-workflow-agent-size-budget-ratchet.md, Decision §3) makes byte growth the gated event and byte *shrink* free — …
   REC: Adopt end-state evaluation as the eval design for GSD: assert on the ARTIFACT the run produces (PLAN.md task count/shape, must_haves populated, VERIFICATION.md verdict) against a fixture repo, not on transcript steps. Start with 2 orchestrator pairs (plan-phase→gsd-planner, execute-phase→gsd-executor) and ~10 scenarios each — Anthropic's …

[high|PRIMARY_VERIFIED] 3. LLM-as-judge has known, measured failure modes — position bias, verbosity bias, self-enhancement bias, and limited reasoning — and needs explicit mitigation (position swapping, reference-guided grading, explicit rubrics).
   SRC: https://arxiv.org/abs/2306.05685
   QUOTE: Abstract: "We examine the usage and limitations of LLM-as-a-judge, including position, verbosity, and self-enhancement biases, as well as limited reasoning ability, and propose solutions to mitigate some of them." … "strong LLM judges like GPT-4 can match both controlled and crowdsourced human prefe…
   GSD NOW: agents/gsd-plan-checker.md, agents/gsd-verifier.md, agents/gsd-code-reviewer.md, agents/gsd-doc-verifier.md exist as judge roles; gsd-core/references/verification-patterns.md and honest-verifier.md carry the rubric prose. NOT_CHECKED whether any judge does position swapping or reference-guided gradi…
   REC: Two concrete moves. (1) Calibrate: build a labelled corpus of ~50 plans/verifications with human verdicts and measure each judge's agreement rate; without a number, "the verifier works" is an assertion. GSD already has a candidate corpus in its verifier-reach research (the 43-item corpus and the model-wrong corpus noted in project memory)…

[high|PRIMARY_VERIFIED] 4. When comparing two eval runs (e.g. prompt v1 vs v2), report standard error / confidence intervals and use paired-difference analysis; naive comparison of two scores routinely detects differences that do not exist.
   SRC: https://www.anthropic.com/research/statistical-approach-to-model-evals
   QUOTE: "we encourage researchers to report the SEM, derived from the Central Limit Theorem, alongside each calculated eval score" … "Could a measured difference between two models be due to the specific choice of questions in the eval, and randomness in the models' answers?" … "conducting a paired-differen…
   GSD NOW: No statistical machinery for prompts exists. GSD does have rigorous statistical-ish gating for CODE — stryker.config.mjs (mutation testing), tests/*.property.test.cjs with fast-check, c8 70% line threshold — governed by ADR-456. The asymmetry is stark: code changes face mutation testing; prompt chan…
   REC: Bake this into the eval ADR from day one rather than retrofitting: (a) run every scenario on both the base and the PR prompt version (paired design — same fixture, same seed set), (b) report mean paired difference with a 95% CI, (c) gate on the CI excluding zero in the harmful direction, not on a raw score drop. Also use the post's power …

[high|PRIMARY_VERIFIED] 5. Progressive disclosure with a hard practical ceiling: keep the always-loaded instruction body under ~500 lines and split overflow into files loaded on demand; keep file references exactly one level deep; add a table of contents to reference files over 100 line…
   SRC: https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices
   QUOTE: "Keep SKILL.md body under 500 lines for optimal performance. Split content into separate files when approaching this limit." … "Claude may partially read files when they're referenced from other referenced files. When encountering nested references, Claude might use commands like `head -100` to prev…
   GSD NOW: Over the 500-line guidance: agents/gsd-debugger.md 1513 lines, agents/gsd-plan-checker.md 1024, agents/gsd-planner.md 1000, agents/gsd-verifier.md 970; gsd-core/workflows/execute-phase.md 1679, plan-phase.md 1665, new-project.md 1634. GSD's own caps sit far above: tests/workflow-size-budget.test.cjs…
   REC: Three ratchets, all cheap: (1) flatten the 8 nested references — inline the second-level content or promote the target to a direct import from the entry file, so no reference is reached via another reference; (2) add a TOC to every gsd-core/references/*.md over 100 lines (mechanical, lintable, near-zero risk — a good first quick win becau…

[high|PRIMARY_VERIFIED] 6. Long-context degradation is real, non-uniform, and starts well below the advertised window; distractors compound it; and HOW information is positioned/presented matters at least as much as whether it is present.
   SRC: https://www.trychroma.com/research/context-rot
   QUOTE: "We evaluate 18 LLMs, including the state-of-the-art GPT-4.1, Claude 4, Gemini 2.5, and Qwen3 models." … "Model performance degrades as input length increases, often in surprising and non-uniform ways." … "Even a single distractor reduces performance relative to the baseline (needle only), and addin…
   GSD NOW: ADR-1610 docs/adr/1610-workflow-agent-size-budget-ratchet.md, Context section, states the context-rot rationale without citation and calls it "the load-bearing one". Enforcement is size-only: per-file byte baseline (tests/{workflow,agent}-size-baseline.json via scripts/lib/allowlist-ratchet.cjs asse…
   REC: Amend ADR-1610 with (a) the two citations above so the load-bearing rationale stops being an assertion, and (b) a second enforced dimension beyond bytes: an eager-import relevance rule — an `@~/` import in a skill/workflow header must be needed on EVERY invocation of that command, otherwise it moves to a conditional Read at the step that …

[medium|PRIMARY_VERIFIED] 7. Relevant information placed in the middle of a long context is used significantly less reliably than information at the beginning or the end (U-shaped positional curve).
   SRC: https://arxiv.org/abs/2307.03172
   QUOTE: "performance is often highest when relevant information occurs at the beginning or end of the input context, and significantly degrades when models must access relevant information in the middle of long contexts" … "Performance can degrade significantly when changing the position of relevant informa…
   GSD NOW: NOT_CHECKED whether GSD has any convention about where in a workflow file the load-bearing constraints sit. Structurally, workflows are `<purpose>` → `<required_reading>` → `<available_agent_types>` → `<process>` with numbered steps (CLAUDE.md, Key Abstractions), so gates are inline in step logic — …
   REC: Add a positional convention to the workflow/agent authoring standard: the hard prohibitions and the non-negotiable gates for a workflow get restated in a compact block at the END of the file (and/or immediately after the objective), not only at their point of use in the middle. This is additive, so it fights the byte budget — which makes …

[medium|PRIMARY_VERIFIED] 8. A skill's `description` is the discovery surface and must state BOTH what the skill does AND when to use it, including specific trigger terms. It may be up to 1,024 characters and must be third-person.
   SRC: https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices
   QUOTE: "The `description` field enables Skill discovery and should include both what the Skill does and when to use it." … "Be specific and include key terms. Include both what the Skill does and specific triggers/contexts for when to use it." … "The description is critical for skill selection: Claude uses…
   GSD NOW: scripts/lint-descriptions.cjs:20 `const MAX_LENGTH = 100;` ("Enforces the 100-char description budget for commands/gsd/*.md files", line 5; "Exits 1 if any description exceeds 100 chars", line 11). Samples: commands/gsd/plan-phase.md:3 "Create detailed phase plan (PLAN.md) with verification loop"; c…
   REC: Do not simply raise the cap — that re-triggers #3408. Resolve the tension the way ADR-0011 already set up: cut the number of eagerly-listed skills (profiles/clusters, install-profiles.cjs + clusters.cjs), then spend the recovered budget on trigger-bearing descriptions for the skills that remain. Concretely: raise MAX_LENGTH to ~180-200 fo…

[medium|PRIMARY_VERIFIED] 9. Set degrees of freedom to match task fragility: high freedom (prose heuristics) where many approaches work, low freedom (exact scripts, no parameters) where operations are fragile — and don't offer multiple options where one default plus an escape hatch will d…
   SRC: https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices
   QUOTE: "Match the level of specificity to the task's fragility and variability." … "Narrow bridge with cliffs on both sides: There's only one safe way forward. Provide specific guardrails and exact instructions (low freedom)." … "Open field with no hazards: Many paths lead to success. Give general directio…
   GSD NOW: NOT_CHECKED as a systematic classification — no GSD document classifies its prompt content by freedom level. Circumstantially, GSD already has both poles: low-freedom mechanical shims (workflow bash blocks calling gsd-tools.cjs, which is exactly the 'run exactly this script' pattern) and very long h…
   REC: Before any prose-trim quick win, tag each section of the top-10 largest workflow/agent files as HIGH / MEDIUM / LOW freedom using this framing, and make the rule: LOW-freedom content is never trimmed (it is the guardrail); HIGH-freedom content is the trim budget. That converts the audit's abstract 'load-bearing vs mechanical' check into a…

[medium|PRIMARY_VERIFIED] 10. Tool/interface descriptions are evaluated empirically, with runtime, call-count, token-consumption and error metrics collected per eval run — and tool responses should return only high-signal information.
   SRC: https://www.anthropic.com/engineering/writing-tools-for-agents
   QUOTE: "Building an evaluation allows you to systematically measure the performance of your tools." … "We recommend running your evaluation programmatically with direct LLM API calls." … "Prompts should be inspired by real-world uses and be based on realistic data sources and services." … "We recommend col…
   GSD NOW: gsd-core/bin/gsd-tools.cjs routes all subcommands and returns JSON (CLAUDE.md Pattern Overview: "All gsd-tools commands return JSON (optionally raw strings via `--raw` or `--pick <field>`)"). NOT_CHECKED: no measurement exists of how many tokens a typical gsd-tools response injects, and no lint or t…
   REC: Extend the context budget from static prose to runtime returns: (1) instrument the conformance-trace hook GSD already has (per project memory, a hook logs gsd-tools dispatches) to record response byte size per subcommand, giving an evidence base rather than an assertion; (2) set a per-response soft budget and make the top offenders return…

[medium|PRIMARY_VERIFIED] 11. Prompt-regression testing in CI is an established, tooled practice: declare scenarios + assertions, run on every change to a prompt file, fail the build on threshold breach.
   SRC: https://www.promptfoo.dev/docs/integrations/ci-cd/
   QUOTE: "Integrate promptfoo into your CI/CD pipelines to automatically evaluate prompts, test for security vulnerabilities, and ensure quality before deployment." … "Test prompt changes before they reach production" … "Catch regressions early" … "Fail the build when quality thresholds aren't met" using the…
   GSD NOW: No prompt-eval harness of any kind. GSD's dev tooling already includes non-shipped quality tiers with the same shape — stryker.config.mjs + .github/workflows/mutation.yml ("Evaluate gate" step), c8 coverage, fast-check property tests — so a devDependency eval harness is architecturally consistent wi…
   REC: Evaluate promptfoo (or an equivalent) as a devDependency before writing a bespoke harness — GSD's own 'choose boring technology' instinct applies. Scope the spike narrowly: one skill (gsd-quick or gsd-plan-phase), 5 scenarios, measure cost per run and flake rate over 20 runs. If it holds, wire it as a manual/nightly workflow keyed to chan…

[medium|PRIMARY_VERIFIED] 12. Codex silently truncates concatenated AGENTS.md instruction docs at `project_doc_max_bytes` (32 KiB default), and later files override earlier ones because they appear later in the combined prompt.
   SRC: https://learn.chatgpt.com/docs/agent-configuration/agents-md
   QUOTE: "Codex stops adding files once the combined size reaches the limit defined by `project_doc_max_bytes` (32 KiB by default)." … "Raise the limit or split instructions across nested directories when you hit the cap." … "Codex concatenates files from the root down, joining them with blank lines. Files c…
   GSD NOW: ADR-1610 Decision §1 asserts "Codex caps instruction docs at 32,768 bytes, `project_doc_max_bytes`, and truncates past it" with no citation, and §Alternatives explicitly rejects 32 KiB as a universal cap: "the XL/LARGE tiers sit above 32 KiB because they are top-level orchestrators loaded by Claude,…
   REC: Two actions. (1) Add this citation to ADR-1610 §1 — the anchor number is currently unsourced in a ratified ADR. (2) VERIFY the reasoning in the Alternatives section: confirm by inspection of the Codex install path whether any GSD artifact lands in the AGENTS.md concatenation window. If any does, the ADR's justification for exempting XL/LA…

[medium|PRIMARY_VERIFIED] 13. Model-tier configuration should track current model IDs and effort levels: on the current Opus generation, `xhigh` is the recommended setting for coding and agentic use cases, while `max` can show diminishing returns and is prone to overthinking.
   SRC: file:///tmp/claude-1000/bundled-skills/2.1.219/e258381e49eab14445bfe342105fda0f/claude-api/SKILL.md
   QUOTE: SKILL.md §Thinking & Effort: "`xhigh` (added on Opus 4.7, between `high` and `max`) is the best setting for most coding and agentic use cases on Fable 5 / Opus 4.7/4.8 / Sonnet 5, and the default in Claude Code" … "Use a minimum of `high` for intelligence-sensitive work, `max` when correctness matte…
   GSD NOW: gsd-core/bin/shared/model-catalog.json:11 `"opus": { "model": "claude-opus-4-8" }` (repeated at :26, :31, :36, :41, :96 for other runtimes/presets); adaptive tiers at :103-104 also route to claude-opus-4-8; only the top adaptive tier at :108 reaches claude-fable-5. Effort: across 71 files in command…
   REC: (1) Add claude-opus-5 to the catalog's Claude tier — per the vendor migration guide it is a drop-in at identical pricing, so this is a low-risk, high-value refresh; note it needs the two Opus 5 breaking-change checks (thinking on by default; `thinking: disabled` rejected above `high` effort) though GSD does not appear to set `thinking` ex…
